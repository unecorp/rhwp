---
title: "[오라클] unmatched-143E433F503322BD33: pdf_pages 0 (>= 12)"
labels: bug
draft: true
submit: never
---

# [오라클] unmatched-143E433F503322BD33: pdf_pages 0 (>= 12)

> **초안 — 제출하지 않음.** 이 파일은 디스크에만 남긴다. `gh issue create` 로
> 올리지 말고, 사람이 수치·재현 커맨드를 확인한 뒤 수동으로 등록한다.

## 현상

문서 `samples/143E433F503322BD33.hwp` 가 한컴 기준 대비 임계 게이트를 넘었습니다.

- 지표: `pdf_pages` = **0**
- 게이트: `pdf_pages >= 12` 이면 실패
- 쪽수: 0
- 최악 페이지: (없음)

짝 없는 샘플. 이유 `no_oracle_pdf` · 근접 PDF 1건.


## 재현 방법

작업 디렉터리: `.`

```
python tools/oracle_public/oracle_resolver.py --pretty --validate
```

위 명령을 그대로 다시 돌리면 같은 수치가 나와야 합니다. `scripts/visual_sweep.py`
자체를 수정하지 않습니다.

## 기대 결과

한컴 기준 PDF `pdf-large/hwpx/143E433F503322BD33.pdf` 와 같은 쪽수·레이아웃이어야 합니다.
`pdf_pages` 이 게이트(`>= 12`)에 걸리지 않아야 합니다.

## 실제 결과

| 지표 | 값 |
| --- | --- |
| `pdf_pages` | 0 |
| `pdf_bytes` | 0 |
| `sample_bytes` | 73216 |
| `worst_pages` |  |

## 환경

- 리포트 출처: `oracle_public.fatten_catalog.cheap`
- 리포트 시각: 2026-08-18T10:37:44Z
- rhwp 바이너리: `(unused)`
- DPI: 96
- pixel_diff_threshold: 12
- 문서 id: `unmatched-143E433F503322BD33`

## 제출

제출은 **수동**입니다. 이 생성기는 GitHub 이슈를 만들지 않습니다.
