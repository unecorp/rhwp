# 표 워크스루

replay 케이스 4 건. 각 파일은 attest / verify 일치 / verify 불일치 세 경로를 닫는다. 상세 why 는 케이스 JSON 과 `catalog.py` 가 정본이다.

| ident | 장르 | steps | 제목 | why |
| --- | --- | ---: | --- | --- |
| `table_label` | 라벨 | 1 | 일련번호 헤더 표기 정정 | 표 헤더 문자열. replace_text 로 두되 표 좌표 편집(set_cell)과 구분. |
| `set_cell_name` | 성명셀 | 1 | 표 (0,1,0) 성명 칸 | 명단 표 성명. 금액 셀과 좌표·의미가 다르다. |
| `multi_table` | 세칸 | 3 | 표 세 칸 연속 set_cell | steps=3. 감사 steps 대조의 긴 쪽 표본. |
| `keep_style_cell` | 스타일유지 | 1 | 칸 값만 바꾸고 스타일 유지 | keepStyle 은 set_cell 선택 필드. 값만 바뀌는 계약. |

