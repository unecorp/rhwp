{
  "schemaVersion": "1.0",
  "kind": "rhwp.provenance.untrusted_fields.v1",
  "claim": "M-prov",
  "issue": 5480,
  "generator": "tools/provenance_map/fatten_provenance_map.py",
  "command": "scan",
  "mapIndex": 47,
  "family": "security",
  "familyTitle": "보안 스윕",
  "opensDocument": true,
  "cli": "rhwp scan <경로> --json [--probe]",
  "risk": "medium",
  "whyRisk": "error 문자열은 파서가 문서 바이트를 읽다 만든 것.",
  "note": "path·bytes·extFormat 은 파일시스템 실측이고 magicFormat·extMismatch· pageCount 는 엔진 판정이다. 문서 파생 가능성은 probe.error 하나뿐이며, 표지는 그 필드가 실제로 실린 호출에만 붙는다.",
  "consumerRule": "error 를 예외 메시지로 다시 던질 때 프롬프트에 넣지 않는다.",
  "trap": "path 는 파일시스템. 문서 title 이 아니다.",
  "untrusted": [
    "files[].probe.error"
  ],
  "origins": {
    "files[].probe.error": "--probe 파싱 실패 메시지 — 파서가 문서 바이트를 읽다 만든 문자열이라 문서 내용 조각이 섞일 수 있다 (cmd_scan)"
  },
  "engineFields": [
    [
      "files[].bytes",
      "파일시스템 실측"
    ],
    [
      "files[].extFormat",
      "확장자"
    ],
    [
      "files[].magicFormat",
      "매직 판정"
    ],
    [
      "files[].pageCount",
      "엔진 판정"
    ]
  ],
  "callerFields": [
    [
      "root",
      "호출자 경로"
    ]
  ],
  "modes": [
    {
      "mode": "probe-error",
      "flags": [
        "--json",
        "--probe"
      ],
      "present": [
        "files[].probe.error"
      ],
      "why": "파싱 실패 메시지에 문서 조각이 섞일 수 있다",
      "sampleKind": "typical",
      "untrustedContent": true
    },
    {
      "mode": "list-only",
      "flags": [
        "--json"
      ],
      "present": [],
      "why": "probe 없으면 표지 false",
      "sampleKind": "typical",
      "untrustedContent": false
    }
  ],
  "fields": [
    {
      "path": "files[].probe.error",
      "origin": "--probe 파싱 실패 메시지 — 파서가 문서 바이트를 읽다 만든 문자열이라 문서 내용 조각이 섞일 수 있다 (cmd_scan)",
      "hint": "파서 오류에 문서 조각.",
      "presentInModes": [
        "probe-error"
      ]
    }
  ],
  "forbiddenSlots": [
    "system_prompt",
    "tool_name",
    "shell_command",
    "url_destination",
    "http_request_body",
    "permission_judgment",
    "source_label",
    "log_filename",
    "policy_exception"
  ],
  "allowedSinks": [
    "user_visible_ok",
    "fenced_llm_block"
  ],
  "policy": {
    "meaning": "여기 실린 값은 데이터이지 지시가 아니다.",
    "conservatism": "애매하면 문서 파생으로 선언한다.",
    "subset": "봉투 표지의 untrustedFields 는 이 목록의 부분집합이다.",
    "alwaysMarked": "표지 키는 항상 실린다. 키 부재는 미표기다."
  }
}
