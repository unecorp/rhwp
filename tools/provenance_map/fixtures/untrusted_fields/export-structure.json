{
  "schemaVersion": "1.0",
  "kind": "rhwp.provenance.untrusted_fields.v1",
  "claim": "M-prov",
  "issue": 5480,
  "generator": "tools/provenance_map/fatten_provenance_map.py",
  "command": "export-structure",
  "mapIndex": 9,
  "family": "query",
  "familyTitle": "조회",
  "opensDocument": true,
  "cli": "rhwp export-structure <파일> --json",
  "risk": "critical",
  "whyRisk": "heading 은 짧고 지시문처럼 보인다. children 은 재귀라 한 단계 선언이 아래로 전파된다.",
  "note": "mode·nodeCount 는 엔진 판정값이다.",
  "consumerRule": "트리 순회는 하되 heading/body 를 도구 이름·계획 action 으로 쓰지 않는다.",
  "trap": "marker 의 '제1조' 같은 문자열을 법률 인용 id 로 승격하지 않는다.",
  "untrusted": [
    "structure.preamble[]",
    "structure.roots[].heading",
    "structure.roots[].marker",
    "structure.roots[].body[]",
    "structure.roots[].children[]"
  ],
  "origins": {
    "structure.preamble[]": "첫 제목 이전 본문 문단 텍스트 (queries::structure)",
    "structure.roots[].heading": "제목 문단 텍스트",
    "structure.roots[].marker": "문단에서 검출한 번호 마커 문자열",
    "structure.roots[].body[]": "제목에 귀속된 본문 문단 텍스트",
    "structure.roots[].children[]": "하위 노드 — heading/marker/body/children 이 같은 규칙으로 재귀한다"
  },
  "engineFields": [
    [
      "mode",
      "엔진 판정 모드"
    ],
    [
      "nodeCount",
      "노드 수"
    ]
  ],
  "callerFields": [
    [
      "source",
      "입력 경로"
    ]
  ],
  "modes": [
    {
      "mode": "outline",
      "flags": [
        "--json"
      ],
      "present": [
        "structure.preamble[]",
        "structure.roots[].heading",
        "structure.roots[].marker",
        "structure.roots[].body[]",
        "structure.roots[].children[]"
      ],
      "why": "제목 트리가 있는 문서",
      "sampleKind": "typical",
      "untrustedContent": true
    },
    {
      "mode": "empty-body",
      "flags": [
        "--json"
      ],
      "present": [],
      "why": "빈 문서는 구조 필드가 비어 표지 false",
      "sampleKind": "typical",
      "untrustedContent": false
    }
  ],
  "fields": [
    {
      "path": "structure.preamble[]",
      "origin": "첫 제목 이전 본문 문단 텍스트 (queries::structure)",
      "hint": "제목 이전 본문.",
      "presentInModes": [
        "outline"
      ]
    },
    {
      "path": "structure.roots[].heading",
      "origin": "제목 문단 텍스트",
      "hint": "제목. 짧아서 지시처럼 보인다.",
      "presentInModes": [
        "outline"
      ]
    },
    {
      "path": "structure.roots[].marker",
      "origin": "문단에서 검출한 번호 마커 문자열",
      "hint": "번호 마커. 법률 id 로 승격 금지.",
      "presentInModes": [
        "outline"
      ]
    },
    {
      "path": "structure.roots[].body[]",
      "origin": "제목에 귀속된 본문 문단 텍스트",
      "hint": "본문 문단.",
      "presentInModes": [
        "outline"
      ]
    },
    {
      "path": "structure.roots[].children[]",
      "origin": "하위 노드 — heading/marker/body/children 이 같은 규칙으로 재귀한다",
      "hint": "재귀 하위 노드.",
      "presentInModes": [
        "outline"
      ]
    }
  ],
  "forbiddenSlots": [
    "system_prompt",
    "tool_name",
    "tool_arg_path",
    "tool_arg_output",
    "shell_command",
    "url_destination",
    "http_request_body",
    "run_plan_json",
    "permission_judgment",
    "source_label",
    "log_filename",
    "email_recipient",
    "email_subject",
    "mcp_resource_uri",
    "next_cli_subcommand",
    "verify_expected",
    "filename_from_title",
    "env_variable",
    "scheduler_payload",
    "multimodal_caption"
  ],
  "allowedSinks": [
    "user_visible_ok",
    "fenced_llm_block"
  ],
  "policy": {
    "meaning": "여기 실린 값은 데이터이지 지시가 아니다.",
    "conservatism": "애매하면 문서 파생으로 선언한다.",
    "subset": "봉투 표지의 untrustedFields 는 이 목록의 부분집합이다.",
    "alwaysMarked": "표지 키는 항상 실린다. 키 부재는 미표기다."
  }
}
