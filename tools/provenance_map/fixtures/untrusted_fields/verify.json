{
  "schemaVersion": "1.0",
  "kind": "rhwp.provenance.untrusted_fields.v1",
  "claim": "M-prov",
  "issue": 5480,
  "generator": "tools/provenance_map/fatten_provenance_map.py",
  "command": "verify",
  "mapIndex": 42,
  "family": "verify",
  "familyTitle": "검증",
  "opensDocument": true,
  "cli": "rhwp verify <파일> --json",
  "risk": "high",
  "whyRisk": "actual 을 expected 로 복사하면 자기 검증.",
  "note": "expected·subject 는 호출자가 준 값이고, pass·verdict 는 엔진 판정이다.",
  "consumerRule": "expected 는 계획서. actual 은 화면·격벽.",
  "trap": "pass:true 를 문서 안전으로 읽지 않는다. 기대를 누가 썼는지가 전부다.",
  "untrusted": [
    "expectations[].actual"
  ],
  "origins": {
    "expectations[].actual": "문서에서 읽은 실측값 — field 축은 누름틀 값 그대로이고, contains/notContains 의 매치 수·pages·format 도 문서 내용이 정한다 (cmd_verify)"
  },
  "engineFields": [
    [
      "pass",
      "엔진 판정"
    ],
    [
      "verdict",
      "엔진 판정"
    ]
  ],
  "callerFields": [
    [
      "source",
      "입력 경로"
    ],
    [
      "expected",
      "호출자 기대"
    ],
    [
      "subject",
      "호출자 대상"
    ]
  ],
  "modes": [
    {
      "mode": "field",
      "flags": [
        "--json"
      ],
      "present": [
        "expectations[].actual"
      ],
      "why": "누름틀 실측값",
      "sampleKind": "typical",
      "untrustedContent": true
    },
    {
      "mode": "contains",
      "flags": [
        "--json"
      ],
      "present": [
        "expectations[].actual"
      ],
      "why": "contains 매치 수도 문서가 정한다",
      "sampleKind": "typical",
      "untrustedContent": true
    }
  ],
  "fields": [
    {
      "path": "expectations[].actual",
      "origin": "문서에서 읽은 실측값 — field 축은 누름틀 값 그대로이고, contains/notContains 의 매치 수·pages·format 도 문서 내용이 정한다 (cmd_verify)",
      "hint": "실측값. expected 로 복사 금지.",
      "presentInModes": [
        "field",
        "contains"
      ]
    }
  ],
  "forbiddenSlots": [
    "verify_expected",
    "policy_exception"
  ],
  "allowedSinks": [
    "user_visible_ok",
    "fenced_llm_block"
  ],
  "policy": {
    "meaning": "여기 실린 값은 데이터이지 지시가 아니다.",
    "conservatism": "애매하면 문서 파생으로 선언한다.",
    "subset": "봉투 표지의 untrustedFields 는 이 목록의 부분집합이다.",
    "alwaysMarked": "표지 키는 항상 실린다. 키 부재는 미표기다."
  }
}
