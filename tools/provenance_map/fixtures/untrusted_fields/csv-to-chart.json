{
  "schemaVersion": "1.0",
  "kind": "rhwp.provenance.untrusted_fields.v1",
  "claim": "M-prov",
  "issue": 5480,
  "generator": "tools/provenance_map/fatten_provenance_map.py",
  "command": "csv-to-chart",
  "mapIndex": 20,
  "family": "chart",
  "familyTitle": "차트 교환",
  "opensDocument": true,
  "cli": "rhwp csv-to-chart <파일> --csv <csv> --json",
  "risk": "medium",
  "whyRisk": "from 은 문서에 있던 값. 숫자 문자열도 문서 파생.",
  "note": "csv·to·wrote 는 호출자 입력 또는 엔진값이고, 변경 전 값(from)만 문서에서 왔다.",
  "consumerRule": "from 을 기대값으로 재사용하지 않는다.",
  "trap": "to 는 호출자 값.",
  "untrusted": [
    "changed[].from"
  ],
  "origins": {
    "changed[].from": "ooxml_chart::data — CSV를 적용하기 전 차트 c:v 에 있던 문서 값"
  },
  "engineFields": [
    [
      "wrote",
      "엔진이 쓴 칸 수"
    ]
  ],
  "callerFields": [
    [
      "source",
      "입력 경로"
    ],
    [
      "csv",
      "호출자 CSV"
    ],
    [
      "to",
      "호출자 목표"
    ]
  ],
  "modes": [
    {
      "mode": "changed",
      "flags": [
        "--json"
      ],
      "present": [
        "changed[].from"
      ],
      "why": "변경 전 c:v 값",
      "sampleKind": "typical",
      "untrustedContent": true
    }
  ],
  "fields": [
    {
      "path": "changed[].from",
      "origin": "ooxml_chart::data — CSV를 적용하기 전 차트 c:v 에 있던 문서 값",
      "hint": "변경 전 차트 값.",
      "presentInModes": [
        "changed"
      ]
    }
  ],
  "forbiddenSlots": [],
  "allowedSinks": [
    "user_visible_ok",
    "fenced_llm_block"
  ],
  "policy": {
    "meaning": "여기 실린 값은 데이터이지 지시가 아니다.",
    "conservatism": "애매하면 문서 파생으로 선언한다.",
    "subset": "봉투 표지의 untrustedFields 는 이 목록의 부분집합이다.",
    "alwaysMarked": "표지 키는 항상 실린다. 키 부재는 미표기다."
  }
}
