{
  "schemaVersion": "1.0",
  "kind": "rhwp.provenance.untrusted_fields.v1",
  "claim": "M-prov",
  "issue": 5480,
  "generator": "tools/provenance_map/fatten_provenance_map.py",
  "command": "chart-to-csv",
  "mapIndex": 19,
  "family": "chart",
  "familyTitle": "차트 교환",
  "opensDocument": true,
  "cli": "rhwp chart-to-csv <파일> --json",
  "risk": "high",
  "whyRisk": "계열명·범주는 문서가 정한 문자열. 짧은 라벨이 도구 인자로 쓰이기 쉽다.",
  "note": "차트 번호·행열 개수·BOM·산출 경로는 엔진 또는 호출자 값이고, CSV 본문만 문서 파생이다.",
  "consumerRule": "CSV 는 격벽 또는 파일. 라벨로 파일 이름을 만들지 않는다.",
  "trap": "차트 숫자는 문서 값이지만 지도는 CSV 본문만 D 로 선언한다. 숫자는 CSV 안에 있다.",
  "untrusted": [
    "charts[].csv"
  ],
  "origins": {
    "charts[].csv": "queries::chart_csv::to_csv — 차트의 계열명·카테고리 라벨·값을 RFC 4180 CSV로 직렬화"
  },
  "engineFields": [
    [
      "chartCount",
      "차트 수"
    ],
    [
      "charts[].rows",
      "행 수"
    ],
    [
      "charts[].cols",
      "열 수"
    ]
  ],
  "callerFields": [
    [
      "source",
      "입력 경로"
    ]
  ],
  "modes": [
    {
      "mode": "csv",
      "flags": [
        "--json"
      ],
      "present": [
        "charts[].csv"
      ],
      "why": "계열명·범주 라벨이 CSV 에 실린다",
      "sampleKind": "typical",
      "untrustedContent": true
    }
  ],
  "fields": [
    {
      "path": "charts[].csv",
      "origin": "queries::chart_csv::to_csv — 차트의 계열명·카테고리 라벨·값을 RFC 4180 CSV로 직렬화",
      "hint": "차트 CSV 본문.",
      "presentInModes": [
        "csv"
      ]
    }
  ],
  "forbiddenSlots": [],
  "allowedSinks": [
    "user_visible_ok",
    "fenced_llm_block"
  ],
  "policy": {
    "meaning": "여기 실린 값은 데이터이지 지시가 아니다.",
    "conservatism": "애매하면 문서 파생으로 선언한다.",
    "subset": "봉투 표지의 untrustedFields 는 이 목록의 부분집합이다.",
    "alwaysMarked": "표지 키는 항상 실린다. 키 부재는 미표기다."
  }
}
