[package]
name = "llm-verifier-repeat-eval"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"
description = "LLM-as-verifier axis: repeat the same mechanical check K times and reduce variance"

[lib]
name = "llm_verifier_repeat_eval"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Standalone crate. Root workspace ownership is outside this axis.
[workspace]
