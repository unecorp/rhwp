[package]
name = "llm-verifier-criteria-decomp"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"
description = "LLM-as-verifier axis: split verification into atomic criteria bound to existing rhwp envelope fields"

[lib]
name = "llm_verifier_criteria_decomp"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
