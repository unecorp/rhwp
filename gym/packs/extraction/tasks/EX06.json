{
  "id": "EX06",
  "tier": 2,
  "title": "수량 수확 — extract-data --kind number",
  "input": "samples/20250130-hongbo.hwp",
  "instructions": "extract-data --kind number 로 이 문서에서 수량만 수확해, 항목 수(itemCount)를 보고하라. 답 형식: {\"numbers\": <수>}. 힌트: rhwp extract-data <문서> --kind number --json 의 itemCount. 단위가 없는 맨 숫자는 항목이 아니다.",
  "submit": {
    "kind": "answer"
  },
  "checks": [
    {
      "name": "numbers 일치",
      "op": "answer_eq",
      "answer": "numbers",
      "cmd": [
        "extract-data",
        "{input}",
        "--kind",
        "number",
        "--json"
      ],
      "path": "itemCount"
    }
  ]
}
