# #6175 어울림 개체 옆 문단이 전폭으로 재래핑돼 그림 아래로 가려진다

문서: `samples/issue6175/seed_expo_square_float_body.hwpx`
(농촌진흥청 156655489 의 **구조 보존 슬라이스** — BinData 이미지만 8x8 PNG 로 치환,
940KB → 101KB. 좌표는 원본과 동일하다. 그래서 PNG 의 그림은 회색 판으로 나온다.)

영역: 1쪽 (원본 좌표 y 370~580px, `-p 0 --scale 1.5 --compat 2022`)

| | 본문 줄 폭 | 결과 |
| --- | --- | --- |
| 종전 (`before.png`) | 642.5px (전폭) | 글자가 그림 밑으로 들어가 소실 |
| 수정 (`after.png`) | 355.9px (= 저장 sw 26692HU) | 그림 옆에서 정상 접힘 |

한글 2022 오라클(문서 편집 버전 = 한글 2020, major 11 → 가장 가까운 설치본):
1쪽 본문 줄의 오른쪽 끝 323~329pt — 수정본과 일치한다.
