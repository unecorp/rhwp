# Task M100 #4968 — Stage W9-Q3-5R4E-3 최종 gate·이슈 disposition

- 작성일: 2026-08-27 KST
- 작업 브랜치: `task_m100_4968`
- 작업 HEAD: `a267d3b5485e29a06140482a44e858b15946fb1d`
- 최신 통합 기준: `upstream/devel@529ab90c25d5aead724cb67d0d63920c1f580ad3`
- 수행계획: `mydocs/working/task_m100_4968_w9_q3_5_r4e_entry.md`
- 상태: **R4E-3 검증·종료 판정과 메인테이너 결과 승인 완료, 단계 커밋 대상**
- remote push·PR·comment·issue close: 수행하지 않음

## 1. 결론

#4968의 기술 완료 조건을 전항 충족했다. 같은 공개 runtime fixture와 exact face를 native와 실제 Docker WASM
배포 산출물에 등록했을 때 kerning-off 11행은 그대로였고 kerning-on 11행은 모두 pair positions가 변했다.
22개 문맥의 native/WASM canonical projection은 byte-exact였으며, source 오류·상한·충돌 6종도 같은
fail-closed 결과를 냈다. Canvas2D와 CanvasKit은 layout owner가 게시한 positions를 재생하고 별도 shaping으로
결과를 맞추지 않았다.

최신 `devel` 통합 뒤 드러난 구조체 초기화 누락 두 곳을 `layout_positions: None`으로 닫았다. 이 정정으로
production Rust가 한 줄 바뀌었으므로 R4D의 전체 결과를 재사용하지 않고 계획대로 전체 nextest, Native Skia
3종과 Docker WASM을 새 병합 후보에서 다시 실행했다. 모든 게이트가 통과했다.

따라서 현재 변경은 **PR 준비 가능** 상태다. 다만 이 보고서 승인, 단계 커밋, push와 PR 생성은 각각 별도
승인 경계이며, #4968 close는 PR merge와 후속 확인 뒤 별도 승인을 받아 수행한다.

## 2. 원격 동기화와 병합 정정

R4E-3 직전에 `origin/devel`, `upstream/devel`, 로컬 `devel`을 다시 조회했다. 세 ref는 모두
`529ab90c25d5aead724cb67d0d63920c1f580ad3`으로 같았고 로컬 `devel...upstream/devel`은 0/0이었다.
작업 브랜치의 merge commit `a267d3b54`는 이 SHA를 두 번째 부모로 가진다.

자동 병합에는 text conflict가 없었지만 compile-time 구조 계약 두 곳이 교차했다.

| 위치 | 원인 | 정정 |
| --- | --- | --- |
| `src/renderer/layout/paragraph_layout.rs` | 최신 `devel`의 #6111 wrapped guide overlay가 #4968의 새 `TextRunNode.layout_positions` 필드를 모름 | editor-only guide는 exact layout positions를 게시하지 않으므로 `None` |
| `tests/cases/issue_6117_layer_decoration_trim.rs` | 최신 `devel`의 test helper가 같은 새 필드를 모름 | replay positions가 없는 helper이므로 `None` |

두 값은 커닝을 새로 켜는 우회가 아니라 기존 경로를 fail-closed로 보존하는 최소 정정이다. #6111 focused
1/1과 #6117 focused 3/3이 통과했고, `cargo check --all-targets`가 추가 누락이 없음을 확인했다.

review worktree에서 최초 focused resolver가 파생 suite의 과거 배정과 현재 source-weight 계산을 다르게 읽어
#2225와 #6117을 0건 실행한 사례가 있었다. 0건을 성공으로 세지 않고 정본 절차인
`rust-test-suite-manifest.mjs --prepare`와 `--check`로 파생 harness를 다시 만든 뒤 실제 테스트 수를 확인했다.
파생 suite·manifest와 Cargo marker 변경은 검증 worktree에만 있으며 source PR 제출 대상이 아니다.

### 2.1 PR #6214 integration source 정정

최초 PR head의 CI는 `issue_4968_kerning_capability_provider.rs`를 `root_mod + path_attr` singleton exception으로
분류했고, 일반 source PR에 포함하지 않은 Cargo exception registry와의 drift를 `--prepare`에서 올바르게
실패시켰다. Adapter·Proptest·Lint·Native·archive builder의 동시 실패는 모두 이 한 준비 단계에서 파생됐다.

이 원본은 generated module 안에서도 상대 `#[path]`를 안전하게 해석하고 29개 계약을 실행할 수 있으므로,
`tests/suites/suite-policy.json`에 해당 두 blocker만 허용하는 좁은 `moduleIntegrationOverrides`를 추가했다.
정정 후 review worktree에서 `--prepare`·`--check`가 983 sources, 4,417 static test attrs, 32 suites + 9
exceptions, 41/48 targets로 통과했고, `regression_suite_012` 내부의 #4968 계약 29/29가 통과했다. generated
suite·manifest와 Cargo marker는 여전히 PR에 포함하지 않는다.

두 번째 CI correction에서는 generated suite를 준비한 뒤에만 드러나는 원본 포맷 불일치를 직접
`rustfmt`으로 정정했다. 이어진 base-ref 검사는 `src/renderer/render_tree.rs`의 source-side test가
18개에서 21개로 늘어난 것을 거부했다. 정책 기준선을 완화하지 않고, 새 private helper와 테스트 3개를
source 본문에서 제거해 기존 public integration 계약 안으로 옮겼다. K0/K1 직렬화, 정상 positions replay,
손상·문자열 불일치·상한 초과 시 fallback을 공개 `TextRunNode`·`CanvasRenderer` 경로로 검증한다.
source-unit 총량은 base와 같은 4,221개로 복귀했고 #4968 integration은 29/29를 유지했다.

## 3. native·WASM 최종 교차 결과

| 항목 | 결과 |
| --- | ---: |
| runtime 문맥 | body 18 + table-cell 2 + text-box 2 = 22 |
| exact source 등록 | 18 slots / 1 source / 1,236 bytes |
| kerning-off 등록 전후 | 11/11 무변화 |
| kerning-on 등록 후 | 11/11 positions 변화 |
| native/WASM canonical projection | byte-exact PASS |
| runtime canonical SHA-256 | `aaca1249d096965c662043bf3ed09a0aafdc9f9d85f9cefb24382f70695d85a7` |
| 실패 행렬 | 6/6 PASS |
| 실패 행렬 canonical SHA-256 | `f16df3a02d7028e742673d319b44eefa2f7456c61e2c9fb113e9a0f67ad54f7b` |

실패 행렬은 malformed SFNT, pair table 미지원, unavailable face index, invalid language index, 32MiB+1 byte,
slot conflict를 포함한다. registration 성공과 pair capability 성공을 구분하고, 실패 뒤 render tree·SVG·Canvas
상태가 시도 전과 같음을 native와 WASM에서 함께 확인했다.

ratio 100/90/80의 pair delta는 각각 1,600/1,440/1,280 milli-px였고 spacing 0/-5/-10과 독립적이었다.
따라서 ratio는 pair delta에 한 번만 적용되고 spacing은 glyph advance에만 남는다.

## 4. Docker WASM·embedding 경계

최신 병합·정정을 포함한 루트 source에서 표준 Docker 경로를 다시 실행했다.

```text
docker compose --env-file .env.docker run --rm wasm
=> PASS
=> wall time 393.91s
```

| 산출물 | bytes | SHA-256 |
| --- | ---: | --- |
| R4E-1 기준 `pkg/rhwp_bg.wasm` | 9,593,739 | `7907c24bfdeda5bbef9d9f06d88d5de1aa66ab0474563d31c8406f3689e21cc3` |
| 최신 병합 후보 `pkg/rhwp_bg.wasm` | 9,604,623 | `87380c7a43e086013a1c9e041a38417fe198b3c1d5c075117d2996082c042ce2` |
| 차이 | +10,884 (+0.113449%) | 참고값 |

이 차이는 #4968만의 전후 비교가 아니다. 두 빌드 사이에 최신 `devel` 변경도 함께 들어왔으므로 #4968 단독
크기 회귀로 귀속하지 않고 code-only 참고값으로만 보존한다. build wall time도 제품 runtime 성능이 아니라
검증 환경 비용이다.

최종 blob에 대해 9개 needle을 직접 검사했다. full Noto bytes, full smoke face bytes, runtime fixture bytes,
두 fixture 이름, smoke SHA 문자열, `/home/edward/`, `corpus_10k`, `hwpsamples`가 모두 없었다.

- embedding: 9/9 absent, PASS
- evidence canonical SHA-256:
  `6af6421f500455a81a8a0ab0901d28831e54e4d78dc34be5ebac9a32ea33f9f6`
- evidence file SHA-256:
  `a97c12764280fb45af5819807a3b4cb05201d0f4def0ce708786a3ded989254f`

증적에는 입력의 공개 digest와 크기만 있으며 font payload, source path, private 문서 본문·파일명·문서 hash는
없다.

## 5. 최종 검증

### 5.1 결정성·정책·focused gate

```text
Python fixture/Q2 결정성·정본 보호       12/12 PASS
Python compileall                         PASS
R4E Node unit gate                         7/7 PASS
integration manifest 정책                18/18 PASS
Rust unit-tier base-ref 정책              4,221 tests / 299 modules, PASS
public #4968 integration                  29/29 PASS
#6111 focused                              1/1 PASS
#6117 focused                              3/3 PASS
cargo fmt --all
cargo fmt --all -- --check
git diff --check                           PASS
```

최종 PR correction review worktree의 manifest 준비/check 결과는 983 sources, 4,417 static test attrs,
32 suites + 9 exceptions, 41/48 integration targets, minimum nextest 6,559 cases였다.

### 5.2 Rust·renderer 전체 gate

```text
cargo check --locked --all-targets         PASS
cargo clippy --locked --all-targets -- -D warnings
                                             PASS, warning 0
cargo check --locked --lib --target wasm32-unknown-unknown
                                             PASS
cargo nextest run --locked --cargo-profile release-test \
  --tests --no-fail-fast
                                             8,451/8,451 PASS
                                             43 skipped, 1,345.094s
Native Skia lib                            root 3,949 PASS / 13 ignored
                                             internal crates 182 PASS
Native Skia #2225                           2/2 PASS
Native Skia direct PDF                      4/4 PASS
Docker WASM                                PASS
```

로컬 `cargo-nextest`는 0.9.137이고 저장소 권장은 0.9.140이라 경고가 발생했다. 실행 자체와 8,451건의 결과에는
영향이 없었으나 환경 차이로 기록한다.

검증 worktree checkout 과정에서 기존 `pdf-large/hwpx/2026_oss_rst.pdf`가 LFS pointer가 아니라는 경고도
재현됐다. 이번 변경이 만든 파일이 아니며 수정·stage하지 않았다.

## 6. #4968 완료 조건 disposition

| 이슈 완료 조건 | 근거 | 판정 |
| --- | --- | ---: |
| kerning flag가 style resolution부터 최종 glyph positioning까지 전달 | Q3-1, R4C, R4D, R4E native/WASM runtime | 충족 |
| on/off controlled fixture가 pair 차이와 비대상 무변화 검증 | 공개 runtime fixture, off 11/11, on 11/11 | 충족 |
| stored `LineSeg`와 fresh layout cohort 분리 | Q0 cohort, runtime stored 12/fresh 10, R4C fresh consumers | 충족 |
| 실제 kerning 사용 aggregate와 backend parity 보고 | Q0 157문서·175,466자 비식별 집계, R4E 22문맥 parity | 충족 |
| 실패·미지원 상태가 구조화 진단으로 fail-closed | capability reason과 failure matrix 6/6 | 충족 |

## 7. 범위·보호 불변식 대사

1. Q0~Q2 공개 정본과 한컴 판정 JSON은 byte identity를 유지했다.
2. exact source는 family 추측 없이 slot·bytes·face index로만 등록했다.
3. kerning-off와 source 부재 경로는 K0를 유지했다.
4. native와 WASM은 같은 face·request·lane에서 같은 measurement·line·positions를 냈다.
5. backend는 layout positions만 재생했고 자체 source lookup·shaping을 하지 않았다.
6. full font·fixture·private identity는 WASM blob에 내장되지 않았다.
7. 32MiB font, 4,096 scalar, 4,097 positions, 256 segment 상한을 우회하지 않았다.
8. stored `LineSeg` validity는 버전 분기가 아니라 현재 구조의 feature detection을 유지했다.
9. fallback·metric DB·Studio 자동 slot binding·GSUB·vertical metrics·variable axis는 변경하지 않았다.

## 8. 다음 승인 경계

메인테이너가 이 R4E-3 결과를 승인하면 다음 순서로 진행한다.

1. 현재 보고서와 병합 정정·embedding probe를 단계 커밋한다.
2. push 직전에 원격 `devel`을 다시 fetch해 정확한 SHA와 conflict 여부를 확인한다.
3. 필수 format gate를 다시 통과시킨 뒤 push 승인을 받는다.
4. 별도 승인 후 `devel` 대상 PR을 생성한다.
5. 최신 PR head의 CI 성공 뒤 self-review, merge와 #4968 close를 각각 승인받는다.

메인테이너가 R4E-3 결과와 단계 커밋 진행을 승인했다. 이 커밋 이후에도 remote push, PR, GitHub comment,
issue close는 각각 다음 승인 경계를 따른다.
