# #5543 Stage 1 — compat 2024 Δ1 확장: 이월 앵커 사다리

## 무엇을·왜

#5524 Δ1(선행 앵커 줄)의 두 번째 형상. 빈 호스트의 자리차지 스택(다중 TAC)이 쪽 하단에
걸리면 한글 2022 계열은 호스트의 마지막 앵커 줄을 **다음 쪽 상단으로 이월**해 계상한다
(저장 lineseg 마지막 세그 vpos=0). 한글 2024 는 이월 계상을 하지 않는다.

실측(156609754 = `samples/issue5543_carried_anchor_ladder.hwpx`): 2022 재저장 앵커 문단
ls=3(이월 세그 3,597HU) ↔ 2024 재저장 ls=2, 다음 쪽 사다리 전체가 3,597HU 위. 하류 쪽 경계
pi42 가 4쪽↔3쪽으로 갈린다.

## 어떻게

`typeset_tac_table` 다중 TAC 분기: compat 모드에서 새 단 최상단(current_items 비어 있음)의
호출이 빈 호스트의 이월 사다리(마지막 저장 세그 vpos==0·비합성, 매핑 세그 vpos>0)를 계상하려
하면 계상을 0 으로 하고 `hangul2024_reclaimed` 에 적립 — #5524 의 회수·재적합 기제를 그대로
재사용한다.

## 검증 실측

- Δ1 프로브 40건 게이트: **IMPROVE 11(완전일치 9) / SAME 29 / REGRESS 0** — #5524 랜딩분
  (10/0) 대비 156609754 완전일치 +1, 회귀 0 유지.
- 기본값(플래그 없음) 247건 코호트 지문 diff **0행**.
- 통합 테스트 `issue_5543_carried_anchor_ladder`(실샘플): 기본 pi42=4쪽 / compat 3쪽 / 복귀.
- fmt --all --check·manifest·tiers·clippy -D warnings 통과, nextest 전체 실행.

## 이번 반복에서 실측 기각한 설계 (기록)

tac=false 블록 표 앵커 줄(36355686/156458354 형)의 자격 확장 3종 — ① 광역 저장-흐름 gap
자격(트림-드리프트와 미구분, 회귀 13) ② 계상-불변 토큰+보너스(회귀 4) ③ 토큰·잉여 일치
판정(진짜 경계는 불일치·가짜 경계가 우연 일치, 회귀 2). 이 형상은 rhwp 흐름 계상이 이미
2024 와 같고 저장 경계 신호만 2022 를 고정하는 구조라, 신호-단위 판별자가 없으면 안전하게
덮을 수 없다 — 후속 축으로 보류.
