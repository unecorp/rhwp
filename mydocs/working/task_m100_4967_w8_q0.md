---
kind: working-note
status: completed
issue: 4967
stage: W8-Q0
last_verified: 2026-08-25
---

# Task M100 #4967 Stage W8-Q0 — 기존 증거 호환성과 rank 8 cohort

- **계획**: [`task_m100_4967.md`](../plans/task_m100_4967.md)
- **공개 baseline**:
  [`rank8_qualification_baseline.json`](../tech/investigations/issue-4967/rank8_qualification_baseline.json)
- **실행 정책**: 10k 재계측 없음, Hyper-V Oracle 재실행 없음

## 1. 기존 자료 재사용 결과

W3의 10k final aggregate와 10,000-entry checkpoint journal, W4 공개 ranking, W5 rank 8 ladder와 현행
W7.5 registry를 hash·semantic identity로 대사했다. corpus 문서를 다시 열거나 parse하지 않고 journal의
기존 decision만 streaming 조회했다.

rank 8 `KoPubWorld바탕체 Light`를 사용한 실제 문서는 6건이며 HWP 3, HWPX 3이다. 문서별 journal의
target usage row는 169개이고, 전건 final aggregate가 동일 조건을 합친 row는 164개다. 두 층의 문자 합계는
모두 43,432자로 같다.

| 항목 | 결과 |
| --- | ---: |
| 위험 문자 | 43,374 |
| face-miss / heuristic / char-miss | 43,342 / 32 / 0 |
| 압축 노출 전체 / 위험 category | 30,882 / 30,838 |
| 압축·fixed-context 위험 | 9,034 |
| stored / fresh 위험 문자 | 43,374 / 0 |
| body / table-cell / footnote / header | 23,442 / 18,937 / 928 / 125 |

W4의 category, format, 압축·fixed-context와 fresh lane 수치에 모두 정확히 일치했다. fresh 실사용 위험량은
0이므로 이후 Stage W8-Q1에서 fresh는 공개 fixture의 비대상 회귀 가드로만 사용한다.

## 2. 공개·비공개 경계

`scripts/font_rank8_qualification.py`는 511MB journal을 1회 streaming 처리하고 입력 크기·line 크기·index
중복·document count·W4 count와 W5 ladder, registry decision plane을 fail-closed 검증한다.

- local-only 원장: `output/4967/w8-q0/rank8_private_cohort.json`, mode `0600`
- tracked baseline: document count·aggregate·evidence digest만 포함, mode `0644`
- 공개 baseline의 absolute path, 문서명·본문·문서 hash·font bytes: 0

현행 registry에서 target active rule은 `canvas2d-webfont`, `canvaskit-sfnt`의 supply 두 개뿐이며 다른
decision plane은 없다.

## 3. 검증

```text
python3 -m unittest scripts.tests.test_font_rank8_qualification
5 tests OK

projector result
documents=6, riskCharacters=43374, hyperVOracleRerun=false
```

projector 계약 테스트는 private/public 분리, canonical hash, journal index 중복, W4 count drift와
cross-plane registry drift를 판정한다.

## 4. 다음 gate

Stage W8-Q0 변경은 승인된 local stage commit으로 고정했다. Stage W8-Q1에서 공개 rank 8 fixture와
current HEAD native/Docker WASM trace 기준선을 만든다.
