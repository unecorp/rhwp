# Task M100 #4969 W10-Q3-E5 — 성능·크기·전체 회귀 최종 결과

- **결과 상태**: `qualified`, 메인테이너 결과 승인·checkpoint 생성 승인
- **실행일**: 2026-08-30 KST
- **제품 기준 HEAD**: `8bcaa54ed872c5390620ce190ad8122b09036f31`
- **최신 upstream/devel**: `a71ebf2141ae3bd1ca3b5d3ea438bbe3454edaf9` (`33 ahead / 0 behind`)
- **기계 판독 증적**:
  [`w10_q3_e5_final_qualification.json`](../tech/investigations/issue-4969/w10_q3_e5_final_qualification.json)

## 판정

Q3-E5는 **qualified 후보**다. 명시 요청이 없는 기존 출력과 explicit default 출력은 결정적으로 보존되고,
interior/max variable instance만 bounded `GlyphRun`·`GlyphOutline`을 게시한다. 반복 layer build의 병목은 variable
outline 자체가 아니라 매 호출마다 1.5MB font bytes를 복사하고 source digest·face preparation을 되풀이한 데 있었다.
font bytes를 `Arc<[u8]>`로 공유하고 기존 page render tree·JSON과 같은 generation에서 무효화되는 page-local,
profile-keyed `PageLayerTree` cache를 최대 4개 variant로 제한해 원인을 제거했다.

성능, WASM 크기, cache/resource 선형성, Studio·CanvasKit, native full, Native Skia와 lint gate가 모두 승인된
hard gate 안이다. cold first build는 과거 동등 기준이 없어 이번 값을 최초 baseline으로 공개하며 사후 ceiling을
적용하지 않았다.

## 성능 결과

같은 host·release-test profile에서 각 case를 warm-up한 뒤 9개 독립 process로 측정했다. 표의 warm 값은 64회
`build_page_layer_tree` 합계다.

| case | cold p50 / p95 | warm 64회 p50 / p95 | default 대비 판정 |
| --- | ---: | ---: | --- |
| exact-default | 5,238,942 / 5,543,362 ns | 29,063 / 30,059 ns | 기준 |
| explicit-default | 5,365,069 / 6,900,372 ns | 29,659 / 41,543 ns | +2.05% / +38.20%, 상대 gate 통과 |
| interior 650/650 | 7,883,886 / 9,389,626 ns | 143,394 / 255,633 ns | +114,331 / +225,574 ns, 절대 gate 통과 |
| max 900/900 | 7,980,947 / 10,440,417 ns | 126,818 / 128,915 ns | +97,755 / +98,856 ns, 절대 gate 통과 |

default warm p50가 1ms 미만인 noise 구간이므로 non-default는 승인된 절대 증가 상한 p50 1,000,000ns,
p95 2,000,000ns로 판정했다. 호출당 상한은 각각 15.625us·31.25us이고, interior/max 모두 충분히 낮다.

no-request Source Han fixture는 upstream 기준 p50 45,933,129ns / p95 46,983,320ns에서 후보 p50 84,673ns /
p95 141,188ns로 줄었다. layer JSON 619,562 bytes와 BLAKE3
`0b5212cc076c34dce706039d7c4da85936c0a6769e83f08baa7f158cbd9029de`, CanvasKit plan 1,755 bytes와 BLAKE3
`a98e6933d76901cfa55a43205c758675a8864527f87f8bed7a689d16267cbb56`는 그대로다.

수정 전 component 64회 p50은 interior 129,690,030ns, max 130,233,534ns였다. 원인 분해에서 fresh-page
lowering p50 128.8ms/64와 shared-page-cache p50 2.78ms/64가 관찰되어 약 97.8%가 반복 font preparation/resource
copy 비용임을 확인했다. 수정 뒤 제품 warm 경로는 0.13~0.14ms/64 수준이다.

## 출력·resource 불변식

- exact-default와 explicit-default: TextRun 1, GlyphRun 0, GlyphOutline 0, font blob/face 0이며 layer·CanvasKit hash가 같다.
- interior/max: TextRun·GlyphRun·GlyphOutline 각 1, unique font blob/face 각 1이다.
- `1/2/8 instance × 1/2/8 run` 전 조합에서 digest pass·source face parse·arena intern은 unique source당 1회다.
- parsed instance face와 cached result는 instance 수 1/2/8에만 선형이고, 추가 run은 0/1/7, 0/2/14,
  0/8/56 cache hit로 흡수된다.
- 최대 8 instance × 8 run에서도 source bytes 1,503,064, font blob/face 1, payload 2,613,391 bytes이며
  glyph run/outline 각 64로 bounded하다.
- cache hit은 기존 output/profile fingerprint를 key로 사용하고, page tree·JSON cache와 같은 편집 generation에서
  함께 무효화된다. page당 variant 상한은 4다.

## WASM·CanvasKit

최종 소스의 표준 Docker WASM build와 `wasm-opt`가 10분 17초에 통과했다. `pkg/rhwp_bg.wasm`은
9,810,922 bytes, SHA-256 `c851f8fd2b04221b1a68950f5c0ab921ac9255dfb343f39bc5953e903cbc8408`다.
Q3-E0 기준 9,759,368 bytes보다 51,554 bytes, 0.528251% 증가해 `<=65,536 bytes`와 `<=0.75%` 두 gate를
모두 통과한다.

CanvasKit software WASM surface E2E는 exact Source Han face에서 실제 픽셀을 만들었다. 현재 sqrt-ratio 계약은
58×34px, 과거 x-only 축소 계약은 58×38px로 같은 폭을 유지하면서 잘못 컸던 세로 ink를 줄였다. font cache는
blob 1, typeface 1, font 2, bytes 456,688로 bounded했다.

## 전체 검증

- `cargo build --locked --release`: 통과, 첫 release build 10분 29초
- `cargo test --locked --release --lib`: 통과
- 최종 `cargo nextest ... --tests --test-threads 12 --no-fail-fast`: **8,759 passed / 45 skipped / 2 slow**,
  실행 387.490초
- Native Skia lib: rhwp **3,948 passed / 13 ignored**, missing-picture **2/2**, direct PDF **4/4**
- page cache lineage 보호: 머리글·바닥글 숨김 및 빈 문서 교체 **2/2**
- Q3 focused: atomic **7/7**, glyph lowering **18 passed / 1 ignored**, shadow context **18/18**
- passthrough classification guard: **5/5**
- Studio TypeScript·전체 test·production build: **1,276 passed / 1 skipped**, 242 modules
- CanvasKit #4969 pixel E2E: 통과
- Node WASM/editor 계약: **3/3**
- Rust doc test: **8 passed / 3 ignored**
- native·wasm32·workspace all-target Clippy `-D warnings`: 통과
- `cargo fmt --all`, `cargo fmt --all -- --check`, manifest prepare/check, `git diff --check`: 통과
- integration 파생 상태: 1,039 sources / 4,608 static attrs / 32 suites + 16 exceptions / 48 targets

로컬 nextest 0.9.137은 저장소 권장 0.9.140보다 낮아 기존 version warning과
`report-skipped` 미인식 warning을 냈지만 test 실행·판정에는 영향을 주지 않았다.

## 검증 중 발견·정정한 절차 문제

1. 최초 matrix fixture가 run마다 독립 registry/context를 만들어 source identity 보호 불변식에 거절됐다. 한
   registry/context/transaction으로 고쳐 실제 cache ownership을 측정했다.
2. generated suite 재배치 후 atomic test를 이전 suite 022로 호출해 0건을 실행했다. 현재 manifest를 조회해 suite
   024로 정정했고, 이후에는 공식 router를 사용했다.
3. all-target Clippy가 고정 8개 instance fixture의 불필요한 `Vec`를 검출했다. 배열로 최소 정정해 재통과했다.
4. 첫 full nextest에서 exact-font instance set/clear API가 미분류 문서 뮤테이터로 탐지됐다. 두 API는 문서 IR이
   아니라 request·layout cache만 바꾸므로 기존 #4968 registry API와 같이 `SessionState` 근거로 등재했다. focused
   5/5 뒤 최종 full 8,757/8,757을 다시 통과했다.
5. EXEMPT 추가로 source weight가 바뀐 직후 stale generated suite를 한 번 호출해 0건을 얻었다. 이를 성공으로
   기록하지 않고 prepare/check 후 재실행했다.
6. 기존 warm aggregate를 cold ceiling으로 재사용하려던 초안은 단위·측정 조건이 달라 철회했다. cold는 이번
   최초 baseline으로 공개하고 비교 가능한 warm gate만 hard 판정했다.
7. 최종 diff 감사에서 빈 문서 교체와 머리글·바닥글 숨김이 기존 page tree만 직접 비우고 새 `PageLayerTree`
   cache까지 무효화하지 않는 경로를 발견했다. 전체·단일-page 공통 헬퍼로 세 캐시를 함께 비우고 실제 cache를
   채운 보호 테스트 2건을 추가한 뒤 전체 회귀·Native Skia·WASM을 최종 소스에서 재실행했다.
8. 신규 보호 테스트의 첫 focused 호출은 짧은 이름에 `--exact`를 써 0건을 선택했다. 이를 통과로 기록하지 않고
   `--list`로 전체 경로를 확인한 뒤 각 테스트 1/1과 full suite 포함 실행으로 고정했다.

## 보호 불변식과 다음 승인 경계

- no-request/default 및 explicit-default layer JSON·geometry·CanvasKit plan 변화: 0
- unsupported backend의 non-default 선택과 partial publication: 0
- Q2 old-Hangul predicate·segment selector, fallback registry·parser heuristic 변경: 0
- 문서 IR 비변경 session API 때문에 HWP/HWPX raw passthrough를 불필요하게 무효화한 경로: 0
- 새 integration source, generated suite·manifest·Cargo marker stage: 0
- private corpus·Hyper-V·한컴 Oracle 사용: 0
- generated `pkg`·Studio `dist` tracked diff: 0

최신 `upstream/devel`은 이미 현재 HEAD의 조상이며 behind 0이다. 메인테이너가 이 결과와 `qualified` 판정 및
구현·결과 checkpoint 생성을 승인했다. 다음 경계는 Q4 인계 여부 결정이다. push, PR, GitHub comment는 자동
승인되지 않는다.
