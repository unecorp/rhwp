# Task M100 #4969 W10-Q3-E4 — atomic portable publication·backend matrix 결과

- **결과 상태**: `qualified-atomic-portable-publication`, 메인테이너 승인 완료
- **checkpoint 상태**: 생성 승인·이 문서와 구현을 같은 commit으로 고정
- **실행일**: 2026-08-30 KST
- **기준 checkpoint**: `d1b9f495f8177124bd3c09e48828acfdf20fc5da`
- **검증 기준 upstream**: `2bcf9b261c3b761d114bc2b3a35ed85ccd1e461e`
- **현재 upstream**: `97c4d71550f24182f920d23c0f65d4a6371a6e48` (`29 ahead / 5 behind`)
- **기계 판독 증적**:
  [`w10_q3_e4_atomic_portable_publication.json`](../tech/investigations/issue-4969/w10_q3_e4_atomic_portable_publication.json)

## 판정

Q3-E4는 **qualified-atomic-portable-publication**이다. canonical non-default instance가 완전한
`GlyphRun`과 producer-resolved monochrome `GlyphOutline`을 만들었을 때만 같은 text source, equivalence group,
anchor에 두 variant를 한 번에 게시한다. outline 생성과 resource 상한 검사는 resource mutation보다 먼저 끝나며,
둘 중 하나라도 없으면 nominal claim을 얻지 않고 기존 `TextRun`만 남는다.

CanvasKit policy와 Browser/Studio selector는 완전하고 재생 가능한 `GlyphOutline`만 선택한다. 빈 path 또는 multipart
불완전 outline은 variable `GlyphRun`도 선택하지 않고 `TextRun`으로 되돌아간다. Native Skia, SVG, Canvas2D는 Q3
non-default outline을 capability 미지원으로 거절하고 같은 fallback을 선택한다.

## 구현 결과

- explicit measurement의 `GlyphRun` diagnostics를 `q3ExplicitInstanceGlyphRunProjectionV1`로 분리했다.
- variable outline 생성 실패는 font resource 등록 전에 `variableOutlineUnavailable` 또는 구체 실패 사유로 닫힌다.
- page lowerer는 explicit instance에서 `GlyphRun`과 `GlyphOutline`이 모두 있을 때만 두 op와 claim을 함께 게시한다.
- Q3 variable outline은 CanvasKit·CanvasKit Browser에서만 선택 가능하며 다른 backend에는 영향이 없다.
- Studio selector는 outline 우선순위를 유지하되 재생 실패·구조 불완전 시 `TextRun` fallback을 선택한다.
- explicit default 요청은 registry·cache provenance에서는 독립 상태로 남지만 제품 composer activation에는 진입하지 않는다.
  최초 검사에서 explicit default가 no-request 폭 `21.0px`을 `19.6267px`로 바꾸는 결함을 발견했고, 검사를 약화하지
  않고 canonical axis가 빈 transaction을 기존 Q2 composer로 돌려 layer JSON과 bbox를 정확히 복원했다.

## 출력·backend 증적

Happiness Sans VF의 `가변`, Title instance(`opsz=900`, `wght=900`) 제품 영수증은 다음과 같다.

| 항목 | 결과 |
| --- | ---: |
| no-request TextRun width | 21.0 px |
| Title TextRun width | 19.840000000000003 px |
| delta | -1.1599999999999966 px |
| explicit GlyphRun | 1 |
| explicit GlyphOutline | 1 |
| CanvasKit·Browser 선택 | GlyphOutline |
| Native Skia·SVG·Canvas2D 선택 | TextRun fallback |

explicit default는 request generation 2와 빈 canonical axis로 별도 등록되지만, 제품 layer JSON과
TextRun x/y/width/height가 no-request baseline과 정확히 같다. Q3-D의 measurement 계약은 Q2 default와 explicit
default의 settings hash·advance가 같고 cache `Arc`와 request provenance만 다름을 별도 고정한다.

기존 default 영수증도 E0~E3와 동일하다.

- layer JSON: 619,562 bytes,
  BLAKE3 `0b5212cc076c34dce706039d7c4da85936c0a6769e83f08baa7f158cbd9029de`
- CanvasKit plan: 1,755 bytes,
  BLAKE3 `a98e6933d76901cfa55a43205c758675a8864527f87f8bed7a689d16267cbb56`

## 검증 결과

- composition handoff: **5 passed / 0 failed**
- atomic product activation: **7 passed / 0 failed**
- glyph lowering·backend·malformed/incomplete fallback: **17 passed / 0 failed**
- Q3-E red filter: **4 passed / 0 failed**
- Studio 전체: **1,247 passed / 0 failed / 1 skipped**
- Studio Q3-E4 focused: **1 passed / 0 failed**
- Studio production build: 239 modules, 통과
- Rust unit-tier inventory: 4,221 tests / 299 modules, 통과
- `cargo fmt --all`, `cargo fmt --all -- --check`, `git diff --check`: 통과
- all-target Clippy `-D warnings`: 통과
- Docker WASM release + `wasm-opt`: 6분 50초 wall time, 통과

WASM 산출물은 9,796,113 bytes,
SHA-256 `94d622aa3b6ec5a6288b46606b703609dc069c0ed08322f0db9d7a1bf6d28607`다. E3보다 873 bytes
(0.008912%) 증가했다. 최종 크기·성능 hard gate와 실제 CanvasKit visual E2E는 계획대로 Q3-E5에서 판정한다.

## 보호 불변식

- no-request/default layer JSON·CanvasKit plan 변화: 0
- explicit default 제품 layer JSON·geometry 변화: 0
- 불완전 pair 또는 실패 resource의 부분 게시·partial claim: 0
- Native Skia·SVG·Canvas2D non-default 선택: 0
- Q2 old-Hangul predicate·segment selector 확대: 0
- font fallback registry·parser heuristic 변경: 0
- 새 integration source·generated suite·manifest·Cargo marker stage: 0
- private corpus·Hyper-V·한컴 Oracle·static instance font 사용: 0
- generated `pkg`·Studio `dist` tracked diff: 0

## 원격 drift와 다음 승인 경계

검증 뒤 fetch에서 `upstream/devel`이 기준보다 5 commit 전진했다. 실제 원격 delta는 회전 저장 비트·CLI 진단·review
문서 14개 파일이며 E4 변경 7개 파일과 교집합은 0, merge-tree conflict도 0이다. E4 결과와 구현·결과 checkpoint를
먼저 승인받아 고정한 뒤 최신 devel을 병합하고 focused/default/Studio/WASM 재자격화를 수행하는 것이 안전하다.

Q3-E4 결과와 구현·결과 checkpoint 생성을 승인받았다. 다음 경계는 최신 devel 병합·재자격화 계획 승인이다.
Q3-E5 착수, remote push, PR, GitHub comment는 자동 승인되지 않는다.
