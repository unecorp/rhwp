---
kind: working-note
status: completed
issue: 4967
stage: W8-R1-Q0
last_verified: 2026-08-25
---

# Task M100 #4967 Stage W8-R1-Q0 — rank 1 증거 호환성과 source 경계

- **계획**: [`task_m100_4967_v2.md`](../plans/task_m100_4967_v2.md)
- **공개 baseline**:
  [`rank1_qualification_baseline.json`](../tech/investigations/issue-4967/rank1_qualification_baseline.json)
- **source attestation**:
  [`rank1_source_provenance_attestation.json`](../tech/investigations/issue-4967/rank1_source_provenance_attestation.json)
- **실행 정책**: 10k 재계측 없음, Hyper-V Oracle 재실행 없음, 제품 mutation 없음

## 1. 기존 자료 재사용 결과

W3의 10,000-entry checkpoint journal, W4 공개 ranking, W5 rank 1 ladder, W7.5 v2 registry와 W7 metric
projection을 hash·semantic identity로 대사했다. corpus 문서를 다시 열거나 parse하지 않고 journal의 기존
decision만 streaming 조회했다.

rank 1 `문체부 바탕체` 실제 cohort는 22문서이며 HWP 15, HWPX 7이다. 문서별 target usage row는 813개,
final aggregate의 같은 face usage row는 525개다. 두 층의 target 문자 합계는 모두 209,066자다.

| 항목 | 결과 |
| --- | ---: |
| 위험 문자 | 208,986 |
| face-miss / heuristic / char-miss | 208,949 / 37 / 0 |
| compressed 전체 / 위험 | 166,944 / 166,896 |
| compressed·fixed-context 위험 | 153,061 |
| stored / fresh 위험 | 208,986 / 0 |
| table-cell / body / footnote | 189,610 / 19,143 / 313 |

W4의 category, format(HWP 169,593자·HWPX 39,473자), compressed fixed-context와 fresh lane 수치에 모두
정확히 일치했다. 위험 문자의 90.7%가 table-cell이고 79.9%가 ratio 축소 또는 음수 자간에 노출된다.
그러므로 이후 판정은 평균 폭이나 page count가 아니라 actual table frame의 line crossing을 보호해야 한다.

## 2. exact source와 현행 metric anchor

local-only `/home/edward/mygithub/ttfs/hwp/MT.TTF`는 W5가 봉인한 SHA-256
`d10509215d923fef07c1f2dffe8ebf55cbca706476559a861dff6f7cf969ff44`와 일치했다.

| 항목 | 관찰 |
| --- | --- |
| SFNT family | `MBatang`, `문체부 바탕체` |
| unitsPerEm | 1,000 |
| glyph / cmap / hmtx | 2,357 / 2,356 / 2,357 |
| `OS/2.fsType` | 2, restricted-license embedding |
| 현행 metric anchor | `MBatang`, index 370, regular |
| v2 explicit rule | `문체부 바탕체`·`MBatang` 모두 0 |

localized name과 English name이 같은 SFNT에 있다는 사실은 identity 후보를 강화한다. 그러나 제품 runtime이
raw document face를 실제로 `MBatang` metric까지 전달한다는 증거는 아니다. serializer alias, HWP/HWPX
parser와 layout metric lookup을 Stage W8-R1-Q1에서 따로 관찰한다.

## 3. provenance·portable supply 판정

[문화체육관광부 공식 자료](https://www.mcst.go.kr/site/s_notice/press/pressView.jsp?pCurrentPage=1&pMenuCD=0302000000&pSearchType=&pSearchWord=&pSeq=15621&pTypeDept=)의
첨부 안내는 문화체육관광부 바탕체를 누구나 자유 이용할 수 있고 유료 판매를 금지하며 출처 표시를 요구하는
조건으로 소개한다. 다만 이번 Q0에서는 그 공식 자료가 제공한 artifact와 local `MT.TTF`의 byte lineage를
확인하지 못했다. local font의 `fsType=2`도 embedding 제한을 선언한다.

따라서 다음을 분리한다.

- metric 계측 input: local-only 사용 가능
- installed exact-font opt-in 후보: Q1 이후 기능 탐지 대상으로 유지
- font bytes 저장소 포함·webfont/CanvasKit 공급: `blocked-unmatched-official-artifact-and-restricted-embedding`
- `KoPubWorld바탕체 Light`: W5 문서 지정 substitution일 뿐 identity·official successor가 아님
- 정부상징 legacy font와 `ROKG`: rank 1과 무관한 별도 family 계보

## 4. 공개·비공개 경계와 검증

`scripts/font_rank1_qualification.py`는 511MB W3 journal을 1회 streaming 처리하고 입력 크기·line 크기·index
중복·document count·W4 count·W5 ladder·source identity·metric anchor·registry drift를 fail-closed 검증한다.

- private 원장 mode `0600`, tracked baseline mode `0644`
- 공개 baseline의 absolute path, 문서명·본문·문서 hash·font bytes: 0
- public canonical SHA-256: `51c2c6354ee5f8821db49e6e71690928461d1dc811ee76a8f1b246d382a13003`

```text
python3 -m unittest \
  scripts.tests.test_font_rank1_qualification \
  scripts.tests.test_font_rank8_qualification

9 tests OK

projector result
documents=22, riskCharacters=208986,
hyperVOracleRerun=false, portableSupplyQualified=false
```

## 5. 다음 gate

Stage W8-R1-Q1은 HWP와 HWPX 공개 fixture에서 다음 경계를 전건 trace한다.

```text
raw document face
  → parser/serializer normalization
  → layout-name decision
  → layout-metric lookup
  → paint family
```

`문체부 바탕체`가 이미 `MBatang` metric anchor에 도달하면 W4 face-miss는 제품 miss가 아니라 계측 projection
차이일 가능성이 높다. 도달하지 않으면 첫 divergence를 정확히 한 decision plane으로 좁힌다. Q1은 별도
승인 뒤 시작하며 Q0 변경은 제품 font 동작을 바꾸지 않는다.
