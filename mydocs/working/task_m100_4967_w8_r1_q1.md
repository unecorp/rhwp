---
kind: working-note
status: completed
issue: 4967
stage: W8-R1-Q1
last_verified: 2026-08-26
---

# Task M100 #4967 Stage W8-R1-Q1 — runtime name·metric 관찰 경계

- **계획**: [`task_m100_4967_v2.md`](../plans/task_m100_4967_v2.md)
- **선행 기준선**: [`rank1_qualification_baseline.json`](../tech/investigations/issue-4967/rank1_qualification_baseline.json)
- **결과 정본**: [`rank1_runtime_boundary.json`](../tech/investigations/issue-4967/rank1_runtime_boundary.json)
- **권한 경계**: 공개 fixture의 읽기 전용 계측, 제품 font 정책 변경 없음

## 1. 판정 질문과 결론

Q1의 질문은 W4가 센 `문체부 바탕체` face miss가 raw document name만 본 계측 오탐인지, 현재 제품
runtime도 `MBatang` metric에 도달하지 못하는 실제 miss인지였다.

결론은 runtime의 실제 unresolved 경계다. HWPX와 HWP5 모두 parser 이후에도 localized name이 그대로이고,
layout-name 변환 step이 0이라 기존 `MBatang` metric anchor 앞에서 멈춘다. 따라서 첫 divergence는
`layout-metric`이 아니라 `layout-name`이다. Q1 disposition은
`qualified-for-q2-layout-name-hypothesis`다.

## 2. 공개 HWP·HWPX fixture 계보

기존 #4963 공개 HWPX fixture를 source로 사용했다.

- HWPX SHA-256:
  `8ded3aff6f0286ee5ee4ad9c66732026fa627220b529e5d0fa7b9d51bc3ddb3f`
- 변환: `rhwp convert <source> <output> --verify --verify-pages`
- HWP5 SHA-256:
  `081b597eb85c8431f691236ce36517edf863c1f10563d09edf2617a48cfbcb6b`
- 반복 변환 byte digest 동일, IR difference 없음, page count 1→1

생성 HWP와 계보 manifest는
[`fixtures/rank1_runtime_boundary.hwp`](../tech/investigations/issue-4967/fixtures/rank1_runtime_boundary.hwp),
[`fixtures/rank1_runtime_boundary.manifest.json`](../tech/investigations/issue-4967/fixtures/rank1_runtime_boundary.manifest.json)에
고정했다. font bytes와 private corpus identity는 넣지 않았다.

공개 `samples/` 832건도 선언 face를 확인했다. `문체부 바탕체`를 font table에 선언한 파일은 3건이었지만,
기존 두 HWP는 single-pass SVG에 target paint run이 없었다. font table 선언을 실제 사용 증거로 오인하지
않기 위해, 실제 target run 1,556건을 가진 공개 HWPX와 그 결정적 HWP 변환본을 Q1 경계 fixture로 삼았다.

## 3. same-snapshot 결과

| 관찰 | HWPX | HWP5 |
| --- | ---: | ---: |
| target trace record | 1,556 | 1,556 |
| layout-name step 0 | 1,556 | 1,556 |
| metric entry `null` | 1,556 | 1,556 |
| match kind `none` | 1,556 | 1,556 |
| heuristic full/half/narrow | 390 / 1,114 / 52 | 390 / 1,114 / 52 |
| native ↔ WASM | byte-exact | byte-exact |

두 형식에서 requested·normalized·alias-resolved face는 전건 `문체부 바탕체`다. 형식 간 runtime decision
semantics와 canonical trace digest도 같았다.

```text
canonicalTraceSha256=1f90e24b14bc47315b19e358e5db16ff60dbed1a35a313290f493eb944e74b20
targetDecisionPlane=layout-name
disposition=qualified-for-q2-layout-name-hypothesis
```

paint trace는 각 backend에 요청한 raw family만 보여 준다. Studio snapshot 없이 actual Canvas2D·CanvasKit
paint family나 glyph identity를 추론하지 않는다.

## 4. 보호 불변식

- HWP와 HWPX를 독립 계측하고 format 간 의미 동등성을 fail-closed로 검사한다.
- native와 현행 WASM의 full trace canonical byte parity를 형식별로 검사한다.
- `maxCharacters=4096` 상한을 유지하고 1,556건 전건 완료를 확인한다.
- 제품 metric DB·registry·fallback·paint·supply source를 바꾸지 않는다.
- 10k corpus 전수·Hyper-V Oracle을 다시 실행하지 않는다.
- private 문서 경로·이름·본문·식별 hash, absolute path, font bytes와 full trace를 tracked output에 넣지 않는다.

## 5. 재현·검증

- projector: `scripts/font_rank1_runtime_boundary.mjs`
- 계약 테스트: `scripts/tests/font_rank1_runtime_boundary.test.mjs`
- 공개 결과 canonical SHA-256:
  `b95e77121f939cfd58440afd941a100e1eb7539bf81b182e7ade708d7ea52720`
- rank 1·rank 8 Node 계약 테스트: 8/8 통과

2026-08-26 `upstream/devel@ee7e8a6ed`와 PR 생성 직전의 `upstream/devel@6240d255b`를 차례로 통합할
때마다 네이티브 진단 바이너리와 Docker WASM을 다시 빌드했다. 최신 HWPX·HWP5 각 1,556건의 trace
semantics와 native/WASM byte parity는 그대로였고, 위 canonical hash 변경은 새 binary·WASM 입력 digest를
반영한 결과다.

## 6. 다음 게이트

Q2에서만 가상 `문체부 바탕체 -> MBatang` name relation을 적용한다. 그 상태에서 현행 generated
`MBatang` metric과 exact local `MT.TTF hmtx`를 fixture의 실제 사용 문자에 한정해 비교한다. 이는
가설 계측이며 제품 alias·metric·paint·supply 변경을 승인하지 않는다. metric 호환성이 없거나 이미
exact-derived라면 `no-change`로 회수한다.
