# Task M100 #4969 W10-Q3-E0 — 최신 기준·red baseline 결과

- **결과 상태**: `qualified-red-baseline`, 메인테이너 승인
- **checkpoint 상태**: 생성 승인·고정 완료
- **승인일**: 2026-08-29 KST
- **기준 checkpoint**: `558e27f6d` (Q3-D)
- **원격 기준**: `upstream/devel@955abb5268c3`, branch `19 ahead / 0 behind`
- **실행일**: 2026-08-29 KST
- **제품 source 변경**: 없음
- **기계 판독 증적**:
  [`w10_q3_e0_red_baseline.json`](../tech/investigations/issue-4969/w10_q3_e0_red_baseline.json)

## 판정

Q3-E0는 **qualified-red-baseline**이다. 최신 devel은 Q3-D branch의 merge-base와 같아 병합이 필요하지 않았다.
Q2 default output은 9회 모두 같은 layer JSON·CanvasKit plan hash를 냈고 기존 Q3-D·atomic product·composition
control 21건이 통과했다. Q3-E1~E4의 미구현 경계는 계획대로 정확히 4건의 red로 분리됐다.

E0 사전 감사에서는 최초 계획의 잘못된 전제도 수정했다. 현행 Q2 product gate와 segment selector는 old Hangul
전용이므로 modern Hangul `가변`과 Latin `Typography`가 기존 Q2 transaction에 진입하지 않는다. Q2 gate를 넓히지
않고 exact request 존재 뒤에만 평가하는 explicit-only single-run 정책을 별도로 둔다는 정정 계획을 승인받았다.

## 최신 기준 확인

```text
HEAD                 558e27f6d5c83d721c20d64eedc335dbfd83e787
upstream/devel       955abb5268c3a6a93a41328633729fd095b7390a
merge-base           955abb5268c3a6a93a41328633729fd095b7390a
ahead / behind       19 / 0
```

따라서 E0에는 merge·conflict·post-merge requalification이 필요하지 않았다.

## default product baseline

기존 Q2 old-Hangul one-line/one-run stored-LineSeg fixture에서 page layer tree를 한 측정당 64회 만들고 warm 상태로
9회 반복했다. 이 수치는 debug test profile의 구조 비교 기준이며 release SLA가 아니다. Q3-E5에서 같은 명령·host·
target cache로 candidate를 비교한다.

| 항목 | 값 |
| --- | ---: |
| 반복 측정 | 9 |
| 측정당 layer build | 64 |
| aggregate p50 | 201,314,277 ns |
| aggregate p95 (nearest-rank) | 207,370,667 ns |
| build당 p50 | 3,145,535.58 ns |
| build당 p95 | 3,240,166.67 ns |
| layer JSON bytes | 619,562 |
| layer JSON BLAKE3 | `0b5212cc076c34dce706039d7c4da85936c0a6769e83f08baa7f158cbd9029de` |
| CanvasKit plan bytes | 1,755 |
| CanvasKit plan BLAKE3 | `a98e6933d76901cfa55a43205c758675a8864527f87f8bed7a689d16267cbb56` |

9회 aggregate ns 원문은 다음과 같다.

```text
199422918 199403891 200487884 207370667 199123448
204676076 202651079 201314277 204218872
```

모든 회차에서 두 hash와 byte length가 같았다. baseline layer에는
`q2CommonShapingCondensedDrawProjectionV1`이 한 개 있고 `GlyphOutline`은 없다.

## Studio·WASM baseline

Q3-D의 Docker WASM 산출물은 제품 source가 같은 checkpoint에서 만들어졌으며 Studio production build가 이를 그대로
복사했다. 두 파일의 byte·SHA-256이 일치했다.

| 항목 | 값 |
| --- | ---: |
| `pkg/rhwp_bg.wasm` | 9,759,368 bytes |
| Studio `rhwp_bg-CxEnH7zj.wasm` | 9,759,368 bytes |
| WASM SHA-256 | `be2e7cdb827a935812c5176d901a8ce43b1e15c3dc47265ea7ee26e428db3d58` |
| Studio main JS | 1,699,677 bytes |
| Studio main JS SHA-256 | `0eb165343f3d14d4d97e8e6aec67f8666ef3daf639370fc3a4de62fed99943a4` |
| Studio CSS | 106,910 bytes |
| Studio CSS SHA-256 | `17e81926a0b122e311840b76b70f7315ea5d8b4a420184aabdbac8ca6b482f57` |
| `npm run build` wall time | 4.34 s |
| Vite transform/build | 239 modules / 845 ms |

Vite의 CanvasKit `fs`·`path` browser externalization과 500 kB chunk 경고는 기존 warning이며 build는 성공했다.

## green control

- Q3 shadow context, red 제외: **13 passed / 0 failed**
- Q2 atomic product + E0 baseline: **4 passed / 0 failed**
- Q2 composition handoff와 old-Hangul gate 음성 계약: **4 passed / 0 failed**
- 합계: **21 passed / 0 failed**
- modern Hangul `가변`, Latin `Typography`, ordinary text는 기존 Q2 candidate predicate에서 모두 false
- `cargo fmt --all`, `cargo fmt --all -- --check`, `git diff --check`: pass

## red 원인

`issue_4969_q3_e0_red` filter는 **0 passed / 4 failed**로 끝났고 다른 실패는 없었다.

1. Q3-E1: per-slot `clear_exact_font_instance_native` owner 부재
2. Q3-E2: `setExactFontInstance`·`clearExactFontInstance` WASM adapter 부재
3. Q3-E3: request-gated modern-Hangul/Latin single-run candidate·line transaction 부재
4. Q3-E4: qualified variable `GlyphOutline` product publication 부재

이 red는 구현 이름·경계를 먼저 고정하는 source contract다. 각 절편에서는 behavior test를 먼저 추가해 실동작을
증명한 뒤 source contract를 green으로 만든다. 문자열만 추가해 red를 우회하는 처리는 허용하지 않는다.

## 보호 불변식

- Q2 old-Hangul predicate·segment selector 변경: 0
- product source·public API·layout output mutation: 0
- request 없는 modern Hangul·Latin의 explicit path 진입: 0
- 새 integration source·generated suite·manifest·Cargo marker: 0
- private corpus·Hyper-V·한컴 Oracle·static instance font 사용: 0
- Docker/Studio 산출물의 tracked diff: 0

## 다음 승인 경계

E0 결과와 checkpoint는 승인·고정됐다. 이후 최신 `upstream/devel@b54f20e39102`을 merge `b5fbed37c`로 통합했다.
tip 비교에서 overlap으로 보였던 Q3 경로는 merge-base 양측 변경 집합에는 없었고 실제 교집합은 오늘할일 한 파일뿐이었다.
병합 뒤 결과는 [별도 재자격화 보고서](task_m100_4969_w10_q3_e0_requalification.md)에 기록했다. 그 결과 승인과
증적 checkpoint를 완료했다. 이후 PDF 정답지 갱신 최신 devel을 별도 승인받아 merge `351817b21`로 통합했다.
다음 경계는 Q3-E1 reversible native owner 구현 착수 승인이다.
