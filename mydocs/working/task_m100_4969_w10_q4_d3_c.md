# Task M100 #4969 W10-Q4-D3-C — publication 교차 검증 결과 후보

- **상위 계획**: [`task_m100_4969_w10_q4_d3_revised.md`](../plans/task_m100_4969_w10_q4_d3_revised.md)
- **기준 checkpoint**: `d49400aad` (Q4-D3-B `qualified-atomic-leaf-publication`)
- **D3-C checkpoint**: `43d50ad99`
- **기준 devel**: `upstream/devel@bd78a53122e4` (merge `049b73d0cd80`)
- **기계 판독 증적**:
  [`w10_q4_d3_c_publication_validation.json`](../tech/investigations/issue-4969/w10_q4_d3_c_publication_validation.json)
- **실행일**: 2026-08-30 KST
- **사전 판정 승인**: `qualified-publication-parity-pre-docker`
- **최종 승인 판정**: `qualified-publication-parity`
- **메인테이너 결과 승인**: 2026-08-30 KST
- **표준 Docker WASM**: 통과 — wasm-pack 6분 43초, 최적화 WASM 9,874,453 bytes
- **backend 선택 변경**: 0 — Q4-D4 전까지 `TextRun` fallback

## 결론

D3-C 사전 게이트에서 native와 격리 Node WASM은 같은 D3-B target에 대해 source 2개, fallback
`TextRun` 2개, leaf별 vertical `GlyphRun` 2개, portable font blob/face 1개를 확인했다. 각 glyph JSON은
자기 `TextSourceId`, `text-N` group, `verticalGlyphRun`, `vertical-rl`, `vertical-upright` 및 한 개의
glyph/position/advance/cluster를 보존한다.

Rust selector와 `text_v2`는 두 leaf 모두 strict variant를 열지 않고
`verticalGlyphOrientationAuthorityPending` 사유로 fallback을 선택했다. 따라서 제품 layer에는 portable
candidate가 존재하지만 backend가 잘못 선택한 경우는 0이다.

native에서 고정한 값과 동일한 assertion을 wasm32+Node에서도 통과시켜 JSON·resource 수치의 런타임 차이는
0으로 판정했다. 메인테이너가 사전 결과를 승인한 뒤 표준 Docker WASM 배포 빌드도 통과했고, 빌드 뒤 같은
native 영수증을 재실행해 수치 변화 0을 확인했다. 따라서 D3-C 최종 판정 후보는
`qualified-publication-parity`다.

## 1. publication·fallback 대사

| 항목 | 값 |
| --- | ---: |
| line publication target | 1 |
| `textSources` | 2 |
| fallback `TextRun` | 2 |
| vertical `GlyphRun` | 2 |
| leaf equivalence group | 2 |
| portable font blob/face | 1 / 1 |
| nominal duplicate | 0 |
| `text_v2` fallback slot | 2 |
| strict vertical slot | 0 |
| backend false selection | 0 |

거부 control은 layer JSON 9,078 bytes이며 vertical glyph와 font resource를 하나도 포함하지 않는다. 따라서
부분 publication residue는 0이다.

## 2. 크기 영수증

| 항목 | bytes |
| --- | ---: |
| portable font payload | 2,519,996 |
| accepted layer JSON | 3,375,512 |
| rejected layer JSON | 9,078 |
| accepted-rejected JSON 증가량 | 3,366,434 |

이 값은 현재 synthetic target의 직렬화 기준선이다. JSON 증가량 대부분은 portable font의 base64 표현을
포함하므로 성능 개선·악화의 결론으로 사용하지 않는다. 이 target은 accepted line 한 개와 unique interned
source 한 개만 측정한다. 여러 line에서 실제 source preparation 호출이 캐시되는지까지 증명하지 않으며, 그
판정은 D5 계측 범위에 남긴다.

## 3. 교차 런타임

| runtime | 결과 |
| --- | --- |
| native Rust | publication 값·크기 assertion 통과, JSON 영수증 출력 |
| 격리 wasm32 + Node | native와 같은 assertion 1 passed / 0 failed |
| 표준 Docker WASM | 성공, wasm-pack 6분 43초 / 전체 명령 429.34초 |

최적화된 `pkg/rhwp_bg.wasm`은 9,874,453 bytes이며 SHA-256은
`4213af83ef6b38c2a8abd923ffe3545ffc920edb8c8526ba4a77e0ac008610f1`이다. 이 절대값은 현재 source의 배포
빌드 영수증이다. 동일 기준의 pristine D3-B 대조 빌드를 별도로 짝짓지 않았으므로 D3-C의 WASM 크기 delta나
성능 결론으로 사용하지 않는다.

격리 harness는 저장소 source를 복사하지 않고 현재 rhwp를 path dependency로 참조했다. 검증 뒤 harness와
418 MiB target은 휴지통으로 이동했으며 worktree에 산출물을 남기지 않았다.

## 4. 회귀·정답지

| 게이트 | 결과 |
| --- | --- |
| atomic activation | 9 passed / 0 failed |
| Q2/Q3 관련 모듈 | 65 passed / 0 failed / 1 skipped |
| Q4 request 계약(D4 red 제외) | 21 passed / 0 failed |
| #6029 vertical cell | 1 passed / 0 failed |
| integration manifest | 1,040 sources / 4,639 attrs / 48/48 targets |
| integration 정책 | 19 passed / 0 failed |
| focused lib Clippy `-D warnings` | 통과 |
| `cargo fmt --all -- --check`·`git diff --check` | 통과 |

Q4-D4의 `canvaskit vertical feature detection` 적색 계약은 의도대로 이번 green 집합에서 제외했다.

| canonical sample | pages | overflow | SVG bytes | SHA-256 | mismatch |
| --- | ---: | ---: | ---: | --- | ---: |
| `samples/table-004.hwp` | 1 | 0 | 214,449 | `7711baa0afe033c044605ff1afd5597534a8f246819bf12e929ac3c5ab93873f` | 0 |
| `samples/issue6029/3200477_icao_procedure.hwpx` | 1 | 0 | 273,101 | `27ac3f000875a1413a9a99bb3f8ad4c1fb8c21a942a3b9fd6476df72030328e9` | 0 |

임시 SVG 디렉터리는 해시 확인 뒤 휴지통으로 이동했다.

## 5. 보호 불변식

- D2 geometry·node order·fallback text 변화: 0
- layer source/leaf/glyph/resource mapping mismatch: 0
- rejected batch glyph/resource residue: 0
- Rust selector·`text_v2` false strict selection: 0
- horizontal lowerer·authority guard 변경: 0
- 새 integration `.rs`, generated suite·manifest, Cargo marker: 0
- private corpus·Hyper-V·GitHub mutation: 0

## 다음 승인 경계

메인테이너가 `qualified-publication-parity` 최종 결과와 별도 checkpoint 진행을 승인해 `43d50ad99`로
고정했다. Q4-D4 착수 승인 뒤 최신 devel을 병합·재자격화했으며, 상세 D4 수행계획 승인 전에는 selector
구현을 시작하지 않는다.
