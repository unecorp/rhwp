# Task M100 #4969 W10-Q4-D0 — red baseline·target contract 결과

- **상위 계획**: [`task_m100_4969_w10_q4_d.md`](../plans/task_m100_4969_w10_q4_d.md)
- **계획 checkpoint**: `7b3a7bab4`
- **기계 판독 증적**:
  [`w10_q4_d0_red_baseline.json`](../tech/investigations/issue-4969/w10_q4_d0_red_baseline.json)
- **실행일**: 2026-08-30 KST
- **판정**: `qualified-red-baseline`
- **메인테이너 결과 승인**: 2026-08-30 KST
- **제품 source·출력 변경**: 0

## 결론

Q4-D0는 계획과 일치하는 `qualified-red-baseline` 후보이다. Q4-C dormant 계약 27건과 D0 target policy
1건은 모두 통과했고, product activation에 필요한 다섯 source contract만 0/5로 실패했다. 실패는
제품 module 등록, exact-source-bound owner, atomic table-cell layout commit, vertical GlyphRun publication,
CanvasKit feature detection 부재와 정확히 일치한다.

test-only target policy는 HWP5 semantic provenance, `text_direction=2`, one cell·paragraph·line·run·column,
pure CJK, exact source 등록, variation 없음이 모두 참일 때만 후보로 판정한다. 같은 `한글` 입력은 Q4-C
dormant owner에서 exact transaction을 만들지만 `product_published=false`를 유지한다. HWPX provenance,
code 1, mixed `한A`, multi paragraph, exact source 부재는 각각 독립적으로 거부된다.

## 1. green baseline

| gate | 결과 |
| --- | --- |
| Q4-A~C + D0 target policy, red 제외 | 28 passed / 0 failed |
| 기존 Q4-A~C 계약 | 27 passed / 0 failed |
| integration source 정책 | 19 passed / 0 failed |
| Studio CanvasKit font/replay control | 1 passed / 0 failed |
| focused Clippy `-D warnings` | 통과 |
| `cargo fmt --all -- --check` | 통과 |

새 integration `.rs` source, generated suite·manifest, Cargo marker는 추가하지 않았다.

## 2. non-target canonical baseline

두 sample은 exact slot을 자동 등록하지 않은 현행 screen-profile SVG다. D2~D5에서 같은 명령으로 재실행해
byte·SHA-256 mismatch 0을 요구한다.

| sample | pages | overflow | SVG bytes | SHA-256 |
| --- | ---: | ---: | ---: | --- |
| `samples/table-004.hwp` | 1 | 0 | 214,449 | `7711baa0afe033c044605ff1afd5597534a8f246819bf12e929ac3c5ab93873f` |
| `samples/issue6029/3200477_icao_procedure.hwpx` | 1 | 0 | 273,101 | `27ac3f000875a1413a9a99bb3f8ad4c1fb8c21a942a3b9fd6476df72030328e9` |

계측 임시 디렉터리는 해시 확인 뒤 휴지통으로 이동했고 worktree에 산출물을 남기지 않았다.

## 3. red 원인

`issue_4969_q4_d0_red` filter는 의도대로 **0 passed / 5 failed**이며 다른 실패는 없었다.

1. Q4-D1: `shaping_vertical` 제품 module 등록 부재
2. Q4-D1: exact-source-bound `VerticalShapingContext` 부재
3. Q4-D2: `commit_bounded_vertical_hwp5_table_cell` atomic layout commit 부재
4. Q4-D3: `lower_vertical_shaping_page_sidecars` publication 부재
5. Q4-D4: Rust·Studio의 `boundedVerticalHwp5TableCellV1` 기능 탐지 부재

이 red는 구현 경계 이름만 고정하는 source contract다. 각 후속 절편에서는 behavior test로 실제 source identity,
transaction ownership, no-mutation rejection, layer resource와 backend selection을 먼저 증명한 뒤 해당 red를
green으로 만든다. 문자열만 추가해 우회하는 것은 완료 조건이 아니다.

## 4. 보호 불변식 대사

- `src/**`, `rhwp-studio/src/**` 변경: 0
- 제품 module registration·caller·sidecar·publication: 0
- horizontal authority guard 변경: 0
- `table-004`·#6029 canonical output mismatch: 0
- parser/font-name 자동 exact source 등록: 0
- private corpus·Hyper-V·GitHub mutation: 0

## 다음 승인 경계

메인테이너가 D0 `qualified-red-baseline` 결과를 승인했다. format·JSON·링크·diff 최종 검사를 재실행한
이 commit을 D0 checkpoint으로 고정한다. Q4-D1 exact-source-bound dormant owner 구현은 별도 진행 승인
전에는 시작하지 않는다.
