---
kind: working-note
status: completed
issue: 4967
stage: W8-R1-Q2
last_verified: 2026-08-26
---

# Task M100 #4967 Stage W8-R1-Q2 — exact SFNT와 현행 metric 제한 비교

- **계획**: [`task_m100_4967_v2.md`](../plans/task_m100_4967_v2.md)
- **선행 경계**: [`rank1_runtime_boundary.json`](../tech/investigations/issue-4967/rank1_runtime_boundary.json)
- **결과 정본**: [`rank1_metric_hypothesis.json`](../tech/investigations/issue-4967/rank1_metric_hypothesis.json)
- **실행 정책**: 가상 name relation만 적용, 제품·private corpus·Hyper-V 변경 없음

## 1. Q2 질문과 결론

Q1은 `문체부 바탕체`가 runtime에서도 unresolved이며 첫 divergence가 `layout-name`임을 확인했다. Q2는
그 이름을 가상으로 `MBatang`에 연결했을 때 현행 generated metric 또는 exact `MT.TTF hmtx`가 실제 조판
폭을 개선하는지 묻는다.

결론은 `no-change`다. name relation은 metric match metadata를 만들지만 공개 fixture의 base/final advance를
한 건도 바꾸지 않는다. exact source를 적용해도 같다. layout 이득이 없으므로 제품 alias를 추가하지 않고
W8-R1-Q3에도 진입하지 않는다.

## 2. 현행 metric의 계보 경계

현행 `MBatang`은 entry 370, UPEM 1,000인 historical generated snapshot이다.

```text
entryId=font-metric.e6fdb023c2acf414807d
metricDataSha256=4a731b237f741c99c34f9bbb937a1083a5f033b1af1ccc0439e89a98a8c711e7
widthProjectionSha256=db1a7f375e382fdbff1e06c7e25f2220ee3e7fb7c77556ddbc4fa3ea4a47a12b
originStatus=unknown
```

저장 형태는 Hangul 11,172자 전부 1,000, space 500이며 나머지 Latin range 값은 0이라 character-miss다.
generated 영역에 있다는 사실만으로 exact `MT.TTF`에서 생성됐다고 주장하지 않는다.

## 3. exact source와 exhaustive domain

exact `MT.TTF`는 UPEM 1,000, cmap 2,356개다. layout-bearing codepoint는 Hangul 2,350개와 space 1개이고,
현행 entry와 advance mismatch가 0이다. 나머지 cmap 5개는 layout glyph 비교에서 제외한 control codepoint다.

| 영역 | current raw face | virtual `MBatang` | exact source |
| --- | ---: | ---: | ---: |
| Hangul | heuristic 1,000 | metric 1,000 | cmap 보유 2,350자 모두 1,000 |
| space | heuristic 500 | metric half-space 500 | 500 |
| 그 밖의 문자 | heuristic | metric miss 후 같은 heuristic | source miss면 virtual 결과 보존 |

generated metric은 exact cmap에 없는 Hangul 8,822자에도 1,000을 반환한다. 따라서 advance compatibility는
font identity·glyph identity나 source-exact lineage를 뜻하지 않는다.

이 비교는 특정 fixture나 private cohort보다 넓은 전체 layout-bearing 문자 영역을 닫는다. base advance가
같으면 ratio, letter spacing, justification additive spacing과 negative clamp의 결정 함수도 같은 값을 낸다.
그러므로 Q0의 22문서를 다시 열어 문자 목록을 만들 필요가 없고 bounded cohort 재parse는 0이다.

## 4. public fixture transform replay

Q1과 같은 공개 fixture 1,556건의 current transform을 재생했다.

| projection | total advance (HWPUNIT) | current 대비 delta |
| --- | ---: | ---: |
| current | 847,977 | 0 |
| virtual name relation | 847,977 | 0 |
| exact `hmtx` | 847,977 | 0 |

- exact source covered record: 616, source miss 후 보존: 940
- generated metric hit record: 616, miss 후 heuristic 보존: 940
- current transform replay mismatch: 0
- 장평·자간·justification axis: 13, 전축 delta 0

## 5. fixed-frame 경계

| context | representative | frame HU | current total | virtual/exact total | first crossing |
| --- | ---: | ---: | ---: | ---: | --- |
| table-cell | 0 | 28,980 | 36,100 | 36,100 | 43 → 43 |
| table-cell | 1 | 28,980 | 32,524 | 32,524 | 49 → 49 |
| table-cell | 2 | 28,980 | 28,889 | 28,889 | none → none |
| text-box | 0 | 29,434 | 36,100 | 36,100 | 44 → 44 |
| text-box | 1 | 29,434 | 29,452 | 29,452 | 57 → 57 |
| text-box | 2 | 29,434 | 26,352 | 26,352 | none → none |

첫 crossing이 앞당겨지지 않은 수준이 아니라 모든 누적 advance가 동일하다. Q3의 실제 reflow에서 확인할
candidate delta 자체가 없다.

## 6. 보호 불변식과 판정

최종 disposition audit에서 Q0의 기존 aggregate를 다시 보니 target 209,066자 중 bold 요청이 38,090자였다.
`MBatang`은 regular entry만 가지므로 virtual relation은 이 영역을 `nameFirst` regular metric과
`boldFallback` metadata로 선택한다. Font Decision Trace가 명시하듯 faux bold는
`regularMetricAdvance`를 그대로 쓰며 layout advance를 바꾸지 않는다. italic 요청은 0자였지만 projector는
bold·italic 4개 조합 모두 같은 regular metric advance를 쓰는 style-domain 계약을 추가로 고정했다.

따라서 문자-domain 동치뿐 아니라 style-domain 동치도 성립한다. 이 audit은 기존 Q0 aggregate만 읽었으며
문서 재parse·identity 공개는 0이다.

- exact font bytes와 absolute path를 tracked output에 넣지 않았다.
- private 문서 경로·이름·본문·hash와 full trace를 저장하지 않았다.
- 10k 전수·bounded private cohort·Hyper-V를 실행하지 않았다.
- registry·metric DB·fallback·paint·supply 제품 source를 바꾸지 않았다.
- paint identity·portable supply는 계속 qualification되지 않은 상태다.

```text
status=no-change
layoutBenefitObserved=false
decisionPlaneCount=0
productMutationAuthorized=false
nextStage=stop-rank1-no-change
canonicalSha256=d660fc18c1a7f3100c2cf3b1ce9e70f0c893f46ce8e3345c33bab7fb1a2fe23c
```

2026-08-26 `upstream/devel@ee7e8a6ed`의 `text_measurement` 변경과 PR 생성 직전
`upstream/devel@6240d255b`의 renderer 변경을 각각 통합한 뒤 새 Q1 입력으로 projector를 다시 실행했다.
최신 공개 1,556건, 전체 layout-bearing domain과 fixed-frame 6축의 delta는 계속 0이었으며 Node 8/8,
Python 12/12 계약 테스트가 통과했다. canonical hash 변경은 입력 source·binary digest 갱신이며
`no-change` 판정 변화가 아니다.
