---
kind: working-note
status: completed
issue: 4967
stage: W8-R7-Q1
last_verified: 2026-08-26
---

# Task M100 #4967 Stage W8-R7-Q1 — 공개 fixture와 current runtime boundary

- **계획**: [`task_m100_4967_v3.md`](../plans/task_m100_4967_v3.md)
- **Q0 baseline**:
  [`rank7_qualification_baseline.json`](../tech/investigations/issue-4967/rank7_qualification_baseline.json)
- **Q1 baseline**:
  [`rank7_runtime_boundary.json`](../tech/investigations/issue-4967/rank7_runtime_boundary.json)
- **실행 정책**: 공개 fixture만 사용, 10k·Hyper-V·제품 mutation 없음

## 1. fixture 계보와 중단 조건 통과

W5의 hash-sealed `generate_oracle_typesetting_fixture.py`에 rank 7 exact face와 distinct document
substitution face를 지정했다.

| 역할 | 값 |
| --- | --- |
| producer / consumer issue | #4963 / #4967 |
| document face | `KoPubWorld돋움체 Light` |
| document substitution | `KoPubWorld바탕체 Light` |
| HWPX SHA-256 | `1cc8062c6fd0da39cfddc4182115226717516d4250e693b43596293374236f9e` |
| W5 ladder와 byte-exact | yes |
| generator manifest SHA-256 | `38778e8ee3ecc8a9c5177c082eea2528214c5b4a7d71c66c009873922befd9df` |
| font bytes embedded | false |
| stored / fresh context | 14 / 12 |

현재 HEAD의 release CLI로 HWPX를 HWP5로 두 번 변환했다. 두 결과는 SHA-256
`3a844e0530ecede89301ab1f3c2381865412f8472aa08733cdb9d1d25223ee7f`로 같고
`--verify` IR 차이 없음, `--verify-pages` 1쪽 동치를 통과했다. tracked HWP도 두 반복본과 byte-exact하다.

다만 HWPX의 `substFont`는 HWP5 변환본에 남지 않았다. 이 차이를 변환 동치에 묻지 않고 runtime trace에서
layout과 paint plane으로 분해해 판정했다.

## 2. current native·WASM trace

현재 HEAD에서 release native 세 바이너리와 표준 Docker WASM을 새로 빌드했다. trace query는 기존 core
상한 `maxCharacters=4096`, child process는 60초·64 MiB 출력을 강제한다.

| 항목 | HWPX | HWP5 |
| --- | ---: | ---: |
| characters seen / emitted / omitted | 1,556 / 1,556 / 0 | 1,556 / 1,556 / 0 |
| metric entry `null` / match kind `none` | 1,556 / 1,556 | 1,556 / 1,556 |
| heuristic fullwidth / halfwidth / narrow | 390 / 1,114 / 52 | 390 / 1,114 / 52 |
| native/WASM canonical trace | byte-exact | byte-exact |

HWPX canonical trace SHA-256은 `48616a8a…30ddb9`, HWP5는 `7e98f026…303ae`다. 전체 trace hash가 다른
이유는 document substitution·paint 후보 metadata이고, source+layoutMetric projection SHA-256은 두 형식
모두 `a7388990…aff12`로 같다.

## 3. 형식별 substitution 경계

| 경계 | HWPX | HWP5 | 판정 |
| --- | --- | --- | --- |
| requested / normalized / metric alias | target face | target face | 동일 |
| document `substFont` | KoPubWorld 바탕 Light | `null` | 형식 차이 |
| layout-name metadata step | `documentSubstFont` | 없음 | 형식 차이 |
| metric entry / match | `null` / `none` | `null` / `none` | 동일 |
| paint requested chain | 돋움, 바탕 | 돋움 | 형식 차이 |
| source+layoutMetric projection | `a7388990…aff12` | `a7388990…aff12` | byte-equivalent |
| actual layout run projection | `18c545b5…b8456c` | `18c545b5…b8456c` | byte-equivalent |

즉 HWPX의 document substitution은 CSS·paint 후보 체인에는 참여하지만 current layout metric을 바꾸지 않는다.
W5에서도 subst-only와 none-related가 같은 HCR fallback으로 귀결됐으므로 이 metadata를 exact fallback이나
identity relation으로 승격하지 않는다.

## 4. actual fixed-frame geometry

두 형식의 실제 render run에서 fontFamily metadata만 제외한 geometry가 byte-equivalent했다.

| context | lane | ratio / spacing | lines | max line px | min slack px | crossing |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| table-cell | stored | 100 / 0 | 1 | 481.0 | -94.6 | yes |
| table-cell | fresh | 90 / -5 | 2 | 387.0 | -0.6 | yes |
| table-cell | stored | 80 / -10 | 1 | 386.0 | 0.4 | no |
| text-box | fresh | 100 / 0 | 2 | 353.0 | 39.5 | no |
| text-box | stored | 90 / -5 | 1 | 393.0 | -0.5 | yes |
| text-box | fresh | 80 / -10 | 2 | 259.0 | 133.5 | no |

0.4px·-0.5px·-0.6px처럼 경계에 붙은 세 축이 있으므로 Q2 exact metric 후보는 작은 평균 개선만으로 통과할
수 없다. 각 축의 첫 crossing 변화와 Q0의 ratio 95·spacing -9 실사용 주축을 별도로 판정해야 한다.

## 5. 검증

```text
node --test
  font_rank7_runtime_boundary + rank1/rank8 shared contracts
13 tests OK

python3 -m unittest
  oracle stage2/stage4 + rank7 Q0 contracts
28 tests OK

cargo test --locked --profile release-test --target-dir target/pr-review
  --test regression_suite_031 agent_q_font_trace_contract::
8 tests OK

cargo build --release --bin rhwp --bin rhwp-q-font-trace --bin rhwp-q-text-layout
docker compose --env-file .env.docker run --rm wasm
all passed
```

public canonical SHA-256은
`70ad1a94851c1cd8ec4329dd284025882bec6f802b32d0e5cef17e4179fbe49b`다. full trace·font bytes·
private corpus identity는 tracked output에 포함하지 않았다.

## 6. 다음 gate

Stage W8-R7-Q2의 target decision plane은 `layout-metric` 하나다.

1. official exact TTF와 CDN OTF·WOFF2의 hash·SFNT name·cmap·unitsPerEm·`hmtx`를 분리한다.
2. 공개 fixture 1,556건에서 current heuristic과 exact metric의 base·transform 후 advance를 비교한다.
3. HWPX substitution은 paint metadata로 유지하고 metric 후보에 섞지 않는다.
4. actual fixed-frame 6축과 Q0 style domain에서 첫 crossing 개선·회귀를 판정한다.

Q2는 별도 승인 뒤 시작하며 제품 registry·metric DB·fallback·paint·supply를 변경하지 않는다.
