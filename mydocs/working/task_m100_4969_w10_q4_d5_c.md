# 결과 보고서 — Task M100 #4969 W10-Q4-D5-C 최종 회귀·분류

- **상위 계획**: [`task_m100_4969_w10_q4_d5.md`](../plans/task_m100_4969_w10_q4_d5.md)
- **D5 성능 정정 checkpoint**: `e7f814330`
- **최신 기준 devel**: `upstream/devel@77bcaaa49` (merge `aecdde843`)
- **제품 최종 검증 head**: `513068f8e`
- **비제품 incoming 재자격화 head**: `aecdde843`
- **D5-C 결과 checkpoint**: `a7de96b44`
- **기계 판독 증적**:
  [`w10_q4_d5_c_final_classification.json`](../tech/investigations/issue-4969/w10_q4_d5_c_final_classification.json)
- **수행일**: 2026-08-31 KST
- **최종 분류**: `blocked`
- **correctness·성능 상태**: qualified

> **후속 해소**: 이 문서는 D5-C 당시의 역사적 `blocked` 판정을 보존한다. 이후
> [D5-D clean-room causal WASM 결과](task_m100_4969_w10_q4_d5_d.md)가 동일 hard gate를 충족해
> Q4-D 최종 분류를 `qualified-bounded-subset`으로 갱신했다.

## 결론

Q4-D의 bounded HWP5 세로 표 셀 구현은 correctness, fail-closed 경계, 성능 정정, Native Skia fallback,
Docker/Node WASM, Studio actual CanvasKit 및 full nextest를 통과했다. D5 정정 뒤 9-process Rust 계측은 반복
source 준비 병목을 warm layout 약 598.64배, layer build 약 339.16배 줄였고, 최종 병합 head의 1회 canary도
같은 구조·resource·JSON/replay hash를 유지했다.

그러나 계획이 최종 `qualified-bounded-subset`의 hard gate로 지정한 same-tree causal WASM A/B size 비교는
완성하지 못했다. Q4-D 제품 checkpoint를 최신 tree에서 역적용하는 첫 단계가 문서와 Studio E2E에 충돌했고,
계획은 충돌 강제 해소와 과거 절대값 대체를 모두 금지한다. 따라서 구현 오류나 미해결 회귀 때문이 아니라
**인과적 WASM 크기 증적 미완료** 때문에 최종 분류는 `blocked`다.

이 결과를 `qualified-bounded-subset`으로 올리거나 Q4-E로 넘어가려면 메인테이너가 별도 수정 계획에서 causal
size hard gate의 충족 방법을 다시 정해야 한다. 현재 결과만으로 hard gate를 사후 완화하지 않는다.

## 1. 최종 source와 회귀 게이트

| gate | 결과 |
| --- | --- |
| 최신 devel | `77bcaaa49`, merge `aecdde843`, 66 ahead / 0 behind |
| full nextest | 8,859 passed / 0 failed / 46 skipped, 312.798초 |
| #4969 request→publication 계보 | 103 passed / 0 failed / 2 ignored performance receipts |
| #6029 vertical cell | 1 passed / 0 failed |
| Native Skia library | 4,130 passed / 0 failed / 13 ignored |
| Native Skia placeholder·direct PDF | 2 + 4 passed / 0 failed |
| Studio unit | 1,324 passed / 0 failed / 1 skipped |
| Studio production build | passed |
| #4969 focused·actual CanvasKit A/B/A | passed, pixel mismatch 0 |
| CanvasKit font coverage·renderer contract | passed |
| Node WASM declarations | 1 passed / 0 failed |
| native↔WASM canonical SVG | 2 documents / 2 pages, mismatch 0 |
| Docker WASM | passed, 359초 |
| native/wasm32·workspace Clippy `-D warnings` | passed |
| workspace build·all-targets Clippy | passed |
| format·manifest·diff check | passed |

review harness는 1,073 source, 4,709 static test attributes, 28 generated suites와 20 exception targets를
합쳐 48/48 target, 최소 6,559 cases로 준비·검사했다. generated suite·manifest는 검증 증적일 뿐 stage하지
않는다.

설치된 cargo-nextest 0.9.137은 저장소 권고 0.9.140보다 낮아 버전 경고와 알 수 없는 JUnit 설정 키 경고를
출력했다. 실행 자체는 8,859건 전부 통과했으며 이 경고를 테스트 성공으로 숨기지 않고 도구 경고로 분리했다.

전체 검증 뒤 원격 devel이 3개 commit 전진했다. incoming은 trusted post-merge CI reuse workflow와 검증
script·PR review record 5개 파일뿐이며 Rust 제품 source, renderer, fixture, D5 파일과 직접 중첩은 0이었다.
merge-tree conflict 0을 확인하고 정상 merge했다. incoming 전용 Node 계약 18/18과 Python workflow 계약
171/171, format·diff check를 통과했다. 로컬에는 `actionlint` 실행 파일이 없어 재실행하지 못했으며, 추적된
upstream self-review의 actionlint 통과 기록을 그대로 명시한다. 제품 source 변화가 0이므로 full nextest와
WASM/Studio를 다시 반복하지 않았다.

## 2. 성능 정정과 최종 canary

D5-B 최초 계측이 드러낸 병목은 CanvasKit `drawGlyphs`가 아니라 같은 2.52 MiB exact source를 매 layout과
layer publication에서 다시 해시·parse·copy한 것이었다. registry 진입 시 immutable source identity를 한 번
준비하고, 인증된 `Arc`와 face/resource metadata를 layout→paint에 전달하도록 정정했다.

9-process p50 기준 결과는 다음과 같다.

| 축, 64회 | 정정 전 | 정정 후 | 개선비 |
| --- | ---: | ---: | ---: |
| B warm layout | 1,142,878,416 ns | 1,909,123 ns | 598.64× |
| B layer build | 223,300,253 ns | 658,389 ns | 339.16× |

최종 head canary는 A1/A2 legacy hash가 서로 같고 B의 `GlyphRun` 2, font blob/face 각 1, payload 2,519,996
bytes를 유지했다. B layer JSON hash `79451e38…53adc2`, replay-plan hash `700b4141…704f1`도 9-process
증적과 일치했다. JSON serialization과 replay-plan 비용은 의도적으로 운반하는 2.52 MiB payload에 비례하며
이번에 제거한 반복 source 준비 병목과 구분한다.

## 3. WASM 크기 판정

현재 최종 head의 표준 Docker WASM은 9,894,929 bytes, SHA-256
`d7e4b1198ac17737aa6d423bcdb4ffe35e6083dfda02338d9bffc3503e590aee`다. 이는 현재 산출물의 절대 검증값일
뿐 Q4-D의 인과 delta가 아니다.

짝지은 A축을 만들기 위해 최신 tree의 disposable worktree에서 여섯 Q4-D 제품 checkpoint를 최신 것부터
역적용했다. 첫 `b1a33b6fc`부터 계획·결과 문서와 Studio #4969 E2E가 충돌했다. 계획에 따라 충돌을 강제로
풀지 않았고, revert를 abort한 뒤 worktree를 제거했다. 과거 D4 WASM 절대값도 대체값으로 사용하지 않았다.
따라서 causal size 상태는 `causal-size-unavailable`이며 `blocked` 분류의 유일한 미충족 hard gate다.

## 4. canonical 대사와 upstream 기준 변동

| canonical sample | pages | overflow | SVG bytes | SHA-256 | native↔WASM mismatch |
| --- | ---: | ---: | ---: | --- | ---: |
| `samples/table-004.hwp` | 1 | 0 | 214,010 | `b3887ea7833c90dc7ee7d15c415df7dd9089f914435543129477127fc2ae9abe` | 0 |
| `samples/issue6029/3200477_icao_procedure.hwpx` | 1 | 0 | 273,101 | `27ac3f000875a1413a9a99bb3f8ad4c1fb8c21a942a3b9fd6476df72030328e9` | 0 |

`table-004.hwp`는 D4의 214,449 bytes·`7711baa0…`에서 변했다. 이 차이를 D5 회귀로 처리하지 않고 최신
`upstream/devel@28057f02e` detached worktree를 같은 release-test target/toolchain으로 빌드해 대조했다.
upstream 단독도 214,010 bytes와 같은 SHA-256을 냈다. 따라서 변동은 최신 devel 유입의 canonical 기준 변경이고,
우리 head는 그 최신 기준과 byte-for-byte 일치한다. 비교 worktree는 확인 뒤 제거했다. #6029 canonical은 D4와
같다.

## 5. 보호 불변식

- `boundedVerticalHwp5TableCellV1` 지원 tuple 확장 0
- HWPX·code 1·horizontal·mixed/Latin/punctuation·multi-line/run/column의 fallback 유지
- unsupported backend false selection, fallback disappearance, partial publication 각각 0
- Native Skia·SVG·Canvas2D vertical authority 확장 0, backend reshape 0
- font/text/glyph/position 상한 완화 0, 반복 resource multiplicity 증가 0
- malformed SFNT rejection은 `Dormant(ShapingRejected(MalformedSfnt))`로 보존
- 새 integration source·public API/schema·Cargo marker·generated 제출물 0
- private corpus·Hyper-V·한컴 Oracle·GitHub mutation 0

## 다음 경계

이 `blocked` 결과는 자동 승인 범위에서 checkpoint `a7de96b44`로 고정했다. 다음 구현 절편은 자동으로
시작하지 않는다.
다음 단계는 causal size hard gate를 그대로 충족할 새로운 clean-room A축을 설계할지, 검증된 절대 WASM과
correctness/performance 자료를 근거로 gate를 수정할지에 대한 메인테이너의 정책 결정이다. 어느 쪽도 현재
수행계획의 단순 승인으로 간주해 사후 선택하지 않는다.
