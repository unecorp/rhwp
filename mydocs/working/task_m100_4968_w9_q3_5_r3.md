# Task M100 #4968 — Stage W9-Q3-5R3 공개 exact kerning face 보고서

- 작성일: 2026-08-26 KST
- 작업 브랜치: `task_m100_4968`
- 선행 커밋: `e1419ab78` W9-Q3-5R2 layout-session source cache
- 수정 계획 근거: `mydocs/working/task_m100_4968_w9_q3_5_stop.md`
- 실제 pair advance·layout position 변경: 0
- 상태: 구현·native/WASM 검증 완료, 메인테이너 결과 승인 대기

## 1. 결론

Q2 capability boundary에서 이미 생성·검증하던 synthetic GPOS face를 1,236 bytes의
`RHWPExactKerningSmoke.ttf`로 materialize했다. fixture는 원본 glyph와 metadata로 구성되고 저장소 MIT
라이선스를 적용한다. 생성기, 인접 라이선스 문서, 고정 SHA-256으로 provenance를 재현할 수 있다.

동일 integration 원본의 target-neutral test를 native와 Node wasm-bindgen runner에서 실제 실행했다. 두
환경은 같은 fixture bytes, exact handle, borrowed provider, `KerningSourceSession`, pair engine을 사용하며
`AV To WA HH`에 동일한 -120 design units를 냈다.

Noto Sans KR 2.52 MiB face는 Q2의 실제 OpenType truth와 digest 증거로 유지한다. 새 자동 parity fixture는
Noto를 제품 core에 등록하거나 이름으로 선택하지 않는다. Docker WASM은 R2와 byte-exact이며 fixture 이름과
SHA도 binary에 존재하지 않는다.

## 2. 공개 fixture 계약

| 항목 | 값 |
| --- | --- |
| 경로 | `tests/fixtures/fonts/RHWPExactKerningSmoke.ttf` |
| 크기 | 1,236 bytes |
| SHA-256 | `775667d1980cd734e331f01e9390e02191bc35d669325291c842968cb0a4a9fc` |
| 생성기 | `scripts/generate_exact_kerning_fixture.py` |
| 원 생성 계약 | `scripts/font_kerning_boundary.py`의 Q2 synthetic GPOS face |
| 라이선스 | 저장소 MIT, 인접 `RHWPExactKerningSmoke.LICENSE.md` |
| units per em | 1,000 |
| GPOS pair | `AV=-80`, `To=-40` design units |
| 비대상 pair | `WA=0`, `HH=0` |

생성기는 결과 SHA가 고정값과 다르면 파일을 정상 산출물로 인정하지 않고 실패한다. 따라서 fontTools 버전이나
생성 코드가 결과 bytes를 바꾸면 조용히 fixture를 갱신하지 못한다. `scripts/requirements-font-fixtures.txt`의
고정 fontTools 환경으로 재현할 수 있으며 현재 환경의 재생성도 같은 SHA를 냈다.

## 3. native/WASM 동일 경계

기존 `tests/cases/issue_4968_kerning_capability_provider.rs`에 target-neutral test 하나를 추가했다.

1. 추적 fixture bytes에서 exact source handle을 식별한다.
2. expected size와 SHA-256을 확인한다.
3. borrowed provider를 `KerningSourceSession`에 연결한다.
4. capability가 `gpos-kern`인지 확인한다.
5. 같은 pair engine으로 `AV To WA HH` 후보를 계산한다.
6. 합계 -120 DU와 fallback 부재를 확인한다.

native에서는 표준 `#[test]`, wasm32에서는 `#[wasm_bindgen_test]`로 같은 함수 본문을 실행한다. target별 font
선택이나 pair 상수 분기는 없다. 이 단계는 host가 제공한 exact bytes를 R1/R2 계약으로 소비할 수 있음을
검증하지만, 아직 제품 font registry나 measurement positions에는 연결하지 않는다.

## 4. fixture CI 생명주기

새 생성기를 render tool로 선택 분류하는 classifier·mirror·workflow 등록은 제품 변경과 enforcement surface
변경을 섞지 않기 위해 이번 PR 제출 범위에서 제외했다. 기본 브랜치 `main`에 trusted controller가 활성화되지
않은 상태에서 이를 함께 제출하면 PR 번호 발급 뒤 review-only 기록만 추가해도 Full CI가 다시 실행되기
때문이다.

현재 PR은 `src/renderer/**`와 `tests/fixtures/fonts/**` 변경만으로도 Rust·Render Diff·Native Skia를 모두
실행하므로 검증 강도가 낮아지지 않는다. 별도 운영 이슈 #6215와 후속 PR이 generator-only 선택 분류를 등록하기 전까지
생성기 단독 변경은 unclassified 상태에서 Full CI로 fail-closed한다. 관련 Node 정책 검사 65건은 운영 등록을
제외한 현 제출 상태에서도 전항 통과했다. 추적 TTF 자체가 바뀌면 기존 font fixture 규칙에 따라 Rust와
render/native 검증을 모두 수행한다.

## 5. 검증 결과

| 검증 | 결과 |
| --- | --- |
| fixture 재생성 | 1,236 bytes, 고정 SHA-256 일치 |
| `cargo fmt --all` | 통과 |
| `cargo fmt --all -- --check` | 통과 |
| `cargo check --locked --lib` | 통과 |
| `cargo test --locked --lib --no-run` | 전체 library test target 컴파일 통과 |
| integration 원본 native 실행 | 8 passed, 0 failed, 0.35초 |
| 동일 원본 Node wasm32 실행 | 1 passed, 0 failed, 0.03초 |
| native/WASM public fixture candidate | `AV To WA HH = -120 DU` |
| CI classifier/policy Node 검사 | 65 passed, 0 failed |
| Docker WASM build | 통과, 2분 25초, cache 재사용 |
| R3 WASM | 8,978,378 bytes |
| R3 WASM SHA-256 | `a0081e2798b4dfc7f78134e65efa1c97b463b0f91a9568d2907eb006850d3aa3` |
| R2 대비 | **0 bytes / byte-exact** |
| WASM fixture name/SHA probe | 모두 부재 |

외부 native/WASM integration용 임시 manifest·lockfile·target은 제거했다. source checkout에서 generated
integration suite·manifest와 Cargo target marker는 만들거나 변경하지 않았다. Docker가 갱신한 `pkg`도
기준과 동일하여 작업트리 변경이 없다.

## 6. 보호 불변식 판정

- K0 및 현재 제품 layout: 변경 없음
- exact source: 동일 fixture bytes와 handle을 native/WASM이 공유
- 공개 재현성: generator·license·size·SHA·expected pair를 추적
- 대형 font 격리: Noto는 Q2 truth로 유지하되 core fixture registry로 사용하지 않음
- payload 격리: Docker WASM에 fixture 이름·SHA·bytes가 포함되지 않음
- backend parity: target별 상수 분기 없이 같은 provider/session/candidate 경로 실행
- CI 비용: 현재 PR은 source·font fixture 영향으로 Full 검증, generator-only 후속 변경은 운영 등록 전까지 Full fallback

## 7. 다음 절편

메인테이너가 R3 결과를 승인하면 이 절편을 커밋한다. Q3-5R4에서는 base glyph advance와 pair adjustment를
하나의 공통 run measurement 결과로 만들고, token total·긴 단어 fallback·line measurement·character
positions가 이를 공유하게 한다. 256 segment 상한과 줄 경계를 넘는 pair adjustment 제거를 실제 실행 경로와
함께 도입한다. 이때부터 제품 layout 영향이 시작되므로 K0 byte identity와 K1 line/position fixture를 다시
게이트한다.
