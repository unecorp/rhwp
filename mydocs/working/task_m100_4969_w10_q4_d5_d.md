# 결과 보고서 — Task M100 #4969 W10-Q4-D5-D clean-room causal WASM 복구

- **상위 계획**: [`task_m100_4969_w10_q4_d5_d_revised.md`](../plans/task_m100_4969_w10_q4_d5_d_revised.md)
- **선행 결과**: [`task_m100_4969_w10_q4_d5_c.md`](task_m100_4969_w10_q4_d5_c.md)
- **계획 checkpoint**: `e866bc0b8`
- **결과 checkpoint**: `95d450dd5`
- **최신 병합 devel**: `upstream/devel@3b301f725` (merge `3d239b5a7`, 71 ahead / 0 behind)
- **A/B 기준 head**: `e866bc0b8c7ff88917d4f915575fb6c345ec26c4`
- **기계 판독 증적**:
  [`w10_q4_d5_d_causal_wasm_result.json`](../tech/investigations/issue-4969/w10_q4_d5_d_causal_wasm_result.json)
- **수행일**: 2026-08-31 KST
- **최종 분류**: `qualified-bounded-subset`
- **결과 승인**: 메인테이너 명시 승인
- **제품 변경**: 없음

## 결론

D5-C에서 유일하게 남았던 same-tree causal WASM size hard gate를 완화하지 않고 충족했다. B exact head의
detached worktree에서 Q4-D와 D5 제품 Rust source commit 일곱 개의 `src/**` diff만 최신순으로 역적용해 A를
만들었다. 문서·test-only·Studio-only commit은 역적용하지 않았고, A의 tracked diff는 계획한 13개 Rust 파일
allowlist와 정확히 일치했다. 수동 conflict resolution, reject, 과거 절대값 대체는 모두 0이다.

A와 B를 같은 Docker image ID와 서로 분리된 빈 cargo·wasm-pack·target volume에서 표준 WASM 절차로 각각
완전 빌드했다. 최적화된 `rhwp_bg.wasm`은 A 9,858,318 bytes, B 9,894,929 bytes였고, 인과 delta는
**+36,611 bytes, A 대비 +0.371371668%**다. 이 양의 delta를 사후 크기 SLA로 pass/fail하지 않고 그대로
공개한다.

D5-C가 이미 correctness·runtime parity·성능 정정을 qualified로 판정했고, D5-D가 마지막 causal size
영수증을 완성했으므로 Q4-D 최종 분류는 `blocked`에서 `qualified-bounded-subset`으로 갱신한다. D5-C의
blocked 기록은 당시 전체 commit reverse 방식이 충돌한 역사적 결과로 보존하며, D5-D가 baseline 구성 방법만
교정해 그 blocker를 해소했다.

## 1. clean-room A 구성

A는 B exact head `e866bc0b8`의 detached disposable worktree에서 다음 제품 commit의 Rust source diff만
최신순으로 역적용했다.

1. `513068f8e` — malformed vertical rejection 보존 정정
2. `e7f814330` — exact-source 반복 준비 비용 정정
3. `c9b551909` — Rust CanvasKit selector
4. `d49400aad` — vertical GlyphRun publication
5. `bb334756b` — source/leaf shadow mapping
6. `992fe4acaf92` — bounded vertical HWP5 table layout
7. `d0948166a` — exact-source-bound dormant owner

`b1a33b6fc`는 Studio TypeScript/E2E만 변경하므로 Rust WASM 역적용에서 제외했다. `e31c5c15d` test-only
receipt와 문서·merge commit도 WASM 제품 source가 아니므로 제외했다.

검증 결과는 다음과 같다.

| 보호 항목 | 결과 |
| --- | --- |
| reverse patch | 7/7 적용, conflict·reject·수동 해소 0 |
| tracked diff | 계획한 13개 `src/**` allowlist와 정확히 일치 |
| unmerged index·conflict marker | 0 |
| Q4-D mode·vertical publication source 잔존 | 0 |
| Cargo.lock·feature·Dockerfile·toolchain drift | 0 |
| A wasm32 `cargo check --locked` | passed, 31.09초 |

`git apply --3way`가 `src/paint/mod.rs` 한 파일에서 direct application fallback을 알렸지만 patch는 충돌·fuzz·
reject 없이 적용됐다. 이는 수동 해소가 아니며 결과 diff가 allowlist와 정확히 일치한다.

## 2. 동일 환경 Docker WASM A/B

compose project는 `rhwp4969d5a`와 `rhwp4969d5b`로 분리했다. compose project label 때문에 최초 image config
digest가 달라지는 것을 발견해, A에서 한 번 빌드한 image를 B tag에도 지정하고 두 tag가 정확히 같은 image ID를
가리키는지 확인한 후 비교를 시작했다.

- Docker image ID: `sha256:5a4c93c403abe0eb4eae8b411cddfa53861865d586837f5c1a096a1f1e745cd8`
- Rust: `rustc 1.93.0 (254b59607 2026-01-19)`, LLVM 21.1.8
- wasm-pack: 0.15.0
- A/B volume: 서로 다른 빈 cargo·wasm-pack·target volume

| 축 | optimized WASM bytes | SHA-256 | build 결과 |
| --- | ---: | --- | --- |
| A — Q4-D/D5 Rust 제품 source 제거 | 9,858,318 | `556ebe8aa85b46b427d0d3954ee3b4fbdc24aea9540c47e813a8baa795de228e` | passed |
| B — 현재 bounded 구현 | 9,894,929 | `d7e4b1198ac17737aa6d423bcdb4ffe35e6083dfda02338d9bffc3503e590aee` | passed |
| B − A | +36,611 | — | +0.371371668% |

A wall time은 약 6분 39초, B는 약 6분 35초였다. 서로 독립된 cold volume이므로 이 wall time은 성능 결론에
사용하지 않는다. causal size 결론은 optimized bytes와 digest에만 근거한다.

## 3. 최종 판정 계보

| 축 | 최종 상태 | 근거 |
| --- | --- | --- |
| correctness·canonical | qualified | D5-C full nextest 8,859/8,859, canonical mismatch 0 |
| runtime parity | qualified | Native Skia fallback, Node WASM, Studio actual CanvasKit pixel mismatch 0 |
| 성능 | qualified | warm layout 약 598.64배, layer build 약 339.16배 병목 개선 |
| 구조적 비용 | qualified | 반복 source/resource multiplicity 증가 0 |
| causal WASM size | available | same-tree A/B +36,611 bytes, +0.371371668% |
| 지원 범위 | unchanged | `boundedVerticalHwp5TableCellV1` tuple 확장 0 |

따라서 모든 계획 hard gate가 완전하며 최종 분류는 `qualified-bounded-subset`이다. 이 판정은 WASM 증가량이
0이어서가 아니라, 인과 자료를 숨김없이 확보하고 correctness·성능·지원 범위 보호가 모두 성립하기 때문이다.

## 4. 결과 뒤 최신 devel 재자격화

결과 checkpoint 뒤 `upstream/devel`이 12 commit 전진한 것을 확인했다. 양쪽 변경 파일의 직접 교집합은
`mydocs/orders/20260831.md` 한 개뿐이었고, 두 독립 일지 섹션을 모두 보존해 add/add 충돌을 해소했다.
incoming의 Rust 제품 변경은 `image_resolver.rs`, `text_measurement.rs`, `svg.rs`이며 Q4-D 제품 변경 경로와
직접 중첩은 0이다.

정상 merge `3d239b5a7` 뒤 다음을 통과했다.

- #4969 이름 필터 계보: 112 passed / 0 failed, 8,793 skipped
- wasm32 library `cargo check --locked`: passed
- source-side tier: 4,223 tests / 299 modules, ready 0 / support 87 / white-box 4,132
- `cargo fmt --all`, `cargo fmt --all -- --check`, merge diff check: passed

causal WASM +36,611 bytes는 B snapshot `e866bc0b8`의 정확한 same-tree 영수증이다. 이를 새 merge head의
절대값으로 재표기하지 않는다. incoming은 Q4-D source와 직접 중첩하지 않고 크기 SLA도 없으므로 causal
receipt를 반복하지 않았으며, 현재 head의 동작·wasm32 컴파일을 별도 재자격화했다.

## 5. 정리·제출 경계

- disposable A worktree를 Git 등록과 파일시스템에서 제거했다.
- `rhwp4969d5a`, `rhwp4969d5b` 전용 volume·network를 제거했다.
- current branch 제품 source는 변경하지 않았다.
- `pkg`, `dist`, target, generated integration suite·manifest를 stage하지 않았다.
- private corpus·Hyper-V·한컴 Oracle·GitHub mutation을 사용하지 않았다.

## 다음 경계

D5-D 결과와 `qualified-bounded-subset` 판정을 checkpoint `95d450dd5`로 고정하고 최신 devel 병합·재자격화를
완료했다. 메인테이너가 결과보고서를 명시 승인했다. 다음은 제출 전 최종 원격 확인과 PR 경계이며 push·PR·
GitHub comment는 별도 절차로 유지한다.
