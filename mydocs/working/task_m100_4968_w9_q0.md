---
kind: working-note
status: completed
issue: 4968
stage: W9-Q0
last_verified: 2026-08-26
---

# Task M100 #4968 Stage W9-Q0 — kerning cohort와 W8 동결

- **계획**: [`task_m100_4968.md`](../plans/task_m100_4968.md)
- **공개 baseline**:
  [`kerning_cohort_baseline.json`](../tech/investigations/issue-4968/kerning_cohort_baseline.json)
- **실행 정책**: 10k 재계측 없음, corpus source open 없음, Hyper-V 재실행 없음, 제품 mutation 없음

## 1. 확정 증거 재사용

W3 final aggregate와 10,000-entry checkpoint journal을 hash·checkpoint identity로 대사했다. corpus 원본을
열거나 parse하지 않고 journal을 한 번 streaming해 kerning flag가 true인 문서만 선택했다.

| 항목 | 결과 |
| --- | ---: |
| kerning 문서 | 157: HWP 115 + HWPX 42 |
| kerning 문자 | 175,466: HWP 160,041 + HWPX 15,425 |
| 문서별 usage row | 2,318 |
| final aggregate dimension row | 1,723 |
| stored LineSeg | 173,469자, distinct 154문서 |
| fresh lane | 1,997자, distinct 4문서 |
| corpus source file open | 0 |

stored와 fresh를 모두 포함한 문서가 하나 있으므로 lane별 distinct 문서 수 154+4를 전체 문서 수로 합산하지
않는다. stored/fresh 결과도 이후 영향 판정에서 별도로 유지한다.

문맥은 table-cell 86,610자, body 83,367자, footnote 2,036자, text-box 1,778자, caption 1,436자 순이다.
가장 큰 장평·자간 조합은 100/0 114,403자, 95/-5 10,618자, 100/-10 8,000자다. 이는 kerning을 일반적인
본문 영문 장식으로만 다룰 수 없고 HWP 사용자의 압축 조판과 fixed context를 함께 판정해야 함을 보여준다.

## 2. journal과 final aggregate 대사

projector는 journal의 document-level row에 상위 record의 format을 명시적으로 결합한 뒤 count를 다시
집계한다. 그 결과 2,318개 문서별 row가 final aggregate의 1,723개 dimension row와 다음 전항에서 일치했다.

- dimension key 전건 일치
- distinct `documentCount` 일치
- `paragraphCount`, `runCount`, `charCount` 합계 일치
- checkpoint entry 10,000, complete 9,909 일치
- aggregate SHA-256
  `378671053ae94c02cda4caf8b8a4a1ac7866c3711fdf72c14aa527f8d9bc2888`
- checkpoint chain
  `94d9bbe3cce4218728ff29def8ab24abb51ffc434ec395d8be663fdab6b03649`

최초 실행에서는 format이 journal record 상위, final row 내부에 있다는 schema 차이를 projector가 반영하지
못해 fail-closed 중단됐다. 출력은 생성되지 않았고, format을 journal dimension에 명시적으로 결합한 뒤 계약
테스트와 실제 실행을 다시 통과했다. 오류 메시지도 row 전체 대신 count와 digest만 남기도록 정정했다.

## 3. W8 겹침과 진입 게이트

| W8 rank | face | kerning 겹침 | #4967 disposition | 제품 mutation |
| ---: | --- | ---: | --- | --- |
| 1 | 문체부 바탕체 | 0문서·0자 | `no-change` | false |
| 7 | KoPubWorld돋움체 Light | 0문서·0자 | `no-change` | false |
| 8 | KoPubWorld바탕체 Light | 1문서·42자 | `no-change` | false |

pair positioning과 face metric/fallback 변경을 같은 candidate에 섞지 않는 진입 게이트는 충족됐다.

## 4. 공개·비공개 경계

`scripts/font_kerning_cohort.py`는 symlink, 입력 크기, journal line 크기, index 중복, document count,
checkpoint identity, final aggregate drift와 W8 mutation drift를 fail-closed 검증한다.

- local-only 원장: `output/4968/w9-q0/kerning_private_cohort.json`, mode `0600`, 2,407,582 bytes
- tracked baseline: aggregate와 tracked evidence digest만 포함, mode `0644`, 20,124 bytes
- 공개 baseline의 absolute path, 문서명·본문·문서 hash·font bytes: 0
- 공개 canonical SHA-256:
  `95309e457f78de38f2b1470b05e6f0fe97f00684ffff5eddd4d82c6438fb71e6`

## 5. 검증

```text
python3 -m unittest scripts.tests.test_font_kerning_cohort
5 tests OK

projector result
documents=157, characters=175466
fullCorpusRerun=false, corpusSourceFilesOpened=0
```

계약 테스트는 private/public 분리, canonical hash, journal index 중복, final aggregate count drift와 W8 mutation
authority drift를 판정한다. 공개 JSON의 absolute path pattern은 0이고 private ledger mode는 `0600`이다.

## 6. Stage 판정

Stage W9-Q0 종료 게이트를 충족했다. 다음 W9-Q1은 kerning-off byte baseline과 capability/trace 계약을 먼저
고정하며, 이 Q0 결과 승인 전에는 시작하지 않는다.
