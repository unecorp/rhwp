# Task M100 #4969 W10-Q4-D4-B — Rust bounded CanvasKit selector 결과 후보

- **상위 계획**: [`task_m100_4969_w10_q4_d4.md`](../plans/task_m100_4969_w10_q4_d4.md)
- **계획 checkpoint**: `c11e84935`
- **기준 devel**: `upstream/devel@8a150f9a8bb1` (merge `bcaf86e18`)
- **기계 판독 증적**:
  [`w10_q4_d4_b_rust_selector.json`](../tech/investigations/issue-4969/w10_q4_d4_b_rust_selector.json)
- **실행일**: 2026-08-31 KST
- **승인 판정**: `qualified-rust-bounded-selector`
- **메인테이너 결과 승인**: 2026-08-31 KST
- **Studio selector·pixel 변경**: 0 — Q4-D4-C에 닫힘

## 결론

Rust selector는 이제 D3-C target의 두 leaf를 `CanvasKit`과 `CanvasKitBrowser`에서만
`verticalGlyphRun`으로 선택한다. Native Skia·SVG·Canvas2D는 기존 `TextRun` fallback을 유지한다. 허용은
backend 이름 하나가 아니라 provenance·variant·writing mode·orientation·direction·shape engine·portable
font 진단의 전체 tuple을 현재 객체의 capability로 판정한다.

bounded provenance가 붙은 run은 tuple 일부를 horizontal 값으로 바꿔도 일반 horizontal lane으로 우회하지
못한다. exact tuple이 아니면 writing-mode와 vertical-orientation authority가 닫힌 것으로 판정해 fallback한다.
generic vertical run의 기존 guard도 그대로 유지된다.

## 1. red-first

D3-C target 계약을 다음 기대값으로 먼저 바꿨다.

- CanvasKit·CanvasKitBrowser: leaf 2개 모두 `verticalGlyphRun`
- Native Skia·SVG·Canvas2D: leaf 2개 모두 `TextRun`
- wrong provenance·variant, sideways, horizontal tuple, glyph transform: 전부 `TextRun`

구현 전 첫 실행은 CanvasKit target이 여전히 fallback을 선택한 지점에서 실패했다. bounded helper 구현 뒤 같은
계약은 통과했다. Rust와 Studio 양쪽 문자열만 검사하는 기존 D4 source red는 Studio가 아직 닫혀 있으므로
의도대로 red를 유지한다.

## 2. 허용 tuple

다음 조건을 모두 만족해야 두 CanvasKit backend의 pending reason을 해제한다.

1. `boundedVerticalHwp5TableCellV1` + `verticalGlyphRun` + one-part non-fallback variant
2. run·shape key 모두 `vertical-rl`, orientation `vertical-upright`, transform 없음
3. run·shape key 모두 `ltr`, bidi 0
4. `rustybuzz-q4-vertical-v1`, fallback policy `none`
5. variation·synthetic style 없음
6. variant와 diagnostics 모두 exact, portable·strict, mismatch/missing/fallback-font 0
7. vertical resource requirement 세 개가 정확히 존재

그 뒤에도 기존 glyph count·position·advance·paint·font resource·digest·face·finite 상한 검사는 전부 실행된다.

## 3. 의미 대사

| gate | 결과 |
| --- | ---: |
| CanvasKit target selection | 2 / 2 GlyphRun |
| CanvasKitBrowser target selection | 2 / 2 GlyphRun |
| replay-plan direct-required selected GlyphRun | 2 |
| published fallback TextRun | 2 유지 |
| Native Skia·SVG·Canvas2D false GlyphRun selection | 0 / 6 |
| malformed tuple false selection | 0 / 10 |
| `text_v2` strict vertical slot | 0 / 2 — backend-agnostic fallback 유지 |

## 4. 검증 결과

| gate | 결과 |
| --- | --- |
| D4-B target + mutation semantic contract | 통과 |
| atomic activation | 9 passed / 0 failed |
| Q4 request 계약(D4 full red 제외) | 21 passed / 0 failed |
| layer renderer selector unit | 44 passed / 0 failed |
| CanvasKit replay-plan unit | 50 passed / 0 failed |
| integration manifest | 1,057 sources / 4,680 attrs / 48/48 targets |
| focused lib Clippy `-D warnings` | 통과 |
| `cargo fmt --all -- --check`·`git diff --check` | 통과 |

manifest 배정은 test 본문 가중치 변화로 `regression_suite_020`에서 `regression_suite_023`으로 이동했다. 최초
파생 harness가 이전 배정인 상태에서 0 tests가 발생했지만 `--prepare` 후 9/9를 실행했다. generated
suite·manifest는 source 변경에 포함하지 않는다.

## 5. 보호 불변식

- generic vertical orientation/writing-mode guard 전역 해제: 0
- Native Skia·SVG·Canvas2D authority 변경: 0
- horizontal Q2/Q3 selector unit 회귀: 0
- D3 glyph/resource/geometry·`text_v2` 변경: 0
- Studio source·runtime capability·pixel 변경: 0
- 새 integration `.rs`, generated suite·manifest, Cargo marker: 0
- public schema·private corpus·Hyper-V·GitHub mutation: 0

## 다음 승인 경계

메인테이너가 `qualified-rust-bounded-selector` 결과를 승인했다. format·diff·상태를 재확인한 뒤 별도
승인을 받아 D4-B checkpoint를 고정한다. Q4-D4-C Studio runtime capability·pixel proof는 checkpoint 뒤
다시 별도 진행 승인을 받아 시작한다.
