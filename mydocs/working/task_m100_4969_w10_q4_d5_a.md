# 결과 보고서 — Task M100 #4969 W10-Q4-D5-A 측정 계약·재현 하네스

- **계획 checkpoint**: `a4248b9c6`
- **기계 판독 증적**:
  [`w10_q4_d5_a_measurement_contract.json`](../tech/investigations/issue-4969/w10_q4_d5_a_measurement_contract.json)
- **수행일**: 2026-08-31 KST
- **자동 승인 판정**: `qualified-measurement-contract`
- **제품 출력 변경**: 0

## 결론

기존 `issue_4969_shaping_atomic_activation.rs`에 release-only ignored Rust A/B/A receipt를, 기존 Studio
`issue-4969-shaping-replay.test.mjs`에 환경변수로만 열리는 actual CanvasKit receipt를 추가했다. 새 integration
source, product `#[cfg(test)]` seam, 공개 API·schema, production timer는 추가하지 않았다.

Rust는 동일 process에서 no-source legacy A1, exact-source bounded B, 별도 no-source legacy A2를 순서대로
측정하고 layout·layer build·JSON·Rust replay-plan을 분리한다. Studio는 같은 actual software Canvas와 Noto
face에서 legacy `drawText` A1/A2와 bounded `drawGlyphs` B를 분리한다. 두 하네스 모두 warm-up 20회, 본 측정
64회와 구조·hash·pixel assertion을 wall-time보다 먼저 고정한다.

기본 실행에서는 Rust receipt가 ignored되고 Studio receipt는 비활성이다. 명시 실행에서만 한 process raw JSON을
냈고 모든 구조적 gate를 통과했다. 따라서 D5-A 판정은 `qualified-measurement-contract`다.

## 1. 구조적 계약

| case | vertical TextRun | vertical GlyphRun | blob / face | payload |
| --- | ---: | ---: | ---: | ---: |
| A1 legacy | 2 | 0 | 0 / 0 | 0 |
| B bounded | 2 | 2 | 1 / 1 | 2,519,996 bytes |
| A2 legacy | 2 | 0 | 0 / 0 | 0 |

A1과 A2의 layer JSON·CanvasKit plan BLAKE3는 각각 완전히 같았다. B는 D4의 2 fallback + 2 glyph + unique
Noto resource 계약을 유지했다.

Studio actual CanvasKit에서 A1/B/A2는 모두 같은 pixel BLAKE3
`3de701e651c3f23c655d35693ca54920507adea9bf3cbf734de0f9fbd1f9169f`를 냈다. 단순 CJK target에서
legacy shaping과 serialized glyph replay가 같은 시각 결과를 낸다는 양성 control이다.

## 2. 단일 process pilot

아래 값은 하네스 동작 확인용 1회 pilot이며 성능 판정이 아니다. D5-B에서 9개 독립 process로 다시 측정한다.

| Rust axis, 64회 합계 | A1 | B | A2 |
| --- | ---: | ---: | ---: |
| warm layout | 648,096 ns | 1,150,149,059 ns | 620,573 ns |
| layer build | 117,262 ns | 225,139,037 ns | 116,107 ns |
| JSON serialize | 2,512,577 ns | 89,903,074 ns | 2,815,302 ns |
| replay plan | 234,925 ns | 69,211,154 ns | 235,618 ns |

| actual CanvasKit draw, 64회 합계 | A1 | B | A2 |
| --- | ---: | ---: | ---: |
| two primitives/iteration | 11,505,596 ns | 10,993,970 ns | 10,834,949 ns |

Rust B의 큰 pilot signal은 full font preparation·resource/JSON 비용이 반복되는지 D5-B에서 원인을 분해해야 할
단서다. 1회 값을 회귀 판정이나 SLA로 사용하지 않는다. CanvasKit draw primitive 자체는 B가 A bracket 안에 있다.

## 3. 검증

- atomic activation 기본: 9 passed / 0 failed / 2 ignored
- Rust release receipt: 1 passed / 0 failed
- Studio 기본 #4969 E2E: passed
- Studio measurement receipt: passed
- Studio 전체: 1,324 passed / 0 failed / 1 skipped
- integration manifest: 1,064 sources / 4,690 attrs / 48/48 targets
- integration 정책: 22 passed / 0 failed
- focused test Clippy `-D warnings`: passed
- `cargo fmt --all -- --check`·`git diff --check`: passed

최초 release compile은 10분 38초였지만 all-target codegen 비용이며 timed region에 포함하지 않았다. D5-B는 생성된
release test executable을 재사용한다.

## 4. 검증 중 정정

1. 존재하지 않는 임시 suite 이름과 router script를 처음 가정했다. 통과로 기록하지 않고 manifest의 실제
   `regression_suite_028` 배정을 조회해 정정했다.
2. Studio receipt가 legacy와 bounded pixel이 달라야 한다고 잘못 가정해 첫 명시 실행이 실패했다. 실제 같은
   pixel이 올바른 양성 결과이므로 equality 계약으로 정정하고 기본·measurement E2E를 모두 재통과했다.
3. `LayerBuilder::build` 반환형에 불필요한 `expect`를 붙인 초안을 컴파일 전에 정정했다.

## 다음 자동 승인 경계

`qualified-measurement-contract` 결과와 checkpoint 생성을 자동 승인 처리한다. checkpoint 뒤 Q4-D5-B의 독립
process 9회 raw 수집·통계 집계·resource 원인 분해와 causal WASM A/B를 계속한다.
