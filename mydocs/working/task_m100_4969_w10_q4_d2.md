# Task M100 #4969 W10-Q4-D2 — 한 표 셀 atomic layout commit 결과

- **상위 계획**: [`task_m100_4969_w10_q4_d.md`](../plans/task_m100_4969_w10_q4_d.md)
- **D1 checkpoint**: `d0948166a`
- **D2 기준 merge**: `7a7b316506ff` (`upstream/devel@628944a33cb5`)
- **최신 원격 확인**: `upstream/devel@bd78a53122e4` (`43 ahead / 22 behind`, D2 WIP 직접 중첩 0)
- **D2 checkpoint**: `992fe4acaf92`
- **최신 devel merge**: `049b73d0cd80` (`upstream/devel@bd78a53122e4`, 충돌 0)
- **기계 판독 증적**:
  [`w10_q4_d2_atomic_layout_commit.json`](../tech/investigations/issue-4969/w10_q4_d2_atomic_layout_commit.json)
- **실행일**: 2026-08-30 KST
- **판정**: `qualified-atomic-layout`
- **메인테이너 결과 승인**: 2026-08-30 KST
- **제품 출력 변경**: 승인된 HWP5 1×1 표 셀 subset만 가능
- **GlyphRun·backend publication**: 0 — Q4-D3·D4는 닫힘

## 결론

Q4-D2는 `layout_vertical_cell_text`의 기존 즉시 mutation 경로 앞에 하나의 제한된 transaction 경계를
추가했다. HWP5 의미 계보, `text_direction=2`, one cell·paragraph·line·source run·column, pure CJK,
100% 장평·0 자간·무합성 스타일, host가 exact slot에 등록한 공개 Noto Sans KR bytes라는 조건을 모두
통과해야만 후보가 된다.

후보는 Q4-C/D1의 동일 geometry `Arc`에서 glyph bbox, run/line bbox, next inline/column origin을 읽는다.
line owner와 문자별 `TextRun` fallback, page-local vertical sidecar를 전부 메모리에서 준비한 뒤
`commit_bounded_vertical_hwp5_table_cell` 한 곳에서만 frame ID와 sidecar, cell children을 게시한다.
실패하면 이 commit을 호출하지 않거나 sidecar validation 전에 멈춰 기존 문자별 legacy 루프를 그대로 실행한다.

## 1. 구현 계약

1. `LayoutEngine`은 exact registry의 immutable clone으로 D2 vertical context를 만들며 parser나 font name은
   source를 등록하지 않는다.
2. 최초 target은 Noto Sans KR Regular 공개 파일의 SHA-256
   `6e06a7fe5d696ca719894a23f36bb2b1be8c816a5937cd4ad0f23ca67780dd74`와 정확히 일치해야 한다.
3. Q4-C line/bbox/next-origin owner는 pointer identity가 같은 `Arc` 하나다.
4. page sidecar는 node 중복, registry generation, source identity, entry/source/byte 상한을 mutation 전에 검사한다.
5. node ID는 읽기 전용 preview로 overflow를 먼저 검사하고, sidecar attach 성공 뒤에만 한 번 전진한다.
6. D2는 sidecar를 paint에 노출하지 않는다. fallback `TextRun`은 남고 vertical `GlyphRun` 생성은 D3 권위다.

## 2. 검증 결과

| gate | 결과 |
| --- | --- |
| D2 synthetic HWP5 target | Q4-C 고정 line/run bbox mismatch 0, one line owner + two fallback runs |
| no exact source control | legacy line children `[1, 1]` 유지 |
| non-Noto exact source | typed source identity rejection, sidecar count·generation mutation 0 |
| duplicate sidecar | typed duplicate rejection, 기존 owner·count·generation 보존 |
| Q4 계약 green | 35 passed / 0 failed, D3·D4 red 2건 제외 |
| 기존 #4969 product suite | 8 passed / 0 failed / 1 ignored |
| #6029 vertical cell | 1 passed / 0 failed |
| integration source 정책 | 19 passed / 0 failed |
| focused lib Clippy `-D warnings` | 통과 |
| `cargo fmt --all -- --check` | 통과 |
| 표준 Docker WASM | 최종 source 재빌드 6m 28s, `pkg/rhwp_bg.wasm` 9,860,053 bytes |

`cargo clippy --lib --all-features -- -D warnings`는 D2 source가 아니라 기존 optional GPU graph의
`vello 0.9`와 `vello 0.10` `Scene` 타입 충돌에서 중단됐다. 기본 제품 gate인 focused lib Clippy와 Docker
WASM은 통과했으므로 D2 회귀로 분류하지 않는다. WASM 크기는 동일 toolchain의 사전 D2 빌드와 짝지은
delta가 아니므로 절대값 참고 자료일 뿐 성능·크기 개선 주장에 사용하지 않는다.

## 3. non-target canonical 대조

두 control은 exact source를 자동 등록하지 않으므로 D0와 byte·SHA-256이 같아야 한다.

| sample | pages | overflow | SVG bytes | SHA-256 | D0 mismatch |
| --- | ---: | ---: | ---: | --- | ---: |
| `samples/table-004.hwp` | 1 | 0 | 214,449 | `7711baa0afe033c044605ff1afd5597534a8f246819bf12e929ac3c5ab93873f` | 0 |
| `samples/issue6029/3200477_icao_procedure.hwpx` | 1 | 0 | 273,101 | `27ac3f000875a1413a9a99bb3f8ad4c1fb8c21a942a3b9fd6476df72030328e9` | 0 |

임시 SVG 디렉터리는 해시 확인 뒤 휴지통으로 이동했고 worktree에 산출물을 남기지 않았다.

## 4. 보호 불변식 대사

- exact source 미등록·비-Noto·HWPX·code 1·multi 구조·mixed/문장부호·합성 style: legacy fallback
- target geometry owner pointer 수: 1
- rejection 뒤 sidecar residue: 0
- D0 canonical sample mismatch: 0
- horizontal shaping authority 변경: 0
- paint resource·GlyphRun·backend feature 선택: 0
- 새 integration `.rs`, generated suite·manifest, Cargo marker: 0
- private corpus·Hyper-V·GitHub mutation: 0

## 5. 최신 devel 재확인

결과 후보 작성 뒤 읽기 전용 fetch에서 `upstream/devel`이 `628944a33cb5`에서 `bd78a53122e4`로
22커밋 전진한 것을 확인했다. 처음 `HEAD..upstream/devel`을 비교하면 원격에 아직 없는 우리 브랜치 43커밋이
역방향 삭제처럼 함께 나타나므로 incoming 판정에 사용할 수 없다. 공통 기준 `628944a33`에서 최신 원격까지의
올바른 범위로 다시 대사한 결과, 현재 D2 WIP 12개 파일과 직접 겹치는 incoming 파일은 0개다.

따라서 uncommitted WIP 위에는 merge하지 않았다. 메인테이너의 결과 승인 뒤 D2를 `992fe4acaf92` checkpoint로
고정하고, 별도 승인에 따라 `upstream/devel@bd78a53122e4`를 일반 merge했다. merge commit은
`049b73d0cd80`이며 충돌은 0건이다.

## 6. 최신 devel merge 뒤 재자격화

최신 `devel` merge 뒤 파생 integration harness를 review worktree에서 다시 준비하고 다음 게이트를 통과했다.
파생 suite·manifest는 커밋 대상에 포함하지 않았고, canonical 임시 SVG는 해시 확인 뒤 휴지통으로 이동했다.

| gate | merge 뒤 결과 |
| --- | --- |
| `cargo fmt --all -- --check`·`git diff --check` | 통과 |
| focused lib Clippy `-D warnings` | 통과 |
| integration manifest | 1,040 sources, 48/48 targets 통과 |
| Q4 계약 green | 35 passed / 0 failed, D3·D4 red 2건 제외 |
| #4969 atomic activation | 8 passed / 0 failed / 1 ignored |
| #6029 vertical cell | 1 passed / 0 failed |
| `samples/table-004.hwp` canonical | 214,449 bytes, 기존 SHA-256 일치 |
| `samples/issue6029/3200477_icao_procedure.hwpx` canonical | 273,101 bytes, 기존 SHA-256 일치 |
| 표준 Docker WASM | 2m 38s, `pkg/rhwp_bg.wasm` 9,860,053 bytes |

merge 뒤 branch는 `upstream/devel`보다 45커밋 앞서고 뒤처짐은 0이다. `pkg/`는 ignore 대상이며 검증 종료 뒤
tracked 작업 트리는 깨끗했다. 따라서 최신 `devel` 유입으로 인한 D2 보호 불변식 회귀는 관찰되지 않았다.

## 다음 승인 경계

메인테이너가 Q4-D2 결과, checkpoint 및 최신 `devel` merge·재자격화를 승인했다. 이 재자격화 기록은
후속 문서 commit 승인 뒤 고정한다. Q4-D3 vertical GlyphRun publication은 별도 계획 재검토와 착수 승인
전에는 시작하지 않는다.
