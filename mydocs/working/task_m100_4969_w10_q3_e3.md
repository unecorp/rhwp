# Task M100 #4969 W10-Q3-E3 — bounded explicit composer activation 결과

- **결과 상태**: `qualified-bounded-explicit-geometry`, 메인테이너 승인 완료
- **실행일**: 2026-08-29 KST
- **기준 checkpoint**: `3005302519f97a884912750f0de9168d92a8bd2f`
- **검증 기준 upstream**: `d29b5fd4ba40a5bd194975be9af7e852232872b6`
- **현재 upstream**: `2bcf9b261c3b761d114bc2b3a35ed85ccd1e461e` (`26 ahead / 7 behind`)
- **기계 판독 증적**:
  [`w10_q3_e3_bounded_explicit_composer.json`](../tech/investigations/issue-4969/w10_q3_e3_bounded_explicit_composer.json)

## 판정

Q3-E3는 **qualified-bounded-explicit-geometry**다. default transaction은 instance request를 암묵적으로 읽지 않고,
composer가 exact slot request의 존재를 확인한 뒤에만 별도 explicit transaction을 연다. 활성 범위는 전체가 modern
Hangul syllable이거나 ASCII Latin letter인 한 줄·한 run·한 slot 문단이다.

Happiness Sans VF Title 요청에서 같은 measurement provenance가 line width·TextRun bbox·다음 origin·page sidecar에
이어져 폭이 `21.0px`에서 `19.840000000000003px`로 변했다. 요청 해제 뒤 layer JSON은 baseline과 완전히 같았다.
mixed script와 가운데 정렬은 요청이 있어도 문단 전체가 baseline JSON과 같았다.

Q3-E4가 소유할 variable `GlyphRun + GlyphOutline` 원자 게이트는 아직 열지 않았다. explicit sidecar는 paint lowerer에서
prepared-source cache나 resource를 건드리기 전에 거절되므로 제품에는 legacy TextRun만 남는다. 따라서 E3의 제품 변화는
조판 geometry뿐이고 부분 portable publication은 0이다.

## 구현 경계

- default/explicit transaction이 공통 paragraph 알고리즘을 호출하는 measurement session 계약을 추가했다.
- Q2 `is_bounded_horizontal_shaping_candidate_text`와 old-Hangul segment selector는 수정하지 않았다.
- 별도 `is_bounded_explicit_instance_candidate_text`는 request 확인 뒤에만 호출된다.
- modern Hangul과 ASCII Latin을 섞지 않고, Latin ligature cluster가 두 scalar 이상을 합치면 전체 rollback한다.
- style·hard boundary·bidi·display projection·condense·control·tab·overlap·scale 중 하나라도 불일치하면 전체 rollback한다.
- layout preflight는 request slot·request generation·source generation을 현재 context와 다시 대조한다.
- E4 전 explicit paint lowering은 `explicitInstancePublicationPending`으로 resource mutation 전에 fail-closed한다.

## 검증 결과

- composition handoff: **5 passed / 0 failed**
- paragraph transaction Q2 회귀: **8 passed / 0 failed**
- atomic activation module: **7 passed / 0 failed**
- glyph lowering: **16 passed / 0 failed**
- shadow context green: **17 passed / 0 failed**
- Q3-E red filter: **3 passed / 1 failed** — 남은 실패는 의도된 E4 atomic outline publication 한 건
- all-target Clippy `-D warnings`: 통과
- `cargo fmt --all`, `cargo fmt --all -- --check`, `git diff --check`: 통과
- Docker WASM release + `wasm-opt`: 6분 19초, 통과

default 출력 영수증은 E0/E1/E2와 동일하다.

- layer JSON: 619,562 bytes,
  BLAKE3 `0b5212cc076c34dce706039d7c4da85936c0a6769e83f08baa7f158cbd9029de`
- CanvasKit plan: 1,755 bytes,
  BLAKE3 `a98e6933d76901cfa55a43205c758675a8864527f87f8bed7a689d16267cbb56`
- explicit Title geometry: baseline 21.0px, selected 19.84px, delta -1.16px
- explicit `GlyphRun`/`GlyphOutline` 제품 게시: 0/0

WASM 산출물은 9,795,240 bytes,
SHA-256 `37e83b9f2225f1ed3e82307e22cad7b7dec18cc02482d3b1b23dc5a7781057ba`다. E2보다 13,134 bytes
(0.134266%) 증가했다. 최초 기록은 request 확인 순서의 마지막 정정 전에 실행한 산출물을 적은 오류였고, 최신 devel
병합 뒤 checkpoint와 제품 source가 동일함을 확인한 상태에서 Docker 빌드를 다시 실행해 이 값으로 정정했다. 이 값은 E3
참고 영수증이며 9회 성능·최종 크기 hard gate 판정은 Q3-E5에서 수행한다.

## 원격 drift

검증 뒤 fetch에서 `upstream/devel`이 기준보다 7 commit 전진했다. 신규 변경은 CI runtime 분배·security sweep 관련
39개 파일이며 E3가 수정한 source/test 7개 파일과 교집합은 0이다. 현재 결과를 승인받은 뒤 checkpoint를 만들고,
최신 devel을 병합해 focused/default/WASM 재자격화를 수행하는 것이 안전하다.

## 보호 불변식

- request 없는 default layer JSON·CanvasKit plan delta: 0
- Q2 old-Hangul predicate·segment selector 확대: 0
- request 존재 확인 전 explicit candidate scan/transaction: 0
- mixed script/run/slot의 부분 geometry activation: 0
- E4 전 variable GlyphRun/GlyphOutline 부분 게시·resource mutation: 0
- parser/font-name/font fallback registry 변경: 0
- 새 integration source·generated suite·manifest·Cargo marker stage: 0
- private corpus·Hyper-V·한컴 Oracle·static instance font 사용: 0

## 다음 승인 경계

Q3-E3 결과와 구현·결과 checkpoint 생성을 승인받아 고정했다. 현재 upstream drift가 있으므로 다음 경계는 최신 devel
병합·재자격화 계획 승인이다. Q3-E4 착수, remote push, PR, GitHub comment는 자동 승인되지 않는다.
