---
kind: guide
status: active
canonical: mydocs/tech/agent_roadmap/README.md
last_verified: 2026-08-10
---

# 에이전트-네이티브 로드맵 R1~R100 — 상세 문서 지도

> **v0.8.4 정책 변경:** 트랙 G의 Python·Node 바인딩과 신규 설치 채널은 #4655에서
> 철회됐다. 트랙 G와 2026H2 조사 문서는 historical evidence로만 보존하며, 현재
> 지원 범위나 착수 승인으로 해석하지 않는다.

이 문서 세트는 루트 [프로젝트 로드맵](../../../ROADMAP.md)의 **“AI 활용과 자동화” 분야를 자세히
설명하는 기술 지도**다. 프로젝트 전체의 버전 우선순위나 v1.0.0 완료 조건을 대체하지 않으며,
이 세트의 단계가 구현·PR·병합을 자동 승인하지 않는다.

이슈 **#3907** 이 조망(스냅샷)이고, **이 문서 세트가 단계별 상세의 canonical** 이다.
진행 갱신은 "이슈 코멘트 + 이 세트의 해당 트랙 문서 PR" 을 짝으로 한다 — 둘 중
하나만 고치면 조망과 상세가 갈라진다.

목표 문장 하나로 요약하면: **"새로 온 에이전트가 매뉴얼 없이 30분 안에 첫 유효
기여를 만들고, 도구의 일상 정비를 에이전트가 맡는 문서 엔진"** — 그 판정 실험이
R100 이다.

## 운영 규칙 (이 지도의 본체)

1. **착수는 단계 번호가 아니라 근거가 결정한다.** `[가설]` 단계는 각 문서에 적힌
   **착수 게이트**(필요한 실측·판단)를 통과하기 전에 구현하지 않는다.
2. **완료 표기는 머지 링크와 함께만** 한다 (#3608 규칙 승계).
3. **열린 PR 캡 10 내외 준수** — 이 지도는 큐를 늘리는 면허가 아니다.
4. 단계 번호는 **의존성 순서**지 일정이 아니다. 트랙 간 병행은 정상이다.
5. 상세의 상위 원천(이슈·구현 PR·기존 설계 문서)이 이 세트와 다르면 **그쪽이
   이긴다** — 이 세트는 조망을 상세화한 것이지 원천을 대체하지 않는다.

## 증거 등급

| 태그 | 뜻 | 착수 조건 |
|---|---|---|
| `[완료]` | 머지된 실적 — 링크 필수 | (없음 — 회귀 가드만 유지) |
| `[실측]` | 측정·재현이 존재 | 설계 확정 후 바로 |
| `[문서]` | 설계 문서가 존재 | 판단 지점 답 후 |
| `[이슈]` | 업스트림에서 추적 중 | 이슈의 다음 행동 |
| `[가설]` | 근거 확보가 선행 조건 | **착수 게이트 통과 전 구현 금지** |

<!-- roadmap-progress:begin — tools/roadmap_progress.py --write 가 생성한다. 손으로 고치지 말 것 -->

집계 (단계 제목 태그 전수, 기계 산출): **완료 36 · 실측 10 · 문서 7 · 이슈 3 · 가설 44 = 100**

| 트랙 | 완료 | 실측 | 문서 | 이슈 | 가설 | 계 |
|---|---|---|---|---|---|---|
| A 봉투 | 7 | 0 | 0 | 1 | 2 | 10 |
| B 가드·보안 | 3 | 2 | 1 | 1 | 3 | 10 |
| C 동시성 | 0 | 1 | 3 | 0 | 6 | 10 |
| D 발견 | 7 | 1 | 0 | 1 | 1 | 10 |
| E 실물 능력 | 7 | 0 | 0 | 0 | 3 | 10 |
| F 규모 | 1 | 2 | 0 | 0 | 7 | 10 |
| G 바인딩 | 1 | 1 | 2 | 0 | 6 | 10 |
| H MCP | 6 | 0 | 1 | 0 | 3 | 10 |
| I 표준 | 3 | 0 | 0 | 0 | 7 | 10 |
| J 자율 | 1 | 3 | 0 | 0 | 6 | 10 |
| **합계** | **36** | **10** | **7** | **3** | **44** | **100** |

<!-- roadmap-progress:end -->

집계는 위 블록이 유일하다 — `tools/roadmap_progress.py`(R94 P2, #4107)가 트랙
문서의 단계 제목 태그를 전수 파싱해 `--write` 로 생성하고, 검증 모드(무인자
실행)가 블록과 실물의 불일치·결번·중복·통제 어휘 밖 등급을 잡는다. 손집계는
지도가 늘면 실물과 갈라진다는 것이 실측이라(#3907 안에서 같은 부류 오류 6건,
[진행률 기계 산출](../autonomous_maintenance/progress_machine_readable.md) §1)
손으로 고치지 않는다.

조망 이슈 #3907 의 개략 수치(완료 35 / 추적 15 / 가설 50)와 다른 이유는, 이
세트를 쓰며 각 단계를 저장소 실물과 대조해 등급을 더 엄격히 매겼기 때문이다 —
예: 파이썬 예제는 실재 9편이라 R69 를 `[완료]` 가 아니라 `[실측]` 로,
R61(바인딩 표류)·R76(문서 지능 서버)은 머지 실적이 아니라 설계 문서라 `[문서]`
로 내렸다. **이 세트가 canonical 이므로 이 세트의 숫자가 맞다.** 가설이 절반에
가까운 것이 정상이다 — 다 채워져 있으면 지도가 아니라 소설이다.

착지 보정(2026-08-06): R4·R5·R6·R32·R34·R81 이 인용한 PR(#3897·#3903·#3808·
#3898)은 개별 close 됐고 **내용은 누적 통합 PR #3912 로 devel 에 머지**됐다 —
이 행들의 "완료 표기는 머지 링크와 함께"가 가리키는 머지 실적은 #3912 다. 같은
통합으로 R12 의 산출물(실패 경로 전수·미선언 명령 감지, `tools/agent_preflight.py`
실재)이 착지해 R12 를 `[실측]` 으로 올렸다(DoD 의 CI 상시화만 잔여 — `[완료]`
는 그 뒤). R94 는 파서 실측(설계 문서 §2 + #4107 구현)으로 `[실측]` 이 됐다.

착지 보정(2026-08-09): R46 이 인용한 PR #3875·#3921 도 개별 CLOSED 지만 내용은
통합 PR #3895(머지 커밋 `c09c67324`)·#3935(머지 커밋 `301d0fe5f`)로 devel 에
착지했다 — R46 을 `[이슈]`에서 `[완료]`로 올렸다. 같은 라운드에서 트랙 D R40
(레시피 09·10, 이슈 #4110·#4111, 머지 PR #4182·#4183)과 트랙 F R55
(`tools/scale_ladder.py` + 실측 보고서 착지)는 `[가설]`→`[실측]`, 트랙 I R84
(관찰 노트)와 트랙 J R93(assignee 세션 잠금)은 통합 PR #4314 실적으로
`[완료]`가 됐다. K 트랙(집계 밖)도 K3~K10 전원이 `[가설]`→`[실측]`으로 착지 확인됐다.

## 트랙 지도

| 트랙 | 질문 | 문서 |
|---|---|---|
| A (R1~10) | 봉투를 믿을 수 있는가 — L1 무결 | [track_a_envelope.md](track_a_envelope.md) |
| B (R11~20) | 가드와 보안이 구멍을 먼저 찾는가 — L2 | [track_b_guards_security.md](track_b_guards_security.md) |
| C (R21~30) | 에이전트 여럿이 안전한가 — L2 동시성 | [track_c_concurrency.md](track_c_concurrency.md) |
| D (R31~40) | 처음 온 에이전트가 길을 찾는가 — L3 | [track_d_discovery.md](track_d_discovery.md) |
| E (R41~50) | 실물 문서를 실제로 다루는가 | [track_e_capabilities.md](track_e_capabilities.md) |
| F (R51~60) | 규모에서 버티는가 | [track_f_scale_perf.md](track_f_scale_perf.md) |
| G (R61~70) | 철회된 바인딩·플랫폼 검토 이력 | [track_g_bindings.md](track_g_bindings.md) |
| H (R71~80) | 서버로서 성립하는가 — MCP | [track_h_mcp_server.md](track_h_mcp_server.md) |
| I (R81~90) | 표준이 되는가 — 생태계 | [track_i_standards.md](track_i_standards.md) |
| J (R91~100) | 에이전트가 스스로 정비하는가 — 엔드게임 | [track_j_autonomy.md](track_j_autonomy.md) |

**집계 밖 트랙** (R1~R100 기계 집계에 포함되지 않음 — L·K 단계는 R번호를 쓰지 않는다):

| 트랙 | 질문 | 문서 |
|---|---|---|
| K (K1~10) | 스킬이 자기서술과 표류 없이 자라는가 | `.claude/skills/` + 가드 |
| L (L1~8) | 에이전트가 어느 문으로 들어오든 같은 축으로 이어지는가 — 다표면 채택 중력 | [track_l_adoption_gravity.md](track_l_adoption_gravity.md) |

## 조사 문서 — 게이트 근거의 원천

가설 단계의 착수 게이트를 여는 실조사는 이 세트 안에 문서로 남긴다. 트랙 문서가
"지금"을 서술하면, 조사 문서는 그 서술의 출처가 된다.

| 문서 | 질문 |
|---|---|
| [trend_survey_2026h2.md](trend_survey_2026h2.md) | 2026H2 도구 설계 동향에 비추어 로드맵에 실제로 비어 있는 표면은 (제안 T1~T4) |
| [user_demand_survey_2026h2.md](user_demand_survey_2026h2.md) | 사용자·생태계 수요 실측 — 도달·배포 축(R38·R39·R63·R86·R87)의 게이트 근거와 사실 정정 (#4327) |

## 단계 서술 형식 (전 트랙 공통)

각 단계는 여섯 항목으로 적는다. 항목이 비면 비었다고 적는다 — 빈 칸을 문장으로
채우지 않는다.

- **한 줄** — 무엇이 달라지는가.
- **지금** — 현재 상태와 증거(실측·머지·이슈 링크).
- **설계** — 어떻게 만드는가(완료 단계는 "무엇이 보장되는가"로 대체).
- **착수 게이트** — 시작 전에 확보해야 하는 근거·판단(가설 단계의 핵심).
- **DoD** — 관측 가능한 완료 판정(테스트·수치·가드).
- **의존** — 선행 단계.

## 층 모델과의 관계

#3880 의 4층(L1 표면 / L2 신뢰 / L3 도달 / L4 표준)이 세로축이고, 이 로드맵의
트랙이 가로축이다: A→L1, B·C→L2, D→L3, I→L4, E·F·G·H 는 층을 관통하는 능력
기둥, J 는 전 층이 맞물려야 성립하는 지붕이다. 층 모델 자체의 canonical 은
#3880(및 그 문서 축)이다.

## 이 세트가 하지 않는 것

- 일정 약속 — 어디에도 날짜가 없다. 순서는 의존성이다.
- 원천 서술의 복제 — 완료 단계의 구현 상세는 해당 PR·이슈가 원천이다.
- 은닉·압축 통신, 잠금 데몬, 에이전트 간 전송 프로토콜 — 트랙 C 문서의
  "비목표" 절이 이유를 적는다.
