# 2026-08-22 오늘할일

## CI 완료 외부 PR 통합 검토

- [x] 최신 `upstream/devel@81ddd8c4e` 위 `review/ci-green-open-prs-20260822`를 만들고, CI가 완료된
  #5888, #5897, #5898, #5902, #5903, #5904, #5905를 번호 순으로 `cherry-pick -x` 적용했다.
- [x] source head와 check-runs를 재확인했다. #5900은 archive B/C 실패, #5909/#5911은 CI 진행 중이라
  포함하지 않았다.
- [x] focused contracts 10건, 전체 nextest 8,171건, clippy, Native Skia, locked WASM, Chrome 실제
  package smoke를 통과했다. Docker는 호스트 미설치로 표준 Docker WASM을 실행하지 못했으며 raw
  locked WASM 결과만 기록했다.
- [x] renderer PR #5903/#5904/#5905의 한글 정본 비교 PNG를 `mydocs/pr/assets`에 보존하고, 원 PR별
  archive 검토 문서에 수용 근거와 정량 결과를 기록했다.
- [ ] 통합 PR을 원격에 만들기 전 최신 source head, `upstream/devel` 정합성, mergeability를 다시 확인한다.
- [ ] 통합 PR의 GitHub CI가 완료된 뒤 각 원 PR/issue 수용 comment, close, 첫 기여자 절차 및 branch/worktree
  정리를 `post_merge.md`에 따라 처리한다.

## PR #5896 첫 기여자 수식 메타데이터 보정

- [x] 작성자 `@Shadungi`가 GitHub `FIRST_TIME_CONTRIBUTOR`임을 확인하고 reviewer `@jangster77`을
  요청했다.
- [x] 원 PR의 구역 0/64 `instance_id` 충돌을 재현하고, source head
  `ac7c82d1f8bad2422b29a72bbbf59123ae336249` 위에 maintainer 보정 `3368a7336`을 분리했다.
- [x] focused 계약 5건, 전체 nextest 8,161건, clippy, fmt, test manifest와 unit-tier 정책을 통과했다.
- [x] archive review 기록과 오늘할일을 source branch에 push하고, code candidate `4ba01d160`의 Full CI,
  CodeQL, Adapter, Proptest와 mergeable `CLEAN`을 확인했다. 첫 기여자 보호 정책의 workflow 승인도 완료했다.
- [ ] 실측 CI를 기록한 trailing docs-only commit의 fast-pass와 최신 head를 확인한 뒤 PR을 merge한다.
- [ ] merge 뒤 [#5891](https://github.com/edwardkim/rhwp/issues/5891) 상태를 확인하고, 첫 기여에 대한 환영과
  구체적 보정·검증 근거를 PR/issue comment로 남긴 뒤 branch 정리를 한다.

## #4962 W4 조판 위험 순위

- [x] 최신 `upstream/devel@0f9ceeb19`을 `task_m100_4962`에 정상 병합하고 W3 Node 계약 47건,
  #4962·장평·저장 frame·LineSeg focused Rust 14건을 통과했다.
- [x] private 32건 ingress가 32/32 성공했고 기존 Stage 4-B와 failure·count·category·join·usage row 및
  semantic body hash가 exact임을 확인했다. 새 10k 전수 계측은 필요하지 않다.
- [x] W3 decision aggregate와 POC 장평·자간·context·stored LineSeg의 compatibility를 감사하고
  [W4 수행계획](../plans/task_m100_4962_w4.md)을 작성했다.
- [x] Stage W4-1에서 W3 동결 입력·historical 정의·candidate identity·risk 식·privacy 계약과 schema를
  고정하고, 공개 fixture 기반 ranker 요구사항이 미구현 모듈로 RED임을 확인했다.
- [x] Stage W4-2에서 결정론적 streaming ranker를 구현하고 보존된 W3 r2 단일 입력을 두 번 실행했다.
  위험 문자 2,061,732자의 category·format·LineSeg lane·후보 분모와 privacy가 exact이고 두 출력 bytes와
  hash도 일치했다.
- [x] Stage W4-3에서 근거 flag와 민감도 변형을 결합해 351개 face의 안정 band를 판정했다.
- [x] Stage W4-4에서 A+B 17개 W5 action queue와 후보별 다섯 질문을 공개 projection으로 고정했다.
- [x] 메인테이너가 최종 보고와 W5 action queue를 승인해 W4 종료 게이트를 통과했다.
- [x] #4962 대형 PR 제출 범위와 최신 `upstream/devel@0f9ceeb19` merge simulation을 검증했다.
- [x] detached review worktree에서 integration manifest, Node·Rust focused, release build·lib, 전체
  nextest 8,129건, Native Skia 3종, clippy, doc test, Markdown 링크와 표준 Docker WASM을 통과했다.
- [x] 승인 후 원격 `task_m100_4962` branch를 push하고 PR #5857을 생성했다.
- [x] PR #5857의 code candidate `683fc2c73` self-review에서 차단 결함이 없음을 확인했고,
  최종 보고서의 명시적 `Issue: #4962` 누락을 문서 정정으로 보완했다.
- [ ] self-review·오늘할일 trailing 문서 commit을 별도 승인 후 push하고, 최신 PR head의
  GitHub Actions·mergeability와 메인테이너 merge 승인을 확인한다.

## #4963 W5 Oracle Profile·controlled ladder

- [x] Stage W5-1에서 W4 동결 queue와 5-state ladder, evidence 상태·관계·환경·privacy 계약을 profile
  schema와 negative fixture로 고정했다.
- [x] Stage W5-2에서 rank 1 공개 synthetic HWPX, SFNT/PDF bounded analyzer와 17개 source readiness
  원장을 구현했다. exact SFNT 6개, 기존 HFT evidence 1개, source-unavailable 10개를 구분했다.
- [x] 한컴 2022 #2430 evidence hash를 재검증해 `한양신명조`·`휴먼명조` historical profile 2개로
  투영하고 누락 provenance를 추정하지 않았다.
- [x] 메인테이너가 준비한 한컴 2020과 공식 파일 접근 보안 모듈에서 HWPX open을 feature detection으로
  재검증했다. build `11.0.0.9136`에서 reference와 W5 fixture 모두 open=true였다.
- [x] 설치 상태 무변경 selection probe에서 rank 9 `맑은 고딕`, rank 13 `휴먼명조`, rank 16
  `한컴 윤고딕 230` exact readback과 rank 1·KoPubWorld 2종·음성 대조군의 `함초롬바탕` fallback을
  분리했다.
- [x] rank 9 read-only canary를 acceptance-primary profile로 고정했다. Windows/Hancom font 914개
  manifest digest, process reset, exact installed SFNT hash, `INPILL+MalgunGothic` subset, U+AC00의
  source hmtx 2048/2048과 PDF advance 8.120862, 30줄·1쪽을 기록했다.
- [x] Stage W5-3 회귀 5건, profile 계약 13건, W5-2 회귀 6건과 tracked profile 3/3 검증을 통과했다.
  raw manifest·PDF·관측 JSON은 저장소 밖 owner-only이며 private corpus와 제품 metric/fallback/paint는
  변경하지 않았다.
- [ ] Stage W5-4 disposable snapshot·복원 절차·대상 font set 승인 전 system font mutation을 금지한다.
- [x] Stage W5-4A에서 rank 1·13·7의 fixture-declared `substFont` 입력을 byte-exact로 고정하고 5개
  질문을 exact-only·subst-only·none-related 3개 고유 실행으로 정규화했다.
- [x] 외부 snapshot attestation, 실행 전후 restore, baseline manifest 복구, unrelated font projection
  동일성과 target exact/subst font만 허용하는 fail-closed validator를 구현했다. 회귀 8건이 통과했다.
- [x] 현재 호스트는 Hyper-V VM inventory 권한과 checkpoint/restore 증거가 없어 `qualified=false`,
  `mutationAllowed=false`로 판정했다. font·registry·한컴 bundle mutation은 0건이다.
- [ ] W5-4B는 외부 control plane에서 식별 가능한 VM/checkpoint와 baseline restore probe가 준비된 뒤
  세 고유 상태의 실제 한컴 PDF 실행을 시작한다.

## 진행 중

- [ ] #5845, #5849, #5850 통합 검토 PR의 trailing docs-only fast-pass 확인 후 자동 승인·admin merge
  - 원 PR별 archive 검토 기록과 Hancom 2020 PDF/visual sweep 증적을 같은 통합 PR에 포함했다.
  - code candidate는 Full CI, CodeQL, Canvas visual diff, Native Skia, archive shard를 포함해 모두 성공했다.
  - 이 커밋으로 실제 CI 시간과 각 PR의 수용 판정을 기록한다.

## 열린 PR #5856-#5887 통합 검토

- 최신 `upstream/devel@65f71270f` 위 `review/open-prs-20260822`에서 CI가 green인 #5856, #5858,
  #5865, #5868, #5869, #5878, #5883, #5887을 번호 순으로 누적 적용했다. source head, mergeability와
  source CI는 통합 PR 작성 시점에 모두 다시 확인했다.
- #5859는 #5848/#5851과 중복되고 source가 `dirty`라 수용 후보에서 제외했다.
- 메인터너 보정 `4b28259bb`은 generated `regression_suite_014`를 써야 하는 IR sweep 명령을 고치고,
  #5865 적용 후 29에서 8로 줄어든 overflow baseline을 재측정해 강화한다.
- 전체 nextest는 **8,160 passed, 39 skipped**이며 clippy, native-Skia, WASM build, IR/overflow baseline,
  fmt, manifest/tier, merge-tree 검증을 완료했다. #5830 p34-35, #5863 p8-9, #5879 p19-20의 visual sweep
  결과와 잔여 fidelity 한계는 원 PR별 archive review에 남긴다.
- 통합 PR #5889의 code candidate `4b28259bb`는 Build & Test, archive build/shard, Lint, Native Skia,
  CodeQL Rust/JavaScript/Python, Canvas visual diff, Adapter inter-diff, Proptest roundtrip을 모두 통과했다.
  WASM과 frontend unit skip은 변경 범위 정책에 따른 정상 결과다.
- 원 PR별 archive review와 대표 asset을 같은 branch의 trailing docs-only commit으로 추가한다. fast-pass와
  최신 mergeability 확인 뒤 작업지시자 승인으로 merge하며, 이후 원 PR/issue comment·close 및 integration
  branch 정리를 수행한다.

## merge 후

- [ ] 실제 merge SHA의 `upstream/devel` 포함을 확인한다.
- [ ] #5843, #5715, #5798, #5821, #5822, #5828, #5847, #5723, #5729와 원 PR #5845, #5849, #5850에
  검증·시각 증적·수용 사유 comment를 남기고 close한다.
- [ ] 통합 branch와 이 검토를 위해 만든 baseline worktree 및 전용 target을 정리한다.

## #3514 Chrome packaged smoke PR 게시

- [x] `upstream/devel`이 `d5f0f8dc9`에서 `65f71270f`까지 71커밋 전진한 것을 확인하고 기존 Stage
  커밋 5개를 충돌 없이 리베이스했다.
- [x] review-only suite를 임시 준비해 `cargo fmt --all`과 CI 동일 `--check`를 통과했다. 파생 harness와
  manifest는 PR에 stage하지 않는다.
- [x] fresh locked WASM, Studio type·1,065 pass/1 skip, extension Node 132/132, Firefox build, dist 3/3을
  최신 base에서 통과했다.
- [x] Chrome 10회 첫 실행은 1/10 surface 전에 fixture proxy socket의 처리되지 않은 `ECONNRESET`으로
  실패했다. Stage 7에서 정상 client abort 처리와 계약 테스트를 추가한 뒤 10회를 처음부터 다시 실행한다.
- [x] Stage 7 socket/page-budget 계약 4/4와 전체 extension Node 134/134를 통과했다.
- [x] 새 code head에서 production build 1회 뒤 새 profile 10개를 retry 없이 10/10 통과하고 dist 계약
  3/3을 재확인했다.
- [x] Stage 7 문서 커밋과 origin push 뒤 Open PR [#5888](https://github.com/edwardkim/rhwp/pull/5888)을
  생성하고 게시 title·base·head·한글 본문을 API로 재검증했다.
- [x] collaborator self-review 라우팅을 적용해 `pr_5888_review.md`를 작성했다. reviewer는 지정하지 않는다.
- [x] code candidate `38c7d2ba5`의 frontend package·Build & Test aggregate·CodeQL·Proptest·Adapter
  checks 성공을 확인했다.
- [ ] review-only trailing commit을 push하고 최신 PR head의 aggregate·mergeability를 다시 확인한다.

## PR #5917 HWP 2024 원격 MCP 클라이언트

- [x] 최신 `upstream/devel@b9eb551070f7e6b8ecf4272efebe1d2094fbeb9e` 기준으로
  `feat/hwp2024-mcp-client`가 `0 behind / 3 ahead`이고 rebase가 필요 없는 상태임을 확인했다.
- [x] dependency-free HWP 2024 원격 MCP client artifact, HWP 저장 버전별 2020/2024 선택 문서,
  인증 사용자 제한과 비동기 우선 흐름을 검토했다.
- [x] 두 tarball CLI, Markdown 상대 링크, JSON example 11개, manifest, fmt와 `git diff --check`를
  통과했다. 실제 HWP 2024 동기 HWP→HWPX와 비동기 HWP→PDF 결과의 client/server SHA-256도 일치했다.
- [x] PR [#5917](https://github.com/edwardkim/rhwp/pull/5917)의 code candidate `408510964`에서 CI,
  CodeQL, Adapter inter-diff와 Proptest roundtrip이 모두 성공하고 `mergeStateStatus=CLEAN`임을 확인했다.
- [x] collaborator self-review로 `pr_5917_review.md`를 작성했으며 작성자 본인이므로 reviewer는 지정하지 않는다.
- [ ] review-only trailing commit을 push하고 최신 head의 required checks와 mergeability를 확인한 뒤
  squash merge한다.
- [ ] merge SHA의 `upstream/devel` 포함, PR 상태, local/remote branch와 임시 검증 자원 정리를 확인한다.
