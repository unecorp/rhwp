---
kind: daily-order
status: active
date: 2026-08-31
---

# 오늘 할 일 - 2026년 8월 31일

## #6534 공공기관 XLSX 다운로드의 HWP 자동 열기 오탐

- [x] 몇몇 공공기관 사이트에서 보고된 XLSX 다운로드 자동 열기 현상을 공유 판정 함수와 격리
  packaged Chrome에서 재현했다. 정상 `.xlsx` URL은 탭 0개, XLSX filename/MIME이지만 `.hwp`
  URL인 응답은 rhwp 탭 1개가 생성됐다.
- [x] 이슈 #6534를 `bug`, `browser-extension`, `rhwp-studio`, `test`, `v1.0.0`으로 등록하고
  `edwardkim`을 assignee로 지정했다.
- [x] 열린 중복 PR·기존 task 문서가 없고 로컬 `devel`과 최신
  `upstream/devel@9edf3d82`가 일치함을 확인한 뒤 `task_m100_6534` 브랜치를 만들었다.
- [x] 사이트별 블랙리스트가 아니라 다운로드 신호 우선순위·이벤트 보류·HWPX 구조 판정의 두
  방어선을 다루는 [수행계획서](../plans/task_m100_6534.md)를 작성했다.
- [x] 메인테이너가 수행계획을 승인했다. 계획 checkpoint를 커밋한 뒤 구현계획서를 작성한다.
- [x] 계획 checkpoint `86bb071b8`을 고정하고 실제 호출 계보·추적 HWPX 534개 구조 감사를 바탕으로
  [구현계획서](../plans/task_m100_6534_impl.md)를 작성했다.
- [x] 메인테이너가 구현계획을 승인했다. 구현계획 checkpoint를 고정한 뒤 Stage 1 red 계약을 작성한다.
- [x] Stage 1에서 공유 6건·Chrome 3건·Firefox 3건·Studio 2건·Rust 2건의 의도된 red를 고정하고,
  기존 observer 14건과 정상 합성 HWPX·format·unit tier 보호계약이 green임을 확인했다.
- [x] [Stage 1 결과](../working/task_m100_6534_stage1.md)를 `qualified-red-contract`로 판정했다.
- [x] Stage 1 checkpoint `6c1fb7b72` 뒤 별도 승인을 받아 공유 3상태 판정과 Chrome/Firefox adapter를
  구현하고 다운로드 계층 80/80 및 CI service-worker 묶음 131/131을 통과했다.
- [x] [Stage 2 결과](../working/task_m100_6534_stage2.md)를 `qualified-download-decision`으로 판정했다.
- [x] Stage 2 checkpoint `e99bf4cef` 뒤 별도 승인을 받아 Stage 3 Studio ZIP 후보·Rust HWPX 구조
  확정을 구현했다.
- [x] 일반 OOXML·불완전 ZIP focused 계약, 실제 ZIP HWPX 525건과 비밀번호 표본을 통과하고
  [Stage 3 결과](../working/task_m100_6534_stage3.md)를 `qualified-hwpx-structure`로 판정했다.
- [x] 독립 release warm 9회에서 525건 전체 중앙값은 934 ms에서 977 ms로 +43 ms(+4.60%)였고,
  계측용 detached worktree·전용 target·복사 바이너리를 정리했다.
- [x] Stage 3 checkpoint 뒤 별도 승인을 받아 격리 packaged Chrome 실제 download E2E를 추가하고,
  XLSX 2건은 탭 0·HWP 2건은 각각 탭 1임을 확인했다.
- [x] 첫 full release-test가 드러낸 기존 threat-scan 합성 HWPX entry 누락과 전체 병렬에서만 실패한
  schema runtime lookup을 두 차례 승인된 계획 변경으로 원인 교정했다.
- [x] Rust lint 묶음, 세 번째 full release-test 8,881/8,881, Docker WASM, Studio와 Chrome/Firefox
  build·test·dist·packaged smoke를 모두 통과했다.
- [x] [Stage 4 결과](../working/task_m100_6534_stage4.md)를
  `qualified-packaged-download-boundary`로 판정했다.
- [x] Stage 4 checkpoint `8d29dfed8` 뒤 별도 승인을 받아 Stage 5 문서·결산을 시작했다.
- [x] 전진한 `upstream/devel@891e395bb`를 병합하고 오늘할일 conflict에서 #6534와 원격 #6513 섹션을
  모두 보존해 merge checkpoint `0e3c5b524`로 고정했다.
- [x] canonical 브라우저 개발·배포 문서에 metadata 3상태 우선순위, ZIP 후보/Rust HWPX 확정 경계와
  실제 packaged download E2E 재현 절차를 반영했다.
- [x] 실제 기관 원응답 미확보, Safari 선행 후보 게이트 차이, 525개 HWPX 성능 기초값과 잔여 위험을
  [최종 보고서](../report/task_m100_6534_report.md)에 명시했다.
- [x] 최신 병합 head에서 Rust lint, 8,908/8,908 full nextest, Docker WASM, Studio 1,330건과
  양쪽 확장 build·dist·packaged smoke·download E2E를 모두 통과했다.
- [x] Stage 5 checkpoint `8a7af6a18` 뒤 exact diff를 감사하고 원격 `task_m100_6534` branch를 push한 뒤
  `devel` 대상 Open PR [#6547](https://github.com/edwardkim/rhwp/pull/6547)을 생성했다.
- [x] [PR #6547 self-review 초안](../pr/archives/pr_6547_review.md)에서 metadata 우선순위·observer 상태
  전이, ZIP/HWPX 경계, packaged browser 증적과 잔여 위험을 재검토했다.
- [x] code candidate `8a7af6a18`의 CI run `33448288095`가 base 대비 source unit test 증가
  `4,223 > 4,221`, `src/parser/mod.rs::tests#1 59 > 57`을 거부한 것을 로컬 base-aware 검사로 재현하고
  self-review 판정을 `머지 보류`로 정정했다.
- [x] 승인 뒤 새 회귀 2건을 `tests/cases/issue_6534_download_format_boundary.rs` 원본으로 이동하고,
  base-aware tier 4,221/4,221, Rust lint, focused 2/2와 full nextest 8,908/8,908을 통과한 correction
  `108d61281`을 push했다.
- [x] 새 code candidate `108d61281`에서 CI run `33449946805`, CodeQL `33449946802`, Render Diff
  `33449946311`, Adapter inter-diff `33449946831`, Proptest `33449946839`가 모두 성공했고
  `MERGEABLE/CLEAN`임을 확인했다.
- [ ] green code candidate 뒤 review-only 기록을 후속 commit으로 push하고 trusted fast-pass·required
  aggregate와 최신 mergeability를 확인한다.
- [ ] 별도 merge 승인 뒤 정상 merge commit 방식으로 병합하고 #6534 close·후속 정리를 수행한다.

## #6513 글뒤로/글앞으로 전면 그림의 #1995 후보 및 분모 정렬

- [x] 외부 PR #6513 code head `ef8b0b66d8ef14d92015b1471d6151553af12b2a`의
  `BehindText`/`InFrontOfText` 그림을 낱장 후보와 전면 그림 과반 분모에서 함께 제외하는
  변경과 HWP5 왕복 계약 3건을 검토했다.
- [x] #703/#1955의 본문 플로우 제외 불변식과 #4654의 #1995 전면 그림 과반 정책을 근거로,
  새 휴리스틱이 아닌 기존 정책 정렬임을 확인했다. 코드 검토 판정은 `승인`이며 메인터너
  코드 보정과 별도 private PDF visual sweep은 필요하지 않다.
- [x] code head의 CI, CodeQL, Render Diff, Adapter inter-diff, Proptest와 실행된 required
  worker가 성공했고, WASM/frontend expected skip을 확인했다.
- [ ] 검토 기록과 오늘할일만 담은 trailing document-only head의 CI 및 `MERGEABLE/CLEAN`을
  확인한 뒤 일반 merge commit으로 병합한다.
- [ ] 병합 뒤 #6511 자동 종료, devel push 검증, contributor 후속 기록과 branch/worktree 정리를
  `post_merge.md`에 따라 처리한다.

## #6523 kevin9327 renderer 통합 검토

- [x] reviewer `postmelee`가 지정되지 않은 #6420, #6423, #6427, #6428, #6435, #6441을 체리픽하고,
  #6423 unit test 호출 및 #6441 쪽 분할 셀 TAC 흐름 범위를 메인터너 보정했다.
- [x] code head `dd00ad020`에서 Build & Test, Lint, Native Skia, CodeQL, Canvas visual diff,
  adapter inter-diff 및 prop roundtrip이 모두 통과했다. 전체 release-test nextest는
  `8870 passed, 46 skipped`였다.
- [x] 현재 `upstream/devel@e718f40bb`는 code head의 rebase base보다 #6522 merge 후 기록 두 commit만
  앞섰다. 이미 통과한 code head를 재작성하지 않고, 검토 기록과 오늘할일만 담은 trailing commit으로
  trusted review-only CI를 확인한다.
- [ ] trailing document-only head의 CI와 mergeability를 확인한 뒤 #6523을 merge한다.
- [ ] merge 뒤 source PR별 검토 결과 comment·close, merge SHA의 `upstream/devel` ancestry와
  branch/worktree 정리를 `post_merge.md`에 따라 완료한다.
## #6522 Dependabot 12건 통합과 quick-xml 0.42 호환

- [x] Dependabot 원 PR #6499~#6510을 provenance-preserving cherry-pick으로 통합하고,
  quick-xml 0.42 문자열 XML API 호환 보정을 `f7ebe3366`에 반영했다.
- [x] 통합 PR #6522를 merge commit `15ea9ab0a8aaa09cd6692b1700f73fe14a308e20`으로 병합했다.
  PR head CI·CodeQL·Render Diff·Adapter inter-diff·Proptest와 로컬 full nextest
  8,862 passed, 46 skipped를 확인했다.
- [x] merge SHA의 devel CI와 CodeQL은 성공했다. Adapter·Proptest aggregate 취소는 후속
  `devel` push 때문이며 해당 실제 worker 두 건은 성공했다.
- [ ] 옵션 2 문서 기록 PR 병합 뒤 원 Dependabot PR별 수용 comment·close와 temporary branch 정리를
  `post_merge.md`에 따라 완료한다.

## CI 녹색 외부 PR 통합 검토

- [x] reviewer `postmelee`가 지정된 PR은 제외하고 #6482, #6483, #6484, #6487만 최신
  `upstream/devel` 위에 누적 cherry-pick 했다.
- [x] 전체 release-test nextest 8,794 passed, 43 skipped와 두 보정 후 focused 23 passed, source-side
  tier 기준선 4,221 tests/299 modules, native/WASM lint를 확인했다.
- [x] #6482/#6483/#6484/#6487의 engine-matched PDF visual sweep과 review asset을 보관했다.
- [x] 통합 PR #6490의 code head `2024da02cde595a179655b8e697d6f0ab8f8b509`에서 Build & Test, Lint,
  Native Skia, CodeQL을 포함한 34 check-run 완료/실패 0건과 `MERGEABLE/CLEAN`을 확인했다.
- [ ] CI 실측값을 담은 trailing 문서 head의 review-only CI를 확인한 뒤 merge한다.
- [ ] merge 뒤 contributor PR별 visual evidence comment, PR close, `upstream/devel` ancestry 확인과
  branch/worktree 정리를 `post_merge.md`에 따라 수행한다.

## planet6897 CI 통과 PR 통합 검토와 메인터너 보정

- [x] #6489, #6491, #6492, #6496, #6497, #6498, #6512, #6515, #6516, #6517(`e634e8b` 포함)을
  provenance-preserving 통합 후보에 적용하고, PR별 review와 stable visual evidence를 기록했다.
- [x] #6489 PATINVERT 연속 취소 범위, #6492/#6515 표·문단 page-flow, #6496/#6517 Hancom 2020
  엔진 및 Windows font alias 보정을 메인터너 후보에 반영했다. Hancom 기준 PDF와 rhwp 결과의
  차이는 사실대로 보존하며, pixel-identical이라고 판정하지 않는다.
- [x] focused 회귀, visual-sweep Python 46건, native-skia lib 3,948건 및 full release-test nextest
  `8,888 passed, 0 failed, 46 skipped`를 확인했다. Windows native-skia release export도 성공했다.
- [ ] 현재 후보의 source·test·증적·오늘할일 변경은 모두 미커밋이다. rebase 없이 현 상태를 유지하며,
  commit, push, PR 생성 또는 병합은 별도 승인 뒤에만 진행한다.

## #4969 W10-Q4-D4 CanvasKit vertical feature detection·pixel proof

- [x] 메인테이너가 8월 30일 작성한
  [Q4-D4 상세 수행계획](../plans/task_m100_4969_w10_q4_d4.md)의 checkpoint 생성을 승인했다.
- [x] 계획 checkpoint `c11e84935`를 고정하고 별도 진행 승인을 받아 Q4-D4-B Rust selector 절편을
  시작했다.
- [x] 두 CanvasKit backend target 4 slots는 GlyphRun을 선택하고 unsupported backend 6 slots·malformed
  tuple 10 slots는 전부 fallback함을 확인했다. selector unit 44건·replay-plan 50건도 통과했다.
- [x] 메인테이너가 D4-B `qualified-rust-bounded-selector` 결과를 승인했다.
- [x] 별도 승인을 받아 D4-B 구현·결과 checkpoint `c9b551909`를 고정했다.
- [x] 별도 진행 승인을 받아 D4-C Studio runtime capability·actual Noto pixel proof를 수행했다.
- [x] target 두 leaf의 draw 1회·고정 alpha bounds와 glyph/position/origin mismatch 0을 확인하고,
  기능·font·tuple 실패가 모두 TextRun fallback을 보존함을 확인했다.
- [x] 메인테이너가 D4-C `qualified-studio-runtime-pixel-proof` 결과를 승인했다.
- [x] 별도 승인을 받아 D4-C 구현·결과 checkpoint `b1a33b6fc`를 고정했다.
- [x] 별도 진행 승인을 받아 최신 `upstream/devel@19b89d967b15`를 merge `eb7d020e4`로 병합하고
  Q4-D4-D 교차검증을 수행했다.
- [x] #4969 계보 103건, Studio 1,324건, Node WASM 4건, Docker WASM, canonical 두 건을 통과하고
  `qualified-canvaskit-bounded-vertical-runtime` 결과 후보를 작성했다.
- [x] 메인테이너가 D4-D `qualified-canvaskit-bounded-vertical-runtime` 결과를 승인했다.
- [x] 별도 승인을 받아 D4-D 결과 checkpoint `f40987220`을 고정했다.

## #4969 W10-Q4-D5 회귀·성능·최종 판정

- [x] 별도 승인을 받아 D4-D 선행 증적과 Q2/Q3 성능 관행을 대조하고 D5 상세 수행계획을 작성했다.
- [x] 메인테이너가 [Q4-D5 상세 수행계획](../plans/task_m100_4969_w10_q4_d5.md)을 승인했다.
- [x] 자동 승인을 적용해 계획 checkpoint `a4248b9c6`을 고정했다.
- [x] Q4-D5-A test-only Rust·actual CanvasKit A/B/A 측정 계약을 구현하고 focused gate를 통과했다.
- [x] D5-A `qualified-measurement-contract` 결과와 checkpoint 생성을 자동 승인 처리했다.
- [x] D5-A checkpoint `e31c5c15d` 뒤 Q4-D5-B 독립 process 9회 계측으로 반복 exact-source 준비 병목을
  분리했다.
- [x] 수정 수행계획을 자동 승인하고 registry-owned source identity·인증된 `Arc` 재사용으로 병목을 정정한 뒤
  warm layout 약 598.64배, layer build 약 339.16배 개선을 확인했다.
- [x] causal WASM A축 역적용 충돌을 강제로 풀거나 과거 절대값으로 대체하지 않고
  `causal-size-unavailable`로 고정했다.
- [x] 최신 `upstream/devel@28057f02e`를 merge `edb9f51e4`로 병합하고 full nextest 8,859건, Native Skia,
  Docker/Node WASM, Studio 및 canonical 교차검증을 통과했다.
- [x] `table-004.hwp` canonical 변동은 upstream 단독 detached 재현으로 D5 회귀가 아님을 분리했다.
- [x] correctness·성능 qualified, causal WASM size hard gate 미충족으로 Q4-D5-C 최종 `blocked` 결과를
  자동 승인했다.
- [x] D5-C 결과를 checkpoint `a7de96b44`로 고정하고 상위 Q4-D 계보에 연결했다.
- [x] 검증 뒤 전진한 `upstream/devel@77bcaaa49`의 비제품 CI workflow 3개 commit을 overlap·merge-tree
  conflict 0으로 확인하고 merge `aecdde843`으로 병합했다. incoming Node 18건·Python 171건을 통과했으며
  로컬 `actionlint` 부재는 upstream self-review 통과 기록과 함께 남겼다.
- [x] 다음 절편 구현 승인을 받아 hard gate를 완화하지 않는 Q4-D5-D clean-room causal WASM 복구
  수행계획을 작성하고 자동 승인했다.
- [x] D5-D plan checkpoint `e866bc0b8` 뒤 source-only inverse A축을 allowlist와 정확히 일치하게 구성하고
  wasm32 check를 통과했다.
- [x] 같은 Docker image ID와 분리된 빈 volume에서 A/B 표준 WASM을 빌드해 A 9,858,318 bytes, B
  9,894,929 bytes, causal delta +36,611 bytes(+0.371371668%)를 확보했다.
- [x] D5-C의 역사적 `blocked` 기록은 보존하고 마지막 hard gate 해소에 따라 Q4-D 최종 분류를
  `qualified-bounded-subset`으로 갱신했다.
- [x] D5-D 결과·최종 분류를 checkpoint `95d450dd5`로 고정했다.
- [x] 전진한 `upstream/devel@3b301f725`의 12 commit을 overlap·merge-tree로 확인하고 일지 add/add 한 건의
  양쪽 섹션을 모두 보존해 merge `3d239b5a7`로 병합했다.
- [x] 최신 merge head에서 #4969 112/112, wasm32 check, Rust tier 4,223 tests/299 modules와 format을
  재통과했다.
- [x] 메인테이너가 D5-D 결과보고서와 Q4-D 최종 `qualified-bounded-subset` 판정을 명시 승인했다.

## #4969 W10-Q2-D5~Q4 PR 제출과 self-review

- [x] 최신 `upstream/devel@3b301f725` 포함 여부와 merge-tree 무충돌을 확인했다.
- [x] full nextest 8,863건, Native Skia, Docker WASM, Studio 1,324건과 #4969 actual CanvasKit pixel
  replay를 제출 직전 재검증했다.
- [x] 원격 branch `task_m100_4969_w10`을 push하고 `devel` 대상 Open PR #6493을 생성했다.
- [x] PR 본문은 `Refs #4969`로 연결하고 Q5/Q6 후속 범위를 명시해 이슈 자동 종료를 막았다.
- [x] 대형 변경 경로에 따라 코드·보호 불변식·성능·WASM 크기·private artifact 포함 여부를
  self-review하고 차단 finding 0건을 확인했다.
- [x] 첫 코드 후보의 Lint가 source-side test 2건 증가를 거부한 원인을 확인하고, 두 cache guard를
  기존 #4969 integration source의 공개 출력 계약으로 이동해 base-aware tier·manifest와 11건을
  재통과했다.
- [x] 정정 코드 후보 head `7a67f2508`의 Full CI 30 success, 4 expected skip, 실패·대기 0과
  `MERGEABLE/CLEAN`을 확인했다.
- [ ] review-only 기록을 별도 commit으로 push하고 trusted fast-pass 및 mergeability를 확인한다.
- [ ] 최신 head 검증과 별도 병합 승인 뒤 merge commit 방식으로 병합한다.

## #4969 W10-Q5/Q6 PR 제출과 종료 인계

- [x] Q5에서 Q2 horizontal old Hangul과 Q4 vertical HWP5 table-cell을 `bounded-subset`, Q3 variable
  instance를 `qualified`, 전체를 `bounded-subset`으로 결산했다.
- [x] 기존 #4969 integration source 3곳에 bounded·malformed·resource guard를 추가하고 focused 117/117,
  Studio resource 5/5와 native/WASM/workspace lint·build를 통과했다.
- [x] 지원·fallback·deferred matrix와 #4969·#4960 tracker 게시 전 placeholder/body-drift guard를 고정했다.
- [x] 원격 branch `task_m100_4969_w10_q5`을 push하고 `devel` 대상 Open PR #6519를 생성했다.
- [x] 코드 후보 `27f037fd5`의 GitHub Full CI 24 success, 5 expected skip, 실패·대기 0과 언어별
  CodeQL 성공, `MERGEABLE/CLEAN`을 확인했다.
- [x] [self-review](../pr/archives/pr_6519_review.md)와
  [구현 검토](../pr/archives/pr_6519_review_impl.md)에서 차단 finding 0건과 최종 판정 `승인`을 기록했다.
- [ ] review-only 기록을 같은 branch의 후속 commit으로 push하고 trusted fast-pass·required aggregate와
  최신 mergeability를 확인한다.
- [ ] 별도 merge 승인 뒤 정상 merge commit으로 병합한다.
- [ ] merge SHA·최종 CI를 tracker 초안에 반영하고 별도 승인 뒤 #4969·#4960 현행화·close와 post-merge
  정리를 수행한다.

## planet6897 CI-green PR 통합 검토

- 대상: #6526, #6528, #6529, #6530, #6532. #6514는 CI 실패로 제외.
- 로컬 integration: `review/planet6897-green-20260831`, 현재 base `upstream/devel@887b4ce15`.
- #6530은 원 PR 충돌을 메인 터너 보정 commit `b0109a1f6`으로 승계하며 원 PR 직접 merge 금지.
- 검토 기록: `mydocs/pr/pr_6526_6528_6529_6530_6532_review_impl.md` 및 개별 PR review 5건.
- 결과: Rust lint/build/전체 nextest, focused regression, Studio contract, Hancom 2020 direct visual sweep 통과. remote push/PR 생성은 미수행.
- 통합 PR #6537은 `2026-08-31T14:23:48Z`에 merge SHA `1636910809ce9d1a394b30144fff19cc5fc32826`으로 병합됐고 `upstream/devel` 포함을 확인했다. review archive 반영 뒤 관련 issue와 source PR comment 후속 처리를 진행한다.
