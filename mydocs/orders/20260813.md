# 오늘 할일 - 2026년 8월 13일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| [#3790](https://github.com/edwardkim/rhwp/issues/3790) | PR #4682 trusted enforcement 리뷰 보정 | 진행중 | focused 검증 통과, 새 head CI·보정 코멘트 대기 |
## PR #4731 - HWPX container 재귀 깊이 한계

- @kevin9327의 원 PR [#4731](https://github.com/edwardkim/rhwp/pull/4731) commit
  `f22568721e`를 최신 `upstream/devel` 위 검토 branch에 적용했다. 원 fork branch는 변경하지 않고,
  메인터너 보정을 포함한 [통합 PR #4738](https://github.com/edwardkim/rhwp/pull/4738)으로 반영한다.
- `parse_container`의 큰 재귀 프레임을 고려해 최대 64개 그룹을 허용하고 65번째를 기본 스레드에서
  거부하도록 보정했다. 원안의 32MiB 전용 스레드는 제거했고, debug·release-test focused 회귀가 각각
  2/2 통과했다. #4730은 다른 세 재귀 경로가 남아 있어 원 PR의 자동 종료 표현도 관련 이슈로 정정했다.
- `git diff --check`, 대상 파일 Rustfmt와 code candidate `3de1aff81`의 GitHub Full CI·CodeQL·
  Build & Test aggregate를 통과했다. 검토 중 `devel` 전진으로 생긴 오늘할일 충돌을 기록 보존 방식으로
  해소한 최신 head `77a61f68f`에서도 focused 회귀와 Full CI·CodeQL·Build & Test aggregate를 다시
  통과했다. `cargo fmt --check`는 이 Windows 호스트의 긴 경로 OS error 206으로 실행되지 않아 대상 파일
  Rustfmt check로 대체했다. Cargo incremental 환경 변수는 지정하지 않았다.
- 세부 판단은 [PR #4731 review](../pr/archives/pr_4731_review.md)와
  [이행 기록](../pr/archives/pr_4731_review_impl.md)에 보존한다. 최신 trailing head의 fast-pass·
  mergeability와 작업지시자 승인 뒤에만 merge하고, #4730 상태 확인과 원 PR close/comment를 후속 처리한다.

## planet6897 renderer 회귀 통합 검토 - #4734, #4735

- 최신 `upstream/devel@b5c14346d` 위 가시성 branch `review/planet6897-20260813`에 #4734을 먼저,
  #4735를 다음으로 누적 적용했다. 기준선 전진분은 오늘 할 일, archive review, 로드맵 문서뿐이어서
  자체 여섯 commit을 충돌 없이 rebase했다.
- #4734는 함초롬바탕/HCR Batang ASCII 폭의 임의 전각 오버라이드를 제거하고 실제 hmtx를 사용한다.
  글꼴 폭 계약 1건, SVG golden 8건과 한컴 2022 PDF의 `form-002` p1, `issue_157` p2 visual sweep을
  확인했으며 두 페이지 모두 자동 후보 0건이다.
- #4735는 저장된 column start 조각이 본문 경계 밖, 음수 폭으로 가지 않도록 p3 좌표 계약을 추가한다.
  신규 fixture가 IR field sweep에 자동 포함되면서 빠진 HWP5 `list_header_width_ref` 124건 baseline 한 행을
  최신 source가 보정했고, 제품 코드는 넓히지 않았다.
- `target/pr-review`에서 release-test nextest 전체 5,930 passed, 37 skipped, 7 slow, 486.981초와
  IR sweep 823 sample, 245,203 divergences, 217.02초를 통과했다.
- 개별 검토 근거는 [PR #4734](../pr/archives/pr_4734_review.md),
  [PR #4735](../pr/archives/pr_4735_review.md),
  [누적 구현 기록](../pr/archives/pr_4734_4735_review_impl.md)에 보존한다. 통합 PR의 최신 CI와
  mergeability를 확인한 뒤 merge하고, 이후 원 PR, 관련 issue close/comment와 branch 정리를 공식
  post-merge 절차로 처리한다.

## PR #4732 - 트랙 B 파서 재귀 깊이 하드닝 백로그 정합

- @kevin9327의 원 PR [#4732](https://github.com/edwardkim/rhwp/pull/4732) commit
  `d532237c7c`를 최신 `upstream/devel` 위 검토 branch에 적용했다. 원 fork branch는
  변경하지 않고, [통합 PR #4736](https://github.com/edwardkim/rhwp/pull/4736)으로 반영한다.
- 전역 R21은 트랙 C에 이미 배정되어 있으므로, 파서 재귀 깊이 하드닝 백로그를 새 R 번호가
  아닌 R13 악성 코퍼스의 후속 항목으로 편입했다. [#4731](https://github.com/edwardkim/rhwp/pull/4731)은
  아직 열려 있어 구현 검토 중으로 기록하고, R17 퍼징 CI의 회귀 유입 경로도 연결했다.
- 문서 메타데이터 555건, 변경 Markdown 링크, 로드맵 번호·집계와 `git diff --check`를 통과했다.
  code candidate `5cc2f994b2`의 GitHub CI preflight·CodeQL preflight·Build & Test aggregate도
  녹색이며 문서 전용 fast-pass에서 heavy Rust 작업은 skipped다.
- 세부 판단은 [PR #4732 review](../pr/archives/pr_4732_review.md)와
  [이행 기록](../pr/archives/pr_4732_review_impl.md)에 보존한다. 최신 trailing head의 fast-pass·
  mergeability를 확인한 뒤 merge하고, #4730 상태 확인과 원 PR close/comment를 후속 처리한다.

## kevin9327 에이전트 표면 통합 검토 - #4719, #4721, #4723, #4725, #4727, #4729

- 최신 `upstream/devel` 위의 가시성 branch `review/kevin9327-20260813`에 여섯 원 PR을 오래된
  번호 순서로 누적 적용했다. planner·조직·frame이 같은 Lint 영역을 건드린 충돌은 세 독립 계약
  단계를 모두 보존해 해소했다.
- 메인터너 보정은 미지정/`any` 이외 부서 ID의 조용한 접수처 fallback을 exit 2 거부로 바꾸고,
  닫힌 #4716의 프레임 상태, 누락된 외부 현실 채점 축과 CI 배선, `--live`의 npm·측정일 갱신,
  외부 채택의 잘못된 상한 단언을 바로잡는다.
- 고정 `target/pr-review`의 MCP 리소스 계약 3건과 계획 스키마 계약 26건, Python 계약 55건,
  JSON·Markdown·프레임 가드, 최신 `upstream/devel` merge tree와 공백 검사를 모두 통과했다.
  renderer·WASM·Studio 경로는 변경하지 않아 시각 sweep은 적용하지 않았다.
- 개별 근거는 [#4719](../pr/archives/pr_4719_review.md), [#4721](../pr/archives/pr_4721_review.md),
  [#4723](../pr/archives/pr_4723_review.md), [#4725](../pr/archives/pr_4725_review.md),
  [#4727](../pr/archives/pr_4727_review.md), [#4729](../pr/archives/pr_4729_review.md)와
  [누적 이행 기록](../pr/archives/pr_4719_4721_4723_4725_4727_4729_review_impl.md)에 보존한다.
- 통합 PR #4733 code candidate `db96780a7`의 Full CI·CodeQL은 통과했다. trailing docs-only head의
  fast-pass·mergeability와 작업지시자 승인 뒤에만 merge 및 원 PR·관련 이슈 후속 처리를 수행한다.

## PR #4717 - Studio 차트 숫자 데이터 편집 UI

- @johndoekim의 차트 숫자 데이터 편집 UI를 검토했다. 문서 순번 기반 차트 주소 대조, WASM 5종,
  대화상자·컨텍스트 메뉴·더블클릭, 코어 dry-run과 snapshot undo 배선이 이번 범위다.
- 표 셀·글상자 OLE의 `cellPath`가 레이아웃 방출부터 Studio 매처까지 같은 좌표 계약으로 이어짐을
  확인했다. 모호한 컨테이너 없는 선택은 다른 차트를 열지 않도록 거부하며, 추가 메인터너 보정은 필요하지 않았다.
- 고정 `target/pr-review`의 차트 계약 5건, Studio 단위 테스트 918건, TypeScript, `wasm-pack build`,
  foreground Vite/headless Chromium e2e를 통과했다. e2e는 메뉴·더블클릭·`4.3 → 91.7` 저장·Ctrl+Z
  원복·무편집 무흔적·비차트 OLE 차단을 실제로 확인했다.
- 원 code candidate `8bf10fa2d`의 Build & Test, Native Skia, Lint, CodeQL, Canvas visual diff가
  모두 녹색이고 최신 `upstream/devel` merge-tree도 충돌 없이 생성됐다. 상세 근거는
  [PR #4717 review](../pr/archives/pr_4717_review.md)에 보존한다.
- trailing docs-only head의 fast-pass aggregate와 mergeability를 재확인한 뒤 merge하고, #4694와
  원 PR의 close/comment 및 branch 정리를 post-merge 절차로 처리한다.

## planet6897 누적 renderer·serializer 보정 - #4688, #4695, #4696, #4697, #4700, #4705, #4707

- 최신 `upstream/devel` 위에 고유 변경만 누적 적용했다. #4695의 넓은 #4088 수정은 #4700의
  목적격 절 경계 보정으로 대체했고, #4707은 source ref의 과거 history가 아닌 고유 commit 하나만 적용했다.
  이후 #4688의 캡션·bookmark·Ruby 후속 세 commit을 원 head CI 녹색 근거와 focused 회귀로 추가했다.
- 누적 중 인라인 그림 줄/주입 탐지 경계, overlay 표 연속 조각 회귀, pagination fixture 초기화에
  메인터너 보정을 더했다. 기능을 넓힌 보정이 아니라 누적 적용에서 드러난 소유·테스트 계약을 고정한 것이다.
- 초기 누적 head의 전체 nextest 5,923건, focused table 35건, Native Skia library 58건과 placeholder 2건,
  fmt·clippy·diff check를 통과했다. 추가 #4688 code는 원 head의 Full CI와 integration focused 4건으로
  확인했다. HWP/HWPX renderer 변경의 시각 증적은 각 PR archive review에 보존했다.
- [PR #4688](../pr/archives/pr_4688_review.md), [#4695](../pr/archives/pr_4695_review.md),
  [#4696](../pr/archives/pr_4696_review.md), [#4697](../pr/archives/pr_4697_review.md),
  [#4700](../pr/archives/pr_4700_review.md), [#4705](../pr/archives/pr_4705_review.md),
  [#4707](../pr/archives/pr_4707_review.md) 및 [누적 구현 기록](../pr/archives/pr_4688_4695_4696_4697_4700_4705_4707_review_impl.md)에 판단 근거를 남겼다.
- 통합 PR의 최신 head CI·CodeQL·Render Diff 및 작업지시자 승인 뒤에만 merge하고, merge 뒤 원 PR과
  관련 이슈의 close/comment·branch 정리를 공식 post-merge 절차로 처리한다.

## PR #4703 - core-cli 기준 풀이 완결 및 트랙 L 정본화 통합

- kevin9327의 [PR #4691](https://github.com/edwardkim/rhwp/pull/4691)과
  [PR #4693](https://github.com/edwardkim/rhwp/pull/4693)을 최신 `devel` 위에 순서대로
  체리픽해 [통합 PR #4703](https://github.com/edwardkim/rhwp/pull/4703)으로 올렸다.
- `target/pr-review`에서 기준 풀이 100/100, 전체 gym 채점 221/221, Python 계약 테스트 32건,
  roadmap 진행률·변경 Markdown 링크·`git diff --check`를 모두 통과했다.
- 원 PR별 검토와 체리픽 적용 SHA는
  [#4703 review](../pr/archives/pr_4703_review.md) 및
  [이행 기록](../pr/archives/pr_4703_review_impl.md)에 남겼다.
- 최신 문서 후속 commit의 GitHub Actions와 mergeability를 다시 확인한 뒤 merge하고, #4689·#4692 및
  원 PR #4691·#4693의 close/comment·branch 정리를 post-merge 절차로 처리한다.

## Issue #4676 - HWPX curve 저장 한글 크래시

- [PR #4684](https://github.com/edwardkim/rhwp/pull/4684)의 `hp:curve` 저장을 `hc:pt` 나열에서
  `hp:seg` 체인으로 바꾸는 기여자 변경을 최신 `devel` 위에 적용했다.
- 원 PR의 HWPX `type="CURVE"`를 HWP5 Bezier `segment_types=1`로 옮기던 구현은 제어점 수 계약이
  달라 공개 HWPX 곡선 경로를 바꿀 수 있어, 메인터너 보정 `3741441a2`로 제거했다. writer의
  한글 호환 `hp:seg` 출력은 유지한다.
- HWPX CURVE XML→IR→XML 회귀를 추가해, 재저장 뒤 점 체인·`hp:seg` 출력은 유지하면서 HWP5
  Bezier 타입이 비어 있는지 자동 검증한다.
- 고정 `target/pr-review`에서 parser·serializer 집중 회귀, release-test 전체 5,910건, fmt·clippy,
  실제 HWPX `--verify`와 ZIP 구조 검증을 모두 통과했다. 보정 전후 HWPX 재저장 바이트는 동일하다.
- 원 #4684 source를 second parent로 보존한 merge commit `87fab352a`에서 parser·serializer 충돌을
  해소했다. 원 contributor history를 재작성하지 않고 기존 source branch에 fast-forward push한 뒤,
  최신 head Full CI·CodeQL을 확인한다.
- #4684 merge 뒤 충돌 해소 commit과 메인터너 보정 이유를 남겨 close한다. #4676은 한글 2022
  오라클 재개방 범위까지 완료 조건을 다시 확인한 뒤 close 여부를 결정한다.

## PR #4699 - Studio 문서 전체 HTML / Word 내보내기

- 첫 기여자인 @keepYaoung의 HTML 및 Word 호환 `.doc` 내보내기 변경을 가시성 branch
  `review/keepYaoung-4699-20260813`에서 검토했다.
- 메인터너 보정 `199caf545`는 embed host의 문서 수명주기 경계를 지키도록 두 export command를 숨기고,
  고정 100만 글자 probe 대신 engine 문단 길이를 써서 긴 마지막 문단이 잘리지 않게 하며, section 실패 때
  부분 문서를 성공으로 내려받지 않도록 export 전체를 실패시킨다.
- export·embed 집중 회귀 22건, Studio 전체 886건, production build, 최신 `devel` merge simulation,
  foreground Vite/headless Chromium 실제 download 검증을 통과했다. 최신 code head의 Build & Test,
  Frontend package gates, CodeQL, Canvas visual diff도 모두 성공했다.
- 검토와 보정 근거는 [PR #4699 review](../pr/archives/pr_4699_review.md) 및
  [구현 기록](../pr/archives/pr_4699_review_impl.md)에 보존한다. 최신 `devel`의 같은 날짜 오늘할일
  추가와 충돌은 current-base docs bridge `2569d1733`에서 이 파일만 해소했다. bridge 뒤 trailing
  docs-only head의 fast-pass aggregate와 mergeable 상태를 재확인한 뒤, 첫 기여자 환영·감사와 실제
  검증 결과를 원 PR에 남기고 merge한다.
## Issue #3438 - 스타일 저장 반환 계약 정리

- `lpaiu-cs`가 등록한 열린 이슈를 Windows 개발 환경에서 분류했다. iOS 전용(#3415), 한컴 Office
  실측 선결(#3351·#3416 및 #3438의 모달 판정), 메인터너 목표값 결정(#3230)은 이 작업에서 제외한다.
- #3438의 독립 항목인 `createStyle`/`updateStyle`/`updateStyleShapes` 반환 계약 보정은
  `local/task_m100_3438`에서 계획했다. 수행·구현 계획서는
  `mydocs/plans/task_m100_3438.md`, `mydocs/plans/task_m100_3438_impl.md`에 기록했다.
- 이슈는 `jangster77`에 할당해 병렬 세션 잠금을 설정했다. 소스 변경은 계획 승인 뒤에 시작한다.
- Stage 1에서 `createStyle`의 불가능한 음수 실패 가드를 없애고, 기존 스타일 수정 API의
  `false`는 snapshot no-op으로, 모양 수정 API의 `false`는 명시 오류로 처리했다. Rust API
  반환 계약까지 대조하는 Studio 가드를 추가했으며, focused 12건·Studio 전체 870 passed/1 skipped·
  TypeScript·프로덕션 번들 빌드를 통과했다. 상세는
  `mydocs/working/task_m100_3438_stage1.md`에 남겼다.
- 한컴 Office 2018·2022·2024 설치를 확인했다. 앞선 레지스트리 조회가 Assistant만 찾아
  Office 미설치로 판단한 것은 정정한다. 한컴 2022 실측 선결 항목은 후속 Windows 검증 대상으로
  유지한다.
- Stage 2에서 한컴 Office 2022의 새 빈 문서에 `oracle3438`을 입력하고 F6 스타일 대화상자에서
  Ctrl+Z를 실행했다. 대화상자는 열린 채로 남았고 Escape 뒤 상태 표시줄도 `10글자`를 유지했다.
  따라서 모달 중 undo 차단은 한컴과 같은 계약이며, `StyleDialog`의 도달 불가
  `history-jumped` 구독·동기화·해제를 제거했다.
- 스타일 저장의 `false`·예외는 이제 `StyleEditDialog` 확인 동작도 실패로 반환해 모달을 유지하고,
  실제 성공 때만 저장 후속 동작을 실행한다. focused 12건, Studio 전체 875 passed/1 skipped,
  TypeScript·production build, fixture를 연 로컬 Studio F6/Ctrl+Z 스모크를 통과했다. 상세는
  `mydocs/working/task_m100_3438_stage2.md`에 남겼다.
- 최신 `devel` 기준 [PR #4710](https://github.com/edwardkim/rhwp/pull/4710)을 열고 collaborator
  `jangster77`의 셀프 검토 코멘트를 제출했다. 외부 reviewer는 요청하지 않았다. code candidate
  `b3ceeb330`의 검토·검증 기록은 [PR #4710 review](../pr/archives/pr_4710_review.md)에 남겼다.
  최신 head의 required check, mergeability와 작업지시자 승인 전에는 merge·#3438 종료를 진행하지 않는다.

## Issue #3414 - 모달 종료 뒤 Ctrl+Z 입력 경로 복원

- `lpaiu-cs`의 열린 이슈를 현 Windows 환경에서 다시 분류했다. iOS 전용 #3415, 수치 목표의
  메인테이너 판단이 필요한 #3230, 한컴 동작 정답지부터 확정할 #3416·#3351은 이번 변경에서
  제외한다. #3315의 핵심 Track 3 PR #3672는 이미 merged 상태임을 확인했다.
- #3414는 마지막 `ModalDialog` 종료 때 활성 InputHandler의 textarea 포커스를 공통 복원하는
  범위로 진행한다. 중첩 모달에서는 부모 overlay가 남아 있으면 복원하지 않는다.
- 이슈를 `jangster77`에 할당해 병렬 세션 잠금을 설정했고, 계획서는
  `mydocs/plans/task_m100_3414.md`, `mydocs/plans/task_m100_3414_impl.md`에 기록했다.
- Stage 1에서 마지막 모달 종료 이벤트를 `main.ts`의 활성 InputHandler focus로 연결했다.
  부모 모달이 남은 자식 종료는 이벤트를 내지 않는다. focused 2건·TypeScript·Studio 전체
  871 passed/1 skipped·프로덕션 빌드를 통과했으며, 상세 기록은
  `mydocs/working/task_m100_3414_stage1.md`에 남겼다. 로컬 Studio 브라우저 스모크에서 일반
  모달과 F6 스타일 중첩 모달 모두 최종 모달 종료 뒤 textarea 포커스 복원과 Ctrl+Z 동작을
  확인했고, 자식 모달만 닫힌 시점에는 포커스가 복원되지 않음을 확인했다.
- Stage 2에서는 모달 외 body 포커스 공백의 undo/redo fallback과 개체 선택 Ctrl+C/X·Delete의
  dispatcher 단일화를 진행한다. 사용자 제스처를 요구하는 Ctrl+V native paste 이벤트는 유지한다.
- Stage 2 완료: body 포커스의 Ctrl+Z/Y는 dispatcher로 실행되고, 그림·표 선택 Ctrl+C/X·Delete는
  `edit:*` command를 공유한다. focused 5건·TypeScript·Studio 전체 874 passed/1 skipped·build와
  textarea 밖 버튼 포커스의 브라우저 Ctrl+Z 스모크를 통과했다. 상세는
  `mydocs/working/task_m100_3414_stage2.md`에 남겼다.
- 최신 `devel` 기준 [PR #4708](https://github.com/edwardkim/rhwp/pull/4708)을 열고 collaborator
  `jangster77`의 셀프 검토 코멘트를 제출했다. code candidate `f90f1a30a`의 검토와 검증 기록은
  [PR #4708 review](../pr/archives/pr_4708_review.md)에 남겼다. CI preflight, CodeQL preflight,
  Render Diff의 최신 head 성공과 승인 조건을 확인할 때까지 merge와 #3414 종료는 보류한다.

## Issue #3931 - 편람 declared 선이월 문턱과 셀 높이 측정 정합

- 이슈는 `edwardkim` 할당, `v1.0.0`, `bug`·`layout` 상태이며 연결된 Open PR과 현재 열린
  renderer/typeset 접합면 중복 PR이 없음을 확인했다.
- 기존 #4704 Studio WIP를 건드리지 않도록 최신 `upstream/devel` `8479c24f7`에서
  `task/3931-declared-rowbreak`와 전용 worktree `/tmp/rhwp-3931`을 만들었다.
- 최신 `release-test` 기준 편람은 HWP 393쪽·HWPX 386쪽이며 PDF 오라클은 383쪽이다.
  sec=10 `pi=14`·`pi=23` 모두 fragment scan 전에 `DIAG_ADVC`로 이월되고, `pi=23` 문제 셀은
  16줄을 `req=422.5/content=392.3px`로 과대측정한다. #3738 표·각주 회귀는 33/33 통과했다.
- 과거 #3674 되감김 패치는 HWP 393→391 효과가 있었지만 측정 과대 상태의 #3738을 깨뜨려 제거됐다.
  이번에는 셀 높이 측정 정합을 먼저 끝내고, 남은 declared 문턱만 구조적으로 일반화한다.
- 수행 계획은 [`task_m100_3931.md`](../plans/task_m100_3931.md)에 기록했다. 계획 승인 전에는
  renderer 동작 코드를 변경하지 않는다.
