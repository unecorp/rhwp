---
kind: daily-order
status: active
date: 2026-08-23
---

# 2026-08-23 오늘할 일

## #4966 — W7 canonical font registry와 backend projection

- [x] 최신 `upstream/devel@bf30bd792`과 로컬 `devel`의 일치를 확인하고 전용 로컬 브랜치
  `task_m100_4966`을 만들었다.
- [x] #4966, 상위 #4960, W1 원장, W2 Decision Trace와 W6 metric lineage를 교차 확인했다.
- [x] relation·decision plane 분리와 생성 전후 행동 불변을 중심으로
  [`task_m100_4966.md`](../plans/task_m100_4966.md) 수행계획을 작성했다.
- [x] 메인테이너가 수행계획을 승인했다.
- [x] Stage W7-1에서 30개 boundary·1,352개 candidate·600 metric anchor와 Studio runtime
  projection을 동결하고, active unknown의 legacy-preservation 계약을 정정했다.
- [x] Stage W7-1 결과 승인 뒤 Stage W7-2 canonical schema와 migration manifest를 시작했다.
- [x] 830개 canonical rule, W1 candidate link 830건(고유 677개)과 W6 metric entry reference 97개의
  일회 이행을 고정하고, active unknown 43개와 backend projection 허용표의 fail-closed 계약을 구현했다.
- [x] Stage W7-2 결과를 승인하고 Stage W7-3 정적 backend projection generator를 시작했다.
- [x] Rust 2개·TypeScript 3개 generated source와 paired manifest를 만들고, fixed allowlist·ownership·
  rollback·stale/manual edit·단일 규칙 최소 영향 계약을 구현했다.
- [x] Stage W7-3 결과 승인 뒤 Rust layout-name·layout-metric projection 소비 전환과 전건 오라클을
  통과하고 Stage W7-4를 완료했다.
- [x] 최신 `upstream/devel@87a8d3dca`의 #5944·#5945를 task branch에 merge하고 충돌과 source 겹침이
  없음을 확인했다.
- [x] Stage W7-5에서 Studio substitution 265행, 정부상징 successor 10행과 webfont catalog 153행을
  Canvas2D·webfont·CanvasKit generated projection 소비로 전환했다.
- [x] pre-migration runtime hash, W1·W2·W6·W7 Node 61건, Studio 집중 회귀 23건, 전체 Studio test와
  production build를 통과했다.
- [x] Stage W7-5 결과 승인 뒤 Stage W7-6 최종 통합 검증·운영 인계와 최종 보고를 시작했다.
- [x] generated Rust schema version 리터럴이 #4329 단일 출처 계약을 위반한 것을 full nextest에서
  발견하고 generator가 중앙 `FONT_RULE_PROJECTION_SCHEMA_VERSION`을 참조하도록 정정했다.
- [x] W1·W2·W6·W7 Node 77건, release-test 8,174건, native-skia·Clippy·rustdoc, Studio 1,068건,
  Docker WASM과 fresh trace 3건을 통과했다.
- [x] 공개 HWP 7문서 167쪽과 HWP/HWPX 대표 6문서 page 0의 native/WASM SVG byte mismatch 0을
  확인했다.
- [x] schema 1.0이 830개 migration population을 봉인한 read-only canonical 판임을 확인하고,
  정정·재생성 절차와 다음 schema 판에서의 추가·수정·retirement 조건을 운영 정본에 기록했다.
- [x] canonical fallback·W2 trace 문서, W7-6 working 보고서와 최종 보고서를 현행화했다.
- [x] W7-6 정정을 `855fbf738`로 고정한 뒤 최신 `upstream/devel@343ed2c01`을 충돌 없이 merge하고,
  새 integration source 기준 release library 4,074건·nextest 8,201건·Studio 1,070건과 native-skia,
  Docker WASM, 공개 173쪽 native/WASM byte parity를 다시 전건 통과했다.
- [x] Stage W7-6 최종 결과에 대한 메인테이너 승인을 받았다.
- [x] 최신 `upstream/devel` 포함과 필수 fmt를 다시 확인하고 `task_m100_4966@4a7c0f431`을 원격에
  push한 뒤 Open PR [#5950](https://github.com/edwardkim/rhwp/pull/5950)을 `devel` 대상으로 생성했다.
- [x] 최초 PR CI의 unit-tier 실패를 조사해 base-ref 없는 로컬 check가 신규 `#[cfg(test)]` support 6개를
  놓친 사실을 확인했다. 수기 함수를 runtime scope로 되돌리는 최초 대응은 원인 해결이 아니므로
  커밋하지 않고 철회했다.
- [x] 영향도 재감사에서 W1 역사 원본 hash와 Rust 후보 238개 migration tuple은 일치했지만, W3 계측기가
  W6의 metric-table selector 이동을 600개 의미 회귀로 오판하고 W7 검증 목록이 W3 계약을 누락한 사실을
  확인했다.
- [x] Stage W7-R 계획에 따라 W1 역사 기준선·현재 authority 수명주기를 분리하고, 제품 source의 수기
  test oracle을 제거해 `tests/cases/issue_4966_font_rule_projection.rs` 공개 API 회귀 원본으로 옮겼다.
- [x] W3 ownership drift를 semantic drift와 분리하고 W1·W2·W3·W6·W7 Node contract 87/87,
  PR-base unit-tier, Rust focused 35/35, integration 2/2와 review-worktree Clippy를 통과했다.
- [x] collaborator의 원격 `745660467`을 부모로 포함한 `fc2194b2c`에서 제품 source 내부 oracle을
  제거하고 공개 API integration 원본만 남기는 최종 tree로 rebase했다.
- [x] W1·W2·W3·W6·W7 Node 87/87, release-test 8,200/8,200, Native Skia 공식 3종, Docker WASM,
  fresh trace 3/3과 공개 173페이지 native/WASM mismatch 0을 통과했다.
- [x] W7-R4 정정 `fc2194b2c`와 기록 `bbfd3ad6d`를 원격에 fast-forward push하고, PR 본문의
  integration 경계·검증 수치를 바로잡은 뒤 Draft를 복원했다.
- [x] code candidate `bbfd3ad6d`의 Full CI·CodeQL·Render Diff·Proptest·Adapter 성공과
  `MERGEABLE/CLEAN`을 확인했다.
- [x] 대형 PR 경로에서 generated data와 handwritten runtime을 분리해 검토하고, 최초 source oracle
  이중 authority가 공개 integration으로 완결됐으며 추가 blocker가 없음을
  [`pr_5950_review.md`](../pr/archives/pr_5950_review.md)와
  [`pr_5950_review_impl.md`](../pr/archives/pr_5950_review_impl.md)에 기록했다.
- [ ] self-review·오늘할일 trailing commit을 push하고 review-only fast-pass와 최신 aggregate를 확인한다.
- [ ] 메인테이너 승인 뒤 Draft를 해제하고, 별도 병합 승인 후 정상 merge commit 방식으로 병합한다.
- [ ] merge 뒤 #4966 close, #4960 W7 상태, devel sync와 branch 정리를 확인한다.

## PR #5930 — #4963 W5 Oracle Profile·controlled ladder

- [x] W5 기술·시각 검증, 공개 Hyper-V three-state 재현 canary와 최종 보고서에 대한 메인테이너 승인을
  완료했다.
- [x] 최신 `upstream/devel@2078a2629`을 task branch에 정상 merge하고 Oracle Python 42건, Node 13건,
  계약·PowerShell AST·HWPX·Markdown·fmt 검증을 다시 통과했다.
- [x] 원격 `task_m100_4963`을 push하고 Open PR [#5930](https://github.com/edwardkim/rhwp/pull/5930)을
  생성해 base/head, UTF-8 한글 본문과 `Closes #4963`을 재검증했다.
- [x] collaborator self-review에서 공개 privacy 계약이 메인테이너의 실제 font root 절대 경로를 함께
  고정한 모순을 발견했다. `675679313`에서 runtime-provided external root 계약과 회귀 검사로 보정하고
  전체 Oracle 검증을 다시 통과했다.
- [x] 대형 PR 경로로 계약·실행기·17개 disposition·Hyper-V 복구·privacy·시각 보조 증적을 나눠 검토하고
  [`pr_5930_review.md`](../pr/archives/pr_5930_review.md)에 조건부 merge 권고를 기록했다.
- [ ] code candidate와 review·오늘할일 trailing commit의 원격 push 승인을 받는다.
- [ ] 최신 PR head의 Full CI·CodeQL·Proptest·Adapter와 `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자 merge 승인 뒤 merge하고 #4963 auto-close, #4960 W5 상태와 branch 정리를
  `post_merge.md`에 따라 확인한다.
# 2026-08-23 오늘할일

## #5931 HWP5 마지막 저장 한컴오피스 버전 판별

- [x] HWP5 FileHeader `5.1.1.0`만으로는 2022/2024를 구분할 수 없음을 확인하고,
  `HwpSummaryInformation.revisionNumber`의 주버전 12/13을 각각 2022/2024로 분류하도록 구현했다.
- [x] HWP 2018/2022/2024, HWP3, HWPX 계약과 지식 지도 필드 사전을 보완했다.
- [x] focused 계약, 전체 release-test 8,173건, clippy, fmt, manifest, unit-tier와 diff check를 통과했다.
- [x] 이슈 [#5931](https://github.com/edwardkim/rhwp/issues/5931)과 code candidate PR
  [#5932](https://github.com/edwardkim/rhwp/pull/5932)을 만들고 archive self-review를 준비했다.
- [x] [#5932](https://github.com/edwardkim/rhwp/pull/5932)가 merge SHA
  `61e4390436644d306bd2e0402bc24d841066b671`로 병합됐고, [#5931](https://github.com/edwardkim/rhwp/issues/5931)이 닫혔다.

## #5934 한컴오피스 2010 마지막 저장 버전 식별

- [x] HWP5 SummaryInformation `revisionNumber`의 주 버전 8을 `hancom-office-2010`으로 분류하고,
  새로 저장한 2010/2018/2022/2024 원본을 `samples/pr5935/`에 SHA-256과 함께 보관했다.
- [x] 새 저장 원본을 직접 읽는 focused 계약, IR field sweep, 반복 overflow-cell 원장, 전체 release-test,
  clippy, fmt, manifest, unit-tier와 diff check를 통과했다.
- [x] 이슈 [#5934](https://github.com/edwardkim/rhwp/issues/5934)와 code candidate PR
  [#5935](https://github.com/edwardkim/rhwp/pull/5935)을 만들고 archive self-review를 준비했다.
- [ ] review·오늘할일 trailing head의 GitHub Actions와 최신 mergeability를 확인한다.
- [ ] 작업지시자 승인 뒤 PR을 merge하고, merge SHA의 `upstream/devel` 포함·issue close·PR/issue comment·
  branch 정리를 `post_merge.md`에 따라 처리한다.

## PR #5938 — #4964 W6 font metric 계보 분리

- [x] historical generated 595개, #2430 measured overlay 5개와 runtime lookup을 행동 변화 없이 분리하고
  600행 lineage manifest·generator ownership 계약·Stage W6-1~5 보고를 완료했다.
- [x] full nextest 8,073건, Native Skia, Docker WASM, 공개 7문서 167쪽 native/WASM byte parity와
  W1·W5·#2430 계보 검증을 통과했다.
- [x] 원격 `task_m100_4964`를 push하고 Open PR [#5938](https://github.com/edwardkim/rhwp/pull/5938)을
  생성해 base/head, UTF-8 한글 본문과 `Closes #4964`를 재검증했다.
- [x] 대형 PR self-review에서 실행 CWD 기반 canonical output 판정 우회, generated/metadata 부분 쓰기와
  test 내부 Cargo `--locked` 누락을 발견해 [`pr_5938_review.md`](../pr/archives/pr_5938_review.md)에
  changes requested로 기록했다.
- [x] 메인테이너 승인 뒤 `f5f71f8c0`에서 checkout-root 고정, paired output commit과 lockfile 보호를
  최소 보정했다. generator 5/5, font metric 9/9, full nextest 8,173/8,173, Clippy·fmt와 Cargo.lock
  불변 검사를 통과했다.
- [x] correction과 직전 review tail을 `4778e1efd`로 push하고 Full CI·CodeQL·Render Diff·Proptest·
  Adapter 성공, 최신 `devel@e9e0d7e4b`과의 merge-tree 및 `MERGEABLE/CLEAN`을 확인했다.
- [ ] 완료된 self-review·오늘할일 trailing commit을 push하고 review-only fast-pass와 최신 aggregate를
  확인한다.
- [ ] 메인테이너 merge 승인 뒤 merge하고 #4964 auto-close, #4960 W6 상태와 branch 정리를
  `post_merge.md`에 따라 확인한다.

## PR #5944 — HWPX 마지막 저장 한컴오피스 버전 판별

- [x] HWPX `version.xml/appVersion`을 읽어 한컴오피스 2018·2020·2022·2024 마지막 저장 제품과
  정규화한 버전을 반환하고, 누락·손상·미지원 주 버전에는 제품 연도를 추정하지 않도록 구현했다.
- [x] 한컴오피스별 실물 HWPX 3개를 바이트 보존하고 HWP5/HWPX 공통 `info --json` 계약과
  CLI·에이전트 지식 지도·버전 대조·MCP 선택 문서를 갱신했다.
- [x] focused 계약 3건, 실물 CLI 4종, 전체 release-test 8,166건, clippy, fmt, suite manifest,
  unit tier, 지식 지도 계약과 Markdown 내부 링크 검사를 통과했다.
- [x] code candidate PR [#5944](https://github.com/edwardkim/rhwp/pull/5944)의 Full CI, CodeQL,
  Proptest, Adapter inter-diff와 Render Diff 성공을 확인하고
  [`pr_5944_review.md`](../pr/archives/pr_5944_review.md)에 self-review를 기록했다.
- [ ] review·오늘할일 trailing head의 review-only fast-pass와 최신 `MERGEABLE/CLEAN`을 확인한다.
- [ ] exact trailing head를 squash merge하고 merge SHA의 `upstream/devel` 포함, PR comment와
  local·remote branch 정리를 `post_merge.md`에 따라 완료한다.

## PR #5945 — PR 검토 PDF 기준 MCP 선택 명확화

- [x] #5944의 HWP5/HWPX `lastSavedWith` 계약과 HWP 2020/2024 MCP 사용 문서가 갱신된 것을 확인했다.
- [x] 기준 PDF 미첨부 시각 검증 절차에 `rhwp info --json` 선택 근거, 2020/2024 제품 매핑, `null` 보류와
  review 증적 기록을 추가했다.
- [x] `git diff --check`, `cargo fmt --all`, `cargo fmt --all -- --check`를 통과하고 문서 candidate
  `948cd3419`를 Open PR [#5945](https://github.com/edwardkim/rhwp/pull/5945)로 생성했다.
- [x] archive self-review와 오늘할일 trailing 기록을 준비했다.
- [ ] 최신 trailing head의 review-only fast-pass 집계와 `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자 merge 승인 뒤 squash merge하고 merge SHA의 `upstream/devel` 포함, PR comment와 branch
  정리를 `post_merge.md`에 따라 완료한다.

## PR #5948 — CI 통과 원 PR 11건 통합 검토

- [x] 원 PR #5900, #5909, #5911, #5915, #5926, #5927, #5928, #5936, #5937, #5939, #5942의 최신
  source head를 최신 `upstream/devel` 위에 누적 적용하고, 개별 archive review에서 모두 수용 권고로
  판정했다.
- [x] #5915의 restore 실패 시 journal 보존과 #5936의 style re-resolution import를 메인테이너 보정으로
  반영했다. #5900 보고서의 EOF whitespace도 정리했다.
- [x] code candidate `ff128fb1c915bfdb7c9c3ae74492835caf29a42d`에서 Full CI, CodeQL,
  Proptest roundtrip, Adapter inter-diff, Render Diff, Native Skia와 Frontend package gates가
  성공했고, 전체 local nextest 8,201건과 clippy도 통과했다.
- [x] HWP 2024 MCP 비동기 변환 smoke 검증에서 `test-2024.hwp`의 direct Hancom PDF 산출을 확인하고,
  다중 환경 사용 절차를 갱신했다.
- [x] 개별 review 기록, 수용 원장과 통합 구현 기록을 archive에 보관했다. generated regression harness
  drift는 이번 diff에 없으며 CI 소관의 기존 항목으로 기록했다.
- [ ] 이 오늘할일 trailing docs-only head의 review-only fast-pass와 최신 `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자 승인에 따라 merge한 뒤 원 PR·연결 issue comment/close, merge SHA의 `upstream/devel`
  포함 확인과 local review branch 정리를 `post_merge.md`에 따라 완료한다.
