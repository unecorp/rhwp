# 오늘 할 일 - 2026년 8월 15일

## Issue #4823 - 문서별 조건부 웹폰트 로드

- 문서 선언 글꼴이 시스템에 없을 때만 검증된 CDN 웹폰트를 `FontFace`로 등록하도록 보정했다.
  KoPub·KoPubWorld·정부상징서체와 검증된 Noonnu 별칭을 포함하고, 조사 TSV에서 사용 가능으로
  판정된 80개 카탈로그 항목을 생성 스크립트로 반영했다.
- 시스템 글꼴 탐색과 웹폰트 카탈로그 등록을 분리해 CDN에 등록됐다는 사실을 로컬 설치 증거로
  오인하지 않게 했다. Chrome Console의 `[FontLoader][debug]` 로그는 시스템 글꼴, CDN 후보,
  URL별 시작·성공·실패를 출력한다.
- `npx tsc --noEmit && npm test`는 933 통과·1 skip·0 실패, `npm run build`와
  `http://127.0.0.1:7700/` Studio 기동을 통과했다. 브라우저 콘솔에서 CDN 두 건의 시작·성공 및
  `2개 성공, 0개 실패`를 확인했다. Rust 파일 변경이 없어 Rust/WASM 재빌드는 수행하지 않았다.
- [PR #4857](https://github.com/edwardkim/rhwp/pull/4857)의 Stage 17~21 최신 head
  `346035fcecb7a96d4e08446047053d848e739cd5`에서 GitHub Actions가 모두 통과했다. Canvas visual diff,
  CodeQL 3축, Native Skia, test archive a/b/slow, 일반·slow test shard와 Build & Test가 성공했고,
  `Frontend unit gates`와 `WASM Build`의 `SKIPPED`는 영향 정책상 정상이다.
- Studio 최종 검증은 `npx tsc --noEmit && npm test` 935 passed/1 skipped/0 failed, `npm run build`,
  renderer contract, manifest 102/102 확인을 통과했다. `table-border-style` baseline의 CanvasKit
  compat/default 최대 비율 `0.005342831700801425`는 제한된 `0.006` 예산 안에 있다.
- [자체검토 기록](../pr/archives/pr_4857_review.md)을 최신 CI와 병합 가능 상태로 갱신했다. collaborator
  self PR이므로 reviewer는 지정하지 않으며, 작업지시자 승인에 따라 `CLEAN`/`MERGEABLE` PR을 admin merge
  대상으로 처리한다.
## Issue #4740 - workflow PR 후행 review-only fast-pass

- #4819에서 Full CI·CodeQL이 성공한 code candidate 뒤에 review 문서만 추가했지만, PR 전체 변경의
  workflow 파일 때문에 `ci-execution-surface-change` Full 실행이 반복되는 현상을 재현했다.
- 최신 `upstream/devel@62a3ba677`에서 `task_m100_4740_trusted_review_fast_pass` 브랜치를 만들고
  [수행계획](../plans/task_m100_4740.md)을 승인 기준으로 기록했다.
- default branch의 `CI Impact Policy Controller`가 same-repository PR의 exact Full candidate,
  single-parent review-only tail, GHAS CodeQL check와 current-base merge tree를 독립 검증하도록 policy v5를
  구현했다. CI·CodeQL·Render Diff는 current head·base·controller run에 결합된 `rfp=1` status가 있을 때만
  workflow 변경 PR의 heavy worker를 fast-pass한다.
- classifier 31건, policy 31건, workflow Python 계약 70건, YAML parse, 변경 manual 링크와 diff 검사를
  통과했다. 상세는 [Stage 1 기록](../working/task_m100_4740_stage1_trusted_review_fast_pass.md)에 남긴다.
- [PR #4824](https://github.com/edwardkim/rhwp/pull/4824)를 `devel` 대상으로 Open 생성했다. 초기
  code candidate 자체검토에서 single-parent 여부만으로는 first-parent 연속성을 보장하지 못하는 경계를
  발견했고, `09b923592`에서 exact 계보와 live PR head 일치를 fail-closed로 보정한 뒤 위 검증을 다시
  통과했다. 이 exact code candidate의 CI·CodeQL·Render Diff는 20 success, 정책상 3 skip,
  failure·cancelled·pending 0으로 완료됐다. 1,000줄을 넘는 workflow 보안 계약 변경이라 즉시 admin
  merge하지 않고, [자체검토 기록](../pr/archives/pr_4824_review.md)과 오늘할일 trailing head를 별도
  cycle로 검증한다.
- controller는 `main` 등록 전에는 live가 아니므로 `devel` 병합만으로 활성화되지 않는다. PR 검증과 별도
  merge 승인 뒤 정상 release에 포함해야 하며, 현재 #4819 run에는 소급 적용되지 않는다. 최신 trailing
  head의 Actions 통과와 mergeability 확인 전에는 통합하지 않는다.

## Issue #4812 - HWP/HWPX 선언 글꼴 웹폰트 공급 경로 전수 조사

- 최신 `upstream/devel@4cf8a5898` 위에 `task_m100_4812_font_webfont_survey` 브랜치를 만들고,
  문서 선언 글꼴의 웹폰트 사용 가능 여부를 재현 가능하게 조사하는 [#4812](https://github.com/edwardkim/rhwp/issues/4812)를 등록했다.
- `rhwp batch info --json`이 DOCINFO의 7개 글꼴군을 전부 내보내도록 보정했다. 조사 도구는 HWP/HWPX
  단일 파일 또는 디렉터리를 입력받아 jsDelivr·Fontsource·Google Fonts CSS API·Noonnu·OnlineWebFonts의
  실제 응답을 확인하며, 원문 선언명과 정규화 검색명을 별도로 보존한다.
- 전체 코퍼스 실행은 고유 글꼴 1,379개, 다운로드 가능 319개, 웹폰트 사용 가능 59개, 라이선스 검토
  260개, 조회 오류 1개를 기록했다. 52건의 개별 파싱 실패는 숨기지 않고 실행 로그에 파일별로 남겼다.
  상세는 [조사 보고서](../report/survey_korea_downloads_font_jsdelivr_20260815.md),
  [TSV 증적](../report/assets/survey_korea_downloads_font_jsdelivr_20260815.tsv),
  [실행 로그](../report/assets/survey_korea_downloads_font_jsdelivr_20260815.run.log),
  [Stage 1 기록](../working/task_m100_4812_stage1_multisource_webfont_survey.md)에 보존한다.
- [PR #4816](https://github.com/edwardkim/rhwp/pull/4816)를 `devel` 대상으로 Open 생성했다. 전체
  `nextest` 6,055건 통과·38건 skip, `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`,
  PR diff 검사를 통과했다. collaborator 셀프 PR이라 reviewer는 지정하지 않았으며,
  [자체검토 기록](../pr/archives/pr_4816_review.md)을 추가했다. trailing head의 CI와 최신 mergeability
  확인 전에는 통합하지 않는다.

## Issue #3950 - 한글 IME 상태 Ctrl+A 모두 선택

- 최신 `upstream/devel@93805ebb0`에서 이슈를 확인하고 `jangster77`을 담당자로 지정한 뒤,
  `codex/issue-3950-ime-ctrl-a` 브랜치를 만들었다.
- `Ctrl+A`는 현재 `e.key === 'a'`에만 의존해 한글 IME의 `ㅁ` 및 조합 중 `Process` 값을 놓친다.
  또한 조합 이벤트는 일반 Ctrl 처리 전에 반환되므로, `KeyA` 물리 키 매칭과 IME 분기 dispatch를 함께
  보정해야 한다.
- 수행계획은 [Task M100 #3950](../plans/task_m100_3950.md)에 기록했다. 코드·테스트·Windows Chrome
  검증 뒤 원격 push·PR 생성 여부는 별도 승인으로 진행한다.
- `Ctrl+A`에 `KeyA` 물리 키 보정을 추가하고, IME 조합 분기에서 Ctrl/Meta 기본 단축키를 dispatcher로
  전달하도록 수정했다. 한글 키값·`Process` 회귀 8건, `npx.cmd tsc --noEmit`, Studio Node 전체
  931 통과·1 skip을 확인했다.
- Windows Chrome에서 한글 테스트 문서의 `Ctrl+KeyA` 전 범위 선택을 확인했다. 운영체제 IME 레이아웃
  전환은 자동화 범위 밖이므로 `ㅁ`·`Process` 이벤트 값은 Node 회귀 계약으로 검증했다. 세부는
  [Stage 1 RED](../working/task_m100_3950_stage1_red.md),
  [Stage 2 보정](../working/task_m100_3950_stage2_ime_ctrl_a.md),
  [Stage 3 검증](../working/task_m100_3950_stage3_validation.md)에 남겼다.
- [PR #4805](https://github.com/edwardkim/rhwp/pull/4805)를 `devel` 대상으로 Open 생성했다. collaborator
  self-review라 reviewer는 지정하지 않았고, 코드 범위·IME 분기 우선순위·검증 결과는
  [자체검토 기록](../pr/archives/pr_4805_review.md)에 남겼다. 이 review·오늘할일 trailing head의 최신
  CI 통과 및 merge 승인 전에는 통합하지 않는다.

## kevin9327 Gym·MCP 통합 검토

- 최신 `upstream/devel@44bcba400` 위에 기본 작업공간에서 확인 가능한
  `review/kevin9327-20260815` branch를 만들고 #4783, #4785, #4787, #4789, #4791,
  #4793, #4795의 원 head를 번호순으로 누적 적용했다. 원 PR의 merge commit은 적용하지 않았고,
  원격 source branch도 변경하지 않았다.
- #4783~#4791은 원 head CI가 모두 성공했다. #4793과 #4795는 각각 `batch-ops`,
  `render-tree` pack을 추가하면서 `gym/profiles/maintainer.json` 등록을 빠뜨려
  `Validate gym scorer contracts`에서 실패했고, 그 결과 `Lint`와 종속 `Build & Test`가 실패했다.
- maintainer 보정 `335fcac05`에서 두 pack을 프로파일에 등록하고, #4783의 정적 MCP prompt
  (`triage-document`, `prove-work`)에 대한 list/get/unknown-name 계약을
  `tests/mcp_server_contract.rs`에 추가했다. contributor의 원격 head와 기능 범위는 바꾸지 않았다.
- 로컬 누적 후보 검증은 Gym Python 계약 58건 통과(의도된 1 skip),
  `mcp_server_contract` 25건 통과, `cargo fmt --all -- --check`,
  `git diff --check upstream/devel...HEAD` 통과다. Rust 변경은 MCP contract test로 집중 검증했고,
  원격 통합 PR의 Full CI는 원격 push/PR 생성 승인 뒤에만 실행한다.
- 원 PR별 검토 근거는 `mydocs/pr/archives/pr_4783_review.md`부터
  `pr_4795_review.md`까지, 누적 적용 및 보정 근거는
  `mydocs/pr/archives/pr_4783_4785_4787_4789_4791_4793_4795_review_impl.md`에 남긴다.

## Issue #4739 - Canvas2D 첫 local face와 PDF oracle provenance

- 기본 작업공간 `/home/edward/mygithub/rhwp`를 `task/4739-canvas-local-font-first-layout`
  브랜치로 전환하고 최신 `upstream/devel@44bcba400`을 기준으로 삼았다. 기존 브랜치에는 고유
  커밋이 없었고 사라진 worktree 등록만 정리했다.
- 오늘 추가된 한글 2010 PDF를 embedded TrueType outline까지 대조했다. KoPub 설치 PDF의 6개
  subset은 제공 원본 2,603개 글립과 윤곽·advance가 전부 일치한다. 미설치 PDF의 본문은 실제
  `Haansoft Batang` subset 하나로 치환됐고, 설치 PDF의 같은 Haansoft subset과 공통 26개 글립이
  윤곽·advance 모두 일치했다.
- 한글 2010은 KoPub 미설치 389쪽, KoPub 설치 388쪽이고, 한글 2020 KoPub 설치 PDF는 383쪽이다.
  PDF 정답지는 한글/PDF producer 버전과 설치 글꼴·해시가 포함된 provenance별로 분리한다.
- 현재 `devel`은 `KoPub바탕체 Light`를 `GulimChe`·monospace로 오분류하며, 저장 local-font
  snapshot도 첫 Canvas2D view load 뒤에 읽는다. 호스트 Chrome 150의 Local Font Access는 현재
  `granted`이고 KoPub 6종을 모두 반환한다.
- Rust의 KoPub돋움 `1.0em`은 과거 미설치 Haansoft PDF 오라클에 묶인 값이다. 제공 KoPub 원본의
  현대 한글 hmtx는 Dotum `0.872em`, Batang `0.936em`이므로 paint 수정과 layout metric 결정을
  분리한다. 수행계획 `mydocs/plans/task_m100_4739.md`를 작성했으며 Stage 1~3 구현은 메인테이너
  승인 대기, Rust metric 변경은 A/B 뒤 별도 승인 대기다.
- HWPX가 실제 사용하는 `정부상징 부처명_16040911`은 embedded되지 않았고 한글 대체 face를
  `한컴바탕`으로 선언한다. 물리 145쪽 표장 `행정안전부장관`은 한글 2020 PDF에서 실제
  `Haansoft Batang`으로 subset embed됐으며, 한글 2010 미설치 PDF의 같은 7글자와 윤곽·advance가
  7/7 일치한다. 제공 원본의 7글자는 `0.912109em`, PDF Haansoft는 `1.0em`이고 윤곽도 0/7
  일치이므로 exact-font와 missing-font profile을 분리했다.
- 함께 제공된 `ROKG_R.ttf`는 원문 font table에 직접 등장하지 않지만, 문화체육관광부 공식 배포
  installer가 현재 `ROKG_R.ttf`를 설치 대상으로 가진다. 구형 face와 공통 1,805자 중 1,804자의
  advance가 `0.001em` 이내이고 `행정안전부장관` raster IoU도 `0.979030~0.996408`이므로, 구형
  exact face가 없을 때만 쓰는 큐레이션된 current successor 후보로 계획을 보정했다.
- 두 TTF는 동일 바이너리가 아니다. 구형 cmap의 outline 없는 현대 한글 54자를 ROKG가 채우지만,
  U+3000 폭은 `0.299805em` 대 `0.912em`이고 세로 메트릭도 다르다. 폴백은 exact legacy → ROKG
  successor → HWPX `한컴바탕` → portable 순서로 두고, metric profile을 전역 alias로 합치지
  않는다. 세부 근거는
  [정부상징 폰트 후계·대체 매트릭스](../tech/investigations/issue-4739/task_m100_4739_government_font_successor_matrix.md)에
  기록했다.
- 구형 TTF, ROKG TTF, 공식 ZIP, 공식 Windows installer 네 파일은 모두
  `/home/edward/mygithub/ttfs/gov`에 유지한다. 공식 ZIP과 installer의 SHA-256은 각각
  `594ca3048a20788eb94afa65bde06da99f56bc02731b49ae4e1acc3b9ffd2cef`,
  `93a56bccf44837a69b43a37596d84e77c2292d0772bd4e4eb71a365e7e742e4a`이며 저장소·PR에는 포함하지
  않는다.
- 메인테이너가 수행계획을 승인했다. Stage 1 RED에서 KoPub바탕체 monospace 오분류, 정부상징
  exact→ROKG→문서 대체 face 순서 부재, 저장 snapshot의 첫 Canvas paint 후행, 감지 뒤 view repaint
  부재, Rust의 non-embedded `substFont` 소실을 각각 실패 테스트로 고정했다. 상세는
  [Stage 1 기록](../working/task_m100_4739_stage1_red.md)에 남겼다.
- Stage 2에서 저장 snapshot을 첫 Canvas paint 앞으로 옮기고 KoPub바탕을 비례폭 serif로 정정했다.
  style별 local full name을 보존하며, 정부상징 exact legacy → 확인된 ROKG → 문서 `한컴바탕` →
  portable 순서를 Rust/Studio 양쪽에 연결했다. WASM `DocumentInfo`는 non-embedded `substFont` 쌍을
  전달하고 CanvasKit preflight는 primary face만 수집한다. TypeScript focused 28건과 tsc, Rust
  style resolver 29건 및 접합면 focused 3건이 통과했으며 상세는
  [Stage 2 기록](../working/task_m100_4739_stage2_font_resolution.md)에 남겼다.
- Stage 3에서 `local-fonts-changed`를 Canvas2D의 단일 view repaint와 CanvasKit local Typeface 준비
  후 단일 repaint에 연결했다. snapshot 세대 토큰으로 중복 이벤트를 억제하고 두 backend 모두
  `loadDocument()` 재호출을 금지했다. 초기화 순서 6건, 글꼴 해소 9건, local-font 14건과 TypeScript
  검사가 통과했으며 상세는 [Stage 3 기록](../working/task_m100_4739_stage3_single_repaint.md)에 남겼다.
- Stage 5 자동 검증을 완료했다. Studio 전체 929 통과·1 skip, production build, native
  `wasm-pack --no-opt`, focused Rust/TypeScript와 실제 Chrome CDP를 통과했다. Canvas2D는 KoPub
  style face와 정부상징 exact → ROKG → HWPX `한컴바탕` 순서를 실제 font setter에서 확인했고,
  CanvasKit은 local Typeface 12개·실패 0·pending 0과 준비 뒤 단일 repaint를 확인했다. 표준 Docker
  WASM은 WSL에 Docker 명령이 없어 미실행이며 Stage 4 metric 변경도 별도 승인 전이라 수행하지
  않았다. 상세는 [Stage 5 기록](../working/task_m100_4739_stage5_validation.md)에 남겼고 최종 시각
  판정도 메인테이너가 통과로 선언했다.
- 최신 `upstream/devel@9eda49613`으로 8개 작업 commit을 rebase했다. 오늘할일 충돌에서
  기존 Gym·MCP 기록과 #4739 기록을 모두 보존했고, 최신 기준 focused Rust·TypeScript,
  `npm test` 929 통과·1 skip, build·fmt·diff·Markdown 링크 검사를 다시 통과했다.
- code candidate `4558eb25e`를 [PR #4800](https://github.com/edwardkim/rhwp/pull/4800)으로
  Open 게시했다. collaborator 자체 PR이므로 reviewer는 지정하지 않았고,
  [자체검토 기록](../pr/archives/pr_4800_review.md)을 추가했다. 작성 시점에 CI·CodeQL·Render Diff
  preflight가 실행 중이므로 최신 trailing head의 Actions 통과와 메인테이너의 별도
  merge 승인 전까지 통합하지 않는다.

## Issue #4741 - Local Font Access 부분 열거 보완

- #4739의 의도적 비범위였던 Local Font Access 부분 열거 누락을 후속 처리했다. API 존재를 전체 설치
  목록의 완전성으로 간주하지 않고, 열거 record로 해소되지 않은 현재 문서 후보만 raw Canvas descriptor로
  제한적으로 확인한다.
- snapshot v3은 열거 face와 probe-only face의 provenance, 확인·probe·미해소 후보를 분리한다.
  probe-only face는 Canvas2D exact chain에만 사용하고 SFNT bytes 없는 CanvasKit Typeface 성공으로
  가장하지 않는다. 같은 감지 세대에서는 결과를 캐시해 hot path 재측정을 금지한다.
- focused TypeScript 37/37, `npx tsc --noEmit`, Studio 전체 930 통과·1 skip, production build와
  E2E manifest 102/102를 통과했다. Chrome 151 CDP의 부분 열거 강제 E2E에서 Local Font Access 1회,
  raw/patched KoPub 폭 delta 0px와 probe-only CanvasKit bytes `null`을 확인했다.
- 실제 `samples/2025 행정업무운영 편람(최종).hwp`는 Canvas2D 383쪽을 유지했고 물리 11쪽의
  `KoPub바탕체 Light` exact face를 확인했다. 메인테이너의 시각 판정도 통과했다.
- [PR #4802](https://github.com/edwardkim/rhwp/pull/4802)를 Open 생성했고 code candidate
  `4084c024d`의 CI, CodeQL, Render Diff와 required `Build & Test`가 성공했다. 최신
  `upstream/devel@bcb65ed68`과의 merge simulation도 충돌 없이 통과했다.
- collaborator 자체 PR이라 외부 reviewer는 지정하지 않았다. 코드·backend 경계·관련 이슈 인계 판정은
  [자체검토 기록](../pr/archives/pr_4802_review.md)에 남겼다. review·오늘할일 trailing head의 최신
  Actions 통과와 메인테이너의 별도 merge 승인 전에는 통합하지 않는다.

## Issue #3931 - declared 선이월과 저장 fragment 소유권

- 전용 worktree `/tmp/rhwp-3931`의 `task/3931-declared-rowbreak` 브랜치를 당시 원격 최신
  `devel` `fbca0aa6c` 위로 1차 재기준화했다. 기본 작업공간의 #4704 WIP는 건드리지 않는다.
- 1차 재기준화 release-test 바이너리에서 HWP 393쪽, HWPX 386쪽, 한컴 2020 KoPub 설치 PDF 383쪽을
  재확인했다. sec=10 `pi=14`·`pi=23`은 모두 fragment scan 전에 `DIAG_ADVC`로 이월되고,
  `pi=23` 문제 행 422.5px·대표 셀 콘텐츠 392.3px도 유지된다.
- 보호 게이트 `issue_3738_rowbreak_table_footnote_fragment`는 33/33 통과했다.
- 한컴 2020 PDF를 물리 p287~p288에서 직접 재대조해 과거 줄높이 과대 가설을 기각했다. 대상 셀은
  24.5px pitch의 12줄+4줄로 두 쪽에 나뉘며, rhwp만 두 문단을 page index 291에 함께 둔다.
- 작업지시자가 계획의 순차 진행을 승인했다. Stage 1은 동작 불변 진단과 RED 계약을 고정하고,
  Stage 2에서 구조 증거가 완결된 HWP 표의 flow anchor만 저장 상단으로 재동기화한다. 전역 셀
  줄높이는 변경하지 않는다.
- 조사 중 발견한 Canvas2D KoPub 치환(#4741)과 160쪽 표 hit-test 선점(#4753)은 별도 이슈로
  유지한다. #3931의 Rust 조판 수정에 섞지 않는다.
- Stage 1·2를 완료했다. `pi=23`은 저장 pitch를 유지한 채 page index 290의 12줄과 index 291의
  4줄로 분할되고 첫 조각은 본문 bbox 안에 남는다. HWP 전체는 392쪽이며 #3931 3건과 인접
  focused 회귀 전건이 통과했다. 다음 실행 지점은 `pi=14` declared 선이월을 분리해 다루는
  Stage 3이다.
- Stage 3을 완료했다. `pi=14`는 0.1px declared 선이월 대신 기존 scanner가 155.8px 첫 조각을
  만들며 답변을 page index 286/287로 나눈다. HWPX는 이미 scanner 경로임을 확인해 386쪽
  래칫만 유지했다. red-check와 #3738 33건을 포함한 focused 회귀가 통과했으며, 다음 실행 지점은
  Stage 4 광범위·플랫폼·시각 검증이다.
- #4763 통합 전 Stage 4 자동 검증을 완료했다. 첫 전체 회귀의 중첩 partial table panic 19건은 local paragraph
  slice에 root `para_index`를 강제한 것이 원인이었고, host 미확인 시 margin 회수를 하지 않는
  fail-closed lookup으로 보완한 뒤 `release-test` 5,941/5,941를 통과했다. Native Skia 58+2+4건,
  공식 Docker WASM과 Studio 922 통과·1 skip도 확인했다.
- 이 시점의 비공개 10k 비교는 성공/오류 전이 0건, 쪽수 변화 8건 모두 HWP5 감소 방향이었다.
  이 수치는 뒤의 #4763 재기준화 결과로 대체한다.
- #4763(`#f8c784235`) 병합 뒤 Stage 1~3 여섯 커밋을 최신 `devel` 위로 rebase했다. #4763의
  terminal response tail과 질문 7의 plain-text 저장 reset이 충돌한 원인을 ordinary cut 직후의
  control-free 문단 경계로 좁혀 보완했다. control-only local reset은 제외해 질문 14 도형 조판과
  HWP/HWPX 383쪽을 모두 보존한다.
- 최신 검증은 `release-test` 6,023/6,023, Native Skia 58+2+4건, 공식 Docker WASM, Studio
  922 통과·1 skip이다. 최신 10k A/B는 성공/오류 전이 0건이고, 쪽수 변화 5건은 모두 HWP5 감소
  방향이며 변화 문서 전수 SVG export에서 양쪽 `overflowCellLines=0`이다. 원본·식별 목록·raw
  결과는 외부에 노출하지 않는다.
- 동일 물리 p284·p285·p287·p288 비교를
  `output/3931/visual/current/issue3931-current/review/`에 생성했다. 7702 전용 Studio 서버도
  최신 Docker JS/WASM과 해시가 일치한다. 공식 #3820 E2E도 이 서버에서 HWP/HWPX 383쪽과
  page owner를 통과했고, 작업지시자가 시각 판정 통과를 선언했다. 다음 실행 지점은 검증용 생성물
  제외와 로컬 통합 커밋이며, 원격 push·PR·이슈 close는 별도 승인 전에는 수행하지 않는다.
- PR 직전 원격 `devel` `99f6c9312` 위로 다시 rebase했다. 사이 변경에는 renderer 파일이 없고
  rebase 충돌도 없었다. 최신 기준 focused 8/8, `release-test` 6,027/6,027, Native Skia
  58+2+4, clippy·fmt·diff, 공식 Docker WASM, Studio 922 통과·1 skip을 재확인했다.
- 재시작한 Windows Chrome 150 CDP에서 WSL 절대 파일 입력 대신 Vite HTTP fixture 경로를 사용해
  HWP/HWPX 각각 383쪽, source format과 지정 page owner를 통과했다. 로컬 검증 보고서는
  `output/3931/validation/cdp_wasm_contract.html`에 두었다. 기존 비공개 10k A/B는 renderer 기준
  `f8c784235` 비교 증거로 유지하고 원본·식별 정보는 외부에 노출하지 않는다.
- 검증 완료 후보 `d81d11e54`를 원격 `task_m100_3931`에 push하고 Ready PR #4773을 생성했다.
  reviewer는 `jangster77`로 지정했다. PR은 14 files, +1,412/-28의 대형 PR이므로 즉시 admin
  merge하지 않고 `mydocs/pr/archives/pr_4773_review.md`와 대표 시각 asset을 trailing commit에
  포함한 뒤 최신 CI와 reviewer 의견을 별도 cycle로 확인한다.

## PR #4776 - 저장 시 본문 손실 4종 보정

- [PR #4776](https://github.com/edwardkim/rhwp/pull/4776)의 HWP3 문자, OWPML FieldType,
  숨은 설명, 누름틀 범위, 소프트 하이픈 저장 경로를 검토했다.
- 빈 문단의 연속·중첩 0-텍스트 필드에서 `fieldEnd`가 문단 말미로 밀리는 결함을 발견해,
  maintainer 보정 `9f6a51a01`으로 각 내부 슬롯 직후 종료 마커를 출력하도록 수정했다.
  상세는 [Stage 1](../working/task_m100_4776_stage1_empty_paragraph_field_end_order.md)에 남겼다.
- 보정 head의 GitHub full CI, CodeQL Rust, Native Skia, 전체 archive·test shard, Canvas visual
  diff가 모두 통과했다. 상세 근거는 [PR review](../pr/archives/pr_4776_review.md)와
  [구현 기록](../pr/archives/pr_4776_review_impl.md)에 보존한다.
- 이 문서 head를 원본 PR에 push한 뒤 fast-pass와 최신 mergeability를 확인한다. 병합은
  작업지시자 승인 뒤에만 진행한다.
## PR #4774 - 저장 LineSeg 기반 Square 그림 스택 앵커 유지

- [PR #4774](https://github.com/edwardkim/rhwp/pull/4774)는 [#4770](https://github.com/edwardkim/rhwp/issues/4770)의
  비-TAC `Square` 그림 스택이 한글의 저장 LineSeg 판단과 달리 쪽당 한 장으로 분산되는 문제를
  앵커 쪽 유지 조건으로 보정한다.
- contributor 원 변경 뒤 `rendering`과 `typeset`의 스택 판정이 비대칭인 것을 확인했다. collaborator
  보정은 빈 문단, `Square`, 겹침 불허, 공통 세로 대역이라는 공통 구조 계약과 페이지 절대 본문 하단
  비교로 통일해 #1995 낱장 분할 억제의 과적용을 막았다. `TopAndBottom` 스택은 억제하지 않는 회귀도
  추가했다.
- rustfmt, native-skia clippy, 새 unit test, release-test 전체 integration test를 통과했고,
  code candidate `85cafced1`의 GitHub CI Build & Test, Native Skia, Rust CodeQL, Canvas visual diff도
  모두 성공했다. 상세 근거는 [PR review](../pr/archives/pr_4774_review.md)와
  [구현 검토](../pr/archives/pr_4774_review_impl.md)에 보존한다.
- 이 trailing review-only head의 fast-pass aggregate와 최신 mergeability를 재확인한 뒤,
  작업지시자가 승인한 일반 squash merge를 수행한다. merge 뒤 #4770의 close 상태, contributor 감사
  댓글, `planet6897` fork branch 보존 및 로컬 검토 branch 정리를 공식 post-merge 절차로 처리한다.

## PR #4682 - 선택 실행 독립 정책 감사

- [PR #4682](https://github.com/edwardkim/rhwp/pull/4682)의 code candidate
  `9e4eb9ac6a09a04c8f6d4ee205c82b7b0bcd325d`를
  `upstream/devel@a5a92ca3bbd65c54d7ab3bd9525c831a1ff71e9e` 위에서 maintainer 검토했다.
- privileged controller가 PR head·merge ref·artifact를 실행하지 않고 base SHA의 classifier·policy만
  sparse checkout하는 경계, 같은 PR head의 workflow/job/step 증거 대조, stale status 게시 차단을
  확인했다. 추가 메인터너 보정이 필요한 finding은 없었다.
- 로컬 Node classifier+policy 55건, Python workflow 계약 67건, `actionlint`, `git diff --check`,
  최신 base merge simulation을 통과했다. 같은 code candidate의 GitHub CI·CodeQL·Render Diff도 모두
  성공했다. 상세 근거는 [PR review](../pr/archives/pr_4682_review.md)에 보존한다.
- 최신 `upstream/devel@e65f81776`을 source branch에 병합하면서 발생한 이 파일의 add/add 충돌은
  #4774와 #4682 항목을 모두 보존해 해소했다. merge head `da7b931ab`의 Full CI·CodeQL·Render Diff는
  모두 통과했고 `MERGEABLE/CLEAN`을 재확인했다.
- 이 trailing review-only head의 fast-pass aggregate를 확인한 뒤, 작업지시자의 명시적 승인으로만 merge한다.
  `CI Impact Policy` required context는 strict up-to-date 보호 규칙과 GitHub Actions expected source를
  실제로 확인하기 전에는 활성화하지 않는다.

## PR #4775 - HWP3 HWPX 내보내기 IR 보존

- [PR #4775](https://github.com/edwardkim/rhwp/pull/4775)는 [#3739](https://github.com/edwardkim/rhwp/issues/3739)의
  동일 글자모양 경계 소실을 보정하고, 암호 HWP3의 개체 슬롯·하이퍼텍스트·빈 그림 영역이 HWPX
  표현으로 보존되도록 한다.
- 최신 `upstream/devel@86b966ac5` 위 code candidate `d7585993`에서 release-test 빌드, 실제 암호 HWP3의
  IR 무차이·24쪽 검증, #3739 통합 회귀 4건, BOM stdin 바이너리 회귀 1건, workspace build와 변경 파일
  format·diff 검사를 통과했다. 전체 nextest·WASM은 로컬에서 실행하지 않았고, 전체 clippy는 이 Windows
  호스트의 Cargo 내부 대기로 완료 결과를 내지 못했으나, code head GitHub Actions의 Lint·Build & Test·
  Native Skia·Canvas visual diff·Rust CodeQL은 모두 성공했다.
- collaborator self-review로 처리하며 외부 reviewer는 요청하지 않는다. 상세 판단·표현 정규화 경계·
  시각 검증 범위는 [PR review](../pr/archives/pr_4775_review.md)에 보존한다.
- 이 trailing review·오늘할일 head의 GitHub Actions가 성공하고 최신 mergeability와 작업지시자 승인을
  다시 확인하기 전에는 merge하거나 #3739을 종료하지 않는다.

## PR #4801 - planet6897 기여 3건 체리픽 통합

- [PR #4801](https://github.com/edwardkim/rhwp/pull/4801)은 `planet6897`의 #4796, #4797, #4798을
  최신 `upstream/devel@44bcba400` 위에서 원래 순서대로 체리픽한 통합 PR이다. 원 SHA와 적용 SHA는
  #4796 `397982cc0` → `d69b52996`, #4797 `a1b3a9bb` → `2ce9bfd57`, #4798 `bfe531ad` → `c7e2f1fe5`다.
- code candidate `c7e2f1fe5`는 `MERGEABLE/CLEAN`이며 GitHub Lint(WASM check 포함), Build & Test
  전체 shard, Native Skia, Canvas visual diff, CodeQL을 모두 통과했다. 로컬에서는 #4709 SVG 주석,
  #4778 손상 위치 축, #3739 marker slot 회귀와 sweep PowerShell/Python 정적 검사를 통과했다.
- 메인터너 보정은 필요하지 않았다. 원 PR별 판단과 보존 경계는
  [#4796 검토](../pr/archives/pr_4796_review.md),
  [#4797 검토](../pr/archives/pr_4797_review.md),
  [#4798 검토](../pr/archives/pr_4798_review.md)에 남긴다.
- 이 review·오늘할일 trailing head의 fast-pass와 최신 mergeability를 확인한 뒤, 작업지시자의
  병합 지시에 따라 #4801을 squash merge한다. 병합 후에는 원 PR #4796~#4798을 통합 PR 링크와 함께
  close하고 #4709·#4749·#4751·#4778·#4677 종료 상태, `devel` 동기화, 임시 검토 branch를 확인한다.

## PR #4819 - kevin9327 Gym 감사 통합

- 최신 `upstream/devel@4cf8a5898` 위의 가시적 로컬 branch
  `review/kevin9327-20260815`에서 #4804, #4807, #4809, #4811, #4815 원 head를 번호순으로 누적 적용했다.
  원격 contributor source branch는 변경하지 않았다.
- #4809의 artifact 계약은 input copy만 거부하고 garbage를 허용해 6개 과제가 false-pass하는 것을 실제로
  재현했다. 입력 copy·garbage 이중 제어와 SVG/JSON/CSV/BOM 최소 의미 검사를 추가했다. #4811은 마지막
  실제 action을 제거하도록 보정해 OM03의 중복 thumbnail action을 제거했다.
- #4807의 인증 지문은 task 선언뿐 아니라 pack asset과 채점·조립·보고 프로토콜까지 포함하도록 넓혔다.
  #4815의 HWP3 줄간격은 i32 포화 산술로 보정하고, 손상 감사기의 빈 입력·Windows crash code 경계를 고정했다.
- 로컬에서는 Gym Python 계약 89건 통과·의도된 1건 skip, 전 pack 정합 17 pack·위반 0건, 판별 감사
  106 과제·156 제어·false-pass 0건, 트라젝토리 감사 25 과제·theater 0건, 인증서 236/236, 손상 강건성
  16 표본·128 변형·panic/hang 0건을 확인했다. HWP3 포화 회귀 focused test와 `git diff --check`도 통과했다.
- code candidate `bd01f5834`의 GitHub Full CI는 lint, Frontend package gates, Native Skia, test archive
  3개, 일반 shard 3개, slow shard, 최종 `Build & Test`를 모두 통과했다. CodeQL의 JavaScript/TypeScript,
  Python, Rust 분석 및 aggregate도 성공했다.
- 코드 후보 뒤 `upstream/devel@62a3ba677`로 전진했지만, source 변경 없이 review 기록만 이어 붙이는
  fast-pass 경로이므로 devel 병합·rebase를 수행하지 않는다. 이 trailing docs-only head의 preflight,
  aggregate, mergeability가 녹색이면 작업지시자 승인에 따라 merge하고 #4804, #4807, #4809, #4811,
  #4815 원 PR을 통합 반영 comment와 함께 close한다. #4808, #4810, #4814는 PR 본문의 closing keyword
  처리 여부를 merge 뒤 확인한다.
