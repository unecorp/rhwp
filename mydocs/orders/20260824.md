---
kind: daily-order
status: active
date: 2026-08-24
---

# 오늘 할일 - 2026년 8월 24일

## PR #6007 - 레거시 HWP 2020 MCP 안내 제거

- [x] superseded HWP 2020 MCP 사용 매뉴얼과 과거 client archive를 제거하고, 2022 이하 저장본도 같은
  Windows `hwp-convert-2024` client의 `engine: 2020`으로 처리하도록 현행 manual·manifest를 통일했다.
- [x] `git diff --check`, Markdown 링크 검사 601개를 통과했다. metadata 전수 검사의 기존 front matter
  누락 16건은 변경 범위 밖이므로 이번 PR에 섞어 수정하지 않았다.
- [x] Open PR #6007을 생성하고 self-review 기록을 준비했다. Rust·renderer·fixture·CI workflow 변경이 없는
  문서-only PR이므로 Cargo·시각 검증은 적용 대상이 아니다.
- [ ] self-review·오늘할일 trailing commit을 push하고, 최신 head의 review-only fast-pass aggregate와
  `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자의 merge 승인 뒤 #6007을 squash merge하고, merge SHA의 `upstream/devel` 포함 및 local·remote
  작업 branch 정리를 `post_merge.md`에 따라 수행한다.

## PR #6004 - 다른 이름 저장 문서명과 최근 문서 갱신

- [x] #6003의 다른 이름 저장 뒤 상태 표시줄이 이전 파일명을 유지하고 최근 문서에 새 이름을 남기지 않는
  경로를 분석했다. 상태 표시 책임, 최근 문서 저장소 상한, 암호 저장 경로의 무로그 계약을 스테이지 1 문서에
  기록했다.
- [x] File System Access 저장과 다운로드 fallback 저장에서 새 최근 문서를 기록하고 상태 표시줄을 갱신했다.
  최근 문서 영속 상한은 20개로 늘리고 메뉴는 기본 8개와 더보기로 표시한다.
- [x] Studio production build, Studio test 1,075 passed / 1 skipped, `cargo fmt --all -- --check`,
  `git diff --check`를 통과했다.
- [ ] PR #6004의 최신 CI와 실제 Save As 파일 선택 대화상자에서 새 파일명·최근 문서 갱신을 확인한다.
- [ ] 작업지시자의 병합 승인 뒤 #6004를 merge하고 #6003 close, merge SHA의 `upstream/devel` 포함 및
  작업 branch 정리를 `post_merge.md`에 따라 수행한다.

## Dependabot PR #5972-#5981 의존성 통합 검토

- [x] open Dependabot PR 중 CI가 모두 통과한 #5972, #5973, #5974, #5975, #5976, #5977, #5978,
  #5979, #5980, #5981을 최신 `upstream/devel@01e2e7422` 위 `review/dependabot-20260824`에
  오래된 PR 번호 순서로 `git cherry-pick -x` 적용했다.
- [x] 변경 범위는 `Cargo.toml`/`Cargo.lock`, npm `package.json`/`package-lock.json`, GitHub Actions
  action pin으로 한정됨을 확인했다. 제품 Rust·renderer·fixture 코드는 직접 변경하지 않았다.
- [x] `git diff --check`, `cargo metadata --locked --format-version 1 --no-deps`, `npm --prefix`
  `rhwp-studio`/`rhwp-vscode`/`rhwp-firefox`/`rhwp-chrome ci --dry-run --ignore-scripts`가 통과했다.
- [x] 작업지시자가 의존성만 올린 PR이라 전체 회귀 테스트는 불필요하다고 명시했으므로 전체
  `cargo nextest` 회귀는 생략했다.
- [ ] 통합 PR 생성 승인 후 remote push, PR 생성, 최신 CI 모니터링을 진행한다.
- [ ] 통합 PR merge 뒤 원 Dependabot PR별 comment/close, merge SHA의 `upstream/devel` 포함 확인,
  branch/worktree 정리를 `post_merge.md`에 따라 처리한다.

## PR #5957 - Windows HWP MCP 클라이언트 0.9.0

- [x] dependency-free client tarball을 server 계약 0.9.0으로 갱신하고 engine `2020|2024`, 선택적 password,
  engine별 기본 출력명과 비동기 `start → status → download` 계약을 문서와 함께 정렬했다.
- [x] 실제 tarball의 CLI·stdio schema·local mock HTTP MCP를 검증해 engine `2020`, password-file 전달,
  결과 비노출, byte 수·SHA-256 검증과 외부 dependency 0건을 확인했다.
- [x] code candidate `7205bf66e0c7cb30415d2d970c17d6972069cb11`의 CI, CodeQL, Proptest roundtrip,
  Adapter inter-diff가 모두 성공했다.
- [x] 변경 Markdown 링크·metadata·suite manifest·fmt·diff gate를 통과했다. 전체 metadata 검사의 기존
  `mydocs/tech` 4개 오류는 변경 범위가 아니며, 변경 장기 문서 6개는 오류 0건이다.
- [ ] self-review·오늘할일 trailing commit의 review-only fast-pass aggregate와 최신
  `MERGEABLE/CLEAN`을 확인한다.
- [ ] 자동 merge 승인에 따라 #5957을 merge하고, merge SHA의 `upstream/devel` 포함, PR comment와
  local·remote 작업 branch 정리를 `post_merge.md`에 따라 완료한다.

## PR #5954 - #5907 p122 저장 vpos 및 #5910 kps-ai rowspan fidelity

- [x] #5913 `7c7e515`와 #5914 `9fb1e19`의 기능 commit을 최신 기준선에 누적 적용하고, 중복 merge
  commit은 제외했다.
- [x] p122 stored-vpos guard 범위를 control-anchor 전환으로 축소하고, TAC 인라인 그림의 `PAPER`/`PAGE`
  기준 크기를 `046e7da61`에서 보정했다. #5914의 77쪽 page index 계약도 같은 candidate에 반영했다.
- [x] focused gate, 전체 release-test nextest 8,208 passed / 41 skipped, Native-Skia lib gate와
  한글 기준 PDF visual sweep을 통과했다. p122 1-3쪽의 blocker 후보는 0건이며 대표 PNG와 JSON을
  `mydocs/pr/assets/`에 보존했다.
- [x] code candidate `046e7da61fcb6b529f0f96b9f492c037f2abf579`의 GitHub Full CI, CodeQL,
  Canvas visual diff, Proptest roundtrip, Adapter inter-diff가 모두 성공했다.
- [x] 최신 `upstream/devel@5057a7fca`과의 merge-tree가 충돌 및 whitespace 오류 없이 통과했다.
- [ ] 이 trailing review-only head의 fast-pass aggregate와 최신 `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자 merge 승인 뒤 #5954를 merge하고, #5907/#5910 auto-close 상태, #5913/#5914의 연결
  comment와 close, merge SHA의 `upstream/devel` 포함 및 review branch 정리를 `post_merge.md`에 따라 처리한다.

## PR #5956 - #4960 W7 이후 폰트 로드맵 방향 수정

- [x] W0~W7 결과를 대사해 W7.5 registry lifecycle gate, W8의 세 운영 lane과 W9·W10의 cohort별 동결
  조건을 로컬 canonical·report 문서에 반영했다.
- [x] #5955를 #4960의 W7과 W8 사이 sub-issue로 등록하고 #4960·#4967~#4969 본문과 maintainer
  comment를 같은 실행 graph로 현행화했다.
- [x] 변경 7개 문서의 링크·diff·format gate와 GitHub body hash·상태·sub-issue 순서를 재검증했다.
- [x] code candidate `b97f3708ad2926681554c468e86685ad5a940440`의 CI, CodeQL, Proptest와 Adapter
  preflight 및 최종 Build & Test가 review-only fast-pass로 성공했다.
- [ ] self-review·오늘할일 trailing commit을 push하고 최신 head의 fast-pass aggregate와
  `MERGEABLE/CLEAN`을 다시 확인한다.
- [ ] 메인테이너 merge 승인 뒤 정상 병합하고 #4960을 OPEN으로 유지한 채 merge SHA 반영, issue comment와
  branch·worktree 정리를 `post_merge.md`에 따라 처리한다.

## PR #5985 - HWP MCP 엔진 검증 기준 보정

- [x] 최신 HWP 2024 client에서 요청 `--engine`과 비동기 `start`·`status`의 논리 engine을 일치시키는
  성공 기준을 문서화하고, concrete backend 식별자인 `server.engine`과 분리했다.
- [x] Hancom Office 2020 저장본 `kps-ai.hwp`를 `--engine 2020`으로 `queued → succeeded → download`까지
  변환해 `status.engine: 2020`, PDF 서명, 634,466-byte 출력과 SHA-256 일치를 확인했다.
- [x] `cargo fmt --all`, `cargo fmt --all -- --check`, `git diff --check`를 통과했다.
- [ ] self-review·오늘할일 trailing 문서 commit을 push하고, 최신 head의 review-only fast-pass aggregate와
  `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자의 병합 승인 뒤 #5985을 merge하고 merge SHA의 `upstream/devel` 포함 및 local review branch
  정리를 `post_merge.md`에 따라 처리한다.

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
| ------ | ------ | ---- | ---- |
| #5776 | PR #5989 CLI render-output adapter CI 영향 분류 복구 | 진행중 | 리뷰 보정 candidate `bacec40ee` 및 trailing head Actions 검증 대기 |

## PR #5990 - #5986 암호 문서 저장 보호 의도 보존

- [x] 암호로 연 HWP3/HWP5/HWPX 문서의 저장 보호 의도를 boolean으로 유지하고, 평문 load·새 문서·release의
  비보호 상태와 실패 시 기존 상태 보존 계약을 구현했다.
- [x] 실제 암호 fixture와 저장 실패 하네스로 회귀 테스트를 추가하고 Studio 전체 테스트·빌드·관련 E2E를
  통과했다. E2E manifest의 기존 미등재 3건은 변경 범위 밖 기준선 실패로 기록했다.
- [x] 하이퍼 워터폴 감사를 거쳐 누락된 구현계획·단계 보고·계획 대비 실제·절차 이탈 기록을 사후 보정했다.
  원 구현의 계획 사후 승인 누락과 단일 커밋 단계 혼합은 소급해 지우지 않고 명시했다. (완료: 13:26)
- [x] manifest 기존 미등재 3건을 범위 밖 예외로 공개한 뒤 작업지시자의 생성 지시에 따라 Open PR #5990을
  만들었고, 후보 `ecb63c1c7`의 CI·CodeQL·Proptest·Adapter·Canvas 검증이 성공했다. (완료: 13:42)
- [x] 검증 중 전진한 `upstream/devel@01e2e7422`와 오늘할일 한정 충돌을 current-base `mydocs/` bridge로
  해결하면서 #5985와 #5990 기록을 모두 보존했다. (완료: 13:42)
- [ ] self-review·오늘할일 trailing commit을 push하고 최신 head의 review-only fast-pass aggregate와
  `MERGEABLE/CLEAN`을 확인한다.
- [ ] 작업지시자의 별도 병합 승인 뒤 #5990을 merge하고 #5986 close, #5987 OPEN 유지와 branch·worktree
  정리를 `post_merge.md`에 따라 처리한다.

## CI 통과 Open PR 통합 체리픽 검토

- [x] 최신 `upstream/devel@f4ba7c565e81b0236ca1c52266ff75540b164fa7` 위
  `review/open-ci-green-20260824`를 만들고, 조회 시점 non-draft이며 CI가 성공/skip/neutral로 닫힌
  #5951, #5958, #5960, #5964, #5965, #5968, #5971, #5982, #5984, #5988, #5989, #5990,
  #5993, #5994, #5996을 번호 순서로 `git cherry-pick -x` 적용했다.
- [x] #5997, #5970은 실패 check가 있어 제외했고 draft PR은 제외했다. 포함한 PR head는 체리픽 뒤
  GitHub에서 다시 조회한 최신 head SHA와 일치한다.
- [x] #5989/#5990의 `mydocs/orders/20260824.md` 충돌은 기존 #5985 기록을 보존하면서 해결했다.
- [x] #5996의 `src/renderer/float_placement.rs`, `tests/fixtures/ir_field_sweep_baseline.tsv` 충돌은
  #5982/#5993/#5996의 독립 조건과 baseline 행을 모두 보존하는 메인터너 보정으로 해결했다.
- [x] 검증 중 별도 의존성 보정 후보로 보였던 `Cargo.toml`/`Cargo.lock` 변경은 이번 CI 통과 PR
  통합 범위 밖이므로 제거했고 통합 후보에 포함하지 않았다.
- [x] `cargo fmt --all -- --check`, suite manifest prepare/check, unit-tier check, 전체 nextest
  8292 passed / 42 skipped, Studio test 1074 passed / 1 skipped, Studio production build, CI impact
  Node tests 65 passed, render-diff workflow unittest 6 OK, 기본 `cargo clippy --locked --all-targets`
  및 `git diff --check`가 통과했다.
- [x] 개별 PR review 문서와 통합 검토 구현 문서를 `mydocs/pr/archives/`에 준비했다.
- [ ] 통합 PR 생성은 작업지시자 사전 승인 뒤에만 remote push와 PR 생성을 진행한다.
- [ ] 통합 PR 생성 뒤 최신 GitHub CI, mergeability, review 문서 경로 fast-pass 여부를 확인하고,
  merge 승인 뒤 원 PR comment/close와 branch/worktree 정리를 `post_merge.md`에 따라 처리한다.

## planet6897 Open PR 통합 체리픽 검토 - #5970 제외, #6008 포함

- [x] 최신 `upstream/devel@c8d01e716` 위 `review/planet6897-open-20260824`에서 #5970을 제외하고
  #5999, #6002, #6005, #6006, #6008을 번호 순서로 적용했다.
- [x] #5970은 GitHub CI `test-archive-b-shard-1 / Default-feature tests (Archive B)`와 aggregate
  `Build & Test`가 실패했고, 초기 통합 후보에서 기존 `issue_1880` roundtrip 회귀가 로컬 재현되어
  보류/제외로 기록했다.
- [x] #6005의 `tests/fixtures/ir_field_sweep_baseline.tsv` 충돌은 새 #5866 행과 기존 #5870/#5875 행을
  모두 보존하는 메인터너 보정으로 해소했다.
- [x] focused 검증, `cargo fmt --all -- --check`, suite manifest prepare/check, unit-tier check,
  all-targets clippy, `git diff --check`, 전체 nextest `8304 passed / 42 skipped`가 통과했다.
- [x] HWP/HWPX fixture 포함 PR은 `rhwp info --json`으로 저장 버전/쪽수를 판정한 뒤 HWP 2024 MCP
  `engine 2020` 기준 PDF를 만들고, `visual_sweep.py` 대표 페이지 비교 결과와 review PNG를
  `mydocs/pr/assets/`에 보존했다.
- [x] 개별 PR review 문서와 통합 구현 검토 기록을 `mydocs/pr/archives/`에 준비하고, visual sweep 증적을
  반영했다.
- [ ] 통합 PR 생성은 작업지시자 사전 승인 뒤에만 remote push와 PR 생성을 진행한다.
- [ ] 통합 PR 생성 뒤 최신 GitHub CI, mergeability, 원 PR comment/close와 branch/worktree 정리를
  `post_merge.md`에 따라 처리한다.

## planet6897 CI 통과 Open PR 통합 체리픽 검토 - #6009 #6011 #6014

- [x] 최신 `upstream/devel@fd2231a37` 위 `review/planet6897-ci-green-20260824`에서 조회 시점
  non-draft이며 GitHub CI가 통과한 #6009, #6011, #6014를 번호 순서로 적용했다.
- [x] #6012와 #5970은 GitHub CI `test-archive-b-shard-1 / Default-feature tests (Archive B)` 및
  aggregate `Build & Test` 실패 상태라 이번 CI 통과 통합 후보에서 제외했다.
- [x] `git cherry-pick` 적용은 conflict 없이 끝났고, 메인터너 보정이 필요한 추가 코드 변경은 발견하지
  않았다.
- [x] focused 검증, `cargo fmt --all -- --check`, `cargo clippy --locked --target-dir target/pr-review`
  `--lib --bins --tests -- -D warnings`, 전체 nextest `8306 passed / 42 skipped`, `git diff --check`가
  통과했다.
- [x] HWP/HWPX fixture가 포함된 #6009와 #6014는 `rhwp info --json`으로 저장 버전을 확인한 뒤
  HWP 2024 MCP `engine 2020` 기준 PDF와 visual sweep 대표 review PNG를 생성해 `mydocs/pr/assets/`와
  `pdf/`에 보존했다.
- [x] #6009, #6011, #6014 개별 PR review 문서와 통합 구현 검토 기록을 `mydocs/pr/archives/`에
  준비했다.
- [ ] 통합 PR 생성 뒤 최신 GitHub CI, mergeability, review 문서·증적 포함 여부를 확인한다.
- [ ] 통합 PR merge 승인 뒤 원 PR comment/close, merge SHA의 `upstream/devel` 포함 확인,
  branch/worktree 정리를 `post_merge.md`에 따라 처리한다.

## PR #6018 - visual_sweep 한글 라벨 폰트 fallback 보완

- [x] #6016에서 `mydocs/pr/assets/pr_6014_issue5712_p1_review.png`의 한글 metric 라벨이 tofu로 보이는
  현상을 제품 renderer가 아니라 visual sweep 도구 라벨 폰트 선택 문제로 분리했다.
- [x] `RHWP_VISUAL_SWEEP_LABEL_FONT`, fontconfig `fc-match`, Linux/macOS/Windows CJK 폰트 경로 후보를
  순서대로 적용하고, 모든 TrueType 후보가 실패할 때만 Pillow 기본 폰트로 fallback하도록 보완했다.
- [x] PR review workflow와 visual fixture evidence 문서에 대표 review PNG의 한글 glyph·metric text·overlay
  legend 판독성을 직접 확인하는 gate를 추가했다.
- [x] `python3 -m unittest scripts.tests.test_visual_sweep` 43 tests OK, `py_compile`, Markdown 링크 검사,
  `cargo fmt --all -- --check`, `git diff --check`를 통과했다.
- [x] self-review 문서 `mydocs/pr/archives/pr_6018_review.md`를 준비했다.
- [ ] trailing commit push 뒤 최신 head의 GitHub CI와 `MERGEABLE/CLEAN`을 확인한다.
- [ ] 자동 merge 승인에 따라 #6018을 merge하고 #6016 close 확인, PR/issue comment, merge SHA의
  `upstream/devel` 포함 및 branch/worktree 정리를 `post_merge.md`에 따라 수행한다.
