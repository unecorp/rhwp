---
kind: daily-order
status: active
date: 2026-08-28
---

# 오늘 할일 - 2026년 8월 28일

## PR #6302 - 투명선 병합 경계 안내선

- [x] [#6302](https://github.com/edwardkim/rhwp/pull/6302) `@t2c-lab`의 rhwp 첫 기여를 검토했다.
- [x] 원 기여 구현을 수용하고, maintainer가 가로·세로 span 내부 안내선 제거와 실제 `none`
  테두리 안내선 유지의 회귀 범위를 테스트만으로 보완했다.
- [x] 코드 후보 `9c1bb890a890af45057c958a6a3294697ab2c3a8`의 최신 required CI가 성공했고,
  작업지시자가 실제 동작을 확인했다.
- [ ] review·오늘할일·첫 기여자 공식 절차 trailing head의 문서 CI와 merge 가능 상태를 확인한 뒤 merge한다.
- [ ] merge 뒤 [#6301](https://github.com/edwardkim/rhwp/issues/6301) 상태, 첫 기여자 감사 comment,
  `devel` 동기화와 이번 처리 worktree 정리를 후속 처리한다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #4121 | 머리말/꼬리말 텍스트 선택 구현 | 보류 | Stage 4 Chrome E2E 50/50·WASM·clippy 통과 후 이동으로 안전 중단, checkpoint에서 재개 |

## CI 운영

- [ ] [#6257](https://github.com/edwardkim/rhwp/pull/6257) squash merge 뒤 trusted CodeQL 재사용 보완
  - 로컬 CI policy 및 trusted reuse 계약 70건, Python workflow 계약 166건 통과.
  - 최신 PR head의 GitHub Actions 통과 뒤 merge한다.
  - merge 뒤 devel push에서 동일 PR의 성공 run이 재사용되고 CodeQL worker가 skip 되는지 확인한다.

## #6243 — Render Diff merge bridge review tail 재사용

- [x] 미커밋 계획서와 2026-08-27 오늘할일을 `00830286a` checkpoint로 보존했다.
- [x] 최신 `upstream/devel@1a43a507c`을 merge commit `396abf4dd`로 통합하고 문서 링크·rustfmt·포함 관계를
  검증했다.
- [x] 최근 21개 commit이 Render Diff 대상 경로를 바꾸지 않았고, run `33071270488`의
  `canvas-visual-diff-identity-mismatch`가 현재 원인과 일치함을 재확인했다.
- [x] 현행 Python 계약 18/18 통과와 main controller 부재, devel required `Build & Test`, assignee·열린 PR 없음을
  live 기준선으로 확인했다.
- [x] 실행 가능한 Render Diff jobs API red 계약을 보강한 수정 수행계획을 승인받았다.
- [x] #6243 assignee를 메인테이너로 지정하고 Stage O2-1을 시작했다.
- [x] workflow run·jobs·identity step 실행 harness와 bridge 부재·복수 bridge·wrong identity·pending·failed 음성
  matrix를 추가했다. 전체 21건 중 음성·기존 20건은 통과했다.
- [x] #6214 긍정 계보 1건만 기대 `pending-base-merge-tree`와 달리 현행
  `false / canvas-visual-diff-identity-mismatch`를 반환하는 red 계약으로 원인을 고정했다.
- [x] Stage O2-1 결과를 승인받고 candidate loop 한 곳만 정정하는 O2-2로 진입했다.
- [x] 검증된 base merge bridge가 있을 때만 후보 조회에 prior-base identity를 허용하도록 한 호출을
  정정했고, 실행 계약 21/21과 fail-closed 음성 matrix가 모두 통과했다.
- [x] Stage O2-2 결과를 승인받고 CI impact controller를 포함한 비례 검증 O2-3로 진입했다.
- [x] Render Diff Python 21건, controller Python 31건, Node 정책 32건과 필수
  `cargo fmt --all`·`--check`, Markdown link·diff 검사를 모두 통과했다.
- [x] `upstream/devel...HEAD`의 Render Diff workflow 차이가 승인된 candidate 호출 1줄뿐임을 대사해
  inline JavaScript 실행, event·permission·required check 불변을 확인했다.
- [x] Stage O2-3 결과를 승인받고 최신 `devel@94ff48d2b`를 merge·재검증한 뒤
  [PR #6297](https://github.com/edwardkim/rhwp/pull/6297)을 Open으로 생성했다.
- [x] exact code candidate `0c837f36e`의 Full CI·CodeQL·Render Diff·Adapter inter-diff·Proptest가
  모두 성공했고 API 기준 `MERGEABLE / CLEAN`을 확인했다.
- [x] self-review 직전 `devel@b1485e0a1`까지 29커밋 전진했지만 #6243 workflow·계약 경로는 바뀌지 않았고,
  merge-tree에서 문서 섹션 자동 보존과 source·test·workflow 무충돌을 확인했다.
- [x] [PR #6297 self-review](../pr/archives/pr_6297_review.md)를 code candidate의 Full run 증적과
  main controller 부재에 따른 trailing Full fallback 조건으로 작성했다.
- [ ] self-review·오늘할일 trailing commit을 push하고 최신 head의 Full required checks와 mergeability를
  확인한 뒤 별도 merge 승인을 받는다. #6243은 post-release canary 전까지 닫지 않는다.

## #4969 — W10 common shaping

- [x] Q2-B 결과를 qualified로 판정하고 native·Node WASM parity mismatch 0과 제품 소비자 0을 기록했다.
- [x] Q2-C 진입 승인을 받았다. 이 승인은 제품 mutation이 아니라 activation matrix·rollback 경계 확정까지다.
- [x] 현행 composer·W9 line-break owner를 재감사하고 complex-required horizontal LTR activation matrix를 작성했다.
- [x] Q2-C 단독 line boundary 활성은 Q2-D 전까지 emitted run owner와 불일치함을 확인해 dormant transaction으로
  순서를 정정했다.
- [x] Q2-C dormant transaction 변경 범위와 보호 불변식을 보고하고 구현 승인을 받았다.
- [x] Q2-C0 segmentation·eligibility, Q2-C1 cluster-aware width owner, Q2-C2 bounded final-line transaction을
  제품 소비자 0으로 구현했다.
- [x] native·Node WASM focused 8건에서 parity mismatch 0, later-segment 실패의 문단 전체 rollback,
  `productPublished=false`를 확인했다.
- [x] Q2-C 결과 승인 뒤 Q2-D emitted-run `Arc` sidecar와 line-boundary publication의 원자적 activation 계획을
  별도로 승인받았다.
- [x] Q0~Q2-C 승인 결과를 checkpoint `452b6b741`로 커밋하고 원격 push는 보류했다.
- [x] Q2-D owner를 재감사해 `TextRunNode` public field 대신 `ComposedParagraph` internal `Arc`와
  `PageRenderTree` bounded `NodeId` sidecar table을 권고했다.
- [x] 동일-boundary activation과 NO_LS boundary-changing activation을 분리한 Q2-D 수정 수행계획을 작성했다.
- [x] Q2-D 수정 수행계획을 승인받아 D0 sidecar ABI·bounded table을 제품 소비자 0으로 구현했다.
- [x] native·Node WASM focused 각 4건에서 `Arc` 공유, JSON 무변화, typed fail-closed와 4,096개 상한을
  확인했다.
- [x] D0 결과 승인 뒤 D1 composition owner 인계를 시작했다.
- [x] D1 composition `Arc` handoff, D2 emitted-run mapping, D3 common GlyphRun lowerer를 각각 승인·검증했다.
- [x] D4-A condensed replay projection·exact source certificate, D4-B one-line/one-run atomic activation,
  D4-C actual CanvasKit draw·cross-backend·성능 판정을 완료했다.
- [x] D4는 `qualified-bounded`로 마쳤다. 최초 strict lane은 유지하되 456,688B portable font resource 재사용을
  입증하기 전에는 multi-line/run으로 확대하지 않는다.
- [x] 최신 `upstream/devel@5645e1f5bb`를 merge하고 양쪽 오늘할일 기록을 보존했으며 Rust·Studio·WASM·CI
  정책 회귀를 다시 통과했다.
- [x] [PR #6270](https://github.com/edwardkim/rhwp/pull/6270)을 `devel` 대상으로 생성했다. PR 생성 뒤
  `jangster77`의 singleton shaping test registry 보정 `a271733ba`를 authorship 그대로 반영하고 로컬 검증했다.
- [x] `a271733ba`의 CI·CodeQL·Render Diff·Adapter inter-diff·Proptest roundtrip이 exact head에서 모두
  성공했고 PR은 `MERGEABLE / CLEAN`으로 전환됐다.
- [x] self-review trailing head `34575cee2`의 trusted post-merge reuse와 최신 `Build & Test`를 확인했다.
- [x] 메인테이너 승인 뒤 [PR #6270](https://github.com/edwardkim/rhwp/pull/6270)을 정상 merge commit
  `1a43a507c`로 병합했다. merge SHA가 최신 `upstream/devel`에 포함되고 local·remote task branch와 전용
  worktree가 남지 않았음을 확인했다.
- [x] 최신 `devel@44b81ed5a`에서 `task_m100_4969_w10_q2_d5` branch와 전용 worktree를 만들었다.
- [x] [D5 resource 선행 감사](../working/task_m100_4969_w10_q2_d5_resource_audit.md)에서 D4 기존 계측을
  재사용하고 page-local dedup은 이미 존재하지만 run별 digest·face 준비와 page 간 inline font payload 재전송이
  남았음을 확인했다.
- [x] unique source 준비 cache·font by-key 전달·공개 `1/2/8` 측정 matrix를 반영한
  [수정 수행계획](../plans/task_m100_4969_w10_q2_d5_revised.md)을 작성했다.
- [x] 수정 수행계획을 승인받고 제품 출력을 바꾸지 않는
  [Q2-D5-R0 public matrix·counter red 계약](../working/task_m100_4969_w10_q2_d5_r0.md)을 qualified-red로 고정했다.
- [x] [Q2-D5-R1 unique-source prepared identity cache](../working/task_m100_4969_w10_q2_d5_r1.md)를 구현해
  `1/2/8 run`의 digest·face parse·arena intern을 unique source당 `1/1/1`로 고정했다.
- [x] [Q2-D5-R2 opt-in font-by-key transport](../working/task_m100_4969_w10_q2_d5_r2.md)를 구현해
  default inline JSON·기존 API·NO_LS boundary를 보존하면서 `1/2/8 page` fetch를 한 번으로 고정했다.
- [x] [Q2-D5-R3 resource qualification](../working/task_m100_4969_w10_q2_d5_r3.md)에서 native/WASM·inline/by-key
  parity, `1/2/8` counter, 실제 CanvasKit pixel, warm/cold·JSON 크기·generation invalidation을 대사해 qualified로
  판정했다.
- [x] [Q2-D5-N0 dormant owner·rollback 계약](../working/task_m100_4969_w10_q2_d5_n0.md)에서 NO_LS ordinary
  single-interval의 line selection·bbox·next origin·sidecar가 같은 measurement `Arc`를 공유하고, typed failure는
  pristine W9/K0 geometry만 남김을 qualified로 고정했다.
- [x] N0 결과 승인과 checkpoint commit `f03f25acf` 뒤 Q2-D5-N1 atomic activation 진행 승인을 받았다.
- [x] [Q2-D5-N1 atomic activation](../working/task_m100_4969_w10_q2_d5_n1.md)에서 ordinary single-interval
  NO_LS 문단의 네 geometry 소비자를 같은 measurement `Arc`로 원자 게시하고 TextRun 1 + common GlyphRun 1,
  nominal duplicate 0과 negative 전체 layer JSON baseline 일치를 확인해 qualified로 판정했다.
- [x] N1 결과 승인과 checkpoint commit `a0d40ca32` 뒤 Q2-D5-N2 최종 cross-backend·성능 판정 진행 승인을
  받았다.
- [x] [Q2-D5-N2 최종 판정](../working/task_m100_4969_w10_q2_d5_n2.md)에서 native·Node WASM·CanvasKit과
  fallback backend, 전체 8,562 integration, native-skia, Studio, resource·성능을 대사해 D5를
  `qualified-bounded`로 판정했다. 결과 승인과 checkpoint `422a8f7bc` 뒤 최신
  `upstream/devel@f6a6bee8f`을 `c0998c280`으로 병합했고, cross-impact 98건·전체 8,568건·native-skia·Clippy·
  Docker WASM·Studio를 재통과해 분류가 유지됨을 확인했다. 재자격화 증적 checkpoint와 Q2 최종 support
  classification은 별도 승인 대기다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #3789 | `src/main.rs` 잔여 렌더 경계 분리와 PR #6276 제출 | 진행중 | 로컬 8,519개·필수 clippy 통과, 최신 head CI 대기 |
## PR 검토

- [ ] [#6245/#6246/#6247/#6248/#6249/#6250/#6252/#6254/#6259/#6260/#6261/#6262/#6265/#6268 통합 검토](https://github.com/edwardkim/rhwp/pulls)
  - draft가 아니고 CI 실패·진행 check가 없는 open PR만 선별했다.
  - #5953, #6059는 draft라 제외했고, #6073은 실패 check가 있어 제외했다.
  - #6270은 리베이스 시점 최신 `upstream/devel`에 이미 merge되어 별도 체리픽 대상으로 다루지 않는다.
  - #6083은 실패 check는 없지만 기존 메인터너 재작업 요청이 있어 보류 유지.
  - 작업지시자 지시에 따라 이 목록 이후 새로 발견되는 PR은 승인 없이 자동 포함하지 않는다.
  - 통합 브랜치 `review/open-ci-green-20260828`에서 체리픽 및 개별 review 문서 준비 완료.
  - #6245는 메인터너 보정으로 후속 문단 사다리 흡수 판정을 바로 뒤 실제 lineSeg로 좁혔다.
  - #6246은 force-push 뒤 최신 head `37abb2599dca`와 최종 diff를 맞추고 focused 2건을 다시
    통과시켰다.
  - 로컬 검증 완료: fmt, unit tier, suite manifest, focused #6174/#6186/#6190/#6192/#6196/#6208,
    OOB 관련 lib tests, clippy, 전체 nextest 8,486 pass / 43 skipped, WASM build, Native Skia 3종.
  - 증적자료가 필요한 HWP/PDF/layout 사안은 `rhwp info --json`으로 저장 제품을 확인한 뒤,
    2024 저장본은 MCP `engine 2024`/`-2024.pdf`, `null` 또는 2022 이하 저장본은
    MCP `engine 2020`/`-2020.pdf` 기준 PDF로 산출했다.
  - 통합 head 기준 visual sweep 대표 PNG, summary, overlay metrics, PDF 산출 JSON을
    `mydocs/pr/assets`와 `pdf/pr_6275/by_saved_version`에 보존했고, 개별 review 문서의 코멘트
    처리 절에 merge SHA 고정 raw URL로 남길 대상 asset과 자동 일치율 보조값을 기록했다.

- [ ] [#6274](https://github.com/edwardkim/rhwp/pull/6274) RowBreak 자리차지 표의 꼬리 줄 vpos snap 보정 검토
  - 작성자 `davindev`의 첫 PR로 확인하고 reviewer request를 등록했다.
  - 최신 PR head `92f1ace655c419e02ba052815bcb70157a2647d0`의 GitHub CI, CodeQL,
    Adapter inter-diff, Proptest roundtrip, Render Diff가 모두 통과한 상태를 확인했다.
  - 최신 `upstream/devel` 위로 PR source commit과 review 증적 commit을 rebase했으며, 제품 코드 충돌은 없고
    오늘할일 문서만 최신 devel 기록을 보존해 병합했다.
  - focused 회귀 `issue_6271_rowbreak_float_tail_snap` 2건과 주변 TAC/table float 회귀 6건이 통과했다.
  - `rhwp info --json` 기준 저장 제품은 `hancom-office-2022`라 MCP engine `2020`으로
    `pr6274_issue6271_rowbreak_float_tail_line-2020.pdf`를 산출했다.
  - visual sweep 대표 PNG를 직접 확인해 표가 1쪽에 유지되고 tail 텍스트·그림이 하단에 표시됨을 확인했다.
  - 자동 하단 frame overflow 후보는 남지만 #6271의 2쪽 이월·그림 소실 blocker와는 분리해 비차단 잔여로 기록했다.
  - review 문서와 증적자료를 trailing commit으로 PR head에 push한 뒤 최신 head의 fast-pass/CI와
    mergeability를 다시 확인한다.

- [ ] [#6277/#6281/#6286/#6287/#6292/#6294/#6295 통합 검토](https://github.com/edwardkim/rhwp/pulls/planet6897)
  - 최신 `upstream/devel@94ff48d2b81dee5241110db9d2417dffbfb7f9ec` 기준으로
    `review/planet6897-open-ci-20260828` 브랜치를 만들고 PR 번호 순서로 `git cherry-pick -x` 적용했다.
  - reviewer request는 7개 원 PR 모두에 `jangster77`로 등록했다.
  - #6295는 작업 시작 시 일부 CI가 진행 중이었으나 2026-08-28 18:35 KST 재확인 시
    Full CI·CodeQL·Adapter inter-diff·Proptest·Render Diff가 모두 완료됐다.
  - #6294의 `tests/fixtures/ir_field_sweep_baseline.tsv` 충돌은 최신 devel의 #6264 row와
    #6280 row를 모두 보존하는 합집합으로 해결했다.
  - #6295 적용 뒤 `src/renderer/layout/picture_footnote.rs`의 중복 clippy allow attribute 1줄을
    기능 변화 없이 제거했다.
  - 로컬 검증 완료: fmt, suite manifest prepare/check, unit-tier, focused #6267/#6266/#6264/#6204/#6269/#6280/#6284,
    CI 범위 clippy/check/WASM check, native-skia lib, `git diff --check`.
  - `--all-features` clippy는 기존 `gpu` feature의 `vello`/`vello_svg` type mismatch에서 실패했지만,
    이번 diff는 Cargo/gpu 경로를 변경하지 않고 CI 범위 clippy/check는 통과했다.
  - HWP/HWPX 증적은 `rhwp info --json`으로 저장 제품을 확인했고 모두 `null` 또는 2022 이하 저장본이라
    MCP `engine 2020`과 `-2020.pdf` suffix로 기준 PDF를 산출했다.
  - visual sweep 대표 PNG/PDF/summary/overlay metrics는 `mydocs/pr/assets`와
    `pdf/pr_planet6897_open_ci_20260828/by_saved_version`에 보존했다.
  - #6287은 원본 샘플 p1에 기존 text-flow 자동 후보가 남아 있으나, 실제 수정 시나리오인
    `set-picture` edited output 기준 visual sweep은 flag 0건이고 저장 후 재로드 회귀도 통과했다.
  - 개별 review 문서와 통합 구현 기록을 준비했으며, 통합 PR 생성은 별도 승인 후 진행한다.

- [ ] [#6073/#6083 통합 검토](https://github.com/edwardkim/rhwp/pulls/kevin9327)
  - 최신 `upstream/devel@a6c7e7bb3ae09470c225a4c90c0fc1ad88b6b5a6` 기준으로
    `review/kevin9327-6073-6083-20260828` 브랜치를 만들고 PR 번호 순서로 cherry-pick했다.
  - #6073은 원 PR head `b90d80484249`가 GitHub CI/CodeQL/Adapter inter-diff/Proptest roundtrip을 통과했고,
    이전 메인터너 보정으로 `issue_1880` 오라클 핀 완화와 문서 실측 상수형 승격 경로가 제거되어 수용 권고로
    정리했다.
  - #6083은 `tests/fixtures/overflow_cell_baseline.tsv` conflict에서 원 PR의 기존 문서 overflow-cell 증가를
    반영하지 않고 devel 원장을 보존했다.
  - #6083 메인터너 보정으로 fresh 재래핑 결과를 `height_measurer`, `table_layout`, `table_partial` 측정 경로에
    동일하게 반영하고, 하단 겹침 회귀 test를 추가했다.
  - 로컬 검증 완료: fmt, diff check, suite manifest prepare/check, unit-tier, focused #5952/#6063/#3931,
    overflow-cell 원장, 전체 nextest 8551 pass / 43 skipped, clippy, doc test, Native Skia 3종, WASM build.
  - #6083 HWP 증적은 `rhwp info --json` 기준 `hancom-office-2024 13.0.0.3622`라
    `pdf/2025 행정업무운영 편람(최종)-2024.pdf`와 p69 visual sweep으로 산출했다.
  - #6083 visual sweep은 p69 `visual_accuracy_proxy_percent=50.8623%`, 자동 flag 5종이 남으므로,
    우측/하단 겹침 해소와 분리한 잔여 fidelity 후보로 코멘트 처리한다.
  - PR 생성 전 review 문서와 증적자료를 먼저 커밋했고, `upstream/devel` 동기화 뒤 오늘할일 문서 conflict를
    양쪽 기록 보존으로 해결했다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #4135 | F5 셀 블록의 Ctrl/Cmd+Shift+S 문맥 라우팅·실동작 보정 | self-review 완료, CI 확인 대기 | [PR #6385](https://github.com/edwardkim/rhwp/pull/6385), [검토 기록](../pr/archives/pr_6385_review.md). 병합 셀 뒤 수식 좌표 이동을 보정하고 focused·전체 release-test 8,635건·Clippy·포맷을 재통과. 최신 head CI와 작업지시자 merge 승인 대기 |
| #6108 | 쪽 배치별 맞춤 배율과 명령 계산 단일화 | self-review 완료 | [PR #6289](https://github.com/edwardkim/rhwp/pull/6289), [검토 기록](../pr/archives/pr_6289_review.md). 여러 쪽 `fitPage` 저장·새 세션 재계산 보정 후 Studio 1,226건·build 238 modules·Chrome E2E 32/32 재통과. |
| #6109 | 사용자 배율 검증과 보기 설정 원자 적용 | self-review 완료 | [PR #6290](https://github.com/edwardkim/rhwp/pull/6290), [검토 기록](../pr/archives/pr_6290_review.md). 최신 #6108 head 위에서 Studio 1,235건·build 239 modules·Chrome E2E 37/37와 하단 회귀 32/32 재통과. |
