# 오늘 할일 - 2026년 8월 27일

- [x] [#6159](https://github.com/edwardkim/rhwp/pull/6159)에 #6125, #6153, #6155의 원본 커밋을 보존해 통합했다.
- [x] manifest, tier 정책, 관련 회귀 4건, 전체 integration 8,401건, Native Skia lib test, WASM build를 확인했다.
- [x] HWP 2024 MCP client의 `--engine 2020`으로 기준 PDF와 visual sweep 증적을 생성했다. private 원본 HWP는 Git에 넣지 않았다.
- [x] 원 PR별 review 문서와 공개 가능한 증적, SHA-256 목록을 trailing commit에 준비했다.
- [ ] #6159 trailing head CI 성공 후 merge한다.
- [ ] merge SHA를 기록하고 #5700, #5701, #6126, #6128 및 원 PR #6125, #6153, #6155에 통합과 검증 결과를 남긴 뒤 상태를 정리한다.
- [x] [#6191](https://github.com/edwardkim/rhwp/pull/6191)에 planet6897의 CI 통과 원본 PR 12건을 원본 출처와 commit 순서로 통합했다. #6178은 현재 head CI 실패로 제외했다.
- [x] #6191 전체 integration 8,417건, Native Skia lib, WASM build, Studio build와 local Vite/headless Chrome #6117 E2E를 확인했다.
- [x] 한컴 기준 PDF 11건을 MCP에 순차 요청해 SHA-256과 raster/diff 증적을 `mydocs/pr/assets/pr_6191/`에 보관했다. SVG PDF backend의 #6168/#6169 `InvalidImage`은 pristine `upstream/devel`에서도 재현됨을 확인했다.
- [x] 원본 PR별 review 문서 12건과 통합 review 문서를 준비했다.
- [ ] #6191 trailing 문서/증적 head CI 성공 후 merge한다.
- [ ] merge SHA를 기록하고 포함 원본 PR 및 관련 이슈에 통합 검증 결과를 남긴 뒤 상태와 임시 검토 산출물을 정리한다.
- [x] [#6200](https://github.com/edwardkim/rhwp/pull/6200)에서 기준선 병합 뒤 문서 tail의 fast-pass bridge를 보완했고, merge commit `abeb29ca2fdbad2983d5562af0da75b57663449f`가 `devel`에 반영됐다. [#6197](https://github.com/edwardkim/rhwp/issues/6197)은 closing keyword로 자동 close됐다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #6149 | 저배율 눈금자 밀도와 페이지 간격 보정 | 진행중 | PR #6188 Open. PR #6176 반영 재검증 통과, #6187 분리. CI·리뷰 대기. |
| #6187 | 좁은 데스크톱 눈금자 정책과 resize 깜빡임 정합 | 예정 | #6149 병합 뒤 최신 `devel`에서 독립 계획·브랜치로 진행. |
| #6108 | 쪽 배치별 맞춤 배율과 명령 계산 단일화 | 진행중 | Stage 2 진입점 단일화·focused 42/42·TypeScript 검사 완료. 결과 승인 대기. |
| #6109 | 사용자 배율 검증과 보기 설정 원자 적용 | 예정 | #6108 head에서 분기하는 별도 stacked PR 후보. #6108 결과 승인 뒤 착수. |

## PR #6178~#6198 통합 검토

- [x] `upstream/devel@529ab90c25d5` 기준 `review/open-prs-6178-6198-20260827` 브랜치를 만들고
  #6178, #6182, #6188, #6193, #6195, #6198을 체리픽했다. #6183은 closed, #6191은 already merged라 제외했다.
- [x] reviewer `jangster77`를 REST `requested_reviewers` API로 지정했다.
- [x] #6178은 최초 체리픽 이후 추가된 `8bfd9404f`를 확인해 `d5d43caa9`로 추가 반영했다.
- [x] #6182 `rhwp-studio/package.json` 충돌은 `e2e:responsive`와 `e2e:issue-6053`를 모두 보존하도록 해소했다.
- [x] #6188 `mydocs/orders/20260827.md` 충돌은 기존 devel 기록을 보존하고 #6149 표를 이어 붙였다.
- [x] `git diff --check` EOF 공백 경고는 `81265b486` 메인터너 보정으로 제거했다.
- [x] 로컬 검증: fmt, diff-check, Rust tier/manifest, focused 8개, 전체 nextest 8,438건, clippy,
  native-skia, WASM, Studio unit/build/#6053 e2e/responsive e2e 통과.
- [x] 원 PR별 review 문서와 통합 review 문서, 시각 증적 SHA/검증 요약 asset을 준비했다.
- [ ] 통합 PR 생성 전 사전 승인을 받는다.

## CI 운영 개선

- [x] [#6253](https://github.com/edwardkim/rhwp/pull/6253)에서 Archive A를 단일 worker로 통합하고,
  integration target 41개를 B/C/D에 14/13/14개로 분배했다. 추정 시간은 947.948초,
  947.937초, 948.171초로 정렬됐고 최종 CI와 \`Build & Test\`를 성공한 뒤
  \`bb9e76c738ee04019cbbd5130edecbb09a42d343\`로 squash merge했다.
- [ ] [#6251](https://github.com/edwardkim/rhwp/issues/6251) close와 merge/검증 기록 comment를
  게시하고, 성공한 \`devel\` B/C/D JUnit 측정 artifact가 다음 duration 정책 갱신에 쓰이는지
  추적한다.
- [x] [#6216](https://github.com/edwardkim/rhwp/pull/6216)에서 #6213의 trusted post-merge CI 재사용과 nextest duration 측정 무결성 보강을 구현하고, Node 9건과 Python workflow 계약 86건을 통과시켰다. CI가 발견한 새 계약 테스트 배선 누락도 보정했다.
- [ ] #6216 최신 head CI가 성공하고 첫 배포가 full fallback으로 안전하게 끝나는지 확인한 뒤 merge한다.
- [ ] merge 뒤 첫 적격 PR의 post-merge 실행에서 source PR green 결과와 B/C duration artifact가 정확히 재사용되는지 확인한다.
- [ ] [#6243](https://github.com/edwardkim/rhwp/issues/6243)의 current-base merge bridge 뒤 review tail에서
  Render Diff prior-base identity 재사용을 복구한다. `devel` 완료 뒤 정상 release로 controller와 함께 `main`에
  활성화하며 계획 승인 전에는 workflow를 변경하지 않는다.
- [ ] `main` 활성화 뒤 [#6215](https://github.com/edwardkim/rhwp/issues/6215)의 exact kerning fixture generator
  선택 분류를 별도 운영 변경과 post-release canary로 처리한다.

## PR #6214 - #4968 exact font 커닝 조판 연결

- [x] W9-Q0부터 R4E까지 exact source capability, bounded measurement, fresh line boundary와 backend replay를
  단계별 승인·커밋하고 공개 fixture·한컴 PDF 교차검증·성능 참고값을 기록했다.
- [x] code candidate `9c568b40a`의 Full CI, CodeQL, Render Diff, Proptest와 Adapter 검사를 모두 통과했다.
- [x] 최신 `upstream/devel@2db6fb29b`를 `469ac96fd`로 병합하고 fmt, tier·manifest, trusted reuse와 duration
  workflow 계약을 재검증했다. 최신 PR checks도 trusted post-merge 경로로 성공했다.
- [x] [self-review 기록](../pr/archives/pr_6214_review.md)에 보호 불변식, 시각·성능 근거, 잔여 위험과
  #6215·#4969 후속 경계를 대사했다.
- [ ] PR 본문의 오래된 code head와 source-unit 수를 최신 값으로 정정하고 trailing 문서 head CI를 확인한다.
- [ ] 정상 merge 승인 뒤 #4968 자동 close와 `devel` 반영을 확인하고 후속 정리를 수행한다.

## planet6897 PR #6199/#6203/#6210/#6217 통합 검토

- [x] `upstream/devel@584320e0ee02` 기준 `review/planet6897-6199-6217-20260827` 브랜치를 만들고
  #6199, #6203, #6210, #6217의 CI-green/non-draft head를 체리픽했다.
- [x] 2026-08-28 `upstream/devel@9d6f69b4d1a0`로 충돌 없이 rebase했다. 사용자 지시에 따라 별도
  중복 테스트는 수행하지 않고 문서만 갱신했다.
- [x] #6203의 선행 commit `82a82d154b89`는 최신 `upstream/devel`에 이미 포함되어 중복 적용하지 않았다.
- [x] #6217 `vertAlign` 보존으로 obsolete가 된 `ir_field_sweep_baseline.tsv` row 8건을 메인터너 보정으로 제거했다.
- [x] post-rebase 기준 focused nextest 4종, IR/overflow baseline dump diff, clippy, 전체 nextest 8,473건을 통과했다.
- [x] #6217은 Hancom 2020 MCP 기준 PDF와 visual_sweep page 2 review PNG를 새로 산출해 assets에 보관했다.
- [x] Native Skia 3종과 WASM build 9m37s 검증을 끝내고 개별 PR review 문서의 최종 상태를 갱신했다.
- [ ] 통합 PR 생성 전 사전 승인을 받는다.

## #4969 - W10 vertical metrics·variation·고급 shaping

- [x] #5955·#4967·#4968 선행 완료와 `upstream/devel@6415047a4` 기준선을 확인했다.
- [x] #6243 미커밋 계획과 섞이지 않도록 `task_m100_4969` 전용 worktree를 만들었다.
- [x] 현행 ShapeKey·portable GlyphRun·backend structured rejection과 공개 fixture face 세 종을 조사했다.
- [x] #4960 tracker 현행화와 horizontal GSUB/GPOS → variation → vertical 순서를 포함한 수행계획을 작성했다.
- [x] 수행계획 승인 뒤 W10-Q0 tracker·선행 증거·무변화 기준선을 동결했다.
- [x] 기존 W3 journal schema를 먼저 감사하고, Q0에는 10k 전수 재계측이 필요하지 않다고 판정했다.
- [x] 공개 face 세 종의 identity·table·registry 상태와 W8 overlap 0을 inventory로 동결했다.
- [x] #4960 본문·comment 초안과 라이선스별 SFNT source 수정 A안을 승인받아 Q1 진입 조건을 확정했다.
- [x] #4960 본문·comment를 `gh`로 현행화하고 API 재조회로 게시 결과를 확인했다.
- [x] 공식 Happiness variable TTF와 Source Han OTF의 provenance·semantic equivalence·rustybuzz parse를 동결했다.
- [x] W10-Q1 source supply와 representative shaping oracle을 기계 판독 계약으로 고정했다.
- [x] Q1 bounded request validator와 malformed·미지원·상한 음성 fixture를 구현했다.
- [x] native lib·WASM check, clippy와 focused integration source 4건을 통과했다.
- [x] canonical settings identity와 bounded rustybuzz glyph output을 연결하고 공개 oracle 4종을 재현했다.
- [x] focused integration source 9건, native lib·WASM check, clippy와 diff check를 통과했다.
- [x] ShapeKey·FontInstanceKey·GlyphRun schema는 producer canonicalization 조건으로 충분하다고 판정했다.
- [x] GlyphRun이 생성되지 않는 rejected attempt의 typed disposition이 public trace에서 사라지는 계보 결손을
  확인하고 Q2 필수 sidecar 계약으로 인계했다. Q1 제품 schema 변경은 하지 않았다.
- [x] W9 scalar position map을 억지로 확장하지 않는 cluster-based Q2 수정 수행계획을 작성했다.
- [x] 수정 수행계획 승인 뒤 Q2-A terminal attempt owner와 4,096-record bounded ledger를 구현했다.
- [x] focused integration source 12건, native lib·WASM check, clippy와 diff check를 통과했다.
- [x] Q2-B 착수 승인 뒤 W9 exact-source registry를 재사용하는 horizontal shadow context와 source별 face cache,
  cluster-aligned px measurement를 구현했다.
- [x] K0·W9 K1·Latin liga·옛한글 공개 fixture의 delta를 고정하고 native·Node WASM focused 5건에서
  parity mismatch 0, 제품 layout·paint 소비자 0을 확인했다.
- [x] Q2-B 결과 승인 뒤 Q2-C cluster-aware line-break activation matrix를 확정했다.
- [x] Q2-C dormant paragraph·line transaction을 native·Node WASM에서 검증하고 제품 소비자 0을 유지했다.
