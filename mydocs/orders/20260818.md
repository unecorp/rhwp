---
kind: daily-order
status: active
date: 2026-08-18
---

## #5201 2차 CI 보정

- 새 merge-ref의 정규 테스트 3개 샤드 실패를 조사해, CI 생성 산출물이 아닌 `edit` 하위명령 자기서술과 지식지도 §2-2 계약 불일치로 확정했다.
- `src/main.rs`: 공개 하위명령 중복을 첫 선언 기준으로 제거하고, USAGE를 실제 디스패치 88개와 동기화했다.
- `mydocs/manual/agent_knowledge_map.md`: `form-value` 봉투 5개 필드와 수식 `script`를 추가하고 §2-2 헤딩을 329개로 갱신했다.
- 로컬 통과: fmt, build, `capabilities_subcommands_contract` 4/4, `knowledge_map_field_dictionary_contract` 2/2, diff check. `tests/generated/regression_suite_*` 및 `tests/suites/manifest.json`은 사용자 지시대로 수정·stage하지 않는다.
- 보정 커밋을 만든 뒤 push하고 새 PR head의 CI가 모두 통과할 때까지 재감시한다.
- 추적 대상 `tests/suites/unit-test-tiers.json`은 소스 줄 번호 기준선 1건만 재계산했으며 검사를 다시 통과했다.

# 2026-08-18 오늘할 일

## kevin9327 누적 PR 통합 검토

- 대상: #4886, #5002, #5003, #5004, #5005, #5006, #5007, #5050, #5055, #5062, #5070, #5074, #5076, #5077, #5078, #5079, #5084, #5086, #5105, #5113, #5120, #5127, #5134, #5137, #5143, #5148, #5155, #5156, #5158
- 기준: `upstream/devel@ba097d6bf9f2`, 검토 branch `review/kevin9327-20260817`
- 결과: non-draft 원 PR head를 확인하고 누적 기능을 최신 devel 기준으로 통합했다. #5003은 중첩 표 검색 코드와 계약이 이미 기준선에 있어 빈 cherry-pick으로 건너뛰었다.
- #5158은 오래된 누적 branch의 기존 기능 되돌림을 피하기 위해 `set-section-def` 고유 기능과 후속 계약·문서 보정만 수동 병합했다.
- `tests/generated/regression_suite_*` 및 `tests/suites/manifest.json`은 생성 산출물 정책에 따라 통합하지 않았다.
- 각 PR별 archive review는 `mydocs/pr/archives/pr_<N>_review.md`에 작성했다.
- 로컬 누적 검증은 이미 완료된 결과를 사용한다. 이번 문서 단계에서 전체 Cargo 회귀를 다시 실행하지 않는다.
- `git diff --check`는 통과했다. fmt gate의 기존 포맷 차이와 생성 suite 참조 문제는 원격 CI/정책 확인 대상으로 기록한다.
- 원격 push, 통합 PR 생성, review comment, merge, 원 PR close는 별도 승인 후 수행한다.

## #5198 후속 통합 보정 (#5201)

- 기준: `upstream/devel@efbd8da6a84786dbdad8274c0ced49669e5f3e45`, 후속 PR [#5201](https://github.com/edwardkim/rhwp/pull/5201).
- 최신 CI 실패를 재현해 MCP capabilities의 양식 값 설정 등록 누락 2건과 도구 중복 등록 3건을 메인터너 보정했다.
- 로컬 release-test 계약 suite `029`·`031`·`032`와 fmt, Clippy, unit-tier, 자동 에이전트 문서, diff 검사를 통과했다.
- `tests/generated/regression_suite_*`와 `tests/suites/manifest.json`은 CI 생성 산출물로 남기며 커밋하지 않는다. 보정 commit push 뒤 최신 PR head CI가 통과하면 merge 및 후속 처리를 진행한다.

## #5425 kevin9327 누적 PR 통합 검토
- 대상: #5210, #5211, #5214, #5221, #5227, #5228, #5248, #5270, #5272, #5278, #5282, #5284, #5315, #5352, #5356, #5358, #5360, #5362, #5364, #5366, #5369, #5373, #5375, #5381, #5385, #5387, #5394.
- 기준: `upstream/devel@0bc05ef81107ac61ec38d622f71b44a44d1b4821`, 검토 branch `review/kevin9327-20260818-r1`, 통합 PR [#5425](https://github.com/edwardkim/rhwp/pull/5425).
- 원 PR head를 오래된 순서와 의존 관계에 따라 최신 devel 위에 누적 적용했고, 각 PR별 archive review와 통합 구현 기록을 같은 PR에 포함했다. 충돌은 CI 오라클·문서·테스트 패키지 경계를 보존하는 방향으로 해소했다.
- 메인터너 보정은 두 개의 별도 commit으로 남겼다. `5b9599ffb98c5dcc72c0e73937df076ea1e77031`은 오라클 fixture 줄바꿈과 불필요한 draft를 정리했고, `5f0d7e0a8482617747bdc7d7df57e98e6717a02c`는 fixture 기대값·보안 예제·수신 순서 테스트를 최신 계약에 맞췄다.
- 최초 PR CI에서 보안 수신 테스트의 Clippy `len_zero` 경고가 발생해 `c33bb729ef4fc377f5c313e49c8d028b65a1e60a`에서 `is_empty()` 판정으로 보정했다. 로컬 `cargo fmt`, `cargo clippy --all-targets --target-dir target/pr-review -- -D warnings`, 대상 테스트 5/5와 `git diff --check`를 통과했다.
- GitHub 최신 head `c33bb729ef4fc377f5c313e49c8d028b65a1e60a`에서 CI preflight, Lint, Native Skia, Build test archive, 일반 3개 shard, slow shard, Canvas visual diff, Prop roundtrip, JavaScript/Python/Rust CodeQL을 모두 통과했다. Frontend unit gates와 WASM Build는 변경 영향에 따라 skipped였고, `Build & Test` aggregate도 통과했다.
- 검토 archive는 `mydocs/pr/archives/pr_<N>_review.md`와 `mydocs/pr/archives/pr_5210_5394_review_impl.md`에 보존했다. 원 PR merge·close와 최종 merge는 별도 승인 후 수행한다.

## planet6897 9개 누적 PR 통합 검토

- 대상: #5200, #5202, #5203, #5204, #5220, #5253, #5298, #5323, #5340.
- 기준: `upstream/devel@e79f113080ead96c391391d211a0a64fa8398378`, 검토 branch `review/planet6897-20260818-r1`, 현재 로컬 head `d10030fc8`.
- 원 PR source head는 각각 fetch 후 재확인했으며, 모두 `OPEN`·non-draft이고 추가 push는 확인되지 않았다. devel merge·CI 생성 산출물은 제외하고 고유 기능과 원본 회귀 테스트를 최신 devel 위에 누적했다.
- #5203은 차례 필드·숨은 설명·Hancom 기호·묶음 빈칸·짝 없는 fieldEnd를 포함하는 누적 PR이므로 연결된 고유 변경을 한 번에 검토했다. #5298의 회귀 테스트 주석 정렬만 rustfmt gate를 위해 메인터너 보정 commit `d10030fc8`로 고정했다.
- 각 PR별 review archive와 통합 이행 기록은 `mydocs/pr/archives/pr_5200_5202_5203_5204_5220_5253_5298_5323_5340_review_impl.md`에 보존했다.
- 로컬 검증: focused 17/17, 전체 nextest `7219 passed, 38 skipped, 11 slow`, fmt, manifest/unit-tier, root·WASM·workspace clippy, diff check 모두 통과했다.
- 통합 PR [#5472](https://github.com/edwardkim/rhwp/pull/5472)을 `devel` 대상으로 생성했고 head `34b980bcf2007153d507e1cb00718da6b9690529`를 확인했다.
- 원격 CI는 preflight, CodeQL 3개 언어, Lint, Native Skia, Build test archive, 일반 shard 1·2·3, slow shard, prop roundtrip, Build & Test가 모두 통과했다. Frontend unit gates와 WASM Build는 정상 skip이며, PR 상태는 `MERGEABLE`이다.
- CI 완료 후 이 기록을 trailing docs-only commit으로 갱신한다. merge, 원 PR comment/close, 원격 branch 삭제는 작업지시자 명시 승인 후 처리한다.

- 대상: #5210, #5211, #5214, #5221, #5227, #5228, #5248, #5270, #5272, #5278, #5282, #5284, #5315, #5352, #5356, #5358, #5360, #5362, #5364, #5366, #5369, #5373, #5375, #5381, #5385, #5387, #5394.
- 기준: `upstream/devel@0bc05ef81107ac61ec38d622f71b44a44d1b4821`, 검토 branch `review/kevin9327-20260818-r1`, 통합 PR [#5425](https://github.com/edwardkim/rhwp/pull/5425).
- 원 PR head를 오래된 순서와 의존 관계에 따라 최신 devel 위에 누적 적용했고, 각 PR별 archive review와 통합 구현 기록을 같은 PR에 포함했다. 충돌은 CI 오라클·문서·테스트 패키지 경계를 보존하는 방향으로 해소했다.
- 메인터너 보정은 두 개의 별도 commit으로 남겼다. `5b9599ffb98c5dcc72c0e73937df076ea1e77031`은 오라클 fixture 줄바꿈과 불필요한 draft를 정리했고, `5f0d7e0a8482617747bdc7d7df57e98e6717a02c`는 fixture 기대값·보안 예제·수신 순서 테스트를 최신 계약에 맞췄다.
- 최초 PR CI에서 보안 수신 테스트의 Clippy `len_zero` 경고가 발생해 `c33bb729ef4fc377f5c313e49c8d028b65a1e60a`에서 `is_empty()` 판정으로 보정했다. 로컬 `cargo fmt`, `cargo clippy --all-targets --target-dir target/pr-review -- -D warnings`, 대상 테스트 5/5와 `git diff --check`를 통과했다.
- GitHub 최신 head `c33bb729ef4fc377f5c313e49c8d028b65a1e60a`에서 CI preflight, Lint, Native Skia, Build test archive, 일반 3개 shard, slow shard, Canvas visual diff, Prop roundtrip, JavaScript/Python/Rust CodeQL을 모두 통과했다. Frontend unit gates와 WASM Build는 변경 영향에 따라 skipped였고, `Build & Test` aggregate도 통과했다.
- 검토 archive는 `mydocs/pr/archives/pr_<N>_review.md`와 `mydocs/pr/archives/pr_5210_5394_review_impl.md`에 보존했다. 원 PR merge·close와 최종 merge는 별도 승인 후 수행한다.

## kevin9327 27개 누적 PR 통합 검토 (PR 준비)

- 대상: #5240, #5242, #5274, #5317, #5320, #5321, #5325, #5328, #5330, #5332, #5334, #5336, #5338, #5341, #5348, #5349, #5350, #5351, #5354, #5355, #5371, #5377, #5379, #5383, #5389, #5391, #5393
- 기준: `upstream/devel@3067dc8be`, 누적 branch `review/kevin9327-macos-20260818`, 검토 후보 `933904ac2`.
- 원 PR별 검토 기록: `mydocs/pr/archives/pr_<번호>_review.md` 27개. 원 head·적용 SHA·메인터너 보정·수용 조건을 각 문서에 분리 기록했다.
- 로컬 통과: suite/unit-tier 정책, fmt, clippy, 전체 nextest **7,153/7,153**, Native Skia lib 58/58·renderer fixture 6/6.
- 보류: Docker daemon 미기동으로 WASM compose 게이트는 시작 전 차단됐다. daemon 기동 후 재실행하고 원격 CI까지 통과하면 누적 PR을 생성한다.
- 파생 `tests/suites/unit-test-tiers.json` 변경은 검증 후 정확히 되돌려 커밋에 포함하지 않는다.

## kevin9327 PR #5212–#5314 누적 체리픽 검토

- 대상: #5212, #5213, #5222, #5225, #5226, #5232, #5238, #5239, #5241, #5244, #5266, #5267, #5268, #5269, #5271, #5276, #5280, #5286, #5288, #5290, #5299, #5302, #5303, #5304, #5305, #5310, #5314.
- 기준: 초기 적용 `upstream/devel@0bc05ef81107ac61ec38d622f71b44a44d1b4821`, 최종 검증 기준 `upstream/devel@3344e391536f1e9978040c75083bd160677b5d4c`. 검토 branch `review/kevin9327-5212-5314-20260818`.
- 적용: 원 PR의 기능 commit 40개를 최신 기준 위에 순서대로 적용했다. #5303/#5310 registry 충돌은 기존·신규 CAP 항목을 모두 보존했다.
- 메인터너 보정: fixture LF/EOF 정규화, form-fill contract의 `tests/cases/` 경로 정정, gym check·profiles 계약 보정, Windows CodeQL test line-ending 보정, 신규 Rust contract clippy 보정, CAP-5300 권위 문서 링크 단일화, gym audit fixture schema, `rhwp-doc-triage` 실행 예시까지 별도 commit으로 남겼다.
- 로컬 통과: 변경 Python 1,637개, CI/CodeQL/review-only Python 54개, CI impact Node 62개, manifest 16개, unit tier 11개, fmt, clippy, doctest 8개, 변경 contract regression suite 723/723(6 skipped). #5444 merge rebase 뒤 16 논리 CPU에서 공식 전체 release-test nextest를 12 thread로 다시 실행해 7,274/7,274 passed(15 slow, 38 skipped, test 실행 1,030.178초)를 종료코드 0으로 확인했다. release-test 재컴파일은 26분 16초였다.
- `tests/generated/regression_suite_*` 및 `tests/suites/manifest.json`은 CI 파생 산출물로 검증에만 사용했고 stage/commit하지 않는다.
- 개별 검토 문서: `mydocs/pr/archives/pr_<N>_review.md` 27개. 누적 적용 SHA·보정·검증 기록: `mydocs/pr/archives/pr_5212_5314_review_impl.md`.
- 병합: 통합 PR [#5429](https://github.com/edwardkim/rhwp/pull/5429)는 2026-08-18 `f3302fb745eaf6b81eba2b502fe75f0684141f88`으로 `devel`에 병합됐다. 최신 head `dfeadb55e40583a635d09551adc118c2673fafc8`의 CI·CodeQL·Native Skia·Rust shard·prop roundtrip은 모두 통과했다.
- 후속: 문서 archive 반영 뒤 통합 당시 OPEN이었던 원 PR 27개에 통합·검증 사실을 남기고 close한다. 원 branch는 삭제하지 않는다.
## #5454 PR Rust CodeQL 분석 경로 분리

- 이슈 [#5454](https://github.com/edwardkim/rhwp/issues/5454), 기능 PR [#5455](https://github.com/edwardkim/rhwp/pull/5455)는 `30fa47bcf5ab272c915d1852f69386d8d5b5eca1`로 병합됐다.
- PR 이벤트의 Rust CodeQL은 `.github/codeql/rust-pr.yml`로 `src/**`, `crates/**`, `rhwp-desk/src/**`, `build.rs`만 분석한다. `devel` push와 schedule은 설정 파일 없이 전체 Rust 경로를 계속 분석한다.
- 원격 CI 기준 Rust job은 15분 13초에서 14분 19초로 54초(5.9%) 단축됐고, 오류 없이 추출한 Rust 파일은 1,376개에서 624개가 됐다. 추출·query 시간이 각각 19초·37초 감소했으며, 전체 CI는 성공 또는 정책상 skip으로 완료됐다.
- 병합 뒤 `devel`을 `upstream/devel@30fa47bcf5ab272c915d1852f69386d8d5b5eca1`로 fast-forward했고, #5454의 자동 종료와 기능 head 브랜치의 로컬·원격 삭제를 확인했다.
- 기능 변경과 분리해 오늘할일 및 PR 검증 기록은 docs-only PR에서 보존한다.

## #5463 unit-tier inventory CI 파생 전환 완료

- PR #5473에서 추적 대상이던 `tests/suites/unit-test-tiers.json`을 제거하고, 정적 정책만 `tests/suites/unit-test-tier-policy.json`으로 분리했다.
- inventory는 CI와 로컬 검사 시 소스 및 PR base에서 계산한다. 선택적 진단 출력 `tests/generated/unit-test-tiers.json`은 Git에서 제외해 개발자가 생성하거나 stage할 필요가 없다.
- 로컬 format·manifest·tier 검사와 script unit test를 통과했고, CI의 Rust CodeQL(13분 45초), test archive 생성(7분 51초), nextest shard, Native Skia 검증이 모두 성공했다.
- PR #5473은 `2c639ac0da43adabc792bd0649bbeb7ac1490f08`으로 병합했다. #5463은 종료되었고 기능 브랜치의 로컬·원격 참조를 삭제했다.
- 병합 기록은 `mydocs/pr/archives/pr_5473_review.md`에 보존하며, 본 오늘할일·검토 기록은 문서 전용 후속 PR로 분리한다.

## M050 — v0.5.0~v0.8.x 뷰어 안정화

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #3128 | 분할 표 continuation fragment의 p34 세로 배치 정합 | 진행중 | [Stage 4 전체 검증](../working/task_m100_3128_stage4.md) 완료, commit·push·한국어 PR 생성 승인 완료 |
## #5497 문서 전용 PR의 Proptest fast-pass

- 이슈 [#5497](https://github.com/edwardkim/rhwp/issues/5497), 기능 PR [#5500](https://github.com/edwardkim/rhwp/pull/5500)은 `2f757ad73147e99b0feb4ef7faf5e27750602c32`로 병합됐다.
- `devel` 대상 PR의 변경 경로가 모두 `mydocs/**`이면 Proptest preflight가 `mydocs-only-devel-pr`을 기록하고, required check 이름을 유지한 `prop roundtrip` worker는 명시적으로 skip한다.
- 코드·테스트·workflow 변경, `push`, 수동 실행, GitHub API 판정 실패는 fail-closed로 기존 Proptest 실행 경로를 유지한다.
- 기능 PR 자체는 workflow 변경이므로 fast-pass를 적용하지 않았다. 최신 head `3390f401d57d8d40a9d3d8a02c680dde948593a9`에서 `prop roundtrip`이 2분 11초에 실제 실행되어 통과했고, Build & Test, Native Skia, 세 regular shard, slow shard 및 CodeQL 3개 언어도 모두 통과했다.
- 검증 기록은 `mydocs/pr/archives/pr_5500_review.md`에 보존한다. 이 후속 docs-only PR에서 Proptest preflight와 `prop roundtrip`의 명시적 skip을 확인한다.
