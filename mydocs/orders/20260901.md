# 2026-09-01 작업 기록

## planet6897 #6514/#6536 체리픽 통합 검토

- base `upstream/devel@891e395bb`에서 #6514 (`c8708e2d8`), #6536 (`b8041f23c`)를 충돌 없이 체리픽했다.
- #6514는 fit-test letter-spacing trim 계약 test 보강으로 승인 가능하다.
- #6536은 Hancom 2020 p1에서 표 앞이어야 하는 `2.` 문단이 rhwp에서 표 뒤로 이동하는 P1 시각 결함이 확인돼 변경 요청이다.
- Rust lint/build, focused regression, full nextest는 통과했지만 시각 결함으로 통합 PR은 생성하지 않았다.
- 기록: `mydocs/pr/archives/pr_6514_6536_review_impl.md`.

## #6536 메인터너 보정 및 증적 보관

> 이 절은 `0ff2e25b6` 시점의 중간 작업 기록이다. 후속 bbox 검토에서 표와 종결문 겹침을
> 발견해 "수용 가능" 판정을 철회했고, 최종 보정 `7cf17c1ce`와 현재 판정은
> [`../pr/archives/pr_6536_review.md`](../pr/archives/pr_6536_review.md)를 따른다.

- 검토 브랜치 `review/planet6897-6514-6536-20260831`에서 `0ff2e25b6`으로 양수 offset 빈 host 표의 본문 선행 배치 오류를 보정했다.
- #6535 focused, lint, Native Skia, locked WASM, 전체 nextest `8,912 passed, 46 skipped`를 통과했다.
- Hancom 2020 기준 PDF `mydocs/pr/assets/pr_6536_issue6535_p1_2020.pdf`와 최신 시각 패널 `mydocs/pr/assets/pr_6536_issue6535_p1_maintainer_review.png`을 함께 보관했다. 사람 검토로 1페이지의 `2.` 본문 -> `연번` 표 -> `끝.` 순서를 확인했다.
- 당시 상태: **메인터너 보정 후 수용 가능**. 이 판정은 위 후속 검토로 대체됐다.

## PR #6541 연속 통합 병합과 후속 분류

- 최종 integration head `88d0924e550041746464627bc7bc32b1a2511177`을 메인테이너가 self-review하고
  Ready 상태의 required checks를 확인했다.
- PR #6541은 정상 2-parent merge commit
  `e9d2f8b258b8310fd10d465b486b9ab4d85e771e`로 `devel`에 병합됐다.
- #6514, #6536, #6543, #6546, #6548, #6552의 active review 기록을 archive로 이동하고,
  각 source head -> integration commit -> merge commit 계보와 contributor comment 계획을 고정했다.
- #6514와 #6536 개별 review worktree에 남아 있던 최초 차단 판정 초안도 삭제하지 않고
  `pr_6514_maintainer_review_initial.md`, `pr_6536_maintainer_review_initial.md`로 archive에 보존했다.
- 관련 이슈 판정: #5678·#6535·#5941은 계속 open, #6263은 기존 closed 상태에 범위 설명 추가,
  #6542·#6545는 해결 범위를 확인한 뒤 close 후보다.
- 남은 절차: archive commit의 `devel` 반영 확인 -> 원 PR 6건 계보 comment·close -> 관련 issue
  comment/close -> 소유가 확인된 review worktree·branch 정리.

## #6539 CLI help index와 scoped help

- #6539 등록: root help의 이름순 index와 scoped command help 회귀를 정의했다.
- PR #6540 생성: CLI help index, 진단 명령 상세 help, 매뉴얼, scoped help 회귀 계약을 반영했다.
- 리뷰 보완: 모든 capabilities 경로의 구조화된 `명령·그룹·사용법·옵션·예시` metadata를 정본으로 삼고, 내부 개발·회귀 명령을 root help 별도 이름순 section에 노출한다. 기존 긴 help는 보조 설명으로만 붙였다.
- 전체 검증: manifest, format, native/WASM 및 workspace all-target Clippy, workspace build, nextest 8,915 passed/46 skipped, diff check를 통과했다.
- 남은 절차: PR #6540 최신 head의 required CI와 merge 상태를 확인한 뒤 일반 merge commit과 post-merge 절차를 수행한다.
- 남은 절차: archive commit의 `devel` 반영 확인 → 원 PR 6건 계보 comment·close → 관련 issue
  comment/close → 소유가 확인된 review worktree·branch 정리.

## planet6897 #6554/#6559/#6560 연속 통합 검토

- 최신 `upstream/devel@6d3fd65a3`에서 원 PR 번호순 `#6554 -> #6559 -> #6560`으로 누적했다.
- #6559의 `ir_field_sweep_baseline.tsv` 충돌은 최신 사전순 원장을 유지하고 신규 fixture 행만 추가해
  중복 과거 행을 되살리지 않았다. 원 contributor branch는 변경하지 않았다.
- 세 PR focused test, oracle page-count·off-canvas 각 16 partition, IR field sweep 4/4와 필수 Native·WASM·
  workspace Rust lint/build 묶음이 통과했다.
- 누적 head의 `release-test` 전체 회귀는 8,917/8,917 통과, 46건 정책상 skip, 실패 0건이었다.
  Native Skia 전체 `--lib`와 집중 2/2·4/4, Docker WASM도 통과했다. 로컬 nextest 0.9.137과 권장
  0.9.140의 차이 및 `junit.report-skipped` 설정 경고는 통합 PR에 명시한다.
- #6559와 #6560은 마지막 저장 제품에 맞춰 MCP engine 2020으로 기준 PDF를 새로 산출했다. 한컴
  `12.0.0.4605`, 32-bit worker, client/server SHA-256 일치를 확인했다.
- 직접 visual sweep 결과 #6559는 한컴과 같은 행에서 2쪽으로 분할되고 #6560은 본문·서명·하단 업무표를
  겹침 없이 1쪽에 흡수했다. 두 문서의 자동 후보는 모두 0건이다.
- 판정: #6554 승인, #6559는 원 PR conflict를 integration head에서 의미 기반으로 해결한 뒤 수용 가능,
  #6560 승인. #6544와 #6535는 잔여 원인 때문에 자동 close하지 않는다.
- code candidate `ee4d7c4ad`를 push해 Draft 통합 PR #6563을 만들었고 Full CI·CodeQL·Render Diff·
  Proptest·Adapter inter-diff가 모두 성공했다. 후속 review comment·ready 전환·merge는 아직 수행하지
  않았다.
- 증거 head `220e9fb5a`의 Fast Pass도 성공했다. 세 원 PR 기록에 maintainer self-review를 확정했고
  blocking finding 없이 #6563 merge를 권고한다. Ready 전환과 실제 merge는 각각 승인 gate를 따른다.
- 기록: `mydocs/pr/archives/pr_6554_review.md`, `pr_6559_review.md`, `pr_6560_review.md`.

## PR #6562 인쇄 판형 표준 치수 복원

- contributor code commit `864cc39ff`을 current `devel@c65fc42e`로 update한 source
  head `6ac4d298b`를 정상 merge commit `b33752594e5adaa85adc78481f171f81272aeb58`로 병합했다.
- focused 11/11, Studio 1,341 pass / 1 skip, TypeScript CI-unit 검사와 실제 Chrome PDF
  3조건 계측이 통과했다. source merge 뒤 CI·CodeQL·Proptest·Adapter도 모두 성공했다.
- 수정 전 `210mm 296.995mm`, 수정 후 `210mm 297mm`, Chrome `size: A4`는 모두
  `594.96×841.92pt`로 출력되고 Poppler에서 A4로 판독됐다. #6561의 595.28pt 완료
  조건은 Chrome 출력 계약과 맞지 않으며, 실제 완료 범위는 CSS/API 판형 정체성 복원이다.
- #6561은 자동 종료됐다. archive 반영 뒤 완료 조건 정정 comment와 contributor PR comment를
  게시하고 전용 review worktree·branch를 정리한다.
- 기록: `mydocs/pr/archives/pr_6562_review.md`.

## PR #6564 셀 선택 전 표 경계 hover·resize 복원

- contributor 원 head `0987770e`를 current `devel@0d1540931`에 정상 merge하고, 페이지별
  bbox 실패 memory, 직접 성공 뒤 stale failure, 분할 표 page membership 선형 검색을 메인터너
  보정 `b4df21457`에서 바로잡았다.
- focused 29/29, Studio 1,353 pass / 1 skip, TypeScript, E2E manifest, 필수 Rust lint 묶음,
  Docker WASM build와 #4117 Chrome hover→drag 왕복이 통과했다. mousemove 60회에서 엔진
  호출은 1회였고 첫 열은 39.8px 증가했으며 세 행이 같은 경계로 이동했다.
- code·증적 head `257d81c3e`의 Full CI·CodeQL·Proptest·Adapter·Render Diff와 review-only
  최종 head `4a2be5541`의 Fast Pass가 실패 없이 성공했다. 메인터너 self-review에서 blocking
  finding이 없음을 확인했다.
- PR #6564는 정상 2-parent merge commit `07e1dd7ef6e51bb063b4b4bf10e5694d8eec94c5`로
  `devel`에 병합됐고 #4117은 자동 종료됐다.
- 남은 절차: #4117 maintainer 완료 comment → contributor PR merge 결과 comment → 전용
  review worktree·local branch 정리.
- 기록: `mydocs/pr/archives/pr_6564_review.md`, `pr_6564_review_impl.md`.

## PR #6567 #6544 오라클 판 표기 provenance 정정

- contributor head `b5abb6f6`은 `tests/issue_1139_inline_picture_duplicate.rs`의 주석·assert
  실패 메시지 7곳만 한글 2022에서 실제 재측정 환경인 한글 2024로 바로잡았다. 수치·조건·fixture·
  renderer 동작은 바뀌지 않았다.
- 최신 `devel@8b55fcf06` current-base merge-tree가 무충돌이었고 focused module 86/86과 exact-head
  Full CI·CodeQL·Proptest·Adapter가 실패 없이 통과했다.
- PR #6567은 정상 2-parent merge commit `86d5a361e5e7a5f349717c50d295d56d2c50e733`으로
  `devel`에 병합됐다.
- 프로젝트의 engine 선택은 저장 버전을 따른다. 한글 2024는 이번 기여자 측 측정 provenance이며,
  2022 이하 저장본은 engine 2020을 쓴다는 canonical 정책은 그대로다.
- 관련 #6544는 `pi=659` 잔여 원인 때문에 계속 open이다. 남은 절차는 archive 운영 기록 반영,
  contributor PR merge 결과 comment와 전용 local review branch 정리다.
- 기록: `mydocs/pr/archives/pr_6567_review.md`.

## #5949 Linux AArch64 릴리즈 바이너리

- v0.8.5 선행 작업으로 #5949를 메인테이너가 착수했다.
- 최신 `upstream/devel@cd1fe7181`에서 `task_m100_5949` 브랜치를 만들었다.
- 현재 4플랫폼 Release Binary matrix에 native `ubuntu-24.04-arm` 기반
  `aarch64-unknown-linux-gnu` 자산을 추가하는 O3 수행계획을 작성했다.
- #5949 구현 merge와 실제 v0.8.5 release asset 검증을 분리하며, GitHub Release에
  Linux AArch64 archive·checksum이 게시되기 전에는 이슈를 닫지 않는다.
- `release-binary.yml`은 Render Diff trigger가 아니므로 #6243 canary는 후속 release-prep PR에서
  별도 수행한다.
- Stage O3-1에서 기존 release workflow 계약 47건의 선행 GREEN을 확인하고, Linux AArch64 target·runner·
  suffix 누락만 검출하는 신규 계약 1건을 의도된 RED로 고정했다. workflow 구현은 아직 시작하지 않았다.
- Stage O3-2에서 `aarch64-unknown-linux-gnu` / `ubuntu-24.04-arm` / `linux-aarch64` matrix 항목을
  추가해 신규 계약을 GREEN으로 전환했다. 기존 포함 48개 workflow 계약과 문서 링크·diff 검사가 통과했다.
- trigger·permission·release 조건과 기존 네 target은 바꾸지 않았으며 원격 실행은 아직 하지 않았다.
- Stage O3-3에서 48개 계약을 재통과하고 PyYAML로 5개 matrix와 ARM64 exact row를 파싱했다. 최신
  `upstream/devel@cd1fe7181` 대비 behind 0, required `Build & Test`, rollback 경계를 확인했다.
- 로컬 제출 후보는 통과했지만 push·PR·`tag=test` workflow dispatch는 별도 승인 gate로 남겼다.
- 후보 branch를 원격에 push하고 `devel` 대상 Open PR #6573을 생성했다. PR 본문은
  `Relates to #5949`로 연결해 구현 merge가 이슈를 자동 종료하지 않게 했다.
- code candidate `eaf1bcfc9` self-review에서 matrix·권한·trigger·release 조건을 대조했고
  blocking finding은 없었다. 판정은 승인이나, 최신 head Full CI와 별도 승인된 `tag=test`
  native ARM64 실행·artifact 검증을 merge 필수 gate로 유지한다.
- trailing head `ea20afc4b`의 Full CI run 33509308330과 CodeQL이 성공했다. workflow 변경 PR이라
  review-only fast-pass 대신 heavy CI를 실행한 안전 경로였다.
- Release Binary `tag=test` run 33510934562에서 5플랫폼 build가 모두 성공하고 release job은
  정상 skip됐다. Linux AArch64는 `ubuntu-24.04-arm`에서 8분 14초에 native release build를
  마치고 `rhwp v0.8.4`를 실행했다.
- `rhwp-test-linux-aarch64.tar.gz`를 내려받아 필수 4개 파일, 실행 권한, ELF64 little-endian PIE와
  `Machine: AArch64`를 확인했다. 내부 archive SHA-256은
  `ba55608c2ea67ebdd9b2aff46334d33824d9ecea878a86d4b3ee7d520126fe8c`다.
- Stage O3-4 원격 실행 검증은 완료됐다. 결과 기록 trailing head의 CI를 다시 확인한 뒤 정상 merge
  후보가 되며, #5949는 실제 v0.8.5 asset 검증 전까지 open으로 유지한다.
- 기록: `mydocs/pr/archives/pr_6573_review.md`.
- 기록: `mydocs/plans/task_m100_5949.md`.
- 단계 기록: `mydocs/working/task_m100_5949_stage1.md`.
- 구현 기록: `mydocs/working/task_m100_5949_stage2.md`.
- 제출 준비 기록: `mydocs/working/task_m100_5949_stage3.md`.
- 원격 실행 기록: `mydocs/working/task_m100_5949_stage4.md`.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
| --- | --- | --- | --- |
| #6187 | 눈금자 상시 표시·resize 무깜빡임 PR #6570 self-review | 완료 | code candidate `88ca4d1bf` 로컬·사용자 Stage 3·CI 통과, 판정 `승인`; trailing 기록과 정상 merge 뒤 #6432 정리 |
