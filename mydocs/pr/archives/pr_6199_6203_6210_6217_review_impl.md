---
kind: pr-review-implementation
status: local-validation-complete
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-28
---

# planet6897 PR #6199/#6203/#6210/#6217 통합 검토 구현 기록

## 기준과 포함 범위

- 통합 브랜치: `review/planet6897-6199-6217-20260827`
- 최신 기준: `upstream/devel@9d6f69b4d1a0`
- 검증 실행 기준: `upstream/devel@584320e0ee02`
- 포함 PR:
  - #6199 `7e52aa3d402f` - #6172 합성 네모 숫자 전진폭
  - #6203 `926848c3026a` - #6185 자기 높이 음수 오프셋
  - #6210 `e97c1db04070` - #6184 이월 직전 host 꼬리 줄
  - #6217 `d93e12d2c50d` - #6186 꼬리말 세로 정렬
- 제외/중복:
  - #6203의 `82a82d154b89`는 최신 `upstream/devel`에 이미 포함되어 중복 적용하지 않았다.

## 체리픽과 메인터너 보정

- 원 PR head를 `upstream/pr6199-head`, `upstream/pr6203-head`, `upstream/pr6210-head`,
  `upstream/pr6217-head`로 fetch한 뒤, `git cherry -v upstream/devel` 결과를 기준으로 미반영 commit만
  `-x`로 누적 적용했다.
- 적용 후 `git log --cherry-pick --right-only HEAD...refs/remotes/upstream/prN-head`로 추가 누락 commit이
  없음을 확인했다.
- #6217 serializer 보정으로 `list_attr` 결손 baseline이 개선되어
  `tests/fixtures/ir_field_sweep_baseline.tsv`의 obsolete row 8건을 제거했다. 이 보정은 회귀 은폐가 아니라
  ratchet tightening이며, 수정 후 dump/baseline sorted diff는 0건이다.
- 2026-08-28 `upstream/devel@9d6f69b4d1a0`로 충돌 없이 rebase했다. 사용자 지시에 따라 rebase 후 별도
  중복 테스트는 수행하지 않고 문서 기준만 갱신했다.

## 로컬 검증

| 검증 | 결과 |
| --- | --- |
| `cargo fmt --all -- --check` | 통과 |
| Rust suite manifest `--prepare`/`--check` + unit tiers | 통과, 987 sources / 4,422 static test attrs / 최소 6,559 cases |
| #6172 focused nextest | 통과, 1 pass |
| #6184 focused nextest | 통과, 1 pass |
| #6185 focused nextest | 통과, 1 pass |
| #6186 focused nextest | 통과, 2 pass |
| IR field sweep baseline dump/diff | 통과, 메인터너 보정 후 baseline-only/dump-only 0건 |
| overflow cell baseline dump/diff | 통과, diff 0건 |
| `cargo clippy --locked --all-targets --target-dir target/pr-review -- -D warnings` | 통과, 1m32s |
| 전체 `cargo nextest run --locked --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast` | 통과, 8,473 passed / 43 skipped / 10 slow, 947.400s |
| Native Skia lib | 통과, 3,946 pass / 13 ignored + contracts 15 pass + ooxml-chart 165 pass + password 2 pass |
| Native Skia `issue_2225_missing_picture_placeholder` | 통과, 2 pass / 124 skipped |
| Native Skia `render_p37_direct_pdf_export` | 통과, 4 pass / 133 skipped |
| `CARGO_TARGET_DIR=target/pr-review scripts/wasm-pack-locked.sh --target web --out-dir pkg` | 통과, 9m 37s |

## 시각 증적

- #6199 source report after/oracle PNG를 직접 확인해 boxed number와 `□-□□□` 겹침이 사라진 것을 확인했다.
- #6203 source report after PNG를 직접 확인해 로고 글상자가 담당부서 표 둘째 행을 덮지 않음을 확인했다.
- #6210 source report after PNG를 직접 확인해 host 꼬리 줄은 이전 쪽 하단에 남고 다음 쪽 상단 흐름표 중복이
  사라진 것을 확인했다.
- #6217은 통합 head에서 Hancom 2020 PDF를 새로 산출하고 `visual_sweep.py`를 page 2에 대해 실행했다.
  - `rhwp info --json`: `mydocs/pr/assets/pr_6199_6217_156755659_footer_vertalign_bottom_hwpx_info.json`
  - 기준 PDF: `pdf/pr_6217_issue6186_footer_vertalign_bottom-2020.pdf`
  - review PNG: `mydocs/pr/assets/pr_6217_issue6186_footer_p2_review.png`
  - summary: `mydocs/pr/assets/pr_6217_issue6186_visual_sweep_summary.json`
  - 결과: `flagged_page_count=0`, pixel match `93.02684%`, ink match `20.36145%`

## 결론

현재 기준으로 네 PR 모두 수용 권고다. #6217에는 obsolete IR baseline 제거 보정이 필요했고 이미 통합
브랜치에 반영했다. `upstream/devel@584320e0ee02` 기준 fmt, manifest/tier, focused nextest, baseline dump
diff, clippy, 전체 nextest, native-Skia, WASM build 재검증을 모두 완료했다. 이후
`upstream/devel@9d6f69b4d1a0`로 충돌 없이 rebase했으므로, 사용자 지시에 따라 추가 테스트 없이 통합 PR
생성 전 사전 승인 대상으로 올린다.
