---
kind: pr-review-implementation
status: local-review-complete
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-12
---

# planet6897 PR #4670, #4679, #4681 통합 검토 구현 기록

## 기준과 누적 순서

| 항목 | 기록 |
| --- | --- |
| 기준 devel | `upstream/devel@88012c7e09a6bcd6ec3c4065abb194dae9209e01` |
| 가시성 검토 branch | `review/planet6897-20260812-r2` |
| reviewer assign | #4670, #4679, #4681 모두 @jangster77 지정 완료 |
| 검토 순서 | #4670 → #4679 → #4681 |

누적 적용 commit은 다음과 같다.

| PR | source SHA | 검토 branch 적용 commit | 처리 |
| --- | --- | --- | --- |
| #4670 | `3cd54cfa5a658992f95714e71c48f07a776359df` | `3c53c0567` | `src/main.ts` 충돌 해소 |
| #4679 | `ed94b6da8fd53d6f4f3fe9bab01186a199fc5fdc` | `2bec6a9e9` | clean cherry-pick |
| #4681 | `581b53…`, `5e8bec8877daae881db8ec539d40799552ab66ce` | `f0b2407f7`, `60e5c716d` | 두 기능 commit 순서 유지 |

#4670의 충돌에서는 plugin automation 초기화와 최신 devel의 embed file/edit command 필터를
함께 유지했다. 원 PR 브랜치는 문서 작성 시점에도 `CONFLICTING/DIRTY`이므로, 원 PR을
직접 merge하지 않고 이 누적 branch를 source로 하는 통합 PR로 반영하는 경로가 필요하다.

## 메인터너 보정

| commit | 내용 |
| --- | --- |
| `6ba0838d7` | plugin 문서 교체 swap 중복 제거, 실제 교체 E2E, 최신 dispatcher source 계약, Node 24 gate 결과 파서 보정 |
| `be02afb64` | loadsave_sweep README와 Python 도움말의 개인 경로·드라이브·Python 설치 경로 일반화 |

보정은 contributor의 feature 범위를 확장하지 않았다. 첫 보정은 최신 devel과의 실제 호스트
계약을 보존하고 회귀를 자동화했으며, 둘째 보정은 독립 도구를 공통 사용자가 실행할 수 있게
문서화한 변경이다.

## 완료한 검증

| 범위 | 명령 또는 근거 | 결과 |
| --- | --- | --- |
| HWPX serializer 집중 | `--lib hwpx` | 638/638 |
| HWPX 통합 | baseline, integration, CLI, form 4 binaries | 31/31 |
| 전체 Rust | fixed `target/pr-review`, 12 threads | 5,881/5,881, 479.439초 |
| Rust 품질 | fmt, clippy | 통과 |
| WASM | `wasm-pack build --target web --out-dir pkg` | 통과 |
| Studio unit | `npm test`, bridge unit gate | 862/0, HwpCtrl 21/0 |
| Studio browser | automation, lifecycle, plugin, bridge, performance E2E | 87 assertions 통과 |
| Studio build | CI-unit tsc, production build, HwpCtrl standalone gate | 통과 |
| loadsave Python | py_compile, 5개 CLI help | 통과 |
| loadsave PowerShell | Windows PR ref `Parser::ParseInput` | `oracle_run.ps1` 통과 |

`CARGO_INCREMENTAL=0`은 지정하지 않았고, 모든 Cargo 검증은 고정
`target/pr-review`를 순차 재사용했다. Windows에서는 새 worktree나 임시 검증 디렉터리를
만들지 않았으며, PR ref만 fetch해 PowerShell source parser를 실행했다.

## 다음 단계

1. 통합 PR [#4683](https://github.com/edwardkim/rhwp/pull/4683)을 `devel` 대상으로 생성했다.
   code head는 `fd71b181a674a44de2d4c1607b2c49e761fadea2`다.
2. 이 code head의 GitHub Actions, CodeQL, 필요한 frontend/WASM check를 확인한다.
3. 작업지시자 승인 뒤 통합 PR을 merge한다.
4. #4670, #4679, #4681을 통합 반영 commit과 메인터너 보정 이유로 코멘트한 뒤 close한다.
5. #4681의 closing keyword에 따른 #4675 자동 close 여부를 확인한다.

원격 상태는 작성 뒤 바뀔 수 있으므로, merge 직전에 세 원 PR의 head SHA와 통합 PR의 최신
required check를 다시 확인한다.
