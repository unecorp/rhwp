---
kind: pr-review-implementation
status: approved
prs: [5689, 5691, 5695, 5707]
---

# #5689·#5691·#5695·#5707 누적 체리픽 적용 기록

- 검토 브랜치: `review/open-prs-20260820`
- 기준: `upstream/devel@1139f28d1`
- 적용 방식: 기존 VS Code의 `/Users/tsjang/rhwp` 작업공간에서 오래된 PR 번호순으로 `git cherry-pick -x`를 적용했다. 별도 worktree나 VS Code 창은 만들지 않았다.

## 적용 계보

| 원 PR | 원 source head | 체리픽 결과 | 판정 |
| --- | --- | --- | --- |
| #5689 | `66a879b775f506d4b75d894b99e0f147d13a495a` | `8d574f639` | 승인 |
| #5691 | `a4c90a830208ccd4cf6a3699ba1d569389d041dd` | `0fbc09b2e` | 승인 |
| #5695 | `034ac279d55c820e036d089374d6a85554d9b26c` | `9ab35483b` | 승인, 종결 측정 보정 포함 |
| #5707 | `7044877da5744ce1f0c5759bcc0b482d92d3a2f5` | `dac58ed13`..`40c1c0829` | 승인, 텍스트·게이트 보정 포함 |

## 메인터너 보정

- `9f79ed5a97b3bfb640175c77a07b914dce67d8e1`: #5707 증적 텍스트의 LF·trailing whitespace 정규화. `git diff --check upstream/devel`을 통과시키며 의미 변경은 없다.
- `c264a575d`: #5695의 0ms 타이머 해상도 종결 판정을 명확히 하고, #5707 skill gate가 명시한 후보 바이너리만 검사하도록 보정했다.

## 누적 검증

- suite manifest prepare/check, source unit-tier 정책, `cargo fmt --all`, `cargo fmt --all -- --check`, `git diff --check upstream/devel`을 통과했다.
- #5689 Rust contract `4/4`, #5691 Studio 선택 복원 `10/10`, #5695 headless browser 측정, #5707 gate·route·catalog·author·probe·precommit 검사를 통과했다.
- 원 source head 네 건은 모두 현재 `CLEAN`·`MERGEABLE`이며 완료된 CI가 성공 상태다. 보정이 추가된 통합 후보는 별도 PR의 CI로 최종 확인한다.

## 다음 단계

- 이 기록과 개별 검토 문서는 통합 후보 PR에 포함한다.
- 통합 후보 CI가 성공하면 원 PR #5689, #5691, #5695, #5707 및 수용 대상 issue #5688, #5690, #5706을 `post_merge.md` 절차에 따라 코멘트·close한다. #3315와 #5694는 성능 후속 추적을 위해 유지한다.
