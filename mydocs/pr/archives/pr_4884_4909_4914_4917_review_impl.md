---
kind: review-implementation
status: code-ci-running
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-16
---

# planet6897 PR #4884·#4909·#4914·#4917 누적 검토

`upstream/devel@ae5f2a345` 위의 `review/planet6897-20260816`에 원 PR의 기능 commit만 오래된 번호
순으로 적용했다. 원 contributor branch에는 push·rewrite하지 않았고, 코드 CI 진행 중 재확인한 원 head도
검토 시작 시점과 같다.

| 원 PR | source head | 통합 적용 |
| --- | --- | --- |
| #4884 | `613239a4b` | `b7195286b` |
| #4909 | `cb77c930d` | `6660f53a7` |
| #4914 | `a6e7a1434` | `8b644a870`, `a8f0cc20d` |
| #4917 | `0cccebc59` | `59e599bdc` |

## 검토와 로컬 검증

- 체리픽 충돌이 없었고 `git diff --check upstream/devel...HEAD`를 통과했다.
- HWP3 origin 3건, 소프트 하이픈 HWP/HWPX 2건, 중첩 표 회귀 1건, HWPX field type 2건을 직접 통과했다.
- `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 4 --no-fail-fast`:
  6,395 passed, 38 skipped.
- Native Skia lib 58건, missing-picture placeholder 2건, direct PDF export 4건, Clippy와
  `wasm-pack build --target web --out-dir pkg`를 통과했다.
- Windows에서 `cargo fmt --check`는 긴 파일명 `os error 206`으로 실행할 수 없었고, 변경 Rust 파일 전부에
  `rustfmt --edition 2021 --check`를 직접 실행해 통과했다.
- #4914는 Hancom Office 2020 PDF 3쪽과 rhwp 2쪽을 대조했다. 대표 PNG와 변환 PDF는 같은 통합 PR의
  docs-only 후속 커밋에 포함한다.

통합 PR [#4936](https://github.com/edwardkim/rhwp/pull/4936)의 최초 코드 후보 CI는 통과했다. 최신 devel
동기화 뒤 이 문서와 원 PR별 검토 기록·PDF를 포함하는 docs head의 필수 GitHub Actions와
`MERGEABLE`/`CLEAN`을 확인한 뒤에만 merge하고, 원 PR은 통합 반영 comment와 함께 close한다.
