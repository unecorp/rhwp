---
kind: review-implementation
status: completed-local-pending-push-approval
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-26
---

# kevin9327 누적 PR 검토와 메인터너 보정 기록

## 라우팅

~~~text
base route: pr_review/collaborator_external_pr.md
modifiers: intake_and_review.md, local_validation.md, visual_fixture_evidence.md,
           multi_pr_update_branch.md(4.2.1), post_merge.md
loaded documents: pr_review_workflow.md, pr_review/README.md, collaborator_external_pr.md,
           intake_and_review.md, local_validation.md, visual_fixture_evidence.md,
           multi_pr_update_branch.md
current head: upstream/devel `6b5c4f871` (작성 시점 참고값, merge 전 재확인 필요)
~~~

reviewer는 지정하지 않았다. 직전 통합 선례(#6074·#6076)와 같은 처리이며,
[intake_and_review 2.1](../../manual/pr_review/intake_and_review.md)의 assign 선행 규칙과는 다르다는
사실을 작업지시자 판단으로 기록한다.

작업공간은 세션이 만든 워크트리 `.claude/worktrees/pr-review-merge-e48477`이다.
[collaborator_external_pr 9.1.1](../../manual/pr_review/collaborator_external_pr.md)의 주 작업공간
기본 경로와 다르므로 여기 남긴다.

## 누적 적용

기준선은 `upstream/devel@6b5c4f871`, 통합 branch는 `review/kevin9327-20260826`이다. PR 번호
오름차순으로 기능·CI 보정 commit만 적용했고 merge commit은 제외했다. 13건 모두 충돌 없이 적용됐다.

| 순서 | 원 PR | 이슈 | 원 SHA | 로컬 cherry-pick |
| --- | --- | --- | --- | --- |
| 1 | #6075 | #5886 | `795f71862` | `5eeea35f7` |
| 2 | #6075 | #5886 | `f6800c97d` | `ad0c183bf` |
| 3 | #6075 | #5886 | `10c24b9fa` | `a4f61d0a8` |
| 4 | #6075 | #5886 | `d4bead470` | `6fe56f5fb` |
| 5 | #6077 | #5929 | `ca1fb30d9` | `0c2f1cfe3` |
| 6 | #6079 | #5947 | `6110f56ea` | `75f034cb6` |
| 7 | #6079 | #5947 | `bc1ec23fe` | `0987ef170` |
| 8 | #6080 | #6060 | `a4a45d295` | `a6e713950` |
| 9 | #6080 | #6060 | `7f968da50` | `a4b53b977` |
| 10 | #6084 | #6035 | `76d0865bb` | `9fb18bf64` |
| 11 | #6084 | #6035 | `b2f1715cf` | `85fc74f79` |
| 12 | #6084 | #6035 | `8d4fee954` | `f8dcb9a0a` |
| 13 | #6084 | #6035 | `0146e8d66` | `a04592c16` |
| 14 | 메인터너 보정 | #6060 | — | `136a94677` |

## 제외한 PR

### #6073 (#6063 HWPX 문단 중간 vpos 되감김) — 재작업 요청

1. 원 PR head `c8e1d2b19`의 required check가 적색이다.
   `issue_1880_takeplace_host_before::oracle_3075729_heading_on_page13_both_paths`가
   `left: Some(13) / right: Some(12)`로 실패한다 — 되감김 승격이 한컴 오라클의 heading을 한 쪽
   뒤로 밀었다.
2. 같은 PR이 `tests/issue_1880.rs`의 한컴 오라클 핀을 `assert_eq!(13)` →
   `assert!((13..=14).contains(&n))`로 완화했다. 회귀를 덮는 방향이라 수용하지 않는다.
3. 승격 가드 `keep_hwpx_internal_page_break_on_body_overflow`가
   `break_line == 1 && overflow > 16.0 && remaining ∈ (0, 32)`로, 문서 한 건의 실측
   (issue1880 5쪽, 남은 칸 ≈24px)에 맞춘 상수 조합이다.

### #6083 (#5952 유의사항 상자 재래핑) — 재작업 요청

한컴 기준 PDF·`devel`·PR head를 같은 기계에서 렌더해 비교한 결과, 이 PR은 편람 61쪽에서
사이드바 겹침을 해소하는 대신 **상자 하단 넘침으로 본문 `4) 문서의 "끝" 표시` 줄과 글자 겹침을
만든다.** PR head 단독으로 재현되므로 상호작용이 아니다. 상세 근거와 증적 asset은
[pr_6083_review.md](pr_6083_review.md)에 있다.

## 메인터너 보정

### #6080 — 「」 반각 판정의 측정·페인트 발산 (확정 결함, `136a94677`)

원 PR은 폭 측정을 **메트릭** 기준(`is_monospace_metric`)으로 바꾸면서 페인트는 폰트 **이름 목록**
(`돋움체`/`DotumChe`/`굴림체`/`GulimChe`) 기준으로 나눴다. 두 기준이 달라 양방향으로 갈린다.

- **이름 목록 밖 고정폭**: 메트릭 DB 전수 조사 결과 monospace이면서 `U+300C` 폭이 `em_size`인
  글꼴은 11종인데 이름 목록이 덮는 것은 `DotumChe`·`GulimChe` 둘뿐이다. `BatangChe`(바탕체),
  `GungsuhChe`(궁서체), `D2Coding` 계열 5종은 advance만 반각으로 줄고 글리프는 전각으로 그려져
  다음 글자와 겹친다. 별칭 표에 `"바탕체" => "BatangChe"`, `"궁서체" => "GungsuhChe"`가 있어
  문서의 한글 폰트명에서 그대로 도달한다.
- **이름만 닮은 메트릭 DB 밖 글꼴**: `KoPub돋움체`는 전용 폭 표(`kopub_char_width`)를 쓰고 메트릭
  DB에 없다. `U+300C`에서 그 표는 어느 분기에도 걸리지 않아 `None`을 반환하므로 측정은 반각
  오버레이를 적용하지 않는데, 이름에 `돋움체`가 들어 있어 페인트만 0.5×로 눌렀다.

`devel`에서는 두 경로가 모두 반각이라 정합했으므로 이 발산은 #6080이 새로 만든다. SVG 경로는
`svg_text_length_attrs`가 `textLength`+`lengthAdjust="spacingAndGlyphs"`로 글리프를 advance에 맞춰
누르기 때문에 발산이 가려진다 — 원 PR 증적이 SVG라 여기서는 보이지 않고, skia(PDF/PNG)와 WASM
브라우저에서만 나타난다.

보정은 페인트가 **측정 결정을 그대로 되묻게** 바꾼다.

- `src/renderer/layout/text_measurement.rs`: 반각 오버레이의 `width_source`를 상수
  `HALFWIDTH_PUNCTUATION_WIDTH_SOURCE`로 고정하고, `forces_halfwidth_cjk_quote`를
  `measure_char_width_embedded_decision`의 결정 비교로 재작성했다. 시그니처에 `bold`·`italic`·
  `font_size`를 추가해 측정과 같은 메트릭 변형을 고른다.
- `src/renderer/layout.rs`: 통합 테스트에서 직접 검증하기 위해 `pub use`로 노출했다
  (`map_pua_bullet_char` 선례와 같은 방식).
- `src/renderer/skia/text_replay.rs`, `src/renderer/web_canvas.rs`: 호출부에서 스타일의
  `bold`·`italic`·`font_size`를 넘긴다.
- `tests/cases/issue_6060_cjk_quote_paint_measure_parity.rs`: #2020 돋움체 반각, #6060 휴먼명조·
  HY헤드라인M 전폭, 이름 목록 밖 고정폭 반각, 메트릭 DB 밖 `KoPub돋움체` 비강제, CSS 체인 첫 face
  판정, 낫표 외 문자 비대상을 고정한다.

기대값은 추측이 아니라 `font_metrics_generated.rs`의 실제 폭 데이터로 확인했다.
`DotumChe`/`GulimChe`/`BatangChe`/`GungsuhChe`는 em_size 1024·Latin 폭 전부 512·`U+300C` 1024,
`D2Coding`은 em_size 1000·`U+300C` 1000이다. `Dotum`/`Batang`/`HYHeadLine-Medium`/`휴먼명조`는
비례 글꼴이라 반각 오버레이 대상이 아니다.

보정 전 구현으로 되돌려 같은 시험을 돌리면 회귀 시험 2건
(`monospace_faces_outside_name_list_match_measurement`,
`name_lookalike_outside_metric_db_is_not_forced`)만 실패하고 #2020·#6060 앵커 2건은 통과한다.
시험이 실제 결함을 잡고 기존 계약은 바꾸지 않음을 이 대조로 확인했다.

## 검증

모두 통합 head `136a94677`에서 실행한 결과다. 같은 checkout·`target/pr-review`를 공유하므로 Cargo
계열 명령은 순차 실행했다.

| 게이트 | 명령 | 결과 |
| --- | --- | --- |
| 포맷 | `cargo fmt --all -- --check` | 통과 |
| 통합 test manifest | `node scripts/rust-test-suite-manifest.mjs --check` | 942 sources / 41-48 targets 통과 |
| unit tier | `node scripts/rust-unit-test-tiers.mjs --check` | 4,221 tests 통과 |
| 전체 회귀 | `cargo nextest run --locked --cargo-profile release-test --target-dir target/pr-review --tests --no-fail-fast` | **8,375 pass, 0 fail, 43 skip, 136.573s** |
| clippy | `cargo clippy --locked --all-targets --target-dir target/pr-review -- -D warnings` | 통과 |
| WASM lint | `cargo clippy -p rhwp --lib --target wasm32-unknown-unknown -- -D warnings` | 통과 |
| WASM check | `cargo check --target wasm32-unknown-unknown --lib` | 통과 |
| Native Skia lib | `cargo test --locked --profile release-test --features native-skia --lib` | 165 pass + 2 pass |
| Native Skia #2225 | `cargo nextest run … --features native-skia --test regression_suite_031 issue_2225_missing_picture_placeholder` | 2 pass |
| Native Skia p37 | `node scripts/run-rust-test.mjs render_p37_direct_pdf_export -- … --features native-skia` | 4 pass |
| IR field sweep 원장 | `RHWP_IR_SWEEP_DUMP=… --test regression_suite_028 ir_field_sweep_baseline` | 4 pass, 정렬 diff **차이 없음** |
| overflow-cell 원장 | `RHWP_OVERFLOW_CELL_DUMP=… --test overflow_cell_baseline` | 1 pass, 아래 별도 |
| 문서 링크 | `python3 scripts/check_markdown_links.py <추가 문서>` | 이상 없음 |

`node scripts/run-rust-test.mjs issue_2225_missing_picture_placeholder`는 manifest 재생성으로 suite가
바뀐 뒤 `0 tests run`을 돌려줬다. 0건 실행을 통과로 기록하지 않고, 실제 suite인
`regression_suite_031`을 지정해 다시 실행해 2건 통과를 확인했다.

### Docker 표준 WASM 경로 부재

`docker compose --env-file .env.docker run --rm wasm`은 이 호스트에서 실행할 수 없었다. Docker
Desktop 데몬이 꺼져 있어 `/var/run/docker.sock`과 `~/.docker/run/docker.sock` 모두 연결되지 않는다.
[local_validation](../../manual/pr_review/local_validation.md)이 정한 대로 대체 경로를 사용하고 그
사실을 여기 남긴다. `scripts/wasm-pack-locked.sh --no-opt`는 `wasm32-unknown-unknown` 컴파일까지
성공한 뒤 `wasm-bindgen` 설치 단계에서 로컬 도구 오류로 멈췄으므로, WASM 판정 근거는 위 표의
CI 동일 명령(`cargo clippy … --target wasm32-unknown-unknown`, `cargo check --target
wasm32-unknown-unknown --lib`)이다. 이 두 명령이 이번 보정이 건드린 wasm 전용 경로
(`src/renderer/web_canvas.rs`)를 실제로 컴파일한다.

### overflow-cell 원장의 실제 변화

원장 값은 CI(Linux) 기준이라 검토 환경(macOS)에서 그대로 재산출하면 여러 행이 다르다. 플랫폼
변수를 제거하기 위해 같은 기계에서 `upstream/devel@6b5c4f871`을 먼저 재산출해 비교했다.

| 비교 | 결과 |
| --- | --- |
| 커밋된 원장 vs `devel`(macOS) | 4개 행이 이미 다름 — 편람 `.hwp` 35→32, `issue1891_external_bindata_link.hwpx` 50→48, `table_giant_cell_overfill.hwpx` 94→76, `task1718/table_giant_cell_overfill.hwp` 행 소멸 |
| `devel`(macOS) vs 통합 head(macOS) | **편람 `.hwpx` 51 → 45 감소 한 줄뿐** |

즉 이번 통합의 실제 효과는 쪽 밖 소실 줄 6줄 감소다. 게이트는 증가만 실패시키므로 통과했고,
macOS 수치로 래칫을 조이면 Linux CI가 깨지므로 원장은 수정하지 않았다.

### 시각 검증

[visual_fixture_evidence](../../manual/pr_review/visual_fixture_evidence.md)의 직접 증적 필수 조합에
해당하는 PR별로 통합 head 산출물을 직접 열어 확인했다. 임시 output 경로는
`output/pr-review-20260826/`이고, 대표 asset은 `mydocs/pr/assets/` 아래에 PR 번호를 포함한 이름으로
보존했다. PR별 수치와 판정은 각 review 문서에 있다.

이 검토 환경에는 Windows HWP MCP 접근이 없다. `samples/issue5929`와 `samples/issue6035`는 한컴 기준
PDF가 없어 기준 PDF 대조 대신 통합 head 산출물의 기하와 쪽 구성을 직접 확인했고, 그 한계를 각 review
문서에 적었다.

## 보정하지 않고 기록만 남긴 관찰

- **#6077 표-그림 간격**: 표 상단 괘선이 그림 페인트 bbox 바닥과 정확히 `0.00px` 일치한다. 표의
  바깥 위 여백은 1.0mm(283 HU ≈ 3.8px)인데, 코드 주석이 주장한 "표가 그림 바닥에 붙지 않음"이 이
  fixture에서 관측되지 않는다. 이 fixture에는 한컴 기준 PDF가 없어 정답을 확정할 수 없으므로 원
  저자 확인 대상으로 남긴다.
- **#6084 CENTER 분기의 방향**: `direct_hwpx_cell_has_declared_stored_frame`의 CENTER 분기가 기존
  `sum_fits_declared`를 대체한다. `preceding = 0.9h`, `trailing = 0.9h`이면 합이 `1.8h`라 종전
  판정은 거짓인데 새 판정은 참이다. #6035 해소에 필요한 방향은 좁히기이므로 교집합
  (`sum_fits_declared && preceding …`) 형태가 더 안전하다.
- **#6075 시뮬 비용**: `page_offcanvas_sim`이 참이면 미주 문단마다 scratch `LayoutEngine`으로 단
  전체를 재렌더한다. `column_had_compact_endnote_rewind`는 단 전환까지 유지되므로 되감김 이후 그
  단의 문단 수에 대해 제곱으로 늘어난다. 종전에 이 경로는 `ssot_level >= A3`(비기본) 전용이었다.
- **#6075 잔여**: 통합 head에서 대상 쪽의 최대 text baseline은 `1141.56`, 용지 높이는 `1122.51`로
  `+19.05px`가 남는다. 원 PR이 보고한 수정 전 ~1290px보다 크게 줄었지만 `56.0px` 가드 임계 아래
  잔여는 설계상 남는다.
- **U+2018–U+2027 의 선행 발산**: 페인트는 이 범위를 글꼴·글리프 폭과 무관하게 0.5×로 누르는데
  측정은 `glyph_w >= em_size`일 때만 반각으로 잡는다. `devel`에도 있는 선행 상태라 이번 보정
  범위에 넣지 않았다.
- **overflow-cell 원장의 플랫폼 의존**: 검토 환경(macOS)에서 `devel`을 재산출하면 커밋된 원장과
  4개 행이 이미 다르다(편람 `.hwp` 35→32, `issue1891_external_bindata_link.hwpx` 50→48,
  `table_giant_cell_overfill.hwpx` 94→76, `task1718/table_giant_cell_overfill.hwp` 행 소멸).
  원장에 기준 플랫폼 표기가 없어 로컬 재산출이 회귀처럼 보인다. 별도 이슈 후보다.
