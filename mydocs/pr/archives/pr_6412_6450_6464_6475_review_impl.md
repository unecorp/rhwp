---
kind: pr-review-implementation
status: accepted-with-ci-condition
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-31
---

# 비 지정 기여자 PR #6412, #6450, #6464, #6475 체리픽 통합 검토 기록

## 범위와 기준

- review branch: `review/non-kevin-planet-postmelee-20260831`
- base: `upstream/devel@19b89d967b1505cd4bdcdbba7d1f1413f32a1505`
- 포함 원 PR: #6412 (`jeong-sik`), #6450 (`t2c-lab`), #6464 (`t2c-lab`), #6475 (`jeong-sik`).
- 제외 기준: 현재 Open PR 중 `kevin9327`, `planet6897`, `postmelee` 작성자는 사용자 지시에 따라 제외했다.
  draft는 포함하지 않았다.
- 통합 PR은 [#6486](https://github.com/edwardkim/rhwp/pull/6486)로 생성하고 원본 저장소 head branch에
  push했다. reviewer 지정, source PR comment/close는 merge 뒤 `post_merge.md` 조건을 충족할 때만 한다.

## 적용 순서와 메인터너 보정

| 원 PR | 원 head | 통합 적용 commit |
| --- | --- | --- |
| #6412 | `ba045cdc0a47da643188752782172502e8641412` | `810db7289` |
| #6450 | `7caca9defd7a453cea84e8357e1646b3916fa760` | `262773c29`, `70c0bc3fd`, `e369c53f5` |
| #6464 | `024cf9dea0bd161eb8e78a904e2d5fba916709cd` | `aa9a2386a`, `6184ef986` |
| #6475 | `8ee739a03a285a586e2d762660e60c04e17c58aa` | `c0aedcd32` |

- 모든 cherry-pick은 최신 base 위에서 충돌 없이 적용했다.
- #6450/#6464에는 source behavior를 고정하는 Rust contract가 없어서 test-only 메인터너 보정을 추가했다.
  첫 보정 commit은 `d467175a1` (`test: HTML 붙여넣기 회귀 계약을 보완`)이다. #6486 초기 CI는 이
  commit이 `src/document_core/commands/html_import.rs`의 `#[cfg(test)]` 수를 2개에서 6개로 늘려
  `RustUnitTier` base guard를 위반하면서 실패했다. trailing 보정은 같은 네 계약을 public
  `paste_html_native` 경로의 `tests/cases/html_import_paste_contract.rs`로 옮겼다. production logic은
  변경하지 않는다.

## 완료한 검증

- `git diff --check upstream/devel...HEAD`는 contributor 적용 직후 통과했다. 문서/asset과 test-only
  보정 반영 뒤 최종 commit 전에 다시 실행한다.
- `node --test scripts/tests/cell-lineseg-agreement.test.mjs`: 21 passed.
- #6412 targeted test: 1 passed; 변경 baseline partition: 16/16 passed.
- HTML import source module test: 2 passed; public HTML paste contract: 4 passed.
- `node scripts/rust-test-suite-manifest.mjs --check`: 48/48 integration targets, minimum 6,559 cases 확인.
- `node scripts/rust-unit-test-tiers.mjs --check`: PR base와 같은 4,221 source-side tests 확인.
- `cargo fmt --all -- --check`, 기본/workspace all-target/WASM lib clippy, workspace build,
  `node scripts/rust-unit-test-tiers.mjs --check`는 통과했다.
- 전체 nextest를 두 번 실행했다. 두 실행 모두 `schema_registry::tests::policy_path_points_to_existing_document`
  한 건만 실패했다(각각 8,775/8,776 passed). `mydocs/tech/agent_runtime/version_policy.md`는 존재하며,
  `cargo nextest run ... -p rhwp-contracts` 단독은 15/15 passed, focused retry 30/30 passed였다.
  즉 현 시점 증거는 외부 PR diff가 아닌 전체 병렬 실행 환경의 경로 검사 불안정이다. 이 실패를 통합 code
  candidate의 PASS로 기록하지 않으며, 통합 PR CI 결과와 별도 재현/수정 여부를 merge 전 재확인한다.

## 개별 판단과 증적

| PR | 판단 | 핵심 근거 |
| --- | --- | --- |
| #6412 | 수용 권고 | focused/baseline 통과, Hancom 2024 p68 visual sweep 후보 0 및 직접 PNG 검토 |
| #6450 | 수용 권고 | 장문/과대 markup fallback public contract 보정 포함, source 2 + public 4 passed |
| #6464 | 수용 권고 | raw tag, bold/underline, list public contract 보정 포함, source 2 + public 4 passed |
| #6475 | 수용 권고 | measurement Node test 21 passed |

- #6412 시각 증적의 원본/PDF SHA, rsvg 한계, representative asset과 merge 후 comment 계획은
  `pr_6412_review.md`에 기록했다. source PDF 또는 rsvg proxy만으로 전체 fidelity 통과를 주장하지 않는다.

## 다음 단계

1. trailing 보정 commit을 push한 뒤 #6486 최신 head의 Full CI, mergeability와 네 원 PR source head를
   다시 확인한다.
2. 통합 PR code head의 Full CI가 녹색이면, #6412은 asset이 merge commit에 존재한 뒤에만 visual sweep
   comment 계획을 `--body-file`로 실행하고 API 재조회한다. 원 PR close와 branch/worktree 정리는
   `post_merge.md`를 따른다.
