---
kind: pr-review-integration
status: accepted-with-maintainer-corrections
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-31
---

# kevin9327 non-postmelee 통합 검토 구현 기록

## 범위와 선정

- 검토 branch: `review/kevin9327-non-postmelee-20260831`.
- 검증 base: `upstream/devel@3afbb066fe93724ab44309163a2e04efb954bf18`.
- PR 직전 rebase base: `upstream/devel@cfa4ccacab63b470771720ebed33503cdd62adb6`.
- source PR/head: #6420 `404cfb41`, #6423 `3e426e0d`, #6427 `77d59943`, #6428 `80f5ef26`,
  #6435 `f7706305`, #6441 `1612102b`.
- 2026-08-31 시작과 문서 작성 직전에 모두 Open/non-draft이며 requested reviewer에 `postmelee`가 없음을
  GitHub API로 재확인했다. `postmelee`가 이미 요청된 #6444, #6440, #6439, #6438, #6432는 포함하지 않았다.
- #6423과 #6427은 source base 변경으로 dirty이고 최신 head에 전체 CI가 붙지 않아, 수용 근거를
  통합 branch의 로컬 전체 검증으로 보강했다. 나머지 네 PR은 source head의 required CI가 성공했다.

## 충돌과 메인터너 보정

- #6423은 저장 사다리 suppression과 cell auto-shrink convergence를 현재 devel 규칙과 합성했다. 이 합성의
  새 bool 인자가 과거 unit test 세 곳에서 빠진 것을 all-target clippy가 발견했고, `d8f031c0`으로
  세 테스트의 기존 의미를 유지하도록 `false`를 명시했다.
- #6427은 저장 줄 경계 보존을 실제 TAC inline object 조건으로만 적용해 빈 줄/일반 hard-break를 건드리지
  않도록 합성했다.
- #6441은 실제 paint 높이 전진을 쪽 분할 셀 TAC 그림으로 좁혔다. 이 보정이 없는 넓은 일반화는 수용하지
  않는다.
- source의 빈 CI restart commit은 파일 변경이 없어 적용하지 않았다.

## 실행 완료 검증

- `cargo fmt --all -- --check`, native clippy, WASM library clippy, workspace build, all-target clippy,
  `node scripts/rust-test-suite-manifest.mjs --check`,
  `node scripts/rust-unit-test-tiers.mjs --check`를 모두 통과했다.
- release-test 전체 실행 명령:

  ```bash
  cargo nextest run \
    --cargo-profile release-test \
    --target-dir target/pr-review \
    --tests --test-threads 12 --no-fail-fast
  ```

  결과는 `8870 passed, 46 skipped` (450.949초, exit 0)다.
- 위 로컬 검증은 rebase 전 통합 후보에서 완료했다. PR 직전 rebase는 충돌 없이 적용됐으므로 추가 로컬
  회귀는 수행하지 않았고, 최신 PR head의 CI 통과를 merge 조건으로 둔다.
- code head `dd00ad020b25940245a644581bf820682e409e93`의 GitHub Actions는 Build & Test, Lint,
  Native Skia, CodeQL, Canvas visual diff, adapter inter-diff와 prop roundtrip을 모두 통과했다.
  이후 `upstream/devel@e718f40bbd335b2c8db6e89345f91a5e88c70e16`는 검증 base보다 #6522의 merge 후
  기록 두 commit만 앞섰다. 이미 검증한 코드 head를 다시 rebase하지 않고, 이 기록과 오늘할일만 담은
  document-only trailing commit의 trusted CI를 최종 merge 조건으로 둔다.

## HWP 시각 증적과 판정

| PR | 입력/쪽 | 결과 | 대표 asset |
| --- | --- | --- | --- |
| #6423 | 2020 HWP p1 | flagged=0, 우측 괘선 안의 긴 행 확인 | `pr_6423_issue6303_p1_review.png` |
| #6428 | 2022 저장 HWP, 2020 bucket p6 | flagged=0, 머리말 "홀수형" TAC 박스 위치 확인 | `pr_6428_issue6353_p6_review.png` |
| #6435 | 2024 HWP p17 | base tail 14px/1건 -> 통합 0px/0건 | `pr_6435_issue4318_p17_before_review.png`, `pr_6435_issue4318_p17_review.png` |

모든 sweep은 통합 head의 `target/pr-review/debug/rhwp`와 `rsvg` rasterizer로 실행했다. Chrome webfont
명령이 이 host에서 `google-chrome` 이름을 찾지 못했으므로, `rsvg` 수치는 font raster 차이에 민감한
보조값으로만 사용했다. 각 review 문서에 사람의 대상 경계 판정과 한계를 분리해 기록했다.

## 종합 결론

**여섯 PR 모두 통합 branch 상태로 수용 권고.** 코드+test 적용만으로 source PR을 merge하지 않으며,
후속 통합 PR의 최신 head CI와 작업지시자 승인을 다시 확인한 뒤에만 merge한다. merge 뒤에는 개별
review 문서의 comment 계획에 따라 asset이 devel에 존재하는지 API 재조회하고, visual asset이 있는
PR만 merge SHA 고정 image를 포함한 comment를 게시한다.
