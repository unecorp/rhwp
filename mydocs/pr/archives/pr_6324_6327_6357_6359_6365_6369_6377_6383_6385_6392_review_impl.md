---
kind: integration-review
status: awaiting-approval
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-30
---

# Non-draft PR 통합 검토 - 2026-08-30

## 대상과 기준

- 검토 branch: `review/open-nondraft-20260830`
- 시작 기준: `upstream/devel@2deb3dd6163d83d2932ab58ac5a0bf61bfce6d31`
- 포함 후보: #6324, #6327, #6357, #6359, #6365, #6369, #6377, #6383, #6385, #6392.
- draft #6391/#5953은 제외했다. 검토 시작 뒤 새 PR은 사용자의 별도 승인이 없어 포함하지 않는다.
- 각 source PR에 `jangster77` reviewer request를 등록했고, #6392는 새 source head
  `80e2bc972094169dfffaca554795d1039d1fd1cf`까지 반영했다.
- #6383도 검토 중 갱신된 `db9e60a957f974bc5921f8d8d7af01a5f313be7e`까지 반영했다.
  이 추가분은 중복 모듈 테스트 제거이며 `tests/cases` 계약 7건을 다시 통과했다.

## 메인터너 보정

- `9e84ab0a4`: #6324 `table_layout.rs`의 source rustfmt 실패를 포맷만으로 보정했다.
- `6a2e2ff7d`: #6357의 text-overlap baseline을 최신 integration head 실측값으로 갱신했다.
  #6284의 `54 -> 56`은 #6357 자체 증가임을 pre/post worktree에서 확인했다.
- `605bfaa21`: Studio 첫 실행 skin onboarding이 textarea 포커스를 가려 undo E2E가 실패하던 test
  harness만 해제했다. 제품 동작은 변경하지 않았고 실제 headless browser undo 계약은 통과했다.

## 공통 검증

- suite manifest prepare/check, unit-tier check, fmt, workspace/native/WASM clippy, workspace build 통과.
- release-test 전체: **8,712/8,712 passed**, 43 skipped.
- Native Skia lib 및 지정 renderer regressions, Rust doc tests, locked WASM library check와
  `scripts/wasm-pack-locked.sh --target web --out-dir pkg` 통과.
- Studio: TypeScript, `npm test` **1271 pass / 1 skip**, production build, 실제 Chromium undo E2E 통과.
- full Rust raw WASM CLI build는 current `upstream/devel`에도 존재하는 native-only CLI bin 제약으로
  실패하므로 CI와 동일한 `--lib` check를 사용했다.

## 시각 증적

- #6324: 2020 bucket 5쪽 all-page flag 0.
- #6327: 2024 bucket p1 flag 0.
- #6357: 2020 bucket #6284 p10-31 22쪽 flag 0.
- #6369: 저장 버전에 따라 hwpctl p12-13은 2020, regulatory p121-123은 2024 PDF로 재산출해 flag 0.
- PDF, `info --json`, visual summary, 대표 review PNG는 `pdf/` 및 `mydocs/pr/assets/`에 보관했다.
  Chrome webfont raster의 즉시 종료 race가 난 #6369 hwpctl은 visual_sweep `rsvg` fallback과 사람 검토를
  사용했으며, 이 도구 문제는 제품 PR 범위와 분리했다.

## 결론과 후속

개별 review 문서의 판정은 모두 **수용 권고**다. 이 기록은 통합 PR 생성 전 단계이며, PR 생성은
사용자 승인을 받은 뒤 최신 `upstream/devel`, source heads, mergeability 및 CI를 다시 확인하고 수행한다.
merge 뒤에는 각 원 PR에 개인별 수용 근거와 representative asset raw URL을 남긴다.
