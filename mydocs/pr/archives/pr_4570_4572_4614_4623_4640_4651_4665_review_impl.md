---
kind: pr-review-implementation
status: local-validation-complete
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-12
---

# planet6897 후속 renderer 통합 이행 기록

## 범위와 기준선

| 항목 | 값 |
| --- | --- |
| 통합 branch | `review/planet6897-20260812-r2` |
| 기준 | `upstream/devel@c6b43fbc69e2ec84bfc165f5a0eb2d192186b65d` |
| 원 PR | [#4570](https://github.com/edwardkim/rhwp/pull/4570), [#4572](https://github.com/edwardkim/rhwp/pull/4572), [#4614](https://github.com/edwardkim/rhwp/pull/4614), [#4623](https://github.com/edwardkim/rhwp/pull/4623), [#4640](https://github.com/edwardkim/rhwp/pull/4640), [#4651](https://github.com/edwardkim/rhwp/pull/4651), [#4665](https://github.com/edwardkim/rhwp/pull/4665) |
| 목적 | #4667 이후 contributor head에 추가된 float·TAC·사다리·배분 정렬 변경을 최신 devel 위에 누적 검토 |

```text
base route: collaborator 매개 외부 PR
modifiers: 접수·리뷰 기록, 로컬 검증, 시각·fixture 증적, 다수 PR·update branch
loaded documents: pr_review_workflow.md, pr_review/README.md,
  collaborator_external_pr.md, intake_and_review.md, local_validation.md,
  visual_fixture_evidence.md, multi_pr_update_branch.md
```

## 적용 순서

1. #4570의 Square 예약 공간 `47b173cfb`와 전면 이미지 낱장화 `796179850`을 적용했다.
   중간 `d65e0fc`는 최신 devel과 동등해 빈 체리픽으로 종료되어 건너뛰었다.
2. #4572의 신규 위양성 가드 `b7c2c3aaf`를 적용했다. 앞선 두 commit은 patch-equivalent였다.
3. #4614 `7a6c37b3c`; #4623 `a6057472f`, `7280645f3`; #4640 `3063265ac`;
   #4651 `6b8feffb4`; #4665 `8d25b718f`를 오래된 순서로 적용했다.
4. #4640이 `layout.rs`에서 #4614/#4623과 충돌했다. 사다리 역보정의
   `y_before_vpos_adjust`와 동거 float 판별 `prev_tac_host_sibling_float`를 함께 보존했다.
5. #4570의 전면 이미지 조건이 정확히 절반을 과반으로 포함하던 경계를 발견해, 메인터너 보정
   `68ea700c6`에서 엄격한 과반 helper와 2/4 회귀를 추가했다.

최종 `git merge-tree --write-tree upstream/devel HEAD`와 `git diff --check upstream/devel...HEAD`는
모두 통과했다.

## 완료 검증

| 검증 | 결과 |
| --- | --- |
| `fullpage_image_single_page_policy_requires_strict_majority` | 1 passed |
| `issue_4657_distribute_alignment` | 1 passed |
| `python3 -m py_compile tools/verify_ladder_drift.py` | passed |
| release-test 전체 | `5,782 passed / 36 skipped / 7 slow` |
| `cargo clippy --target-dir target/pr-review --all-targets -- -D warnings` | passed |
| Native Skia | lib 58, issue_2225 2, render_p37 4 passed |
| `wasm-pack build --target web --out-dir pkg` | passed |
| #4665 공개 fixture SVG | 1 page, 14,519 bytes, overflow-cell 0 |

기여자 stage의 PDF 좌표와 대규모 sweep은 비공개 코퍼스이므로 일반 PDF fidelity 수치로 재인용하지
않았다. 공개 fixture가 있는 #4665만 SVG 산출을 현재 head에서 재생성했다.

## 후속 조건

- 통합 PR의 최신 code head에서 Full CI, CodeQL 및 renderer에 필요한 GitHub 검증이 성공한 뒤에만 merge한다.
- merge 뒤 원 PR은 통합 PR 링크와 함께 close하고, #4613/#4622/#4639/#4650/#4654/#4657은 실제 해결 범위를
  다시 확인한 뒤 comment와 함께 close한다. #4533과 #4599는 캠페인 이슈로 계속 연다.
