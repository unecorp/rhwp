---
kind: pr-review-integration
status: accepted
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-30
---

# kevin9327 CI-green cohort 통합 검토와 메인터너 보정

## 범위

- 검토 branch: `review/kevin9327-ci-green-no-postmelee-20260830`.
- base: `upstream/devel` `8a150f9a8bb19a9918e195da3a646690f68f4328`.
- 포함 source와 고정 head: #6403 `31717ee`, #6417 `7db64aa`, #6424 `43d1a8d`, #6430
  `6c9d0e7`, #6431 `8f231d4`, #6434 `6fa7e48`, #6468 `b5302a3`, #6472 `68a5ad9`.
- 모두 고정 시점에 non-draft, CI green이면서 reviewer가 `postmelee`가 아닌 Open PR이었다.
  #6415는 작업 지시로 제외했고, 이후 새 PR은 cohort에 추가하지 않는다.
- 외부 collaborator workflow에 따라 source reviewer `jangster77` 요청을 먼저 완료했다.
- 2026-08-30 최신 `upstream/devel`로 21개 커밋을 리베이스했고 충돌은 없었다. 이 rebase는
  코드 변경을 추가하지 않았으므로 앞서 완료한 검증을 중복 실행하지 않았다.

## 적용과 메인터너 보정

- 20개 source commit은 latest `upstream/devel` 위에 conflict 없이 순서대로 cherry-pick했다.
- #6472의 넓은 HWPX `CELL`/TAC declared-fit은 Hancom 2020 기준 PDF의 실제 3쪽 흐름과 맞지
  않았다. 일반 declared-fit 경로를 복원하고, `3행 x 1열`·3셀·`repeatHeader=0`인 빈 host의
  단일 비합성 `LINE_SEG`가 measured table보다 짧은 형상에만 physical band와 trailing spacing을
  적용하도록 보정했다.
- 수정 regression `issue_6448_tac_cell_uses_measured_band_and_moves_tail_to_page3`는 1쪽 HEAD,
  2쪽 complete table, 3쪽 TAIL을 고정한다.

## 검증 현황

- 완료: `cargo fmt --all -- --check`, native clippy, WASM library clippy, workspace build,
  manifest prepare/check, #6472 focused nextest, #6434 Studio 21/21 node test와 `tsc --noEmit`.
- full nextest는 fixed `target/pr-review`, `--tests --test-threads 12 --no-fail-fast`로 단일
  실행했고 `8772 passed, 43 skipped` (430.908초, exit 0)를 확인했다.
- 보정 후 `cargo fmt --all -- --check`, native/WASM/workspace all-target clippy,
  workspace build, `node scripts/rust-test-suite-manifest.mjs --check`, `git diff --check`를
  다시 실행해 모두 통과했다.
- renderer/layout fixture가 있는 #6424/#6430/#6431/#6468/#6472는 Visual Sweep 결과와 stable
  image/summary asset을 각 개별 review에 연결했다. merge 후 source PR comment는 asset devel
  존재를 재확인하고 `--body-file`로 raw image를 포함해 게시한다.

## 완료 상태

- 각 source review는 `accepted`로 확정했고, #6472는 `accepted-with-maintainer-correction`으로
  원 source 단독 수용이 아님을 명시했다.
- 명시적 지시가 있을 때만 integration PR을 만든다.
