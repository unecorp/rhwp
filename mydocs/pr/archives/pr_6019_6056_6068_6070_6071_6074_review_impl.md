---
kind: pr-review-impl
status: pending-pr
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-26
---

# PR #6019, #6056, #6068, #6070, #6071, #6074 통합 검토 구현 기록

## 범위와 반영 순서

최신 `upstream/devel` `ee7e8a6ed0c4784bea882a30dc94de49c42f6bad`에서
`review/open-ci-complete-20260826` 브랜치를 만들고 다음 순서로 누적 적용했다.

1. #6019 `21e8d1d5febae6ba10ea659c7f82da5233e15f61`
2. #6056 `5e5ede9a94602514cf3b485938e598dd52fa99cb`
3. #6068 `1564337e`, `6cfc4514`, `07b86e70e1b33c362c73bc64cb0199fc17fddec1`
4. #6070 `a82e665f5f79c24b8177b2a37afa48e6f6400dd0`
5. #6071 `9d4134e`부터 `a57ce0457b384eba5afd2bea5b90e64443f26a7e`까지
6. #6074 `c7d3c66398acdb3c093fdea6fef573e14f771a6c`

#6071의 중간 `Merge remote-tracking branch 'upstream/devel' ...` commit `09c3e655`은
통합 절차상 제외했고, 그 밖의 source commits는 누락 없이 적용했다.

## 메인터너 보정

| 원 PR | 보정 여부 | 내용 |
| --- | --- | --- |
| #6019 | 필요 | 보호 문서 autosave 차단 경로가 debounce를 우회하던 문제, 자동저장 비활성화 알림 누수, 상태바 복원 누락을 보정 |
| #6056 | 필요 | 깨진 기존 계약 테스트 갱신, `edit replace-text --dry-run`과 실제 치환 순회 정합, 글상자 안 표 direct search 보정, Studio 타입 보강 |
| #6068 | 불필요 | 원 PR 최신 head에서 CI Lint/Archive B 실패가 후속 commit으로 해소됨 |
| #6070 | 불필요 | 원 PR CI 녹색, 별도 수정 요청 없음 |
| #6071 | 불필요 | self-review와 GitHub Actions 녹색. merge commit은 통합에서 제외 |
| #6074 | 불필요 | 원 PR CI 녹색, #6030 신규 회귀와 IR sweep baseline 통과 |

추가로 전체 nextest 중 `issue_4138_split_cell_stale_linesegs` 2개 계약이 196쪽 기대값으로 실패했다.
통합 후 저장 뒤 재파싱 쪽수는 한글 2024 COM 재실측값인 197쪽으로 돌아왔고, 기존 테스트 주석도
197쪽이면 #6046 해소로 본다고 설명하고 있었다. 이에 따라 `tests/issue_4138_split_cell_stale_linesegs.rs`
기대값과 메시지를 한글 2024 오라클 197쪽으로 갱신했다.

## 개별 판정

| 원 PR | 판정 | 근거 |
| --- | --- | --- |
| #6019 | 수용 | 보안 방향 타당, 보정 후 autosave 14개 focused test와 Studio build 통과 |
| #6056 | 수용 | 검색·치환 핵심 방향 타당, 보정 후 #2792 focused suite와 occurrence 계약 통과 |
| #6068 | 수용 | 최신 원 PR CI 녹색, 과거 실패는 후속 commit으로 해소 |
| #6070 | 수용 | 최신 원 PR CI 녹색, focused 회귀와 증적 포함 |
| #6071 | 수용 | self-review와 frontend-only CI 녹색, 큰 UI 기능 단위로 별도 문서 존재 |
| #6074 | 수용 | 최신 원 PR CI 녹색, #6030 focused 회귀와 IR sweep baseline 재검증 통과 |

## 로컬 검증

- `cargo fmt --check`: 통과
- `cargo fmt --all -- --check`: 통과
- `cargo test --profile release-test --target-dir target/pr-review --test regression_suite_005 issue_2792_search_nested_tables -- --nocapture`: 10 pass
- `cargo test --profile release-test --target-dir target/pr-review --test regression_suite_011 replace_occurrence_contract -- --nocapture`: 4 pass
- `cargo check --profile release-test --target-dir target/pr-review --tests`: 통과
- `node --test rhwp-studio/tests/autosave-manager.test.ts`: 14 pass
- `npm --prefix rhwp-studio test -- autosave-manager.test.ts`: 1,135 pass, 1 skip, 실패 0
- `npm --prefix rhwp-studio run build`: `tsc && vite build` 통과
- 1차 `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast`:
  8,354 pass, 2 fail, 43 skip. 실패는 #4138 쪽수 핀 2개로 한글 2024 오라클 197쪽과 기존 기대값
  196쪽의 불일치였다.
- `cargo test --profile release-test --target-dir target/pr-review --test regression_suite_029 issue_4138_split_cell_stale_linesegs -- --nocapture`:
  보정 후 2 pass
- 2차 `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast`:
  8,356 pass, 43 skip, 208.954s
- #6074 추가 후 `node scripts/run-rust-test.mjs issue_6030_tac_last_line_negative_trailing -- --cargo-profile release-test --target-dir target/pr-review`:
  1 pass, 115 skip
- #6074 추가 후 `RHWP_IR_SWEEP_DUMP=/tmp/ir_field_sweep_current_6074.tsv cargo test --locked --profile release-test --target-dir target/pr-review --test regression_suite_028 ir_field_sweep_baseline::ir_field_sweep_does_not_regress -- --nocapture`:
  1 pass, 130 filtered out, 82.89s
- `diff -u <(LC_ALL=C sort tests/fixtures/ir_field_sweep_baseline.tsv) <(LC_ALL=C sort /tmp/ir_field_sweep_current_6074.tsv)`:
  차이 없음. raw diff는 3개 행의 순서 차이뿐이었다.
- 최종 `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast`:
  run ID `4a3bdd9c-04c9-47f7-ab52-408c6022e116`, 8,364 pass, 43 skip, 208.190s

## PR 전 남은 확인

- 최신 `upstream/devel` 동기화 후 conflict가 없으면 rebase하고, 추가 중복 테스트 없이 PR을 생성한다.
- 통합 PR CI 완료 후 `post_merge.md`에 따라 merge SHA 확인, 원 PR comment/close, 관련 issue comment,
  branch/worktree 정리까지 수행한다.
