---
kind: pr-review-impl
status: local-validated
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-29 19:15 KST
author: lpaiu-cs
branch: review/lpaiu-cs-20260829
prs: [6331, 6333, 6336, 6351, 6356]
---

# lpaiu-cs PR 통합 검토 - 2026-08-29

## 범위

포함:

- #6331 `069cbfcff92247762cb499ae59fc28b24e748f27`
- #6333 `fa3220f6a7b351600c1e629f621a2fd6fc438d2d`
- #6336 `654ce91a7779522b91272ef6c8dc4704b529b50d`
- #6351 `77df8746435467d9eab0a41c3bd822e200783a99`
- #6356 `f5010c385e5b2e6e0aa1d4f98af0743522c0b0dc`

제외:

- #5953: draft 상태라 제외했다.

포함 PR은 모두 로컬 fetch/review 전에 `jangster77` reviewer request를 등록했다.

## 적용

- review branch: `review/lpaiu-cs-20260829`
- initial base: `upstream/devel@86aa6128b8cc928c0cf79ed9bb8f27d0f74b7924`
- final base after rebase: `upstream/devel@cf366d2faad63a57fb663ce38b2e02d99b873e22`
- code candidate head before review-document commit:
  `33c6dc4cf076d03a9bcc6a5b4e797285ed86f97a`

적용 commit:

- #6331: `9e534727b`, `cfb638be0`
- #6333: `66081c530`, `834fd7d80`
- #6336: `a71ceebc5`
- #6351: `2847442ec`
- #6356: `c098dd1ad`
- 메인터너 보정: `33c6dc4cf`
- 제외한 중복 보정: `7d6dc7018` (#6351 추가 push `77df87464354`의 cherry-pick. 이미
  `33c6dc4cf`에서 source-side test 이동을 처리했으므로 통합 PR에는 포함하지 않는다.)

## 충돌과 보정

- #6331: `scripts/tests/fixtures/ci-impact-classifier-prs.json` 충돌. 최신 `devel`의
  `classifier_version: 5`를 유지하고, 원 PR의 command-package reason 및 #5953 fixture를 반영했다.
- #6351/#6356: source-side test 총량 증가 정책을 확인했고, 해당 white-box test를 `tests/cases`
  integration regression으로 이동했다. #6351은 이후 기여자 head에서도 같은 방향의 보정 commit이
  추가됐지만, 통합 브랜치에는 선행 메인터너 보정 `33c6dc4cf`만 유지했다. 보정 뒤 unit-tier
  check, clippy, wasm check가 통과했다.

## 검증

상세 증적: `mydocs/pr/assets/pr_6331_6333_6336_6351_6356_validation_20260829.md`

- CI classifier and policy: 71 pass
- CI impact workflow unittest: 31 OK
- Rust unit-tier tests: 12 pass
- Rust unit-tier check: pass, 4221 tests / 299 modules
- Rust suite manifest prepare/check: pass
- Focused Rust:
  - #6333 `issue_6332_snapshot_budget_coupling`: 1 pass
  - #6351 `issue_6350_equation_setter_raw_passthrough`: 4 pass
  - #6356 `issue_6355_picture_transform_raw_rendering`: 4 pass
- Studio focused node tests: 13 pass
- Studio broader test run: 1242 pass, 1 skipped
- `cargo fmt --all -- --check`: pass
- `git diff --check upstream/devel...HEAD`: pass
- `cargo clippy --workspace --all-targets --target-dir target/pr-review -- -D warnings`: pass
- `cargo clippy -p rhwp --lib --target wasm32-unknown-unknown --target-dir target/pr-review -- -D warnings`: pass
- `cargo check --target wasm32-unknown-unknown --lib --target-dir target/pr-review`: pass

## 판단

- #6331: 충돌 해소 포함 수용 권고.
- #6333: 수용 권고.
- #6336: 수용 권고.
- #6351: 메인터너 보정 포함 수용 후보. 최신 원 PR의 추가 integration-test 이동 commit은
  중복 보정이라 통합 PR에서는 제외한다.
- #6356: 메인터너 보정 포함 수용 후보. 원 PR head 그대로는 lint 실패 상태다.

통합 PR은 사용자의 별도 `pr` 지시 후 생성한다. PR 생성 전에는 원격 PR head와 최신
`upstream/devel`을 다시 확인한다.

## merge 후 코멘트 메모

- #6331: classifier fixture 충돌을 최신 `devel` 기준으로 해소하고 command 계층 package 레인 승격을 수용.
- #6333: snapshot budget 결합을 Rust/Studio 양쪽에서 검증하는 계약으로 수용.
- #6336: `codeOnly()` helper와 고위험 pin 전환을 source-guard tests로 확인하고 수용.
- #6351: 기능 변경은 수용하되, source-side test 증가를 `tests/cases`로 이동한 메인터너 보정과
  최신 추가 commit의 중복 제외를 함께 설명.
- #6356: 값 변화 기반 invalidation은 수용하되, source-side test 증가로 CI lint가 깨져
  `tests/cases`로 이동한 메인터너 보정을 함께 설명.
