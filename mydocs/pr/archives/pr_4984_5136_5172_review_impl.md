---
kind: pr-review-implementation
status: local-review-complete
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-17
---

# #4984·#5136·#5172 통합 검토 이행 기록

## 검토 경로

```text
base route: maintainer_general.md
modifiers: intake_and_review.md, local_validation.md, multi_pr_update_branch.md,
           visual_fixture_evidence.md, post_merge.md
loaded documents: pr_review_workflow.md, pr_review/README.md,
                  maintainer_general.md, intake_and_review.md, local_validation.md,
                  multi_pr_update_branch.md, visual_fixture_evidence.md, post_merge.md
review branch: review/planet6897-20260817
review base: upstream/devel@d4cf27eeb
```

## 원격 source head

| PR | 원격 head | source 상태(확인 시점) |
| --- | --- | --- |
| #4984 | `9281e6f7abe4b340c2b75535ce16adda4f4ad010` | OPEN / DIRTY; checks 성공 또는 해당 없음 |
| #5136 | `e2df9f9bb20e466300300a86b2ec380a447ce8fe` | OPEN / DIRTY; checks 성공 또는 해당 없음 |
| #5172 | `301c3b03831d6c6e3b1d05b7018120a4f72de9e1` | OPEN / DIRTY; checks 성공 또는 해당 없음 |

원격 source branch에는 push하지 않았다. 세 PR은 모두 현재 head 기준으로 다시 확인한 뒤 최종 통합
PR의 근거로 사용해야 한다.

## 적용 순서와 충돌 처리

1. #4984의 고유 commit을 오래된 순서대로 적용했다.
2. #5136의 세 고유 commit을 적용했다.
3. #5172에서 #4984와 중복되는 공통 history를 건너뛰고 고유 commit만 적용했다.

PR 내부의 `6c23b8107` (`Merge remote-tracking branch 'stream/devel'`)은 제외했다. #5172에 포함된
종료 PR #5159의 변경은 #4984와의 공통 history가 아니라 현재 stack에서 필요한 고유 변경만 이어서 적용했다.
생성 manifest 충돌은 세 fixture와 각 integration target을 모두 보존한 뒤 generator를 실행해 정합성을
확정했다. `unit-test-tiers.json` 충돌은 최종 소스 줄번호를 반영하고 검사를 통과시켰다.

통합 branch의 변경은 다음 원 PR 범위를 벗어나지 않는다.

- HWP3 parser/converter 저장 계약 보정
- HWP5 caption attr bit29 serializer 보정
- 관련 HWP3 fixture와 계약·baseline·generated manifest

## 검증 결과

| 검증 | 결과 |
| --- | --- |
| `cargo fmt --all -- --check` | 통과 |
| `node scripts/rust-test-suite-manifest.mjs --check` | 통과 |
| `node scripts/rust-unit-test-tiers.mjs --check` | 통과 |
| `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast` | 6538 passed, 38 skipped, 8 slow |
| `cargo clippy --all-targets --target-dir target/pr-review -- -D warnings` | 통과 |
| `git diff --check` | 통과 |
| #4367 focused contract | 7 passed |
| #5136 focused contract | 2 passed |

renderer·Studio UI 변경은 없어 WASM·브라우저 시각 검증은 적용하지 않았다. 한컴 COM 실측은 source PR의
기록을 확인했지만 이 Linux 검토 환경에서 COM을 재실행하지 않았다.

## 단계와 결정

- [x] 최신 `upstream/devel` 기준 visible review branch 생성
- [x] 원 PR head fetch 및 현재 commit 목록 확인
- [x] PR 내부 devel merge commit 제외
- [x] 누적 적용·manifest/unit-tier conflict 해소
- [x] 전체 local Rust 회귀·clippy·생성기 검사 완료
- [x] PR별 archive review 문서 작성
- [ ] 작업지시자 승인 후 최신 원격 head·mergeability·required checks 재확인
- [ ] 승인 후 통합 PR 생성 및 원 PR close/comment/branch cleanup

## 보정 판단

현재 통합 head에서 추가 메인터너 코드 보정은 필요하지 않다. 적용 과정에서 발생한 manifest와 unit-tier
충돌은 통합 branch의 생성물 정합성을 위한 검토 단계 해결이며, contributor source branch를 수정하거나
force-push하지 않았다.
