---
kind: pr-review-implementation
status: local-review-complete
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-17
---

# PR #4942·#4949·#4951·#4954·#4955·#4958·#4959 누적 통합 이행 기록

## 기준선과 적용 순서

`upstream/devel@418e5b191d23cf0618ce99f0cfec332c19ac1bc2` 위에서 가시성 branch
`review/non-draft-20260816`을 만들고 열린 non-draft 원 PR을 번호순으로 누적했다. 원 source branch를
rewrite하거나 직접 push하지 않았고, contributor의 merge commit은 적용하지 않았다.

| 원 PR / source | 통합 적용 |
| --- | --- |
| #4942 `7c0bfcd1d` | `5aae6d3e8` |
| #4949 `021e42d75` | `320d6d41d` |
| #4951 `0d6114810` | `81a5b8ac1` |
| #4954 `600571bc3` | `c89e77e89` |
| #4955 `9ed4d1237` | `ac475bdac` |
| #4958 `f04dc03b8` | `84ad0e5b5` |
| #4959 `7da57b580` | `9135f037a` |
| #4959 후속 `9ffe4878d` | `40954853e` |

적용과 최신 devel 리베이스 모두 conflict 없이 끝났다. #4959 후속은
`tests/issue_3915_verify_axes_both_reported.rs` fixture 교체만 포함하며, 최신 source head 확인 뒤 별도로
적용했다.

## 메인터너 보정

`566194be7 fix(agent): handoff 산출물 해시를 필수 검증`은 #4949의 output `sha256`을 형식상의 선택 정보로
남기지 않고 수거 파일의 실제 SHA-256과 비교하는 수용 조건으로 바꿨다. 누락·길이 오류·비hex 값은 schema
boundary에서 거부하고, 형식이 맞지만 실제 파일과 다른 해시도 수용하지 않는다.

## 완료한 검증

- 최종 후보 전체: `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast` — 6,519 passed, 38 skipped, 7 slow, 389.510초.
- #4959 최신 source 후속: `issue_3915_verify_axes_both_reported` 3 passed.
- #4959 note 계약: `issue_4916_note_vpos_roundtrip` 1 passed, `issue_1692` 11 passed.
- #4949 maintainer correction: `python3 -m unittest scripts/test_agent_handoff.py` 28 passed 및 `py_compile` 통과.
- 품질: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `git diff --check upstream/devel...HEAD` 통과.

#4942는 renderer 식을 동치 함수로 추출한 보존 refactor이며 전체 visual baseline도 통과했다. 작업지시자의
추가 준비 생략 지시에 따라 Native Skia·WASM build·별도 PDF visual sweep은 반복하지 않았다.

## 다음 원격 단계

이 기록과 원 PR별 review 문서는 동일 통합 PR에 포함한다. merge 직전에는 일곱 원 PR의 최신 head,
`MERGEABLE` 상태와 required Actions를 다시 확인한다. 승인 뒤 통합 PR만 merge하고, 각 원 PR에는 통합
반영 commit/PR 링크와 처리 사유를 남긴 뒤 close한다.
