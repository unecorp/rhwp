---
kind: integration-review-plan
status: review-complete-pending-trailing-ci
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-22
---

# PR #5856/#5858/#5865/#5868/#5869/#5878/#5883/#5887 통합 검토 기록

## 기준선과 적용 순서

- 기준선: `upstream/devel@65f71270f7457d50a92b75601a2339ca305d1fab`
- 가시성 branch: `review/open-prs-20260822`
- code candidate: `4b28259bb3b59e90d0c1a8b242299951b70695ca`
- 적용 순서: #5856 `1db8e68eb`, #5858 `9a07c8ea2`, #5865 `d912b4229`, #5868 `08baf8f2f`/`fea71a537`/`f4e3a0d31`, #5869 `1d2674857`/`d5dada7c1`, #5878 `d7f90eb00`, #5883 `cbcbe82ca`/`81a2332f1`/`90d3a1ea7`/`4f29d3c62`, #5887 `98ef38ed1`/`b3df19de5`.
- 모든 cherry-pick은 충돌 없이 적용됐다. #5859는 #5848/#5851와 중복되고 source 상태가 `dirty`라 제외했다.

## 메인터너 보정

`4b28259bb`에서 두 검토 보정을 추가했다.

1. `ir_field_sweep_baseline.rs`는 standalone test target이 아니라 generated `regression_suite_014`에 포함된다. 잘못된 실행 명령을 generated suite module filter로 정정했다.
2. #5865 적용으로 `hwpctl_ParameterSetID_Item_v1.2.hwp` overflow row가 29에서 8로 감소했다. 780 samples/571 rows dump를 재실행해 exact diff를 확인한 뒤 baseline을 강화했다.

## 완료한 검증

- 전체 nextest: **8,160 passed, 39 skipped**
- clippy `-D warnings`, native-Skia lib/focused render, standard WASM build
- IR sweep: 893 samples, 517 rows, baseline exact diff clean
- overflow sweep: 780 samples, 571 rows, baseline exact diff clean
- fmt, test-suite manifest/tier, `git diff --check`, `upstream/devel` merge-tree clean
- visual sweep: #5830 p34-35, #5863 p8-9, #5879 p19-20. 세부 수치와 제한은 원 PR별 review에 기록했다.

## CI와 trailing 문서 단계

- 통합 PR은 [#5889](https://github.com/edwardkim/rhwp/pull/5889)이며 code candidate `4b28259bb`의 GitHub Build & Test, archive build/shard, Lint, Native Skia, CodeQL Rust/JavaScript/Python, Canvas visual diff, Adapter inter-diff, Proptest roundtrip이 모두 성공했다. WASM과 frontend unit skip은 변경 범위 정책에 따른 정상 결과다.
- 원 PR별 archive review, 대표 visual asset, 오늘할일을 하나의 trailing docs-only commit으로 추가한다.
- trailing commit 뒤 review-only fast-pass와 최신 `MERGEABLE` 상태를 확인한다. 별도 문서 전용 PR은 만들지 않는다.
- merge 뒤에는 `post_merge.md`에 따라 devel 포함, 원 PR/관련 issue의 comment 및 close, 이 integration branch 정리를 수행한다.
