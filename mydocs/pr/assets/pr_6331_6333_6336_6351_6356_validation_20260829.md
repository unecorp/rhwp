---
kind: pr-validation-evidence
status: local-validated
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-29 19:15 KST
prs: [6331, 6333, 6336, 6351, 6356]
author: lpaiu-cs
branch: review/lpaiu-cs-20260829
---

# lpaiu-cs 통합 검토 검증 증적 - 2026-08-29

## 기준

- base before rebase: `upstream/devel@86aa6128b8cc928c0cf79ed9bb8f27d0f74b7924`
- rebase 후 base: `upstream/devel@cf366d2faad63a57fb663ce38b2e02d99b873e22`
- code candidate head before review-document commit:
  `33c6dc4cf076d03a9bcc6a5b4e797285ed86f97a`
- reviewer request: #6331, #6333, #6336, #6351, #6356 모두 `jangster77` 등록 확인

## 원격 PR 상태 재확인

- #6331 `069cbfcff922`: non-draft, `DIRTY`, blocking/non-green checks 없음
- #6333 `fa3220f6a7b3`: non-draft, `CLEAN`, blocking/non-green checks 없음
- #6336 `654ce91a7779`: non-draft, `CLEAN`, blocking/non-green checks 없음
- #6351 `77df87464354`: non-draft, 최신 head CI 진행 중. 추가 commit은 통합 브랜치의
  `33c6dc4cf` 메인터너 보정과 중복돼 제외했다.
- #6356 `f5010c385e5b`: non-draft, `BLOCKED`, `Lint (fmt, clippy, WASM check)` 실패, `Build & Test` 실패
- draft #5953은 제외했다.

## 로컬 검증

- `node --test scripts/tests/ci-impact-classifier.test.cjs scripts/tests/ci-impact-policy.test.cjs`:
  71 pass, 0 fail
- `python3 -m unittest scripts/tests/test_ci_impact_workflow.py`:
  31 tests, OK
- `node --test scripts/tests/rust-unit-test-tiers.test.mjs`:
  12 pass, 0 fail
- `node scripts/rust-unit-test-tiers.mjs --check`:
  `4221 tests / 299 modules / ready 0 / support 87 / white-box 4130 / cfg support items 28`
- `node scripts/rust-test-suite-manifest.mjs --prepare && node scripts/rust-test-suite-manifest.mjs --check`:
  `1031 sources / 4534 static test attrs / 32 suites + 16 exceptions = 48/48 integration targets / nextest 최소 6559 cases / weight 785633..791223`
- `node scripts/run-rust-test.mjs issue_6332_snapshot_budget_coupling -- --locked --cargo-profile release-test --target-dir target/pr-review`:
  1 pass, 142 skipped
- `node scripts/run-rust-test.mjs issue_6350_equation_setter_raw_passthrough -- --locked --cargo-profile release-test --target-dir target/pr-review`:
  4 pass, 127 skipped
- `node scripts/run-rust-test.mjs issue_6355_picture_transform_raw_rendering -- --locked --cargo-profile release-test --target-dir target/pr-review`:
  4 pass, 134 skipped
- `(cd rhwp-studio && node --test tests/command-history-snapshot.test.ts tests/source-guard-support.test.ts)`:
  13 pass, 0 fail
- `npm --prefix rhwp-studio run test -- --run command-history-snapshot.test.ts source-guard-support.test.ts`:
  1242 pass, 1 skipped, 0 fail
- `cargo fmt --all -- --check`: pass
- `git diff --check upstream/devel...HEAD`: pass
- `cargo clippy --workspace --all-targets --target-dir target/pr-review -- -D warnings`: pass
- `cargo clippy -p rhwp --lib --target wasm32-unknown-unknown --target-dir target/pr-review -- -D warnings`: pass
- `cargo check --target wasm32-unknown-unknown --lib --target-dir target/pr-review`: pass

## 제외한 검증 시도

- `npm --prefix rhwp-studio exec -- vitest run ...`는 이 저장소의 해당 파일이 `node:test` 기반이라
  올바른 runner가 아니었다. 이 실패는 변경 결함 증거로 쓰지 않고, 위 `node --test` 경로로 다시
  통과를 확인했다.

## 메인터너 보정 증거

- 보정 commit: `33c6dc4cf076d03a9bcc6a5b4e797285ed86f97a`
- 원 PR #6351/#6356의 source-side `#[cfg(test)]` 증가를 `tests/cases` integration regression으로
  이동했다.
- #6351 추가 push `77df87464354`를 임시로 cherry-pick한 `7d6dc7018`은 같은 보정을 중복 적용하는
  commit이라 최종 통합 브랜치에서 제외했다.
- 보정 후 unit-tier check가 통과해, 원 PR head의 lint 실패 원인이던 source-side test 총량 증가를
  통합 브랜치에서 해소했다.
