# 수행계획 — Task M100 #3789 `src/main.rs` 잔여 렌더 경계 분리

- **이슈**: [#3789](https://github.com/edwardkim/rhwp/issues/3789)
- **작성일**: 2026-08-27 KST
- **작업 브랜치**: `task_m100_3789-render-boundary`
- **작업 worktree**: `tmp/issue-3789-render-boundary`
- **착수 기준**: `upstream/devel@1b91c2025`
- **절차 상태**: PR #6276 생성, self-review trailing head CI 대기
- **선행 작업**: [#5511](https://github.com/edwardkim/rhwp/issues/5511),
  [#5776](https://github.com/edwardkim/rhwp/issues/5776),
  [#6001](https://github.com/edwardkim/rhwp/pull/6001)
- **관련 후속 범위**: [#3790](https://github.com/edwardkim/rhwp/issues/3790)
- **현재 재기준**: `upstream/devel@1a43a507c`, merge commit `7c6ee5461`

## 1. 배경과 판단

#5511은 `src/main.rs`의 렌더 출력 adapter를 `src/cli/outputs/`로 옮겼지만, root에는 아직
`test-caption`의 직접 SVG 렌더 호출과 구조 query 공유 helper가 남아 있다. 이 상태에서 #5776은 실제
workflow consumer를 기준으로 PDF/shared adapter와 raster adapter의 누락된 CI 영향을 복구했고, #6001의
`13ae331db`로 `devel`에 통합되었다.

따라서 #3789는 더 이상 “누락된 adapter를 CI에 추가”하는 작업이 아니다. 선행 작업이 고정한 positive
mapping을 보존하면서 다음 두 책임을 분리한 뒤, `src/main.rs` 전체에 남은 Render Diff fail-closed 경계만
안전하게 제거하는 작업으로 진행한다.

1. `test-caption`의 문서 변형·SVG 렌더 책임을 전용 CLI 모듈로 이동한다.
2. `export-structure`와 공유 JSON 변환 helper를 비렌더 query 소유 모듈로 이동한다.

## 2. 2026-08-27 기준선

| 관찰 항목 | `upstream/devel@1b91c2025` 상태 |
| --- | --- |
| [`src/main.rs`](../../src/main.rs) | 2,101줄, CLI dispatch와 공유 helper가 공존 |
| 직접 렌더 호출 | `test_caption`이 `Document::render_page_svg`를 직접 호출 |
| vector output | [`vector.rs`](../../src/cli/outputs/vector.rs)에 렌더 출력과 `export_structure`가 공존 |
| 구조 JSON helper | `structure_json_value`가 root에 있고 vector output과 batch query가 공유 |
| Render Diff workflow | `src/main.rs`, `outputs/mod.rs`, `outputs/pdf.rs`를 추적 |
| trusted classifier | `src/main.rs`를 `main-render-boundary`로 fail-closed 분류 |
| policy mirror | workflow와 동일하게 `src/main.rs`를 render source로 보유 |
| #5776 보호 계약 | adapter 전수 inventory와 `render=true -> workflow 추적` 불변식이 존재 |
| 중복 작업 | 조회 시점에 #3789와 겹치는 open PR 없음; #3790은 이 경계를 #3789에 위임 |

## 3. 목표와 불변식

- `src/main.rs`에는 renderer 호출·렌더 결과 가공·구조 JSON 생성 구현을 남기지 않는다.
- root는 인자 해석과 전용 모듈 dispatch 같은 composition 책임만 가진다.
- `test-caption`의 stdout/stderr, SVG 출력, 기본 출력 경로와 exit code를 바꾸지 않는다.
- `export-structure`와 batch query의 JSON schema, key 순서·값, provenance와 exit code를 바꾸지 않는다.
- #5776이 고정한 PDF/shared/raster/vector 실제 consumer mapping과 adapter 전수 검사를 약화하지 않는다.
- Render Diff workflow, trusted classifier, policy mirror는 같은 source 경계를 추적한다.
- 단순 파일명이 아니라 실제 workflow consumer 근거로 CI 영향 범위를 결정한다.

## 4. 범위

### 4.1 범위 안

- `test-caption` 구현을 응집도 있는 전용 CLI 모듈로 move-only 분리
- `export-structure`와 `structure_json_value`를 비렌더 query 모듈로 분리
- root와 기존 output/batch module의 import·dispatch 정리
- 새 렌더 소유 경로를 Render Diff workflow·classifier·policy mirror에 반영
- `src/main.rs` blanket Render Diff trigger와 `main-render-boundary` 예외 제거
- 기존 CLI·CI contract test 보강과 변경 범위별 로컬 검증

### 4.2 범위 밖

- renderer 알고리즘, PDF/SVG/raster 출력 의미나 golden baseline 변경
- #5511 Stage 3의 service layer·DIP 또는 `src/main.rs` 전면 재설계
- #3790의 broader test axis·CI architecture 변경
- cache, runner, branch protection, release·publish 설정 변경
- 새로운 사용자 CLI, option, JSON schema 추가

## 5. 단계별 수행계획

### Stage 1 — 계약과 소유 경계 고정

1. `test-caption`, `export-structure`, batch structure query의 호출·출력 계약을 목록화한다.
2. 전용 caption 모듈과 structure query 모듈의 dependency 방향을 확정한다.
3. 현행 focused CLI tests와 CI classifier/workflow tests를 기준선으로 실행한다.

**종료 게이트**: 이동 전후를 비교할 CLI 계약과 CI path matrix가 문서·테스트로 식별된다.

### Stage 2 — source 책임 이동

1. `test-caption` 구현을 전용 모듈로 옮기고 root에는 dispatch만 남긴다.
2. `export-structure`와 구조 JSON helper를 query 소유 모듈로 옮겨 vector output과 root에서 제거한다.
3. 기존 CLI contract와 대표 SVG·JSON 결과가 이동 전후 동일한지 확인한다.
4. source 이동을 독립 local commit으로 고정한다.

**종료 게이트**: `src/main.rs`에 직접 렌더 호출과 구조 JSON 구현이 없고 공개 CLI 결과가 동일하다.

### Stage 3 — CI false-positive 경계 제거

1. 새 caption 렌더 경로를 workflow·classifier·policy mirror의 같은 의미 위치에 등록한다.
2. 세 목록에서 `src/main.rs`를 제거하고 일반 Rust source로 분류되는 계약을 추가한다.
3. #5776 adapter inventory와 render-workflow 추적 불변식이 계속 통과하는지 확인한다.
4. CI 운영 변경과 test를 별도 local commit으로 고정한다.

**종료 게이트**: root-only 변경은 Render Diff를 요구하지 않고, 실제 렌더 경로 변경은 계속 Render Diff를
요구하며 세 경로 목록이 일치한다.

### Stage 4 — 전체 검증과 제출 준비

1. Rust source 변경 게이트와 CI workflow O2 게이트를 실행한다.
2. 포맷·lint·문서 link·diff 검사를 실행하고 파생 test suite는 제출 대상에서 제외한다.
3. 단계 보고와 최종 보고를 작성한 뒤 push·PR 생성을 별도 승인 게이트로 남긴다.

## 6. 완료 조건

- [x] `src/main.rs`의 직접 `render_page_svg` 호출이 전용 렌더 책임 모듈로 이동한다.
- [x] `export-structure`와 `structure_json_value`가 비렌더 query 소유 모듈로 이동한다.
- [x] `test-caption`, `export-structure`, batch query의 CLI·JSON·exit 계약이 유지된다.
- [x] workflow, classifier, policy mirror에서 `src/main.rs` blanket render 경계가 제거된다.
- [x] 새 caption render source가 세 CI 경계에서 일관되게 추적된다.
- [x] #5776의 adapter별 positive mapping과 전수 inventory test가 그대로 통과한다.
- [x] Rust·CI workflow·format·lint·문서 검증 결과를 재현 가능한 명령과 함께 기록한다.
- [x] 사용자 승인 전에는 remote push와 PR 생성을 수행하지 않았다.

## 7. 위험과 중단 조건

| 위험 | 완화·중단 조건 |
| --- | --- |
| move-only 과정에서 CLI 출력이나 exit code가 달라짐 | focused contract test와 대표 출력 비교가 다르면 이동을 중단하고 원인을 분리한다. |
| helper 이동이 순환 의존을 만듦 | root 재수출이나 새 전역 helper로 우회하지 않고 query ownership을 다시 설계한다. |
| 새 렌더 파일이 CI 목록에서 누락됨 | classifier·policy·workflow 일치와 render 추적 불변식을 같은 commit에서 고정한다. |
| 파일명이 렌더처럼 보여 과분류됨 | #5776과 동일하게 실제 workflow consumer 증거가 없는 path는 자동 승격하지 않는다. |
| renderer 동작 변경이 필요해짐 | 이 이슈의 경계 이동 범위를 벗어나므로 구현을 멈추고 별도 이슈 여부를 판단한다. |
| 동시 PR이 같은 경계를 변경함 | 구현·push 전 open PR과 최신 `upstream/devel`을 다시 조회한다. |

## 8. 승인 게이트

접두사 없는 작업 브랜치와 수행·구현 계획을 준비하고 #3789에
[착수 근거](https://github.com/edwardkim/rhwp/issues/3789#issuecomment-5435009101)를 남긴 뒤,
작업지시자의 `진행해줘` 승인에 따라 제품 source와 CI 경계를 구현했다. source 이동과 CI 보정은 각각
`17fa14198`, `514ff74bc`로 분리했고 Stage 4 로컬 검증까지 완료했다. remote push와 PR 생성은 아직
수행하지 않았으며 별도 승인을 기다린다.

구현 뒤 절차 감사에서 Stage 2·3 종료 보고와 중간 승인을 거치지 않고 다음 단계로 이동한 사실을 확인했다.
Stage 문서는 최종 검증 뒤 `3c509c7d1`에서 사후 작성됐다. 작업지시자는 이 이력을 감추거나 재작성하지
않고 사후 감사 사실을 문서에 명시하는 보정을 승인했다. 이 보정은 감사 가능성을 회복하지만 원래 생략된
단계 승인 게이트를 소급해 완전 준수로 바꾸지 않는다.

보정 Stage 승인 뒤 최신 `upstream/devel@2166f4065`를 fetch하고 자동 merge tree가 충돌 없음을 확인한 뒤
`39d6aa1dd`로 current-base merge했다. 새 기준의 #3789 focused Rust 113개, CI policy Node 67개,
workflow Python 70개와 format·manifest·문서 검사를 통과했다. 전체 release-test와 clippy 재실행은 이
Stage 5 결과를 공유한 뒤 별도 승인을 받는 Stage 6 게이트로 당시 남겼다.

Stage 5 보고 뒤 upstream이 다시 52커밋 진전해 작업지시자의 재최신화 승인을 받았다. dry merge tree가
충돌 없이 생성됨을 확인하고 `upstream/devel@5645e1f5b`를 `3db893274`로 다시 current-base merge했다.
#3789 caption positive/root negative 경계와 upstream의 Archive D·duration-policy 계약을 함께 보존한 상태에서
focused Rust 113개, Node 67개, Python 71개와 정적 게이트가 통과했다. 이 동시점 결과는
[Stage 6 보고](../working/task_m100_3789_stage6.md)에 기록하며, 전체 release-test와 clippy는 Stage 7
별도 승인 게이트로 이동한다.

작업지시자가 Stage 7을 승인한 뒤 현재 권위 문서의 직접 nextest 명령으로 전체 8,473개가 통과했고,
필수 `cargo clippy --locked --all-targets ... -D warnings`도 통과했다. 최신 upstream에서 제거된 과거
`scripts/release-test.mjs` 호출과 필수 범위를 넘는 `--all-features` GPU 조합의 upstream 컴파일 실패는
필수 게이트 결과와 구분해 [Stage 7 보고](../working/task_m100_3789_stage7.md)에 기록한다. 다음 게이트는
remote push와 PR 생성에 대한 별도 승인이다.

작업지시자가 remote push와 PR 생성을 승인한 뒤 제출 직전 fetch에서 `upstream/devel`이 #4969 shaping 통합
15커밋만큼 다시 진전한 사실을 확인했다. dry merge tree가 충돌 없이 생성된 뒤 `7c6ee5461`로 current-base
merge하고, focused Rust 113개·전체 nextest 8,519개·필수 clippy와 정적 게이트를 다시 통과했다. 이 결과는
[Stage 8 보고](../working/task_m100_3789_stage8.md)에 기록한다. 이 Stage 8 보고 commit까지를 최초 원격
code candidate로 push한 뒤 Open PR을 생성하고, 번호 기반 self-review·오늘할일은 trailing commit으로
같은 PR에 추가한다.

Open PR [#6276](https://github.com/edwardkim/rhwp/pull/6276)을 `devel` 대상으로 생성했다. 최초 code
candidate는 `764439a15`이며, PR 번호 기반 [self-review](../pr/archives/pr_6276_review.md)와 오늘할일을
single-parent trailing review-only commit으로 추가한다. workflow·CI impact surface 변경이 포함되므로 최신
head의 trusted A.1 재사용 또는 Full fallback 결과와 별도 merge 승인을 기다린다.
