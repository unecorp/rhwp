# 수행계획서 — task_m100_3128

- **이슈**: [#3128](https://github.com/edwardkim/rhwp/issues/3128) `분할 표 continuation fragment의 셀 내용 세로 배치가 한컴과 불일치 — 76076 p34`
- **연계**: [PR #4763](https://github.com/edwardkim/rhwp/pull/4763), [#4764](https://github.com/edwardkim/rhwp/issues/4764), #1891, #2308
- **브랜치**: `codex/issue-3128-continuation-geometry`
- **분기 기준**: `upstream/devel@0bc05ef81` (2026-08-18 재기준화)
- **정답지**: `samples/issue1891/76076_regulatory_analysis-2024.pdf` 34쪽
- **기록일**: 2026-08-18 KST
- **상태**: Stage 4 전체 검증 완료, 게시 승인 완료

> **소급 작성 고지.** 이 문서는 진단·구현·focused 검증이 끝난 뒤 작성했다. “#3128을 진행해줘”를
> 구현 착수까지의 일괄 승인으로 해석해 수행계획서와 구현계획서 승인 게이트를 먼저 거치지 못했다.
> 따라서 이 문서는 사전 승인 기록이 아니며, 실제 관찰·변경·실패를 사후 계획처럼 꾸미지 않고
> 단계 보고서에 그대로 보존한다. 이 문서의 승인 전에는 전체 release/WASM 검증, 커밋, push,
> PR 생성을 진행하지 않는다. 절차 복구를 지시한 작업지시자 판단은
> [`task_m100_3128_hyper_waterfall_recovery.md`](../feedback/task_m100_3128_hyper_waterfall_recovery.md)에
> 원문과 함께 분리해 둔다.

## 1. 문제

`samples/76076_regulatory_analysis.hwp`는 HWP 2024 기준 PDF와 같은 82쪽을 만들지만, 수정 전 34쪽의
첫 continuation 표가 약 60px 더 길어져 뒤따르는 `직접편익` 표까지 아래로 밀렸다. PR #4763은
페이지 소유권과 전체 쪽수를 정합했으나 #3128의 셀 내부 재조판과 terminal host tail 높이는 해결하지
않았다.

[#4764](https://github.com/edwardkim/rhwp/issues/4764)는 문서 전반의 raster·font·paint 차이를 추적하는
상위 영역이다. 이번 작업은 #3128의 34쪽 continuation geometry와 줄바꿈만 다루며 #4764의 전역
폰트·래스터 정합을 섞지 않는다.

## 2. 목표

1. 76076 문서의 82쪽 페이지 수와 p33→p34 continuation 소유권을 유지한다.
2. 34쪽 첫 continuation 외곽을 한컴 PDF raster의 y=77..463px에 맞춘다.
3. 뒤따르는 `직접편익` 표를 PDF의 y=512px 부근에서 시작한다.
4. 첫 들여쓰기 줄의 `연동시스템 등` 줄바꿈을 한컴과 동일하게 유지한다.
5. p81→p82 short RowBreak child와 기존 중첩 표·overflow 회귀를 바꾸지 않는다.

## 3. 범위

### 포함

- 저장 `LINE_SEG`가 없는 literal-space 들여쓰기 문단의 제한적 tracking 복원
- `applyInnerMargin=false`인 해당 1×1 child table의 content-box 폭 계약
- terminal native RowBreak child의 빈 host tail 중복 예약 제거
- #3128 전용 geometry·줄바꿈 회귀 테스트
- 연관된 #1891, #2308, 중첩 표, overflow 회귀 검증
- 기준 PDF와 96dpi 시각 비교

### 제외

- #4764의 전역 폰트 fallback·glyph paint·raster 일치율 개선
- HWP 파서나 Document IR 스키마 변경
- unrelated pagination 휴리스틱 변경
- 이슈 close, push, PR 생성, merge

## 4. 단계

| Stage | 내용 | 현재 상태 |
| --- | --- | --- |
| 1 | 기준 PDF·render-tree 비교, 원인 분리, RED 수용 기준 | 구현 전에 수행됐으나 문서는 소급 작성 |
| 2 | tracking/content-box/terminal host tail 구조 보정 | 구현 완료, 승인 기록 없음 |
| 3 | focused 회귀·overflow baseline·34쪽 visual sweep | 완료, 전체 PR gate는 미실행 |
| 4 | 최신 `upstream/devel` 반영, 전체 release/WASM 검증 | 완료 — Docker 표준 경로 부재는 Stage 4에 기록 |
| 5 | 단계별 커밋, push, `devel` 대상 PR 생성 | 승인 완료, 진행 중 |

## 5. 완료 조건

- #3128 전용 회귀 2건과 관련 renderer 회귀군이 통과한다.
- 82쪽과 p81→p82 기존 owner boundary가 유지된다.
- 34쪽 continuation·후속 표 좌표가 PDF 오차 허용 범위 안에 든다.
- renderer 변경 범위의 전체 release/WASM gate를 승인받아 통과한다.
- PR 본문에 `Closes #3128`을 넣되 #4764는 자동 종료하지 않는다.

## 6. 구현 중 실제로 바뀐 판단

- 처음에는 `CharShapeRef` tracking을 모든 셀 fallback에 복원했으나 80168 문서가 157쪽에서
  156쪽으로 바뀌었다. 적용 대상을 둘 이상의 literal ASCII 공백, 동일 font metric, 구간별 tracking
  차이가 함께 있는 native HWP5 장문 1×1 child로 좁혔다.
- 기존 #2308 회귀는 p34에서 저장 cell margin을 유지한다고 가정했으나, 원본 속성
  `applyInnerMargin=false`와 PDF 우측 paint edge를 다시 대조해 table content box가 권위임을 확인했다.
- tracking과 content-box만으로 본문 높이는 맞았지만 후속 표가 19.2px 더 내려갔다. terminal child가
  이미 source cursor를 소유하는데 generic first-unit reservation이 빈 host Enter를 다시 예약한 별도
  원인을 분리했다.
