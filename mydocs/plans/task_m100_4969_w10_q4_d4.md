# 수행계획 — Task M100 #4969 W10-Q4-D4 CanvasKit vertical feature detection·pixel proof

- **상위 계획**: [`task_m100_4969_w10_q4_d.md`](task_m100_4969_w10_q4_d.md)
- **D3-C checkpoint**: `43d50ad99` (`qualified-publication-parity`)
- **최신 devel merge**: `eb7d020e4` (`upstream/devel@19b89d967b15`)
- **기계 판독 계획**:
  [`w10_q4_d4_execution_plan.json`](../tech/investigations/issue-4969/w10_q4_d4_execution_plan.json)
- **상태**: 계획 checkpoint `c11e84935`; D4-B `qualified-rust-bounded-selector` checkpoint
  `c9b551909`; D4-C `qualified-studio-runtime-pixel-proof` checkpoint `b1a33b6fc`; 최신 devel merge
  `eb7d020e4`; D4-D `qualified-canvaskit-bounded-vertical-runtime` checkpoint `f40987220`
- **checkpoint 승인일**: 2026-08-31 KST
- **작성일**: 2026-08-30 KST
- **제품 출력 변경**: 증명된 `boundedVerticalHwp5TableCellV1` candidate를 실제 CanvasKit에서만 선택
- **공개 API·schema 변경**: 없음

## 1. 착수 재자격화

Q4-D4 착수 직전 `upstream/devel`이 D3-C 기준보다 9커밋 전진했다. 우리 계보와 겹친 파일은
`mydocs/orders/20260830.md`, `src/document_core/commands/document.rs`, `src/paint/json.rs`,
`src/renderer/layout.rs`, `src/renderer/layout/table_layout.rs` 다섯 개였지만 3-way simulation과 실제 merge는
충돌 0이었다. Rust selector와 Studio CanvasKit replay source에는 incoming 직접 변경이 없었다.

merge 뒤 다음을 재확인했다.

| gate | 결과 |
| --- | --- |
| D3-C atomic activation | 9 passed / 0 failed |
| #6029 vertical cell | 1 passed / 0 failed |
| incoming 직접 중첩 회귀 | 9 passed / 0 failed |
| integration manifest | 1,057 sources / 4,680 attrs / 48/48 targets |
| integration 정책 | 20 passed / 0 failed |
| `samples/table-004.hwp` canonical | 214,449 bytes, 기존 SHA-256 일치, overflow 0 |
| #6029 canonical | 273,101 bytes, 기존 SHA-256 일치, overflow 0 |
| D4 exact red | Rust·Studio에 bounded feature detection 부재로 의도한 실패 |

따라서 incoming을 먼저 병합한 현재 head가 D4의 새 기준이다.

## 2. 기능 탐지 계약

버전·User-Agent·CanvasKit package 번호로 분기하지 않는다. candidate 자체와 현재 backend 객체가 아래 기능을
온전히 제공하는지 판정한다.

### 2.1 candidate capability

다음 조건을 전부 만족한 `GlyphRun`만 bounded vertical candidate다.

1. `diagnostics.reason == boundedVerticalHwp5TableCellV1`
2. `variant.variantId == verticalGlyphRun`, `variantKind == glyphRun`
3. run과 shape key의 writing mode가 모두 `vertical-rl`
4. orientation은 `vertical-upright`, per-glyph transform은 없음
5. direction은 양쪽 모두 `ltr`, bidi level은 0
6. exact·strict·portable 진단, missing/cluster/fallback-font count 0
7. synthetic style·variation·효과 없음, portable exact face 검증 성공
8. glyph/position/advance/cluster 수와 finite·resource 상한 통과

조건 일부만 맞는 candidate를 일반 horizontal 허용 규칙으로 우회시키지 않는다. bounded provenance가 붙은 run은
전체 tuple이 맞지 않으면 fail-closed한다.

### 2.2 backend capability

- Rust `CanvasKit`·`CanvasKitBrowser`: 위 bounded tuple이면 vertical pending 두 reason만 해제한다.
- Native Skia·SVG·Canvas2D: 계속 `TextRun` fallback을 선택한다.
- Studio: 실제 render surface의 Canvas 객체에서 `drawGlyphs`가 함수인지 leaf variant 선택 **전에** 탐지한다.
- font blob 검증·face 생성·Font 생성이 실패하면 candidate를 선택하지 않고 fallback을 남긴다.
- draw helper도 `drawGlyphs` 존재를 다시 방어적으로 검사한다.

선택 뒤 draw 실패는 fallback을 이미 제거해 빈 화면을 만들 수 있다. 따라서 “일단 선택하고 catch에서 실패”는
기능 탐지가 아니며 허용하지 않는다.

## 3. 구현 절편

### Q4-D4-A — merge 재자격화·red baseline

1. 최신 `devel`을 병합하고 D3-C·incoming overlap·canonical을 재실행한다.
2. 기존 `issue_4969_q4_d0_red_canvaskit_vertical_feature_detection_is_absent`가 정확한 이유로 실패하는지 고정한다.
3. 구현 파일과 비권위 영역을 다시 확정한다.

**상태**: 완료. 상세 계획 승인 전 product 구현은 시작하지 않는다.

### Q4-D4-B — Rust selector capability

1. `src/renderer/layer_renderer.rs`에 bounded vertical tuple 판정 helper를 둔다.
2. generic writing-mode/orientation rejection은 helper가 승인한 CanvasKit 두 backend에만 예외를 둔다.
3. `src/renderer/canvaskit_policy.rs` preflight가 target leaf 두 개를 `verticalGlyphRun`으로 선택하는지 확인한다.
4. Native Skia·SVG·Canvas2D, wrong provenance·sideways·transform·mode mismatch는 fallback을 유지한다.
5. 기존 D4 red를 green으로 전환하되 source 문자열 유무만 보는 false-pass를 semantic assertion으로 보강한다.

**종료 게이트**: target CanvasKit 2/2 glyph 선택, unsupported backend glyph 선택 0, malformed tuple fallback 100%.

**결과 후보**: [Q4-D4-B 결과 보고서](../working/task_m100_4969_w10_q4_d4_b.md)는 두 CanvasKit
backend만 target leaf 2/2를 `verticalGlyphRun`으로 선택하고 세 미지원 backend 6 slots와 다섯 malformed
tuple 10 slots는 전부 fallback함을 확인했다. generic vertical·horizontal selector unit 44건과 replay-plan
50건도 통과했다. `qualified-rust-bounded-selector` 결과 승인 뒤 checkpoint `c9b551909`로 고정했고,
별도 승인을 받아 D4-C를 시작했다.

### Q4-D4-C — Studio runtime capability·actual font replay

1. `glyph-run-fonts.ts`가 Rust와 같은 bounded tuple만 허용하도록 한다.
2. `canvaskit-renderer.ts`가 실제 Canvas의 `drawGlyphs` 기능을 variant 선택 전에 전달한다.
3. feature 부재·font verify/instantiate 실패·wrong tuple에서는 leaf별 `TextRun`을 보존한다.
4. `ttfs/opensource/NotoSansKR-Regular.ttf` 실제 bytes로 typeface/font를 만들고 target glyph id·position을
   `drawGlyphs`에 그대로 전달한다. backend reshape는 금지한다.
5. 기존 `rhwp-studio/e2e/issue-4969-shaping-replay.test.mjs`에 vertical fixture와 pixel alpha bounds,
   origin·bbox 허용오차, fallback 미중복을 추가한다. 새 별도 E2E source는 만들지 않는다.

**종료 게이트**: actual CanvasKit pixel 생성, target bbox/origin mismatch 0, draw call 1/leaf, fallback double paint 0,
기능 부재 blank 0.

**결과 후보**: [Q4-D4-C 결과 보고서](../working/task_m100_4969_w10_q4_d4_c.md)는 actual Noto face로
`한`·`글` target 두 leaf를 그려 각 1회 draw, glyph/position/origin·고정 alpha bounds mismatch 0을 확인했다.
기능 부재·미검증 blob·지원하지 않는 face index와 malformed tuple 5종은 모두 TextRun fallback을 보존했다.
판정 후보 `qualified-studio-runtime-pixel-proof`를 메인테이너가 승인했으며 checkpoint 승인 전 D4-D는 닫힌다.

### Q4-D4-D — 교차 검증·결과 판정

1. Node WASM layer JSON의 D3-C source/variant/glyph/resource 영수증을 재실행한다.
2. Studio focused unit/E2E와 production build를 실행한다.
3. Q4·Q2/Q3 horizontal, #6029, canonical SVG 두 건을 재실행한다.
4. 표준 Docker WASM과 format·Clippy·integration 정책을 통과시킨다.
5. 실제 지원 subset과 모든 fallback reason을 기계 판독 증적·결과 보고서에 고정한다.

**종료 게이트**: CanvasKit target selection/pixel mismatch 0, unsupported backend false selection 0,
fallback disappearance 0, horizontal/canonical 회귀 0.

**결과 후보**: [Q4-D4-D 결과 보고서](../working/task_m100_4969_w10_q4_d4_d.md)는 최신 devel
23커밋을 충돌 0으로 병합한 뒤 #4969 계보 103건, Studio 1,324건, 격리 Node WASM 4건, 표준 Docker
WASM과 canonical 두 건을 통과했다. 실제 지원 subset과 fail-closed 행렬을 기계 판독 증적에 고정했으며
판정 후보는 `qualified-canvaskit-bounded-vertical-runtime`이다. Q4-D 전체 성능·최종 분류는 D5에 남긴다.

## 4. 변경 범위

예상 product source:

- `src/renderer/layer_renderer.rs`
- 필요할 때만 `src/renderer/canvaskit_policy.rs`
- `rhwp-studio/src/view/canvaskit/glyph-run-fonts.ts`
- `rhwp-studio/src/view/canvaskit-renderer.ts`

예상 기존 test source:

- `tests/cases/issue_4969_shaping_request_contract.rs`
- `tests/cases/issue_4969_shaping_atomic_activation.rs`
- `rhwp-studio/e2e/issue-4969-shaping-replay.test.mjs`
- 관련 기존 Studio unit/contract source

새 Rust integration `.rs`, generated suite·manifest, Cargo marker, public layer schema는 추가하지 않는다.

## 5. 보호 불변식

1. D3-C가 게시한 glyph id·position·advance·cluster·font resource를 backend가 다시 shape하지 않는다.
2. exact bounded provenance가 없는 vertical run은 선택하지 않는다.
3. Native Skia·SVG·Canvas2D의 vertical authority는 열지 않는다.
4. vertical-sideways·mixed·punctuation·Latin·HWPX·multi-line/run/column target은 확대하지 않는다.
5. capability나 font 생성 실패 시 fallback이 사라지지 않는다.
6. horizontal Q2/Q3 selector·font replay 계약을 변경하지 않는다.
7. package version·UA·문서 format build number를 판정 조건으로 쓰지 않는다.
8. private corpus·Hyper-V·GitHub mutation은 사용하지 않는다.

## 6. 중단 조건

- 실제 Canvas capability를 variant 선택 전에 알 수 없으면 selector를 열지 않고 구조를 재설계한다.
- vertical target을 허용하려고 generic writing-mode/orientation guard를 전역 해제해야 하면 중단한다.
- D3 glyph 위치를 CanvasKit에서 다시 측정·reshape해야만 맞으면 D3/D4 권위 분리가 깨지므로 중단한다.
- actual Noto face pixel이 D3 bbox/origin 허용오차를 벗어나면 보정 상수를 넣지 않고 원인을 조사한다.
- public schema나 D2 target 확대가 필요하면 수정 수행계획을 다시 제출한다.

## 다음 승인 경계

메인테이너가 D4-C 결과와 checkpoint `b1a33b6fc`를 승인하고 별도 승인을 받아 D4-D를 수행했다.
`qualified-canvaskit-bounded-vertical-runtime` 결과를 승인하고 checkpoint `f40987220`으로 고정했다. 별도
승인을 받아 Q4-D5 상세 수행계획을 작성했으며, 계획 승인 전에는 D5-A를 시작하지 않는다.
