# 수행계획 — Task M100 #4968 W9 kerning measurement·shaping end-to-end 연결

- **이슈**: [#4968](https://github.com/edwardkim/rhwp/issues/4968)
- **상위 tracker**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행**: #5955 W7.5 registry evolution contract, #4967 W8 face qualification 동결
- **후행**: [#4969](https://github.com/edwardkim/rhwp/issues/4969) W10 vertical metrics·variation·고급 shaping
- **작성일**: 2026-08-26 KST
- **작업 브랜치**: `task_m100_4968`
- **최신 통합 기준**: `upstream/devel@9be8b0562455`, merge `dda3eb075`
- **원격 상태**: #4968 OPEN, 담당자 `edwardkim`, milestone `v1.0.0`, 원격 작업 브랜치·연결된 열린 PR 없음
- **절차 상태**: 계획·Stage W9-Q0·Q1·Q2 승인·커밋 완료, W9-Q3-1..Q3-5R4C-4 승인·커밋 완료,
  최신 devel 병합 후 전건·native-skia·Docker WASM·K0 identity 재검증 완료, R4D 진입 준비

## 1. 목표와 핵심 질문

`ResolvedCharStyle`은 HWP/HWPX의 kerning flag를 보존하지만 `TextStyle` 변환에서 이를 잃는다. 현행
measurement는 glyph별 advance를 합산하고, SVG·Canvas2D·native Skia는 그 결과로 만든 cluster별 위치를
재생한다. portable GlyphRun도 `ttf-parser-nominal-v1` 명목 glyph와 기존 advance만 기록하므로 어느 경로도
문서가 요청한 pair positioning을 적용하지 않는다.

이번 작업의 질문은 다음과 같다.

> 문서의 kerning flag를 손실 없이 전달하고, 실제 font가 지원하는 GPOS `kern` 또는 legacy `kern` 능력을
> 기능 탐지해 pair adjustment를 적용할 때 어떤 glyph pair와 줄·프레임 경계가 달라지며, native·WASM·
> Canvas2D·CanvasKit이 같은 결정을 재생하는가?

목표는 모든 폰트에 임의의 kerning 값을 추정하는 것이 아니다. 정확한 source와 지원 table을 확인할 수 있는
경우만 적용하고, 나머지는 구조화된 `unsupported` 진단과 현행 advance를 유지하는 fail-closed 경로를 만든다.

## 2. 착수 기준선과 진입 게이트

### 2.1 Git·이슈 기준선

- 착수 시 로컬 `devel`과 `upstream/devel`은
  `6b5c4f871972380c0866e2a8d27ac2bc67d257e6`으로 동일했다.
- 작업 트리는 깨끗했고 같은 이름의 local·remote branch와 #4968 연결 열린 PR은 없었다.
- #4968은 이미 메인테이너에게 할당되어 있으므로 별도 착수 comment를 잠금 대체 수단으로 게시하지 않는다.

### 2.2 W3 확정 계측 재사용

보존된 W3 final aggregate와 checkpoint journal은 다음 상태다.

| 항목 | 확정값 |
| --- | ---: |
| attempted document | 10,000 |
| successful document | 9,909: HWP 6,489 + HWPX 3,420 |
| layout character | 54,399,371 |
| checkpoint entry | 10,000 |
| aggregate SHA-256 | `378671053ae94c02cda4caf8b8a4a1ac7866c3711fdf72c14aa527f8d9bc2888` |
| checkpoint chain | `94d9bbe3cce4218728ff29def8ab24abb51ffc434ec395d8be663fdab6b03649` |

기존 journal을 corpus 재파싱 없이 streaming projection한 kerning cohort는 157문서(HWP 115, HWPX 42),
175,466자다. stored LineSeg가 173,469자, fresh lane이 1,997자다. 전체 10,000건 기준 문서 비율은
1.57%이고 정상 분석 9,909건 기준으로는 1.58%다.

이번 작업은 이 확정 자료를 입력으로 사용한다. 10k 원본 전수 계측을 다시 실행하지 않으며 문서명·경로·본문·
문서 hash와 개별 식별 목록은 local-only 원장 밖으로 내보내지 않는다.

### 2.3 W8 겹침 동결

W3 kerning cohort와 W8 대상 face의 겹침은 rank 8 `KoPubWorld바탕체 Light` 1문서·42자뿐이다. rank 1
`문체부 바탕체`와 rank 7 `KoPubWorld돋움체 Light`의 겹침은 0이다. #4967은 세 face를 모두
`no-change`로 종결해 metric DB·registry·fallback·paint·supply를 바꾸지 않았다.

따라서 W9에서 pair positioning 변화와 face advance 변화가 섞일 위험은 동결됐고 진입 게이트를 충족한다.

### 2.4 공개 source와 fixture 기준선

| 입력 | SHA-256 | 용도 |
| --- | --- | --- |
| W5 oracle fixture manifest | `adf7185faab35edaae62b6c77c5a60642b6a62eee3dc7656bd98042f368d1c3a` | 장평·자간·kerning on/off·stored/fresh 축 재사용 |
| `NotoSansKR-Regular.ttf` | `6e06a7fe5d696ca719894a23f36bb2b1be8c816a5937cd4ad0f23ca67780dd74` | 추적된 SIL OFL 공개 GPOS `kern` source |

W5 fixture의 kerning bit는 flag 보존 축으로 재사용할 수 있지만 pair 차이를 명확히 드러내도록 설계되지 않았다.
따라서 W9에서는 알려진 pair와 비대상 문자열을 갖는 작은 공개 fixture를 별도로 추가한다.

추적된 Noto Sans KR regular에는 GPOS `kern` feature와 PairPos lookup이 있고 legacy `kern` table은 없다.
로컬 KoPubWorld Batang Light는 GPOS와 legacy `kern`을 모두 가지므로 local-only 교차 판정에는 사용할 수 있지만,
그 로컬 경로·bytes를 공개 fixture의 재현 조건으로 삼지 않는다. legacy 경계는 라이선스와 provenance가 명확한
소형 공개 fixture 또는 결정적으로 생성되는 test fixture로 검증한다.

## 3. 현행 손실 계보와 설계 제약

```text
HWP/HWPX CharShape.kerning
  -> ResolvedCharStyle.kerning               [보존]
  -> resolved_to_text_style / TextStyle      [현재 손실]
  -> EmbeddedTextMeasurer                    [문자별 hmtx·metric 합산]
  -> compute_char_positions                  [pair context 없음]
  -> TextRun / PaintTextStyle                [capability·source trace 없음]
  -> SVG·Canvas2D·native Skia                [cluster별 고정 좌표 재생]
  -> portable GlyphRun                       [nominal glyph, pair shaping 없음]
```

따라서 `TextStyle`에 bool 하나를 추가하는 것만으로는 완료되지 않는다. 다음 세 책임을 분리한다.

1. **문서 의도**: kerning on/off가 style resolution부터 layout·paint까지 손실 없이 전달되는가.
2. **font capability**: 실제 선택된 face source가 GPOS `kern`, legacy `kern` 또는 어느 것도 지원하지 않는가.
3. **결정 결과**: 어느 pair에 얼마의 adjustment를 적용했고 line/frame crossing이 어떻게 달라졌는가.

현행 `FontMetric`은 단일 glyph advance만 보존하고 pair table이나 source bytes provenance를 갖지 않는다.
historical metric 이름만으로 pair 값을 추정하거나 backend에게 다시 shaping을 맡기면 native/WASM 또는
layout/paint가 발산한다. pair adjustment는 공통 layout에서 한 번 결정하고 backend가 동일 위치를 재생해야 한다.

## 4. 범위

### 4.1 범위 안

- 기존 W3 journal에서 157문서 kerning cohort를 재선정하는 bounded local-only projector
- HWP/HWPX, font face, metric face, 장평·자간, context, stored/fresh의 비식별 aggregate
- `ResolvedCharStyle.kerning`에서 `TextStyle`, measurement, position trace까지의 전달
- GPOS `kern`과 legacy `kern` capability의 기능 탐지 및 명시적 fallback reason
- 공개 kerning on/off pair fixture와 비대상 무변화 fixture
- pair adjustment와 자간·장평·script scale의 적용 순서 고정
- native·Docker WASM layout parity와 SVG·Canvas2D·CanvasKit·native Skia replay parity
- actual cohort의 stored LineSeg와 fresh layout 분리 영향 판정
- kerning-off baseline과 schema/JSON 호환성 회귀

### 4.2 범위 밖

- 10k corpus 전수 재계측
- font fallback, metric face, registry relation, webfont supply 교정
- unsupported font의 pair 값을 평균·이름·대체 face로 추정
- GSUB 전반, complex-script shaping 전반, vertical metrics, variable font axis
- stored LineSeg와 fresh layout 결과 합산
- private corpus 원본·문서명·경로·본문·문서 hash 또는 개별 식별 목록 게시
- 로컬 proprietary font bytes의 저장소 추가
- commit, push, PR 생성, issue close

## 5. 보호 불변식

1. `kerning=false` 문서의 layout position·줄바꿈·paint JSON·SVG 출력은 기존과 동일해야 한다.
2. `kerning=true`라도 exact pair capability를 증명하지 못하면 adjustment를 적용하지 않는다.
3. font face 선택·single-glyph metric 변화와 pair positioning 변화를 같은 candidate에 섞지 않는다.
4. pair adjustment는 공통 결정값이고 backend가 독자적으로 다시 shaping하지 않는다.
5. GPOS `kern`, legacy `kern`, unsupported와 disabled를 서로 다른 진단으로 남긴다.
6. 장평·자간·justification·script scale 적용 순서를 fixture와 trace로 고정한다.
7. stored LineSeg 존재를 validity로 간주하지 않고 현행 feature-detection disposition을 사용한다.
8. 공개 산출물은 aggregate와 공개 fixture만 포함하며 private identity는 fail-closed한다.
9. parser·serializer의 기존 kerning round-trip과 문서 편집 명령을 깨뜨리지 않는다.
10. pair 또는 glyph 처리량에는 명시적 상한을 두고 비정상 입력에서 무제한 shaping을 하지 않는다.

## 6. 단계별 수행계획

### Stage W9-Q0 — 증거 동결과 재계측 금지선

1. W3 final aggregate·checkpoint chain과 W5 fixture·Noto source digest를 검증한다.
2. 기존 journal에서 kerning 157문서를 streaming projection으로 다시 선택한다.
3. local-only cohort 원장은 owner-only 권한을 유지하고 공개 aggregate와 물리적으로 분리한다.
4. W8 rank 1·7·8 겹침과 최종 `no-change` disposition을 기계 판독 가능한 공개 projection으로 고정한다.
5. corpus reparse count, Hyper-V run count, product mutation count를 모두 0으로 고정한다.

**종료 게이트**: 기존 hash가 일치하고 157문서·175,466자와 W8 겹침이 새 전수 계측 없이 결정적으로
재현된다. 결과 승인 전 W9-Q1에 진입하지 않는다.

**실행 결과 (2026-08-26)**: W3 checkpoint journal 10,000 entry를 corpus source 재개방 없이 한 번
streaming해 kerning cohort 157문서(HWP 115, HWPX 42), 175,466자를 재현했다. journal의 문서별 2,318
usage row는 final aggregate의 1,723 dimension row와 count 전항이 일치했다. stored 173,469자와 fresh
1,997자를 분리했으며 두 lane을 함께 가진 문서가 있어 lane별 distinct document 수는 단순 합산하지 않는다.
W8 겹침은 rank 8 1문서·42자뿐이고 rank 1·7은 0이며, 세 disposition 모두 `no-change`·제품 mutation false로
대사됐다. corpus source open, 10k 재계측, Hyper-V, 제품 source 변경은 모두 0이다. 공개 projection canonical
SHA-256은 `95309e457f78de38f2b1470b05e6f0fe97f00684ffff5eddd4d82c6438fb71e6`이다.

### Stage W9-Q1 — kerning-off baseline과 capability/trace 계약

1. `TextStyle`, `PaintTextStyle`, paint JSON, TextRun/GlyphRun schema의 현행 직렬화 계약을 목록화한다.
2. kerning-off 대표 fixture의 native·Docker WASM layout position과 SVG/paint JSON byte baseline을 고정한다.
3. 다음 최소 진단 계약을 설계한다.
   - request: `disabled` / `enabled`
   - capability: `gpos-kern` / `legacy-kern` / `unsupported`
   - disposition: `not-requested` / `applied` / `no-pair-adjustment` / `fail-closed`
   - source provenance와 fallback reason
4. run/pair 수, font bytes, trace record 수 상한과 초과 시 fail-closed 동작을 정한다.
5. 직접 `rustybuzz`를 사용할지 제한된 pair parser를 둘지 native·WASM 크기, 성능, table coverage로 비교한다.
   현재 transitive dependency 존재만을 제품 API 사용 근거로 삼지 않는다.

**종료 게이트**: off-baseline, backend capability matrix, trace schema, shaping engine 선택 근거가 승인된다.
이 단계까지 제품 조판은 바꾸지 않는다.

**실행 결과 (2026-08-26)**: W5 공개 fixture의 `K0` body matrix 9개 run을 native와 Docker WASM에서
비교해 positions 전항 일치, SVG 400,536 bytes의 byte-exact 일치를 확인했다. layer-tree 원문은 synthetic
paragraph의 `usize::MAX`가 native 64-bit와 WASM 32-bit에서 다르게 직렬화되는 기존 sentinel 20건 때문에
byte-exact하지 않다. 해당 값만 `para:MAX`로 정규화하면 전체 tree가 일치하며, 이 예외와 양쪽 원문 hash를
기준선에 함께 동결했다. 현재 TextStyle·PaintTextStyle·TextRun/GlyphRun JSON에는 kerning 의도·capability·
disposition이 없음을 source hash와 runtime field inventory로 확인했다.

capability는 `gpos-kern` → `legacy-kern` → `unsupported`, 결과는 `not-requested`·`applied`·
`no-pair-adjustment`·`fail-closed`로 고정했다. font 32 MiB, code point·glyph·trace 4,096, pair 4,095를
상한으로 삼고 초과 시 현행 advance를 유지한다. 자체 제한 parser는 OpenType class pair·extension lookup·
script/language routing·legacy 형식 누락 위험 때문에 제외했다. `rustybuzz`를 Q2 공개 pair fixture와 Q3
native/WASM 크기·성능·glyph identity 게이트를 통과해야 하는 qualified candidate로 선택했으며 Q1에서는
직접 dependency나 제품 조판을 변경하지 않았다. 공개 기준선 canonical SHA-256은
`74f0310aa61cb4464b7176a4f53b4154aab3b828bbf5608ed7ebc9644689d499`다.

### Stage W9-Q2 — 공개 pair fixture와 OpenType 경계

1. `AV`, `To` 등 알려진 조정 pair와 비대상 pair를 포함한 작은 HWPX fixture를 만든다.
2. ratio 100/90/80, spacing 0/-5/-10, kerning false/true를 교차하되 조합 수와 문자 수를 bounded한다.
3. body/table-cell/text-box와 stored/fresh lane을 분리한다.
4. 추적된 Noto source에서 GPOS `kern` pair adjustment와 unitsPerEm을 재현 가능하게 추출한다.
5. legacy-only, GPOS-only, 둘 다 존재, table 없음, malformed/oversized의 feature-detection 경계를 검증한다.
6. 필요하면 Hyper-V HWP 2020 oracle은 pair 적용 순서에 대한 좁은 adjudication으로만 실행한다. W5 전체
   three-state를 반복하지 않는다.

**종료 게이트**: OpenType source에서 계산한 kerning on/off의 기대 pair 차이, 비대상 무변화, table 우선순위와
fail-closed reason이 공개 fixture에서 결정적으로 재현된다. 특정 한컴 버전의 출력이 on/off 차이를 만들지
않으면 그 결과를 OpenType 정답 대신 쓰지 않고 버전 한정 호환성 관측으로 분리하며 Q3 진입 전 수정 방향을
메인테이너에게 승인받는다.

**실행 결과 (2026-08-26)**: 공개 HWPX 1쪽에 body 18개(run: ratio 100/90/80 × spacing 0/-5/-10 ×
K0/K1), table-cell 2개, text-box 2개를 고정했다. body의 K0/K1은 같은 stored/fresh lane에 배치했다. 현행
rhwp native·Docker WASM은 18개 body position과 SVG가 일치했고, 제품은 K0/K1을 모두 같은 좌표로 처리했다.

추적된 Noto Sans KR은 GPOS-only이며 unitsPerEm 1,000, `AV=-18`, `To=-76`, `WA=0`, `HH=0` design unit다.
결정적 synthetic SFNT로 GPOS-only·legacy-only·동시 존재 시 GPOS 우선·table 없음·malformed·32 MiB 초과의
fail-closed 경계를 동결했다.

HWP 2020 `11.0.0.9136` 좁은 Hyper-V 판정에서는 HWPML2X readback이 fixture 22개 context의 kerning flag,
ratio, spacing, Noto face를 전부 보존했다. 그러나 PDF의 controlled body 9축은 K0/K1 sample position과 네 pair
gap 차이가 전부 0이었다. 따라서 flag는 open에서 유실되지 않았지만 이 버전의 PDF export에서는 flag에 따른
layout differential이 관측되지 않았다. 이것만으로 양쪽이 always-kerned인지 always-unkerned인지는 판정할 수
없고 적용 순서도 측정할 수 없다.

Q2 수정 방향은 한컴 관측을 `version-scoped-negative-compatibility-observation`으로 제한하고, Q3 K1의 계산
정답은 OpenType capability 계약으로 삼는 것이다. 이는 한컴 2020 PDF byte parity가 아니라 명시적 기능
개선이며, K0 현행 출력 불변을 별도 호환성 게이트로 유지한다. 공개 adjudication canonical SHA-256은
`8fce761f58c742a9a6d4fcf1cbfdf7a3037acd8f14f028e5f2c5cd41f64ff1ee`, 이를 결합한 Q2 baseline은
`8ad6b8562bfb19fc9c751d1d74012c334d821028640a1459e27a0b0b6bbbbda1`이다.

### Stage W9-Q3 — end-to-end 공통 pair positioning 구현

1. K0는 현행 호환성, K1은 OpenType capability 기반 기능 개선으로 분리하고 HWP 2020 PDF byte parity를 K1
   acceptance criterion으로 사용하지 않는다.
2. kerning flag를 `ResolvedCharStyle`에서 `TextStyle`과 measurement decision까지 전달한다.
3. exact source capability를 받은 공통 pair-positioning provider를 도입한다.
4. pair adjustment를 line measurement와 `compute_char_positions`가 같은 값으로 소비하게 한다.
5. 장평·자간·추가 spacing과 결합 순서를 단일 함수와 trace로 고정한다.
6. paint replay에는 이미 결정된 positions와 capability metadata만 전달하고 backend 재-shaping을 금지한다.
7. source 없음·미지원·malformed·상한 초과는 현행 advance를 유지하고 구조화된 reason을 남긴다.
8. `kerning=false` fast path가 기존 계산과 직렬화를 그대로 유지하는지 회귀로 고정한다.

**종료 게이트**: 공개 pair fixture의 measurement·line break·positions가 일치하고 off-baseline은 불변이며,
모든 미지원 상태가 fail-closed한다.

### Stage W9-Q4 — backend parity와 bounded 실제 cohort 판정

1. native와 Docker WASM에서 같은 public fixture의 pair decision과 positions를 canonical 비교한다.
2. SVG·Canvas2D·CanvasKit·native Skia가 공통 positions를 재생하고 독자적인 pair shaping을 추가하지 않는지
   확인한다.
3. Q0에서 동결한 동일 157문서만 bounded read-only 판정한다. 10k 전체를 다시 순회하지 않는다.
4. stored-valid/admitted, stored-rejected·unmodelled, fresh layout을 합치지 않고 별도 집계한다.
5. format, face, ratio, spacing, context별 적용·무조정·미지원과 첫 line/frame crossing을 집계한다.
6. 문서별 identity와 본문을 제거한 aggregate만 tracked evidence로 투영한다.

**종료 게이트**: backend parity mismatch 0, off-cohort 변화 0, 157문서 전건 disposition 회계와
stored/fresh 분리 결과가 확보된다. 실제 회귀가 있으면 평균 개선으로 상쇄하지 않고 원인을 좁혀 재계획한다.

### Stage W9-Q5 — 최종 판정과 W10 전달 계약

1. `qualified`, `no-change`, `blocked` 중 하나로 제품 결과를 판정한다.
2. flag 전달, capability, pair decision, backend replay의 계보와 보호 불변식 전항을 감사한다.
3. 공개 fixture·aggregate·검증 명령과 제한 사항을 최종 보고서에 기록한다.
4. W10에는 이번 작업에서 실제 지원한 shaping 경계와 미지원 table/script/axis만 전달한다.
5. commit·push·PR·self-review·merge·issue close는 각각 프로젝트 승인 게이트에 따라 별도로 진행한다.

**종료 게이트**: 메인테이너가 최종 보고서와 PR 후보 범위를 승인해야 원격 절차로 진입한다.

## 7. 예상 변경 후보

계획 승인 뒤 Q1·Q2 결과에 따라 정확한 파일을 좁힌다. 현재 예상 후보는 다음과 같다.

- `src/renderer/style_resolver.rs`
- `src/renderer/layout/text_measurement.rs`
- `src/renderer/mod.rs`
- `src/paint/paint_op.rs`, `src/paint/json.rs`, `src/paint/text_shape.rs`
- `src/renderer/svg.rs`, `src/renderer/web_canvas.rs`, `src/renderer/skia/text_replay.rs`
- 공개 fixture·projection·contract test와 `mydocs/working/task_m100_4968_*`
- 최종 `mydocs/report/task_m100_4968_report.md`

이 목록은 구현 권한이나 전부 수정할 의무가 아니다. Q1 capability 설계에서 공통 decision plane을 찾은 뒤
backend 파일 변경을 피할 수 있으면 더 좁은 범위를 선택한다.

## 8. 검증 전략

단계별 focused gate를 먼저 통과하고, 전체 PR gate는 구현·보고서 승인 뒤 별도 승인으로 실행한다.

- parser/serializer kerning round-trip focused test
- TextStyle 전달과 kerning-off identity test
- GPOS·legacy·unsupported·malformed capability contract
- pair measurement·position·line-boundary controlled fixture
- native↔Docker WASM canonical parity
- SVG·Canvas2D·CanvasKit·native Skia replay parity
- bounded local-only 157문서 projector privacy·determinism test
- 관련 integration source는 `tests/cases/`만 제출하고 generated suite·manifest는 제출하지 않음
- source-side `#[cfg(test)]` 변경 시 `node scripts/rust-unit-test-tiers.mjs --check`
- PR·push 직전 `cargo fmt --all`과 `cargo fmt --all -- --check`
- 변경 범위 확정 뒤 `mydocs/manual/pr_review/local_validation.md` 4.3의 해당 전체 gate

## 9. 중단·재계획 조건

다음 중 하나가 발생하면 구현을 밀어붙이지 않고 해당 Stage에서 중단해 수정 계획 승인을 받는다.

- kerning-off baseline이 style field 또는 JSON schema 추가만으로 바뀜
- exact font source 없이 historical metric name에서 pair 값을 추정해야만 구현 가능함
- native·WASM에서 동일 shaping engine이나 source provenance를 확보할 수 없음
- browser/Skia가 공통 positions 위에 kerning을 중복 적용함
- actual stored-valid lane에서 신규 overflow·line/frame crossing 회귀가 확인됨
- private identity 또는 font bytes가 tracked output에 포함됨
- shaping 상한을 우회하는 입력이나 유의미한 WASM size·시간 회귀가 확인됨

## 10. 현재 승인 게이트

Q0·Q1·Q2 결과와 “K1은 OpenType 기반 기능 개선, 한컴 2020은 음성 호환성 관측”이라는 수정 방향은
메인테이너가 승인했다. Q3-1..Q3-4와 중단·재계획 뒤의 Q3-5R1..R4C-4도 각각 승인·단계 커밋했다.

최신 `upstream/devel@9be8b0562455`를 merge commit `dda3eb075`로 통합한 뒤 #4968 target 24/24,
신규 upstream 집중 회귀 4/4, 전체 nextest 8,427/8,427, native-skia, Docker WASM, K0 native/WASM
identity를 다시 통과했다. 세부 수치와 `K0`·`K1` 로컬 시험 표기 정의는
`mydocs/working/task_m100_4968_w9_q3_5_r4c4.md`를 정본으로 삼는다.

다음 승인 경계는 R4D 진입 감사와 수정 수행계획이다. `TextRunNode`에 layout 결정 positions를 선택적으로
보존하고 SVG·Canvas2D·CanvasKit·native Skia·portable GlyphRun이 이를 재생하는 경계를 먼저 설계한다.
K0 직렬화 baseline, backend 독자 shaping 금지, source bytes·private identity 비보존을 계획 단계부터
중단선으로 유지한다.
