# 수행 계획 — Task M100 #6112

- **이슈**: [#6112](https://github.com/edwardkim/rhwp/issues/6112)
  `rhwp-studio: 한글 2024식 기본 도구 상자 접기/펴기 버튼과 Ctrl+F1 지원`
- **선행 이슈**: [#5738](https://github.com/edwardkim/rhwp/issues/5738)
- **브랜치**: `codex/issue-6112-toolbox-collapse`
- **분기 기준**: `upstream/devel` `70ebacc4c9589e8c778907e179a6dab18cce8eb0`
- **로컬 구현 초안**: `d707d4cf2b4efef757420381b6a460f1b325482e`
- **작성일**: 2026-08-26 KST

## 절차 상태

> **사후 재구성 고지.** 사용자 요청을 처리하며 이슈 등록과 계획 승인 전에 로컬 구현·검증 커밋이
> 먼저 생성됐다. 이 문서는 당시의 조사, 실제 변경과 검증 증적을 대사해 작업 범위를 복구한다.
> 정상 순서로 진행한 것처럼 소급 승인하지 않으며, 경위와 재발 방지는
> [`task_m100_6112_hyper_waterfall_recovery.md`](../feedback/task_m100_6112_hyper_waterfall_recovery.md)에
> 기록한다. 원격 push와 PR은 아직 수행하지 않았다.

2026-08-26 작업지시자가 Hyper-Waterfall 절차 복구를 지시해 #6112를 등록하고 담당자를
`postmelee`로 지정했다. 이 지시는 사후 문서화 착수 승인이지, 누락된 구현 전 승인 게이트의 소급
승인이 아니다. PR 준비·등록은 본 문서 묶음 검토 뒤 다음 게이트에서 진행한다.

## 목표

한글 2024의 `기본 도구 상자 접기/펴기`와 같은 빠른 전환 경로를 Studio에 제공하고, 편집 면적을
우선하는 신규 사용자는 큰 아이콘 도구 상자가 접힌 상태로 시작하게 한다.

## 작업 범위

1. #5738의 `view:toolbox-basic` 커맨드와 설정 저장·복원 경로를 단일 진입점으로 재사용한다.
2. 메뉴바 우측에 기본 도구 상자 접기/펴기 버튼을 추가한다.
3. `Ctrl+F1`과 macOS `Command+F1`을 같은 커맨드에 연결한다.
4. 신규·미설정 상태의 기본 도구 상자는 접힘으로 바꾸되, 명시적으로 저장된 값은 보존한다.
5. 메뉴와 버튼의 표시·접근성 상태, 첫 페인트 숨김을 함께 검증한다.

## 비범위

- 서식 도구 상자의 기본 표시 정책 변경
- 서식 도구 상자를 한 줄 소형 모드로 재배치하는 작업
- 기본 도구 상자 내부 아이콘 구성과 줄바꿈 정책 변경
- 렌더러, DocumentCore, HWP/HWPX 파서 변경
- 새 래스터·SVG 이미지 자산 추가

## 단계와 산출물

| 단계 | 내용 | 산출물 |
| --- | --- | --- |
| 1 | 한글 2024 비교, 변경 기점과 기존 자산 조사, 실패 계약 고정 | `working/task_m100_6112_stage1.md` |
| 2 | 단일 커맨드에 기본값·버튼·단축키·접근성 상태 연결 | `working/task_m100_6112_stage2.md` |
| 3 | 전체 단위·빌드·실제 브라우저·첫 페인트 검증 | `working/task_m100_6112_stage3.md` |

세 단계는 독립 승인·커밋으로 진행된 당시 기록이 아니라, 로컬 초안 `d707d4cf2`와 실행 로그를
대사해 사후 재구성한 감사 증거다. 파일별 설계는 `task_m100_6112_impl.md`, 최종 판정은
`report/task_m100_6112_report.md`에 분리한다.

## 검증 계획

- focused: 도구 상자 상태, 사용자 설정, 단축키 계약 테스트
- 전체: `cd rhwp-studio && npm test`
- 타입·번들: 기존 생성 WASM을 사용한 `cd rhwp-studio && npm run build`
- 브라우저: `cd rhwp-studio && npm run e2e:toolbox-visibility`
- 첫 페인트: 리로드 초기 프레임에서 숨김 도구 상자 노출 횟수 측정
- 문서·diff: `git diff --check`, 관련 Markdown 링크 검사
- push 직전: `cargo fmt --all`, `cargo fmt --all -- --check`

## 완료 조건

- 저장값이 없으면 기본 도구 상자는 접힘, 서식 도구 상자는 보임이다.
- 저장된 `toolbarBasic: true/false`는 값 그대로 복원된다.
- 우측 버튼, 보기 메뉴, `Ctrl+F1`이 같은 `view:toolbox-basic`을 실행한다.
- 버튼의 `aria-expanded`, 접근 가능한 이름과 툴팁이 표시 상태와 일치한다.
- 리로드 첫 페인트에서 저장된 숨김 상태가 깜빡이지 않는다.
- Studio 전체 테스트·프로덕션 빌드·헤드리스 브라우저 E2E가 실패 없이 통과한다.
- 계획 대비 실제, 절차 이탈과 잔여 위험이 최종 보고서에 기록된다.

## 승인 게이트

본 수행 계획서, 구현 계획서, 사후 단계 보고와 최종 보고를 작업지시자가 검토한 뒤 PR 준비·등록으로
넘어간다. 이미 존재하는 구현 초안은 이 승인을 구현 전 승인으로 간주하지 않는다.
