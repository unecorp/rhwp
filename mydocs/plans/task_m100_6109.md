# 수행 계획 — Task M100 #6109

- **이슈**: [#6109](https://github.com/edwardkim/rhwp/issues/6109)
- **브랜치**: `codex/issue-6109-zoom-dialog-transaction`
- **stack base**: `codex/issue-6108-zoom-fit` `6d6a9dbcd`
- **trunk 기준 commit**: `upstream/devel` `1fe0d1480`
- **작성일**: 2026-08-28 KST
- **우선순위**: P2, 64/100
- **절차 상태**: Stage 3 최종 결과 승인·최신 devel 재검증 완료, native stack 게시 준비

## 절차 상태와 stacked PR 경계

- 2026-08-28 작업지시자가 #6108 결과 위에서 #6109를 이어 진행하고 두 작업을 native stacked PR로
  게시하도록 요청했다.
- GitHub에서 #6109의 선행 PR이 없고 담당자가 비어 있음을 확인한 뒤 `postmelee`를 assignee로 지정했다.
- #6109 브랜치는 최초 로컬 완료된 #6108 exact head `534cf40ed`에서 분기했다. 따라서 stack은
  `devel ← #6108 ← #6109` 순서다.
- 이 문서는 코드 변경 전 사전 계획이다. 구현은 작업지시자가 수행·구현 계획을 승인한 뒤 시작한다.
- 2026-08-28 작업지시자가 수행·구현 계획을 승인하고 Stage 1 진행을 지시했다.
- 2026-08-28 사용자 배율 validator·오류 접근성·Enter/Escape 계약을 구현하고 focused test 24/24와
  실제 Chrome smoke 21/21을 통과했다. Stage 2 진입 전 Stage 1 결과 승인을 기다린다.
- 2026-08-28 작업지시자가 Stage 1 결과를 승인하고 Stage 2 원자 보기 설정 transaction 진행을 지시했다.
- 2026-08-28 쪽 배치·쪽 이동·최종 배율·맞춤 모드를 하나의 transaction으로 저장·전달하고,
  CanvasView가 최종 상태로 한 번만 레이아웃하도록 구현했다. focused test 49/49와 TypeScript CI unit
  검사를 통과했으며 Stage 2 결과 승인을 기다린다.
- 2026-08-28 작업지시자가 Stage 2 결과를 승인하고 Stage 3 통합·실제 브라우저 검증 진행을 지시했다.
- 2026-08-28 실제 macOS Chrome E2E 37/37 assertion, Studio test 1,234건(1,233 pass·1 skip),
  production build 239 modules, E2E manifest 120/120, 저장소 필수 format gate를 모두 통과했다. 최종 결과
  승인을 기다린다.
- 2026-08-28 작업지시자가 로컬 UI와 최종 검증 결과를 승인했다. Stage 3 변경을 checkpoint commit으로
  고정하고, remote push와 stacked PR 게시는 별도 승인을 기다린다.
- 2026-08-28 작업지시자가 native stacked PR 게시를 승인했다. 공식 `gh-stack`에 두 기존 브랜치를
  등록하고 최신 `upstream/devel@1fe0d1480` 및 #6108 게시 증적 head `6d6a9dbcd` 위로 cascading
  rebase했다. 최상단 #6109 head에서 Studio 1,234건(1,233 pass·1 skip), production build 239 modules,
  Chrome E2E 37/37, E2E manifest 120/120, 저장소 필수 format gate를 다시 통과했다.
- remote push와 #6108·#6109 PR 생성은 두 작업의 로컬 검증과 별도 게시 승인을 받은 뒤에만 수행한다.

## 조사 결과

### 사용자 배율 입력

- `ZoomDialog.onConfirm()`은 언제나 callback을 실행하고 `void`를 반환한다.
- 빈 문자열은 `Number('') === 0`이 되어 10%로, 유한하지 않은 값은 100%로, 범위 밖 값은 10~500%로
  `clampCustomZoomPercent()`에서 조용히 보정된다.
- 공용 `ModalDialog`는 `onConfirm() === false`이면 창을 유지하는 계약을 이미 제공하며, 문서 저장 암호
  대화상자는 이 계약과 `role="alert"`를 사용하는 선례다.
- 공용 모달의 편집 입력 Enter는 제출하지 않으므로, 입력 안에서 Enter 제출을 검증하려면 대화상자 전용
  capture listener가 필요하다. 같은 패턴은 암호 대화상자에 이미 있다.

### 보기 설정 적용

- 현재 확인 callback은 쪽 이동·쪽 배치를 저장하고 `page-view-settings-changed`를 먼저 발행한 뒤
  `ViewportManager.setZoom()`을 호출한다.
- 첫 이벤트에서 `CanvasView.setPageViewSettings()`가 이전 배율로 `recalcLayout()`과 visible page render를
  시작하고, 뒤의 `zoom-changed`에서 `onZoomChanged()`가 최종 배율로 다시 레이아웃·Canvas 해제·렌더한다.
- `zoom-changed`는 CanvasView뿐 아니라 입력기의 커서·선택 핸들과 눈금자도 소비하므로 이벤트 자체를
  없애거나 최종 레이아웃 뒤에 임의로 미루면 별도 회귀가 생길 수 있다.
- `EventBus.emit()`은 동기 배달이다. 따라서 한 `page-view-settings-changed` transaction 안에서 표준
  `zoom-changed`는 한 번 전달하되, CanvasView 자신의 중첩 zoom handler만 억제하고 transaction method가
  최종 배치·배율로 레이아웃과 렌더를 한 번 수행할 수 있다.
- 사용자 설정은 현재 쪽 이동과 쪽 배치를 각각 저장한다. 두 값을 먼저 정규화해 한 번에 기록하는 API가
  있으면 localStorage에도 중간 조합을 남기지 않는다.

## 목표

1. 사용자 정의 배율을 제출 전에 검증하고 잘못된 입력을 대화상자 안에서 설명한다.
2. invalid 입력에서는 callback·설정 저장·보기 이벤트를 실행하지 않고 대화상자와 입력값을 유지한다.
3. 쪽 배치·쪽 이동·최종 배율·맞춤 모드를 하나의 보기 설정 transaction으로 전달한다.
4. 표준 zoom 이벤트 소비자는 유지하면서 CanvasView의 중간 이전-배율 레이아웃·렌더를 제거한다.
5. 마우스와 키보드 확인·취소, 오류 포커스와 접근성 상태를 실제 브라우저에서 검증한다.

## 범위

### 포함

- 사용자 정의 배율의 빈 값·비숫자·10% 미만·500% 초과 제출 차단
- 정수 백분율 10~500의 정확한 적용과 기존 preset·폭 맞춤·쪽 맞춤 유지
- 오류 메시지, `role="alert"`, `aria-invalid`, `aria-describedby`, 오류 입력 focus/select
- 유효한 값으로 고친 뒤 오류 상태 해제
- 사용자 정의 입력 안의 Enter 제출, Escape·취소 버튼의 무변경 종료
- 쪽 배치·쪽 이동을 한 번 정규화하고 localStorage에 한 번 저장하는 사용자 설정 API
- `page-view-settings-changed` payload에 최종 zoom·fit mode·anchor를 포함하는 transaction 계약
- transaction 중 표준 `zoom-changed`를 한 번 발행하면서 CanvasView의 중첩 zoom layout만 억제
- 최종 배치·배율 기준 `recalcLayout()`·앵커 복원·Canvas 해제·visible render 각 한 경로

### 제외

- 슬라이더·핀치 preview/settle 성능 정책: #6040
- 적응형 Canvas render scale과 surface 예산: #6041
- 페이지 LRU·행 가상화·prefetch scheduler: #6042
- 쪽 배치별 맞춤 계산: stack base #6108
- 반응형 눈금자 표시 정책과 resize 깜빡임: #6187
- 여러 쪽 가로·세로 개수 입력의 별도 오류 UI: #6109 수용 기준은 사용자 배율에 한정

## 단계와 산출물

1. **Stage 1 — 사용자 배율 검증·접근성 계약**
   - 순수 validator와 오류 종류를 정의하고 대화상자 제출 차단·오류 표시·포커스·키보드 처리를 구현한다.
   - 완료 기록: `mydocs/working/task_m100_6109_stage1.md`
2. **Stage 2 — 원자 보기 설정 transaction**
   - 사용자 설정 저장과 event payload를 원자화하고 CanvasView가 최종 상태로 한 번만 레이아웃·렌더하게 한다.
   - 완료 기록: `mydocs/working/task_m100_6109_stage2.md`
3. **Stage 3 — 통합·실제 브라우저 회귀**
   - invalid/valid/Enter/Escape/cancel과 배치+배율 동시 변경의 이벤트·레이아웃 횟수를 실제 Chrome에서 검증한다.
   - 최종 보고서: `mydocs/report/task_m100_6109_report.md`

각 Stage는 focused 검증과 결과 보고 뒤 작업지시자의 결과 승인을 받고 다음 Stage로 이동한다.

## 검증 계획

- `node --test`로 `zoom-dialog`, `zoom-dialog-integration`, `user-settings`,
  `canvas-view-page-arrangement`, `viewport-manager-smooth-zoom` focused test 실행
- `(cd rhwp-studio && npx tsc --noEmit)`
- `npm --prefix rhwp-studio test`
- `npm --prefix rhwp-studio run build`
- 실제 Chrome에서 invalid 네 종류, 오류 접근성·포커스, Enter/Escape/cancel, valid 경계값을 검증
- 실제 Chrome에서 배치+배율 동시 변경의 `page-view-settings-changed`·`zoom-changed`·`recalcLayout`
  호출이 각각 한 final transaction인지 검증
- `cargo fmt --all`
- `cargo fmt --all -- --check`
- `git diff --check`

Studio 화면 전용 TypeScript 변경이므로 Rust release-test·Native Skia·WASM 전체 빌드는 기본 범위에서
제외한다. 렌더 엔진 출력은 바꾸지 않으므로 PDF/SVG visual sweep 대신 실제 browser 상호작용과
transaction 계측을 사용한다.

## 위험과 완화

| 위험 | 완화 |
| --- | --- |
| 표준 `zoom-changed`를 억제해 눈금자·커서가 갱신되지 않음 | 이벤트는 한 번 그대로 발행하고 CanvasView 자신의 중첩 handler만 동기 guard로 건너뛴다. |
| 배치만 바꾸는 기존 이벤트가 깨짐 | zoom fields를 선택적으로 해석하고 기존 payload는 기존 배치 전환 계약으로 유지한다. |
| transaction 중 예외로 guard가 남음 | guard 설정과 해제를 `try/finally`로 묶고 이후 독립 zoom이 항상 정상 경로를 타게 테스트한다. |
| 같은 배율인데도 Canvas를 불필요하게 해제 | 숫자 배율·토폴로지 변화 여부를 분리하고 실제 해상도 변화 또는 토폴로지 변화일 때만 해제한다. |
| 오류를 고친 뒤에도 스크린리더가 이전 오류를 읽음 | 유효 입력 시 `aria-invalid`와 alert 내용을 함께 초기화하고 연결 ID는 안정적으로 유지한다. |
| Enter handler가 편집기 단축키로 샘 | ModalDialog와 같은 document capture 경계에서 처리하고 hide 시 반드시 제거한다. |
| #6108 범위를 top PR에서 중복 수정 | 맞춤 수학은 건드리지 않고 #6108 resolver의 최종 결과를 transaction payload로 전달한다. |

## 승인 게이트

1. 작업지시자가 수행 계획과 구현 계획을 승인한다.
2. 승인 뒤 Stage 1을 구현·검증하고 결과 승인을 기다린다.
3. Stage 1 승인 뒤 Stage 2를 구현·검증하고 결과 승인을 기다린다.
4. Stage 2 승인 뒤 Stage 3 통합 검증 범위를 다시 확인하고 실행한다.
5. 최종 결과 승인 뒤 로컬 commit과 stacked PR 본문 초안을 준비한다.
6. remote push와 `gh stack submit`은 별도 게시 승인을 받는다.
