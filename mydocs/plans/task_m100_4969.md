# 수행계획 — Task M100 #4969 W10 vertical metrics·variation·고급 shaping 확장

- **이슈**: [#4969](https://github.com/edwardkim/rhwp/issues/4969)
- **상위 tracker**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행 완료**: #5955 W7.5, #4967 W8, #4968 W9
- **브랜치·worktree**: `task_m100_4969_w10_q2_d5`,
  `/home/edward/mygithub/rhwp-worktrees/task_m100_4969_w10_q2_d5`
- **기준 commit**: `upstream/devel` `6415047a4dfdb71fe96239eced0017559f699c81`
- **작성일**: 2026-08-27 KST
- **원격 상태**: #4969 OPEN, 담당자 `edwardkim`, milestone `v1.0.0`, 연결된 열린 PR 없음
- **절차 상태**: W10-Q0·Q1 완료, Q2 `bounded-subset` 최종 판정 승인; Q3-A checkpoint
  `1075f5dce`, Q3-B checkpoint `6a156ca98`, Q3-C replay payload·backend capability shadow `qualified` 결과 승인·
  checkpoint `0a248f3b8`; 최신 devel merge `a8ff6a19d` 후 재자격화 결과 승인·증적 checkpoint 완료;
  Q3-D explicit request owner·dormant transaction `qualified-dormant` 결과 승인·checkpoint `558e27f6d` 완료;
  Q3-E 수정 수행계획·explicit-only candidate 정책 정정 승인; E0 `qualified-red-baseline` 결과 승인·checkpoint 완료;
  최신 devel merge `b5fbed37c` 완료; E0 `qualified-red-baseline-reconfirmed` 재자격화 결과 승인,
  증적 checkpoint `8a35f820b` 완료; PDF 정답지 갱신 최신 devel merge `351817b21` 완료;
  Q3-E1 reversible native owner·strict native contract `qualified-dormant-reversible` 결과 승인·checkpoint 완료;
  Q3-E2 public WASM adapter `qualified-public-dormant` 결과 승인·checkpoint 완료;
  Q3-E3~E5 bounded atomic activation·CanvasKit publication·성능·전체 회귀 `qualified` 결과 승인,
  checkpoint `4e0a3822c` 완료; Q4 인수 감사·수정 수행계획 승인,
  인수·계획 checkpoint `73fe7768e` 완료; Q4-A exact vertical oracle `qualified`,
  결과 checkpoint 후 Q4-B 진입

## 1. 목표와 우선순위

W9는 exact font source의 GPOS `kern` pair adjustment를 공통 layout에서 계산해 native·WASM·backend가 같은
위치를 재생하도록 연결했다. 그러나 GSUB로 glyph identity가 달라지는 문자열, non-LTR direction, vertical
writing과 variation instance는 의도적으로 fail-closed했다.

W10의 목표는 이 미지원 입력을 한꺼번에 허용하는 것이 아니다. exact font bytes·face index·script·language·
feature·axis를 기능 탐지하고, 공통 shaping owner가 glyph id·cluster·advance·offset을 한 번 결정한 뒤 backend가
같은 결과를 재생할 수 있는 축만 단계적으로 연다.

우선순위는 다음과 같다.

1. 기존 horizontal K0·K1 보호 계약과 현재 structured rejection을 기준선으로 동결한다.
2. GSUB/GPOS horizontal shaping을 먼저 연결해 W9의 nominal-glyph 제한을 제거한다.
3. 같은 shaping owner에 bounded variation instance를 연결한다.
4. GSUB `vert`/`vrt2`, `vhea`·`vmtx`와 필요 시 `VORG`를 사용하는 vertical shaping을 마지막에 연다.

vertical부터 backend별 회전으로 근사하거나 variable font를 family 이름만으로 생성하지 않는다. 두 경로 모두
공통 glyph shaping과 exact source provenance가 선행돼야 하기 때문이다.

## 2. 현행 구조와 원인 계보

이미 존재하는 schema·보호 경계는 다음과 같다.

- `ShapeKey`: font instance, direction, writing mode, script, language, OpenType feature, shaping engine,
  fallback policy를 보존한다.
- `FontInstanceKey`: exact face key, size, variation axes, synthetic style을 보존한다.
- portable GlyphRun: glyph id, position, advance, cluster와 replay eligibility를 표현한다.
- CanvasKit·native Skia·text-v2: non-LTR, vertical writing, variation instance를 현재 구조화된 reason으로
  fail-closed한다.
- W9 `renderer/kerning.rs`: exact source와 rustybuzz를 사용하지만 nominal glyph·cluster identity가 유지되는
  pair positioning만 layout에 투영한다.

따라서 핵심 결손은 schema 필드가 아니라 다음 연결이다.

```text
HWP/HWPX 문서 의도 또는 명시적 API 요청
  -> script·language·direction·writing mode·axis의 권위 판정
  -> exact font bytes + face index
  -> bounded common shaping decision
  -> measurement·line break·glyph positions의 단일 소비
  -> SVG·Canvas2D·CanvasKit·native Skia portable replay
```

backend가 독립적으로 shaping하면 measurement와 paint가 발산한다. 반대로 shape metadata만 채우고 실제 layout
advance를 기존 문자별 hmtx 합으로 남겨도 줄바꿈과 paint가 달라진다. 공통 shaping decision이 measurement와
replay 양쪽의 단일 권위여야 한다.

## 3. 공개 fixture face 집합과 동결 근거

기존 저장소의 추적·배포 font만 사용해 세 축을 분리한다.

| 축 | 공개 face | SHA-256 | table·axis |
| --- | --- | --- | --- |
| horizontal/vertical | `ttfs/opensource/NotoSansKR-Regular.ttf` | `6e06a7fe5d696ca719894a23f36bb2b1be8c816a5937cd4ad0f23ca67780dd74` | GPOS, GSUB, vhea, vmtx |
| old Hangul/vertical | `assets/fonts/SourceHanSerifK-OldHangul-subset.woff2` | `9e419cd16df2ea3b220aa7751320d956ac2493440ba412484d98325078f09d43` | CFF, GPOS, GSUB, vhea, vmtx, VORG |
| variation | `assets/fonts/HappinessSansVF.woff2` | `cd29de60acf8c466938400561fcbfb14812dd5981f4f78b0c8da74609682423c` | GPOS, GSUB, fvar, gvar; `wght`·`opsz` 400..900 |

세 face는 이미 저장소에 추적되고 font registry v2와 제3자 라이선스 원장에 포함된다. W8에서 교정한 rank
1·7·8 face와 겹치지 않으므로 W8 제품 correction과 W10 shaping 변화를 섞지 않는다. Q0에서 registry digest,
face index와 source URL을 다시 대사한 뒤 이 집합을 동결한다.

Q0 source readiness 감사에서 Noto TTF만 현행 rustybuzz 경로가 직접 parse했다. Source Han과 Happiness의
추적 자산은 WOFF2이므로 identity는 동결하되 source container는 미동결로 판정했다. 이후 공식 Happiness Sans
화면용 archive에 `HappinessSansVF.ttf`가 별도 수록된 것을 확인했다. 수정 불가로 기록된 Happiness WOFF2를
변환하지 않고 이 공식 TTF를 byte 그대로 쓰며, OFL인 Source Han만 OTF 파생물을 생성하는 수정 A안을 Q1
진입 결정으로 제시한다. 승인 전에는 어느 binary도 추가하지 않는다.

## 4. #4960 tracker 현행화

#4960 본문의 W0~W9 checklist는 현재 상태와 맞지만, W7.5·W8·W9의 실제 병합 결과와 W10 착수 상태가
요약되지 않았다. W10-Q0에서 다음 내용을 현행화한다.

| 단계 | 완료 근거 |
| --- | --- |
| W7.5 | PR #6049, merge `242c104bd686928844d2444ce84e4f48f861b2e2` |
| W8 | PR #6069·#6081·#6106, rank 8·1·7 모두 `no-change` disposition |
| W9 | PR #6214, merge `6415047a4dfdb71fe96239eced0017559f699c81`, exact kerning qualified |
| W10 | #4969 계획 승인 뒤 진행 중으로 표시하고 세 실행 lane과 release 전 완료 gate 연결 |

tracker의 historical W0~W7 수치·hash는 다시 쓰지 않는다. #4960은 W10 완료·릴리즈 인계 확인 전까지 OPEN으로
유지한다. GitHub 본문과 comment는 초안을 대사한 뒤 별도 승인받아 `gh issue edit/comment`로 적용하고 즉시
API로 재조회한다.

## 5. 범위

### 범위 안

- exact source 기반 horizontal GSUB/GPOS shaping과 glyph cluster/position 결정
- `vhea`·`vmtx`, 필요 시 CFF vertical origin을 위한 `VORG` feature detection
- `fvar` axis range·default와 선택 instance의 canonicalization, identity·cache key·trace 반영
- script·language·direction·feature와 writing mode의 요청·capability·disposition 계약
- 공개 horizontal complex/old Hangul, variable instance, vertical fixture
- measurement·line break·layout positions와 portable GlyphRun의 단일 shaping 결과 공유
- native·Docker WASM·SVG·Canvas2D·CanvasKit·native Skia parity
- #4960 tracker와 원인 계보·최종 보고서 현행화

### 범위 밖

- fallback·metric registry 규칙 또는 W8 face correction 변경
- 모든 OpenType script·feature·axis의 일괄 지원
- source bytes가 없는 설치 font를 family 이름으로 추정해 portable shaping
- backend가 독자적으로 text를 다시 shaping하는 경로
- HWP/HWPX에 존재하지 않는 variation request를 임의로 생성하는 parser 휴리스틱
- private corpus 원본·문서명·경로·본문·개별 hash 공개
- #6243·#6215 CI 운영 변경과 같은 PR에 혼합
- commit·push·PR·merge·issue close·release의 자동 수행

## 6. 보호 불변식

1. 기존 horizontal kerning-off와 W9 K1 fixture의 measurement·line break·paint output은 byte 또는 canonical
   identity를 유지한다.
2. exact font bytes·face index와 glyph coverage를 증명하지 못하면 기존 TextRun 또는 K0로 fail-closed한다.
3. shaping은 공통 layout owner가 한 번 수행하고 backend는 published glyph result만 재생한다.
4. GSUB glyph 수 변화와 cluster mapping을 원문 char index로 억지 1:1 투영하지 않는다.
5. direction·script·language·feature·writing mode·variation instance는 cache key와 trace에서 누락되지 않는다.
6. axis tag 중복, NaN·Infinity, range 밖 값은 canonical clamp로 숨기지 않고 정책에 따른 구조화 reason으로
   거부한다. 지원하기로 승인한 축만 명시적 범위 검증 뒤 적용한다.
7. vertical advance·origin이 없으면 horizontal advance를 무조건 회전해 대체하지 않는다.
8. stored LineSeg의 존재나 HWP/HWPX version을 capability로 사용하지 않고 현재 객체·table·glyph 결과를
   feature detection한다.
9. font 32 MiB, text/glyph/position 4,096, cluster·feature·axis 수에 bounded 상한을 유지하고 새 무제한 clone·
   serialization을 만들지 않는다.
10. source font bytes·개인 font 경로·private corpus identity를 render tree·trace·artifact에 넣지 않는다.

## 7. 단계별 수행계획

### Stage W10-Q0 — tracker·선행 증거·무변화 기준선 동결

1. #4960을 W7.5·W8·W9 실제 merge 결과와 W10 착수 상태로 현행화할 초안을 만든다.
2. W9 최종 report·public fixture digest·K0/K1 native/WASM baseline을 다시 대사한다.
3. 위 세 공개 face의 digest, table, axis, registry v2 identity와 W8 겹침 0을 기계 판독 가능한 inventory로
   동결한다.
4. W3 journal schema에 direction·vertical·script·variation을 판정할 기존 차원이 있는지 먼저 감사한다.
   없는 차원 때문에 prevalence가 꼭 필요한 경우에만 신규 계측의 목적·비용·privacy를 별도 수정 계획으로
   제시한다. Q0에서는 10k 전수 재계측을 하지 않는다.
5. 현재 structured rejection reason과 horizontal output을 native·Docker WASM에서 기준선으로 고정한다.

**종료 게이트**: tracker 현행화 초안, face inventory, 재계측 필요성 판정과 기존 output 기준선이 승인된다.
제품 source는 이 단계에서 바꾸지 않는다.

### Stage W10-Q1 — shaping capability·request·trace 계약

1. 요청을 direction, writing mode, script, language, ordered feature, variation instance로 정규화한다.
2. capability를 source/table/glyph/backend별로 분리하고 `requested`, `applied`, `unsupported`, `malformed`,
   `bounded-limit`, `non-portable` disposition을 고정한다.
3. rustybuzz 결과의 glyph id, cluster, x/y advance, x/y offset과 source range 계약을 공개 fixture로 동결한다.
4. malformed table, missing vmtx, unsupported axis, out-of-range/duplicate axis, cluster overflow의 음성 fixture를
   결정적으로 생성한다.
5. `ShapeKey`·FontInstance·diagnostics schema의 기존 필드로 충분한지 판정하고, 새 필드는 실제 계보 결손이
   증명된 경우만 추가한다.

**종료 게이트**: 세 축이 같은 요청·capability·disposition vocabulary를 사용하며 제품 동작 변경 없이
fixture oracle이 결정적으로 재현된다.

### Stage W10-Q2 — horizontal GSUB/GPOS 공통 shaping

1. W9 nominal glyph gate를 대체할 bounded horizontal shaping session을 exact source에 연결한다.
2. glyph count가 바뀌는 GSUB, mark positioning, old Hangul cluster와 LTR/RTL direction을 별도 fixture로 검증한다.
3. measurement, line breaking, layout positions, bbox와 다음 run origin이 같은 glyph advances를 소비하게 한다.
4. SVG·Canvas2D·CanvasKit·native Skia가 backend shaping 없이 glyph id·positions를 재생하게 한다.
5. existing kerning-only path는 같은 owner의 subset 또는 무회귀 control로 유지하고 이중 GPOS 적용을 금지한다.

**종료 게이트**: horizontal fixture에서 native/WASM glyph·cluster·position parity mismatch 0, K0·W9 회귀 0,
unsupported direction/script는 구조화 fail-closed한다.

**최종 결과**: Q2-A~D5의 owner·atomic publication·backend·resource·성능 증거를 종합해
[`bounded-subset`](../working/task_m100_4969_w10_q2_final_classification.md)으로 판정했다. exact portable
old-Hangul의 one-line/one-run horizontal LTR stored-boundary lane과 좁은 NO_LS atomic lane만 활성 지원한다.
RTL·Latin default liga 일반화·multi-line/run·variation·vertical은 지원 범위에 포함하지 않는다.

### Stage W10-Q3 — variable font instance

1. `HappinessSansVF`의 `wght`·`opsz` default/boundary/interior instance를 exact source에서 shaping한다.
2. axis ordering과 float canonicalization을 고정해 동일 instance가 같은 identity·cache key를 갖게 한다.
3. glyph outline·advance·position이 달라지는 instance를 native/WASM과 backend replay에서 검증한다.
4. CanvasKit·native Skia typeface construction이 exact instance를 증명하지 못하면 portable GlyphRun outline 또는
   명시적 non-portable fallback으로 닫는다.
5. HWP/HWPX 문서가 axis 값을 직접 표현하지 않는 경우 이를 parser 추정으로 만들지 않고 editor/API 요청
   경계와 default instance만 명시한다.

**종료 게이트**: default instance는 기존 출력과 동일하고, 지원 axis instance는 결정적이며, 미지원·비정상
axis는 cache collision 없이 fail-closed한다.

**수정 계획**: capability 감사에서 official TTF와 standalone variation oracle은 준비됐지만 effective default
identity 중복, source-only mutable face의 cross-instance 누출, 실제 Q2 request/cache·lowerer·backend 연결 부재를
확인했다. [`Q3 수정 수행계획`](task_m100_4969_w10_q3.md)은 canonical instance red 계약부터 시작하며 explicit
opt-in request 외 parser·font-name·bold·ratio axis 추론을 금지한다.

Q3-A red 계약은 checkpoint `1075f5dce`로 고정했다. Q3-B는 `fvar` default canonicalization, instance-local
mutable face와 canonical result cache를 dormant transaction에 연결했고 checkpoint `6a156ca98`로 고정했다.
Q3-C는 canonical vector를 dormant GlyphRun과 producer-resolved outline proof에 연결해 native·Node WASM bbox
mismatch 0, Native Skia exact instance proof와 CanvasKit cache 미개방을 확인한
[`qualified` 결과](../working/task_m100_4969_w10_q3_c.md)를 승인받고 checkpoint `0a248f3b8`로 고정했다.
최신 `upstream/devel@955abb5268c3` 병합 뒤 focused·cross-impact·Native Skia·Studio·Docker WASM과 Node WASM을
재통과했다. 제품 caller·publication은 아직 0이다.

Q3-D는 exact slot별 canonical instance request를 소유하는 내부 CQRS command와 source/request generation-aware
dormant transaction을 연결했다. 공개 native/WASM adapter, parser·font/style heuristic, composer activation은
열지 않았으며 [`qualified-dormant` 결과](../working/task_m100_4969_w10_q3_d.md)를 승인받아 checkpoint로
고정했다. Q3-E 수정 수행계획 작성·검토와 착수는 각각 별도 승인 경계다.

Q3-E는 [`bounded atomic activation 수정 수행계획`](task_m100_4969_w10_q3_e.md)에서 public request,
composer activation, portable publication, backend selection을 여섯 절편으로 분리했다. 최초 visible activation은
producer-resolved `GlyphOutline`을 재생할 수 있는 CanvasKit Browser로 제한하고 Native Skia·SVG·Canvas2D는
`TextRun` fallback을 유지한다. 최초 계획 승인 뒤 E0 사전 감사에서 Q2 gate가 old Hangul 전용임을 확인했으므로,
request 존재 뒤에만 modern Hangul·Latin을 평가하는 explicit-only 정책의 정정 승인 전에는 red baseline을 시작하지 않는다.

Q3-E3~E5는 explicit request만 bounded transaction에 연결하고, producer-resolved outline을
CanvasKit에만 원자적으로 게시했다. no-request/default output·resource·backend selection을 보존했고,
성능·WASM 크기·Native Skia·Studio·CanvasKit pixel·전체 nextest의 승인된 게이트를 통과해
[`qualified`](../working/task_m100_4969_w10_q3_e5.md)로 판정했다. Q4는 인수 감사에서 exact vertical output
oracle 부재와 표면별 vertical intent 의미 손실을 확인했으므로
[`Q4 수정 수행계획`](task_m100_4969_w10_q4.md)의 별도 승인 전에 제품 source를 변경하지 않는다.

Q4-A는 공개 exact TTF/OTF의 vertical glyph·cluster·x/y position을 native/WASM oracle로 고정했고,
Q4-B는 OWPML 스키마와 한컴 HWPX↔HWP5 쌍으로 `VERTICALALL == HWP5 code 2 == Latin upright`를
판정했다. 표면별 typed contract와 HWP5/HWPX 왕복·legacy SVG control은 통과했지만 아직 제품
parser·IR·layout consumer는 열지 않았다. Q4-C는 exact glyph origin·bbox·next origin을 단일 dormant
transaction으로 결합하고 native/WASM에서 재현했으며, `qualified-dormant` 결과 승인을 받아 이 commit을
checkpoint으로 고정한다. Q4-D activation은 수정 계획 재검토와 별도 승인 전에는 시작하지 않는다.
Q4-D 구현 전 감사는 HWPX 표 셀의 `VERTICALALL` 의미 손실과 exact source·layout·publication·backend의
분리된 실패 경계를 확인했다. 최초 target을 HWP5 code 2 pure-CJK 한 셀로 제한한
[`Q4-D 수정 수행계획`](task_m100_4969_w10_q4_d.md)을 메인테이너가 승인했으며 제품 변경은 0이다.
계획 checkpoint `7b3a7bab4` 뒤 D0 red baseline을 실행했고, green 28/28과 계획한 미구현 경계 0/5를
분리한 `qualified-red-baseline` 결과를 메인테이너가 승인해 checkpoint `87d7a13a2`로 고정했다.
D1은 exact registry snapshot과 source handle·generation·동일 bytes `Arc`를 인증하는 dormant owner를
제품 module에 등록했다. native 30/30·격리 wasm32+Node 1/1·표준 Docker WASM build를 통과했고
layout·publication은 0인 `qualified-dormant-certified` 결과와 checkpoint 진행을 메인테이너가 승인했다.
checkpoint `d0948166a` 뒤 최신 `upstream/devel@d3b40a3d7`을 정상 병합하고 D1 native 30/30,
incoming #4121 Rust 7/7, Studio 1,314건·build, Docker WASM과 교차 guard를 재통과했다. D2는 별도 착수
승인 뒤 시작한다.

### Stage W10-Q4 — vertical metrics·writing mode

> **인수 감사 정정**: capability table 존재와 exact vertical output은 다르고, HWP5 0/1/2와
> HWPX `VERTICAL`/`VERTICALALL`의 표면별 의미가 현재 계약에서 일부 합쳐진다.
> 따라서 아래 원 범위는 [`Q4 수정 수행계획`](task_m100_4969_w10_q4.md)의
> Q4-A exact oracle → Q4-B typed intent → Q4-C dormant geometry → Q4-D bounded activation 순서로
> 수행한다. 수정 계획 승인 전에는 아래 항목을 바로 구현하지 않는다.

1. Noto Sans KR과 Source Han Serif K의 vhea/vmtx, 후자의 VORG를 읽어 vertical advance·origin source를 고정한다.
2. HWP/HWPX table cell·text box의 실제 text direction semantic을 parser/model/layout까지 추적한다.
3. `vert`/`vrt2` GSUB와 vertical GPOS를 writing mode·script·language feature detection으로 적용한다.
4. CJK upright, Latin rotate, punctuation substitution과 mixed run boundary를 공개 fixture로 분리한다.
5. line/column progression, bbox, hit-test에 영향을 주는 제품 범위는 renderer 출력과 editor 좌표 계약을
   분리해 검증한다. 범위가 독립 변경 이유를 가지면 같은 #4969 아래 별도 stage commit 또는 후속 issue로
   분리해 승인을 받는다.

**종료 게이트**: horizontal advance 회전 근사가 아니라 exact vertical metric과 substitution으로 positioning이
재현되며, source table이 없는 face는 silent fallback 없이 거부된다.

### Stage W10-Q5 — bounded 영향·backend parity·성능

1. 세 공개 fixture의 native·Docker WASM canonical glyph decision과 positions를 비교한다.
2. SVG·Canvas2D·CanvasKit·native Skia replay와 fallback reason을 전건 회계한다.
3. 동일 환경의 W9 baseline 대비 shaping call, glyph/cluster count, layout wall time, WASM size를 참고 측정한다.
4. 공개 fixture 외 prevalence가 필요한 경우 Q0에서 승인된 기존 aggregate projection 또는 bounded canary만
   사용한다. private identity는 비식별 aggregate로만 기록한다.
5. malformed·oversized·axis explosion·cluster explosion 입력의 상한과 fail-closed를 재검증한다.

**종료 게이트**: parity mismatch 0, 기존 horizontal non-target 회귀 0, 성능·size와 실패 회계가 확보된다.

### Stage W10-Q6 — 최종 판정·tracker 종료·릴리즈 인계

1. horizontal shaping, variation, vertical lane을 각각 `qualified`, `bounded-subset`, `blocked`로 판정한다.
2. 지원 table/script/language/axis/writing mode와 structured unsupported matrix를 최종 보고서에 기록한다.
3. #4960·#4969 본문과 comment에 실제 merge SHA·검증·잔여 범위를 현행화한다.
4. 제품 변경 PR의 최신 CI·CodeQL·Render Diff·Native Skia·Docker WASM·시각 증적을 self-review한다.
5. merge와 #4969·#4960 close는 별도 승인 뒤 수행한다. 일부 lane이 blocked면 완료한 것처럼 tracker를 닫지
   않고 후속 issue와 release 포함 여부를 명시한다.

**완료 조건**: 공개 representative fixture에서 지원 subset이 결정적으로 재현되고 기존 가로 조판이 유지되며,
#4960 tracker가 W0~W10 실제 상태와 릴리즈 인계 근거를 반영한다.

## 8. 검증 계획

단계 focused test 뒤 PR 준비 승인 시 변경 범위별 전체 gate를 실행한다.

```bash
cargo fmt --all
cargo fmt --all -- --check
node scripts/rust-unit-test-tiers.mjs --check        # src/** cfg(test) 변경 시
node scripts/rust-test-suite-manifest.mjs --check   # review worktree prepare 뒤
cargo check --locked --all-targets
cargo clippy --locked --all-targets -- -D warnings
cargo nextest run --locked --cargo-profile release-test \
  --target-dir target/pr-review --tests --no-fail-fast
cargo test --locked --profile release-test --target-dir target/pr-review \
  --features native-skia --lib
docker compose --env-file .env.docker run --rm wasm
git diff --check
```

renderer/layout/WASM 변경이므로 공개 fixture의 SVG·paint JSON·portable GlyphRun, native Skia와 브라우저
CanvasKit 시각 대조를 함께 남긴다. Hyper-V HWP 2020 oracle은 HWP의 vertical semantic 또는 feature 적용 순서를
공개 자료만으로 판정할 수 없을 때 좁은 adjudication으로만 사용하며, variable axis 정답을 HWP 2020에서
추정하지 않는다.

## 9. 예상 위험과 분리 기준

| 위험 | 대응 |
| --- | --- |
| 세 축을 한 commit에 혼합 | Q2 horizontal, Q3 variation, Q4 vertical을 독립 승인·commit·rollback 경계로 둔다. |
| GSUB 뒤 source char 1:1 가정 | cluster source range를 권위로 사용하고 불완전 mapping은 run 전체 fail-closed한다. |
| backend별 재-shaping 발산 | layout의 published GlyphRun만 재생하고 backend shaping API 호출을 계약 테스트로 막는다. |
| variable cache collision | canonical axis vector를 font instance identity에 포함한다. |
| vertical 회전 근사 | vhea/vmtx/VORG와 vert/vrt2 capability가 없으면 지원하지 않는다. |
| 10k 재계측 반복 | 기존 W3 journal schema를 먼저 감사하고 새 계측은 근거·승인 없이는 실행하지 않는다. |
| 범위가 v1.0.0을 초과 | 각 lane을 bounded subset으로 판정하고 미지원 전체 확장은 후속 issue로 분리한다. |
| #4960이 다시 낡음 | Q0 착수, stage 완료, merge·close마다 tracker를 live 상태와 함께 갱신한다. |
