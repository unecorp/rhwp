# 수행계획 — Task M100 #4962 W3

- **이슈**: [#4962](https://github.com/edwardkim/rhwp/issues/4962)
- **상위 추적**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행 작업**: [#4939](https://github.com/edwardkim/rhwp/issues/4939) W0·W1,
  [#4961](https://github.com/edwardkim/rhwp/issues/4961) W2 완료
- **후행 작업**: 같은 이슈의 [W4 조판 위험 순위](task_m100_4962_w4.md), 이후
  [#4963](https://github.com/edwardkim/rhwp/issues/4963) W5 Oracle Profile
- **브랜치**: `task_m100_4962`
- **작업공간**: `/home/edward/mygithub/rhwp`의 현재 checkout
- **기준 commit**: `upstream/devel` `fb434269eea237cc12053914560a2dbaf16270bf`
- **작성일**: 2026-08-21 KST
- **원격 상태**: #4962 OPEN, 담당자 `edwardkim`, milestone `v1.0.0`, labels
  `performance`·`rendering`·`test`, 연결된 Open PR 없음
- **절차 상태**: Stage 4-D private 10k 결정성 반복 완료, W3 종료·W4 별도 계획 작성

## 1. 목표

실제 renderer가 사용한 각 문자를 기준으로 font metric coverage를 다시 측정한다. 선언된 font family의
존재 여부가 아니라 다음 질문에 재현 가능한 수치로 답해야 한다.

1. 실제 사용 문자는 exact metric, 검증된 identity alias, measured overlay, metric surrogate 중 무엇을
   거쳐 advance를 얻었는가?
2. metric face는 있으나 해당 문자가 없는 `char-miss`, face 자체가 없는 `face-miss`, 그리고 최종
   heuristic은 각각 얼마인가?
3. 이 결과가 HWP/HWPX, 언어 slot, bold·italic, 장평·자간, 고정 frame과 stored/fresh LineSeg
   cohort에서 어떻게 달라지는가?

W3의 결과는 기존 `metric mapped 91.16%`를 문자별 renderer decision으로 보강한 coverage 기준선이다.
기존 POC의 유효한 편집 습관·실사용 aggregate는 폐기하거나 다시 만들지 않는다. 이 단계에서는 조판
위험 순위를 확정하거나 metric·fallback·font asset을 바꾸지 않는다.

## 2. 착수 기준선과 이미 확보된 산출물

### 2.1 W0·W1

PR #4950의 W0·W1은 30개 source boundary에서 candidate 1,352개와 ledger rule 1,507개를 고정했다.
근거가 부족한 44개 규칙은 `unknown`으로 보존했고, 검증된 `identity-alias`는 아직 0개다.

W3는 다음 산출물을 읽기 전용 근거로 사용한다.

- `mydocs/tech/investigations/issue-4939/font_rule_ledger.json`
- `mydocs/tech/investigations/issue-4939/font_rule_candidates.json`
- W0 baseline과 validator

### 2.2 W2

PR #5122는 실제 문자 결정 계보를 `FontDecisionTrace` v1로 노출했다.

```text
document face / language slot / altType / substFont
  → normalized layout name
  → metric alias / lookup rung / character hit·miss
  → overlay·heuristic / 장평·자간·extra advance
  → backend paint observation
  → W1 candidateId·ruleId·evidence
```

W3는 이 결정 모델과 실제 layout의 공통 `CharWidthDecision`을 사용한다. 별도 font lookup을 복제해
trace와 다른 답을 만드는 계측기는 허용하지 않는다.

### 2.3 private corpus

로컬 읽기 전용 corpus 기준선은 HWP 6,582건과 HWPX 3,418건, 합계 10,000건이다. 기존 POC는
9,948건에서 54,938,759자와 3,776,306문단을 집계했다.

착수 재감사에서 다음 로컬 gitignored 산출물이 실제로 남아 있음을 확인했다.

| 산출물 | 상태와 역할 |
| --- | --- |
| `output/poc/font-layout-habits/summary-10k-v2.json` | 전체 10k v2 aggregate, 약 23MB |
| `summary-hwp-v2.json` / `summary-hwpx-v2.json` | 포맷별 aggregate, 모든 totals가 전체 결과에 가산 일치 |
| `smoke-20.json` / `smoke-hwpx.json` | 소표본 실행 결과 |
| `briefing-20260816.md` | POC 방법·수치·한계와 후속 제안 |
| commit `631287d4708f144011162179d61f8272cf072ff6` | `examples/poc_font_layout_habits.rs` 생성 source |

v2 schema에는 1,414개 선언 font, 실제 사용 407개 font와 57,395개 usage row가 있다. usage row는
`font`, face-level `metricFace`, language, ratio, spacing, kerning, bold·italic, context, alignment,
stored LineSeg 존재 여부와 문서·문단·run·문자 수를 보존한다. font와 usage의 문자 합은 모두
54,938,759자로 일치하고 HWP·HWPX totals의 합도 전체 totals와 일치했다.

POC 브리핑에는 `elapsedMillis` 제외 정규화 SHA-256을 두 번 재현했다고 기록돼 있다. 현재 작업공간에는
완전한 v2 전체 artifact가 한 벌만 남아 있어 두 원본을 직접 다시 대조할 수는 없다. 이 사실은 기존
aggregate를 무효화하는 이유가 아니라 W3의 새 delta aggregate에 독립 결정성 검증이 필요한 이유다.

### 2.4 기존 계측 자산 재사용 판정

| W3 입력 축 | 기존 자료 | 판정 |
| --- | --- | --- |
| corpus 구성·파싱 성공/실패 | 전체·HWP·HWPX v2 | 그대로 재사용 |
| 실제 사용 font·문서·문단·run·문자 수 | `fonts[]`, `usage[]` | 그대로 재사용 |
| language, bold·italic, 장평·자간, kerning | `usage[]` | 그대로 재사용 |
| 본문·표 셀·글상자 등 context와 고정 frame | `usage[]`, totals, local risk records | 그대로 재사용 |
| stored LineSeg 존재/누락 | `usage[].storedLineSeg`, totals | 존재 여부 기준선으로 재사용 |
| 선언 font와 웹 공급 가능성 | 2026-08-15 font/jsDelivr survey와 TSV | 보조 공급 축으로 재사용 |
| 쪽수·PI·픽셀·오라클 함정 | `survey_10k_r5`~`r31` | W4·회귀 검증 방법으로 재사용 |
| face-level metric 연결 91.16% | v2 `metricFace` | historical 대조값으로만 유지 |
| 문자 code point·character별 metric hit/miss | 없음 | delta 계측 필요 |
| `altType`·`substFont`·normalized layout face | 없음 | W2 decision 기반 delta 계측 필요 |
| identity alias·overlay·metric surrogate provenance | 없음 | W1 ledger와 W2 trace 결합 필요 |
| LineSeg 유효성·fresh layout 경계 | 존재 여부만 있음 | 별도 feature-detection cohort 필요 |
| native·Canvas2D·CanvasKit 실제 관찰 | 없음 | 준비된 snapshot cohort 필요 |

기존 POC 생성기는 `Paragraph::char_shapes`를 재귀 순회하는 구조와 분모·문맥 계산을 이미 구현했다.
W3에서 이를 새로 작성하지 않고 해당 commit의 source와 v2 schema를 기준선으로 가져와 현재 모델에 맞게
최소 보정한다. raw 식별 필드를 그대로 공개 산출물로 승격하지 않는다.

### 2.5 추가 전수 계측의 정당성 경계

현재 v2 aggregate만으로 7개 coverage 분류를 역산할 수 없다. 결정적인 결손은 다음과 같다.

1. usage row에 code point 분포가 없어 face가 존재해도 특정 문자가 없는 `char-miss`를 계산할 수 없다.
2. `metricFace`는 원시 font 이름에 `layout_metric_face_name()`을 직접 호출한 결과라 `altType`, 언어별
   `substFont`, style normalization과 W2 resolver 계보를 거치지 않는다.
3. space·구두점·KoPub 등 문자별 `widthSource`가 없어 exact hit와 measured overlay를 분리할 수 없다.
4. W1 relation이 없어 identity alias와 metric surrogate를 일반 alias·fallback에서 분리할 수 없다.
5. stored LineSeg는 존재 여부 boolean뿐이며 유효성이나 fresh layout과의 경계 차이가 없다.
6. POC 기준 commit `44125461187c158073daf5b6b317b08042e7332a` 이후 현재 `devel`까지 642개
   commit이 있으며, model·parser·style resolver·metric·text measurement 관련 commit도 51개다.

따라서 **문자별 coverage를 얻기 위한 corpus 1회 delta read는 필요하다.** 그러나 다음 작업은 정당화되지
않으며 기본 계획에서 제외한다.

- 이미 확보된 장평·자간·문맥·font 사용 통계를 처음부터 다시 설계하거나 새 기준선으로 덮어쓰기
- 모든 page의 raw W2 trace 저장
- 10k 전체에 native·브라우저 paint snapshot과 fresh layout을 무조건 실행
- 기존 POC와 같은 face-level 91.16%를 다시 계산하는 것만으로 W3 완료 주장

새 delta pass는 기존 usage key를 유지하면서 character decision 분류만 추가하고, 기존 합계와의 차이를
parser·model drift audit로 보고한다. Stage 3 pilot에서 비용과 정확성이 입증되지 않으면 10k 전수 실행을
승인 요청하지 않는다.

## 3. 착수 조사에서 확인한 결손

### 3.1 W2 trace 상한

W2 공개 query는 page 한 개, 기본 1,024문자, hard maximum 4,096문자만 지원하고 offset·pagination이
없다. 현재 `rhwp-q-font-trace`도 기본 상한으로 한 page를 조회한다. 따라서 이를 그대로 반복 호출하면 긴
page의 뒤쪽 문자가 누락된다.

W3는 W2의 공개 진단 상한을 무제한으로 올리지 않는다. 동일한 내부 문자 결정 walker를 사용하는
streaming aggregate collector를 두어 전체 문자를 일정한 메모리로 한 번씩만 집계한다. 원시 문자 trace를
전수 보존하지 않는다.

### 3.2 source와 조판 문맥 join

W2 record는 장평·자간 transform을 제공하지만 고정 frame, stored LineSeg 유효성, fresh layout cohort를
완성된 W3 축으로 제공하지 않는다. 공개 fixture pilot에서도 `sourceMappingMismatch`가 관찰됐다.

W3는 source 좌표를 추정으로 보충하지 않는다. 다음 상태를 구분한다.

- `joined`: 문단·run·문자와 조판 문맥을 유일하게 연결함
- `layout-only`: metric 결정은 유효하지만 frame·LineSeg 문맥 join은 불완전함
- `excluded`: 문자 결정 자체를 만들 수 없거나 파싱에 실패함

coverage 분모와 조판 교차축 분모를 분리하고 각 상태의 수를 함께 대사한다.

### 3.3 backend observation

독립 `DocumentCore`·CLI 조회는 준비된 native/Studio renderer snapshot이 없으면 native Skia,
Canvas2D와 CanvasKit을 `unsupported`로 끝낸다. portable layout coverage에 이 상태를 metric miss로
섞지 않는다.

- corpus 전수: native/WASM 공통 portable layout metric decision
- backend 불일치: 준비된 native renderer 또는 Studio snapshot을 사용하는 별도 관찰 cohort

W3 보고서에는 두 분모를 분리한다. browser가 actual Canvas2D glyph face를 공개하지 않으면
`notObserved`를 face 선택 성공으로 승격하지 않는다.

### 3.4 ledger source drift

W2 trace는 W1 source digest가 trace-only refactor 이전이라는 `ledgerSourceDrift`를 이미 명시한다.
Stage 1에서 W1 collector·validator를 현재 source에 재실행하되 기존 artifact를 즉시 덮어쓰지 않는다.
candidate identity·relation·선택 순서가 달라졌는지 먼저 비교하고, digest만 달라진 경우와 의미 변경을
구분해 보고한다. 의미 변경이 발견되면 W3 구현을 중단하고 계획 변경 승인을 받는다.

## 4. 보호 불변식

[폰트 메트릭·fallback 원인 계보 보고서](../report/font_metrics_fallback_causal_lineage_20260816.md)의
FI-01~FI-14를 적용하며 다음 항목을 W3 hard gate로 둔다.

- metric 값, lookup 순서, fallback target, paint face, font asset과 기본 renderer output을 바꾸지 않는다.
- W2 trace와 W3 collector는 같은 실제 `CharWidthDecision`을 사용한다.
- layout metric face와 paint face를 같은 것으로 강제하지 않는다.
- HWP/HWPX 또는 producer build 번호로 정책을 분기하지 않고 현재 LineSeg 유효성과 객체 상태를
  feature detection한다.
- stored LineSeg와 fresh layout 결과를 같은 cohort나 hash에 섞지 않는다.
- `identity-alias`, document substitution, style fallback, official successor, metric surrogate,
  measured overlay, paint substitute와 heuristic을 서로 대체 가능한 이름으로 축약하지 않는다.
- 모든 성공 분류는 상호배타적이며 분류 합이 해당 분모와 정확히 일치해야 한다.
- parse 실패, page truncation, source join 실패와 backend 미관찰을 0이나 성공으로 가장하지 않는다.
- private corpus 원문·본문·식별 파일 목록·절대 경로·raw trace·재배포 불가 font bytes를 저장소,
  GitHub Issue, PR과 CI artifact에 넣지 않는다.
- default render 경로에는 collector·hash·파일 I/O 비용을 추가하지 않는다.

## 5. W3 분류 계약 후보

분류는 한 record가 둘 이상에 들어가지 않도록 아래 순서로 판정한다. Stage 1 RED test에서 실제 W1
relation과 W2 `widthSource` 조합을 전수 대조한 뒤 확정한다.

| 순서 | 분류 | 판정 후보 |
| ---: | --- | --- |
| 1 | `measured-overlay` | 검증된 overlay relation 또는 `metricSpaceOverlay`·구두점 overlay 등 measured/manual 폭 산출 |
| 2 | `identity-alias-hit` | `identity-alias` 근거가 verified이고 최종 metric character가 hit |
| 3 | `metric-surrogate` | 선택에 참여한 metric relation이 `metric-surrogate`이고 character가 hit |
| 4 | `exact-hit` | metric entry가 존재하고 character가 hit이며 위 관계에 해당하지 않음 |
| 5 | `char-miss` | metric entry는 있으나 `characterMatch=miss` |
| 6 | `face-miss` | 실제 해소된 layout face에 metric entry가 없음 |
| 7 | `heuristic` | 최종 advance의 폭 산출원이 heuristic 또는 metric 외 정책임 |

특수문자, cluster continuation, inline object, tab advance처럼 font metric coverage의 대상이 아닌 문자는
억지로 `heuristic`에 넣지 않는다. Stage 1에서 `not-applicable` 분모를 별도로 둘지, 전체 실제 사용 문자
분모 안의 명시적 하위 상태로 둘지 결정한다. 이 결정을 하지 않고 7개 분류 합계 완료 조건을 주장하지
않는다.

`matchKind=boldOnly` 또는 `nameFirst`는 곧바로 face miss가 아니다. 실제 선택된 metric entry와 style
fallback provenance를 함께 보고 exact hit의 하위 교차축으로 집계한다.

## 6. aggregate와 개인정보 계약

### 6.1 저장소에 남길 값

- 입력 형식별 문서·page·문단·문자 수
- parse·layout·join·backend 상태별 aggregate count
- 7개 coverage 분류와 확정된 non-applicable count
- language slot, bold·italic, 장평 bucket, 자간 bucket, 고정 frame, LineSeg 상태별 교차표
- 분류별 layout face aggregate와 W4 후보를 만들 수 있는 비식별 통계
- 도구·schema version, 기준 source commit, 결정론적 aggregate hash
- 반복 실행 간 count·hash 비교와 성능 요약

### 6.2 저장소에 남기지 않을 값

- 문서 원문과 추출 본문
- 파일명·상대/절대 경로·문서별 hash 목록
- 문서별 raw trace 또는 문자 위치 목록
- corpus directory 구조와 기관별 식별 가능한 실패 목록
- host 사용자명, 재배포 불가 font bytes와 로컬 font 경로

collector는 raw record를 메모리상에서 집계한 뒤 폐기한다. 실행 로그도 문서 경로나 원문을 포함하지 않는
요약 모드만 정식 증적으로 사용한다. 원인 진단에 식별 자료가 필요하면 로컬 임시 조사로만 사용하고
커밋 대상과 명시적으로 분리한다.

### 6.3 hash

aggregate hash는 정렬된 schema와 수치만 포함한다. 다음 값은 제외한다.

- 입력 경로와 파일 순회 순서
- timestamp, elapsed time, host·OS·사용자명
- 오류 stack과 원문 파생 문자열
- hash 필드 자체

face·format·language slot·style·조판 bucket의 key ordering을 고정하고 UTF-8 JSON canonical projection에
SHA-256을 적용한다. 같은 corpus와 source commit을 두 번 실행한 aggregate hash가 같아야 한다.

## 7. 구현 책임 경계 후보

정확한 파일 경계는 Stage 1 RED 결과에서 확정하되 책임은 다음처럼 분리한다.

| 책임 | 후보 경계 |
| --- | --- |
| W2와 공유하는 문자 결정 순회 | `src/document_core/queries/font_decision.rs`의 내부 walker/visitor 추출 |
| coverage 분류·streaming count·canonical projection | 별도 `font_metric_coverage` query 또는 analysis module |
| developer용 corpus 실행과 비식별 출력 | `scripts/`의 runner와 필요한 최소 read-only CLI surface |
| schema·공개 fixture·분류 golden | `mydocs/tech/investigations/issue-4962/` |
| Rust integration 원본 | `tests/cases/issue_4962_font_metric_coverage.rs` |
| Node runner·schema 계약 | `scripts/tests/`의 focused test |
| 단계 기록 | `mydocs/working/task_m100_4962_stage{N}.md` |
| W3 aggregate 회차 보고 | `mydocs/report/font_metric_coverage_r1_20260821.md` 또는 실제 완료일 |

새 공개 CLI나 WASM·npm API는 기본 계획에 포함하지 않는다. 기존 공개 trace schema에 pagination을
추가해야만 정확성을 보장할 수 있다고 판명되면 공개 API 변경으로 분류해 별도 승인받는다.

## 8. 수행 단계와 승인 게이트

### Stage 1 — 기존 자산 동결과 분류·분모·privacy 계약 RED

1. 기존 POC v2 schema·totals·usage key와 생성 source를 재사용 기준선으로 동결하고 무결성 검사를
   자동화한다.
2. W1 collector와 validator를 현재 source에서 재실행해 candidate identity, relation, source digest drift를
   비교한다.
3. W2 `widthSource`, `characterMatch`, `matchKind`, provenance relation의 가능한 조합을 inventory한다.
4. 7개 분류 우선순위와 non-applicable 처리, 전체·coverage·join·backend 분모를 machine-readable schema로
   고정한다.
5. 긴 page, source join 실패, parser 실패, unsupported backend와 raw path 유출을 실패시키는 RED test를
   만든다.
6. aggregate canonicalization과 반복 hash golden을 고정한다.

**종료 게이트:** 기존 POC에서 그대로 재사용할 필드와 새로 측정할 delta가 schema로 분리되고, 분류의
중복·누락이 test로 검출되며, W1 의미 drift 여부와 전수 계측 구현 경계가 설명된다. 결과를
`task_m100_4962_stage1.md`에 기록하고 메인테이너 승인 전 Stage 2로 가지 않는다.

**결과:** [Stage 1 기록](../working/task_m100_4962_stage1.md)에 기존 POC 비식별 projection,
W1 digest-only drift, 7개 분류·독립 분모·privacy·hash 계약과 focused 검증을 기록했다. Stage 2는
메인테이너 승인 전 시작하지 않는다.

### Stage 2 — 기존 POC walker를 확장한 decision delta collector

1. commit `631287d47`의 재귀 문단·context walker와 v2 usage key를 기준선으로 재사용한다.
2. W2와 같은 실제 문자 decision을 결합하고 lookup 사다리를 별도로 복제하지 않는다.
3. page 상한 없이 문자를 한 번씩만 streaming 집계하되 raw record를 저장하지 않는다.
4. 기존 usage aggregate에는 decision category와 필요한 provenance 차원만 추가한다.
5. 문단·frame·LineSeg source join을 추가하고 `joined`·`layout-only`·`excluded`를 구분한다.
6. parse·layout·join·분류 count reconciliation과 bounded memory를 검증한다.

**종료 게이트:** 공개 long-page fixture에서 truncation 없이 모든 문자가 한 번만 집계되고, 기존 v2
usage key의 projection이 POC와 일치하며, default renderer와 W2 trace 결과가 변하지 않는다. Stage 2
보고와 승인 뒤 pilot으로 간다.

**결과:** [Stage 2 기록](../working/task_m100_4962_stage2.md)에 기존 POC 재귀 walker·usage key·정렬을
유지한 decision delta collector, 분모 대사, 5,000자 공개 long-page와 W2 무변경 검증을 기록했다.
이후 보안 감사에서 무제한 성공 집계와 무제한 자원 사용을 분리해야 하는 결함을 발견해
[Stage 2-S 보안 보강](../working/task_m100_4962_stage2_security.md)을 완료했다. private pilot과 전수
코퍼스 실행은 수행하지 않았으며 Stage 3는 메인테이너 승인 전 시작하지 않는다.

### Stage 2-S — 무제한 성공 집계의 자원 고갈 방어

1. CharShape 경계마다 문단 전체를 재검색하는 경로를 단일 UTF-16 위치 병합 순회로 바꾼다.
2. work unit, deadline, cancellation, 중첩 깊이, aggregate row, 출력 byte 예산을 명시한다.
3. 자원 초과와 취소는 partial aggregate 없이 `resource-limit`·`cancelled` 문서 실패로 분리한다.
4. 긴 정상 문서는 상한으로 자른 성공이 아니라, 승인된 예산에서 전수 성공하거나 문서 전체가 실패한다.
5. corpus runner는 문서별 별도 process와 OS timeout·RSS 제한 없이는 실행할 수 없도록 Stage 3 선행
   조건으로 둔다.
6. 긴 문단, 다수 CharShape 경계, 낮은 work·row·output 예산과 사전 취소를 공격형 fixture로 검증한다.

**종료 게이트:** 정상 long-page는 truncation 없이 대사되고, 공격 입력은 전체 process를 고갈시키기 전에
명시적 오류로 종료되며, 부분 결과가 coverage 성공 분모에 섞이지 않는다. corpus worker 격리가 없는
상태에서는 private pilot을 시작하지 않는다.

**결과:** [Stage 2-S 기록](../working/task_m100_4962_stage2_security.md)에 `O(N+R)` 병합 walker,
결정적·시간·취소·중첩·row·output 예산, 실패 상태 확장과 공격형 integration 5건을 기록했다. corpus
worker isolation은 계약상 `required`이며 Stage 3 runner 구현·실행의 첫 번째 hard gate다.

### Stage 3 — 공개 fixture와 private 소규모 pilot

1. 문서별 별도 process, OS timeout·RSS 제한과 비식별 실패 봉투를 가진 corpus supervisor를 먼저
   구현하고 공개 fixture로 강제 종료·고립을 검증한다.
2. 공개 HWP/HWPX fixture로 exact, alias, overlay, surrogate, char miss, face miss, heuristic과
   non-applicable 경계를 고정한다.
3. 비공개 POC의 local risk ranking과 format·usage aggregate를 이용해 결정론적 소규모 cohort를
   재수집 없이 선정하고 로컬에서만 실행한다.
4. pilot의 기존 projection을 v2 usage·totals와 대조하고 새 decision delta만 별도 비교한다.
5. 두 번 실행해 delta aggregate hash와 모든 count가 일치하는지 확인한다.
6. 처리량, peak memory, parse/layout/join 실패율과 full 10k delta pass 예상 시간을 측정한다.
7. 로그와 산출물에 경로·원문·식별 목록이 없는지 검사한다.

**첫 번째 hard gate 결과:** [Stage 3-I 격리 기록](../working/task_m100_4962_stage3_isolation.md)에
문서별 worker process, Linux address-space·CPU hard limit, 부모 wall timeout·stdout budget,
process-group 강제 종료, 비식별 실패 봉투와 실패 후 계속 실행을 구현·검증했다. 공개 HWP/HWPX만
사용했으며 private corpus와 pilot은 실행하지 않았다.

**두 번째 hard gate 결과:** [Stage 3-F 공개 fixture 기록](../working/task_m100_4962_stage3_public_fixtures.md)에
공개 실문서 HWP/HWPX golden, 공개 blank 파생 경계 문서, 6개 현재 도달 가능 category와 5개
non-applicable width source를 고정했다. W1의 verified identity alias가 0이므로
`identity-alias-hit` 양성 문서는 fallback 의미를 조작하지 않고 contract-only 경계로 유지했다. private
corpus와 pilot은 실행하지 않았다. 다음 승인은 기존 비식별 자료만으로 deterministic pilot cohort를
선정하고 그 방법·규모를 보고하는 절편에 한정한다.

**세 번째 hard gate 결과:** [Stage 3-C cohort 선정 기록](../working/task_m100_4962_stage3_pilot_selection.md)에
포맷별 POC 상위 200건을 재사용하는 결정적 selector와 비식별 정책을 고정했다. HWP 16건과 HWPX
16건, 그중 canary 8건을 선정한 식별 manifest는 권한 `0600`으로 gitignored `output/`에만 두었다.
합본 상위 200건의 포맷 편향과 성공 문서 기반 risk pool의 대표성 한계를 명시했으며 원본 문서를 열거나
hash를 재계산하거나 W3 worker를 실행하지 않았다. 다음 승인은 manifest preflight와 canary 8건 실행에
한정한다.

**네 번째 hard gate 결과:** [Stage 3-P0 canary 기록](../working/task_m100_4962_stage3_canary.md)에
manifest 32건의 corpus-root·regular-file·크기·BLAKE3 preflight와 canary 8건의 격리 실행 결과를
기록했다. canary는 8/8 성공, resource-limit 0, truncation 0, 최대 관찰 RSS 595,714,048 bytes로 gate를
통과했다. HWP 문서별 시간이 3.2~35.2초로 크게 갈려 canary만으로 10k 시간을 확정하지 않았다. 다음
승인은 full cohort 32건의 1차 실행에만 적용하며 반복 hash를 위한 2차 실행은 그 비용 보고 뒤 분리한다.

**다섯 번째 hard gate 결과:** [Stage 3-P1 full 1차 기록](../working/task_m100_4962_stage3_full_pilot_r1.md)에
같은 cohort 32건의 첫 full 실행을 기록했다. 32/32 성공, resource-limit·truncation·excluded 0,
6,856,718자, worker 시간 합 205,632 ms, 최대 RSS 595,820,544 bytes로 반복 실행 자격을 충족했다.
고위험 표본의 포맷별 lower-p50을 10k 구성에 적용한 provisional 순차 시간은 17.74시간이며 mean은
21.78시간, p90 stress 값은 36.41시간이다. 다음 승인은 같은 실행 source·manifest의 2차 반복에만
적용하며 두 실행의 count·failure vector·combined decision hash를 비교한다.

**여섯 번째 hard gate 결과:** [Stage 3-P2 반복 기록](../working/task_m100_4962_stage3_full_pilot_r2.md)에
동일 32건의 2차 실행과 1차 비교를 기록했다. 두 실행 모두 32/32 성공했고 failure vector, 모든 count와
category, combined decision hash `bb3e2926…1ae6`가 정확히 일치했다. 총 시간 차이는 +0.559%, peak RSS
차이는 +0.012%였다. 두 회차 평균 provisional 10k 순차 시간은 lower-p50 17.63시간, mean 21.84시간,
p90 stress 36.80시간이다. 현재 supervisor는 완료 aggregate를 callback으로만 전달해 process 중단 후
재개할 checkpoint가 없으므로 Stage 4 전수 실행 전 atomic checkpoint·resume을 별도 승인·검증해야 한다.

**일곱 번째 hard gate 결과:** [Stage 3-R checkpoint·resume 기록](../working/task_m100_4962_stage3_checkpoint_resume.md)에
문서 완료 단위 append-only journal과 atomic state, committed byte 경계, 실행 identity와 SHA-256 chain을
구현했다. 공개 HWP 2건·HWPX 1건을 2건 완료 뒤 강제 중단한 실행은 재개 후 무중단 실행과 모든 안정
집계·chain이 정확히 일치했다. source·runner·worker·coverage contract·manifest·checkpoint policy·분석
옵션·격리 한도 drift, committed corruption, manifest 중복과 저장소 예산 초과는 다음 문서 실행 또는
journal append 전에 거부한다. private 10k 전건은 실행하지 않았다. Stage 4 진입 전 남은 절편은 journal의
문서별 usage row를 최종 비식별 aggregate로 병합하는 finalizer와 full manifest·저장공간 preflight다.

**종료 게이트:** 기존 자료로 답할 수 없는 항목만 새 collector가 채운다는 사실, 전수 delta pass 비용과
실패 처리 경계가 수치로 설명되고 privacy 검사가 통과한다. Stage 3 결과와 10k 실행 예상 시간을 보고한
뒤 별도 승인을 받아 Stage 4를 실행한다. 정당성이 입증되지 않으면 Stage 4는 취소한다.

### Stage 4 — 승인된 private 10k decision delta pass

**Stage 4-A 선행 결과:** [Stage 4-A 기록](../working/task_m100_4962_stage4a_finalizer_manifest.md)에
checkpoint journal의 문서별 aggregate를 format이 포함된 usage key로 결정적으로 병합하는 finalizer와
local-only full manifest builder를 구현했다. 공개 HWP 2건·HWPX 1건의 강제 중단·재개 final aggregate는
무중단 결과와 exact였고, full manifest는 HWP 6,582건·HWPX 3,418건과 5,471,422,390 input bytes를
BLAKE3로 고정했다. 동일 content 14개 그룹의 추가 인스턴스 39건은 기존 10k 분모를 보존하기 위해
제거하지 않았다. manifest와 비식별 preflight는 gitignored `output/`의 `0600` 파일로만 남겼다.

**Stage 4-B hard gate 결과:** [Stage 4-B 기록](../working/task_m100_4962_stage4b_devel_pilot.md)에
`upstream/devel@7df17a0ca`를 병합하고 오늘할일 add/add 충돌의 양쪽 기록을 모두 보존한 뒤, merge source
`308e38f73`에서 기존 private 32건을 checkpoint·finalizer로 재실행했다. 전체 보호 suite가 발견한 legacy
schema version 중앙화 누락을 값 보존 방식으로 고친 뒤 source `4a6c9e242`에 같은 32건을 다시 결박했다.
최종 실행은 32/32 성공했고 failure·excluded·truncation은 0이었다. Stage 3의 문서·count·category와 당시
산식으로 복원한 combined decision hash `bb3e2926…1ae6`가 모두 exact였다. journal 10k 외삽은
lower-p50 2.44 GiB, arithmetic mean 3.25 GiB, lower-p90 stress 5.23 GiB로 16 GiB hard maximum 아래였다.
따라서 semantic drift와 저장공간 blocker는 발견되지 않았다.

Stage 4-C를 승인할 경우 Stage 4-A의 full manifest를 덮어쓰지 않는다. Stage 4-B 보고 commit까지 포함한
현재 HEAD에 결박된 새 manifest와 비식별 preflight를 별도 이름으로 다시 생성·검증한 뒤 첫 private 10k
checkpoint pass만 시작한다. 첫 전건 결과를 보고하기 전에는 두 번째 결정성 pass, Stage 5, 원격 push와
PR로 확장하지 않는다.

**Stage 4-C 목적 잠금:** 메인테이너의 목적 적합성 감사에 따라 여기서 계측 기반시설의 일반화·추가
기능은 동결한다. 다음 산출물은 runner 자체가 아니라 10k 실제 renderer coverage 1차 기준선이다. source
변경은 최신 devel 통합 뒤 발견되는 correctness·privacy·재개 무결성 blocker의 최소 정정만 허용한다.
새 renderer source가 들어오면 기존 32건을 ingress gate로 한 번 대조하고 semantic blocker가 없을 때
전건을 시작한다. fixed-frame 결합, stored/fresh LineSeg feature-detection cohort와 W4 face 위험 순위는
W3 전건 결과 없이 완료로 주장하거나 선행 구현하지 않는다.

**Stage 4-C 전건 ingress 정정:** current HEAD의 첫 전건 실행은 13건을 정상 checkpoint한 뒤,
확장자가 HWP인 입력을 worker가 HWPX aggregate로 판정하면서 fail-closed 했다. 비식별 전수 시그니처
검사 결과 확장자와 지원 컨테이너가 교차된 입력 45건, HWP/HWPX 컨테이너로 인식되지 않은 입력 85건이
있었다. 이는 coverage 결함이 아니라 full manifest가 동결 inventory의 확장자 분류와 실제 parser 실행
형식을 한 `format` 필드에 겹쳐 둔 계측 계약 결함이다. corpus 파일은 변경하지 않는다. manifest는
`inputFormat`으로 기존 6,582/3,418 분모를 보존하고, 지원 HWP OLE/HWPX ZIP 시그니처가 있으면 실제
컨테이너를 실행 `format`으로 사용한다. HWP3·HML·unknown은 W3 HWP/HWPX 범위 밖이므로 worker가
`unsupported` 문서 실패로 회수한다. aggregate와 실행 형식 불일치 거부는 완화하지 않는다. 실패한
13건 r1 checkpoint는 재개하지 않고, 최소 정정 commit에 결박한 새 manifest·checkpoint 이름으로
32건 ingress부터 다시 검증한다.

**Stage 4-C 1차 결과:** [Stage 4-C 기록](../working/task_m100_4962_stage4c_full_pass.md)에 source
`c1ec759f9`로 결박한 10k 전건과 finalizer 결과를 기록했다. 10,000건 중 9,909건이 성공했고 실패
91건은 unsupported 53, empty 24, DRM 8, encrypted 5, resource-limit 1로 설명됐다. 기존 POC 대비
unsupported 증가 38건은 HWP3 38건을 W3 HWP5/HWPX 범위 밖으로 분리한 결과이고, 추가 1건은 font
dimension 문자열 4,096바이트 hard cap이었다. parser·cancelled·truncation은 0이며 coverage
54,326,042자의 성공 계열은 96.2049%, face-miss 3.0230%, char-miss 0.7198%, heuristic 0.0523%다.
finalizer의 분모·privacy·canonical hash 검사를 모두 통과했고 실제 journal은 510.04 MiB였다.

**Stage 4-D 결정성 결과:** [Stage 4-D 기록](../working/task_m100_4962_stage4d_determinism.md)에 1차와
동일한 source·runner·worker·manifest·contract·policy identity로 10k 2차를 실행했다. 10,000개 문서의
status·failure 또는 aggregate hash token, checkpoint chain, failure vector, 모든 count·category·join이
exact였다. final aggregate 두 파일은 각각 110,097,106 bytes이며 byte-for-byte exact, aggregate SHA-256
`37867105…bc2888`과 file SHA-256 `24eb1d15…352a1`도 일치했다. elapsed는 -2.9296%, peak RSS는
8,192 bytes 증가, metrics를 포함한 journal은 54 bytes 감소했으나 모두 의도적으로 비결정성인 운영 값이다.
privacy·reconcile·canonical hash 검사를 다시 통과해 W3 종료 조건을 충족했다.

Stage 4-A 시점의 `upstream/devel@7df17a0ca`에는 착수 기준 뒤 renderer·parser 변경이 있었고 task
branch와 `mydocs/orders/20260821.md` 충돌이 예상됐다. 또한 과거 32건 pilot은 문서별 journal bytes를
보존하지 않아 공개 3건 크기를 10k에 외삽할 수 없었다. 이에 Stage 4-B에서 최신 devel을 정상 병합하고
worker·runner·contract identity를 다시 고정한 뒤 같은 private 32건을 checkpoint/finalizer로 검증하고,
source 최소 정정 뒤 최종 HEAD에 다시 결박해 semantic drift와 journal 용량을 동시에 검사했다. 이는 기존
coverage 결정성을 처음부터 다시 조사한 절차가 아니라 변경된 source와 새 복구 경계의 적합성 gate다.

1. 동일 source commit·도구·schema로 corpus 전체의 문자별 decision delta를 읽기 전용 실행한다.
2. 기존 v2의 HWP/HWPX·font·usage·장평·자간·문맥 totals를 재사용하고, 새 projection과의 차이는
   current parser/model drift로 별도 보고한다.
3. 7개 coverage와 join 상태의 count를 대사한다.
4. fresh layout과 backend snapshot은 Stage 3에서 승인된 별도 cohort에만 실행하고 전수 portable
   coverage와 분모를 섞지 않는다.
5. 첫 실행 비용을 보고한 뒤 승인된 경우에만 동일 delta pass를 한 번 더 실행해 canonical aggregate
   hash를 검증한다.
6. raw trace와 식별 로그가 남지 않았음을 확인하고 비식별 delta aggregate만 저장소 보고서로 옮긴다.

**종료 게이트:** 기존 POC 기준선과 새 decision delta의 책임이 분리되고, 분류 합과 분모가 일치하며,
승인된 반복 실행의 hash가 동일하고, 모든 제외가 이유별 count로 설명된다. 실패나 truncation을 숨긴 채
coverage 비율을 게시하지 않는다.

### Stage 5 — W3 감사·보고와 W4 인계

1. 기존 91.16%가 새 결과와 다른 원인을 실제 renderer 계보로 설명한다.
2. aggregate coverage, 교차축, unknown·layout-only·backend 미관찰 비율과 한계를 보고한다.
3. W4가 사용할 face별 비식별 통계와 위험 점수 입력 필드를 확정한다.
4. FI-01~FI-14와 제품 동작 0-delta를 감사한다.
5. #4960의 W2 체크 상태와 #4962의 W3 진행 상태를 실제 완료 근거에 맞춰 갱신할 후보를 준비한다.

**종료 게이트:** 메인테이너가 W3 coverage 기준선을 승인해야 W4 수행계획을 확정한다. W3 승인만으로
metric·fallback 변경, W4 자동 실행, push 또는 PR 생성을 승인한 것으로 보지 않는다.

## 9. 검증 계획

### 9.1 매 stage 공통

```bash
cargo fmt --all
cargo fmt --all -- --check
git diff --check
```

`src/**`의 `#[cfg(test)]`를 바꾸면 다음 정책 검사도 수행한다.

```bash
node scripts/rust-unit-test-tiers.mjs --check
```

새 integration source는 `tests/cases/`에만 두고 generated suite·manifest를 source commit에 포함하지
않는다.

### 9.2 focused 계약

- coverage 분류 decision table 전수
- W2 trace와 collector의 문자 결정 등가
- 4,096자 초과 page의 누락 0
- parse/layout/source join 실패의 fail-closed count
- stored/fresh LineSeg cohort 분리
- HWP/HWPX, language slot, bold·italic, ratio·letter spacing·fixed frame 교차축
- canonical aggregate ordering·hash 결정성
- path·본문·raw trace 비노출
- collector 호출 전후 기존 SVG·serialization output 0-delta

### 9.3 PR 준비 전 회귀

focused 결과를 먼저 보고한 뒤 메인테이너가 PR 준비와 전체 검증을 승인하면 변경 범위에 맞는
`local_validation.md` 4.3 gate를 실행한다. remote push, PR 생성과 GitHub self-review는 각각 별도 승인
대상이다.

## 10. 중단 조건

다음 중 하나라도 발생하면 해당 stage를 완료로 보고하지 않고 계획 변경 승인을 요청한다.

- W1 재수집에서 lookup target·순서·relation의 의미 변화가 발견됨
- W2 trace와 W3 collector가 같은 문자에 다른 metric decision을 냄
- full coverage를 위해 default renderer 경로에 상시 비용을 넣어야 함
- 긴 page를 누락 없이 처리하려면 공개 trace/API 계약 변경이 불가피함
- source join 실패를 추정 좌표로 메워야만 조판 교차축을 만들 수 있음
- private corpus 원문·식별 정보가 commit 대상 또는 로그에 포함됨
- 두 번 실행한 aggregate count나 hash가 일치하지 않음
- 계측 중 metric·fallback·paint 또는 font asset 변경이 필요해짐

## 11. W3 완료 조건

- 실제 renderer 결정 순서를 사용하는 문자 단위 coverage가 산출된다.
- 모든 포함 문자는 상호배타적 coverage 또는 확정된 non-applicable 상태 하나에만 속한다.
- 분류 합, coverage 분모, 전체 실제 문자와 제외 count가 기계적으로 대사된다.
- 긴 page가 W2 진단 상한 때문에 잘리지 않는다.
- HWP/HWPX, 언어 slot, bold·italic, 장평·자간, 고정 frame과 LineSeg 상태가 교차 집계된다.
- stored LineSeg와 fresh layout 결과가 분리된다.
- 같은 입력을 두 번 실행한 canonical aggregate hash가 동일하다.
- private corpus raw 자료와 식별 정보가 저장소·GitHub·CI에 포함되지 않는다.
- 기본 renderer output, metric DB, fallback, paint와 font asset 변경이 0이다.
- W4가 사용할 비식별 입력과 W5로 이어질 조사 질문의 형식이 준비된다.

## 12. 계획 self-review

- **선행 게이트**: W0·W1·W2 merge commit이 현재 `devel`에 포함됨을 확인했다.
- **기존 자산 검사**: POC v2 aggregate·포맷별 결과·소표본·briefing과 생성 source commit을 확인하고
  재사용/결손 필드를 구분했다.
- **범위 검사**: 이번 후보는 W3 coverage까지이며 W4 ranking과 W5 oracle을 자동 진행하지 않는다.
- **중복 계측 검사**: 장평·자간·font 사용·문맥·stored LineSeg 존재 통계는 기존 v2를 기준선으로
  재사용하며, code point·resolver·hit/miss·relation처럼 역산할 수 없는 delta만 새로 측정한다.
- **정확성 검사**: 선언 face가 아닌 실제 character decision을 사용하고, trace 상한·source join 실패를
  분모에서 숨기지 않는다.
- **계보 검사**: W1 relation과 W2 decision을 재사용하며 별도 lookup table을 만들지 않는다.
- **기능 탐지**: 포맷·producer 버전 대신 LineSeg 유효성, source join과 backend capability 상태를 본다.
- **조판 검사**: 장평·자간·고정 frame과 stored/fresh LineSeg를 독립 축으로 보존한다.
- **privacy 검사**: private corpus는 로컬 읽기 전용이며 aggregate 외 자료의 publication을 금지한다.
- **성능 검사**: raw trace 전수 저장 대신 streaming aggregate와 pilot 기반 비용 게이트를 둔다.
- **승인 검사**: 계획 승인, 각 Stage 결과, 10k 전수 실행, W4 진입, push와 PR을 서로 다른 승인으로 둔다.

## 13. 승인 요청 지점

메인테이너가 목적 적합성 감사 결과를 다음 진행에 반영하도록 승인했고, **Stage 4-C — private 10k
decision delta 1차 checkpoint pass와 그 결과 보고**를 완료했다.

1. 최신 `upstream/devel`을 병합하고 오늘할일 충돌의 양쪽 기록을 보존한다.
2. current HEAD에서 worker·runner·contract를 빌드·검증한다.
3. renderer source drift가 있으므로 기존 local-only 32건을 ingress gate로 한 번 실행해 Stage 3의
   failure·count·category·combined hash와 대조한다.
4. blocker가 없으면 Stage 4-A artifact를 덮어쓰지 않고 새 full manifest·preflight를 current HEAD에
   결박해 private 10k 1차 checkpoint pass를 시작한다.
5. 완료 journal을 finalize하고 coverage 분모·failure·privacy·기존 POC 대사 및 W4 입력 결손을 보고한다.

10k 두 번째 결정성 pass까지 완료했으므로 다음 단계는 같은 #4962의
[W4 조판 위험 순위 계획](task_m100_4962_w4.md)을 따른다.
W4는 보존된 W3 decision aggregate와 기존 POC 장평·자간·고정 frame·stored LineSeg 축의 compatibility를
먼저 감사하고, 새 10k 전수 계측은 기존 자료로 답할 수 없는 결손이 입증될 때만 별도로 제안한다.
fresh layout·backend cohort, metric·fallback·font asset 변경, 원격 push와 PR은 포함되지 않는다.

별도 승인 전에는 W4 진입, 새 전수 계측, 원격 push, Issue 본문 수정과 PR 생성을 수행하지 않는다.
