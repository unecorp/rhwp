# 수행계획 — Task M100 #4969 W10-Q4-D5 회귀·성능·최종 판정

- **상위 계획**: [`task_m100_4969_w10_q4_d.md`](task_m100_4969_w10_q4_d.md)
- **D4-D checkpoint**: `f40987220` (`qualified-canvaskit-bounded-vertical-runtime`)
- **최종 기준 upstream/devel**: `3b301f725` (merge `3d239b5a7`, 71 ahead / 0 behind)
- **기계 판독 계획**:
  [`w10_q4_d5_execution_plan.json`](../tech/investigations/issue-4969/w10_q4_d5_execution_plan.json)
- **계획 checkpoint**: `a4248b9c6`
- **D5-C 결과 checkpoint**: `a7de96b44`
- **D5-D 계획 checkpoint**: `e866bc0b8`
- **D5-D 결과 checkpoint**: `95d450dd5`
- **상태**: Q4-D5-D clean-room causal WASM hard gate 충족·최종 `qualified-bounded-subset` 결과 메인테이너
  명시 승인
- **작성일**: 2026-08-31 KST
- **제품 변경**: exact-source 반복 준비 병목 정정, 지원 tuple·출력 계약 변경 없음
- **private corpus·Hyper-V**: 사용하지 않음

## 1. 목적과 선행 증적 재사용

Q4-D5의 목적은 새 기능을 넓히는 것이 아니라 D1~D4가 연 bounded HWP5 세로 표 셀 subset의 비용과 회귀를
최종 회계하고 Q4-D를 `qualified-bounded-subset`, `dormant`, `blocked` 중 하나로 판정하는 것이다.

D4-D checkpoint는 이미 다음을 현재 head에서 증명했다.

- #4969 계보 103 passed / 0 failed / 2 ignored performance
- Studio 1,324 passed / 0 failed / 1 skipped, actual CanvasKit pixel mismatch 0
- 격리 Node WASM 4 passed, 표준 Docker WASM 성공
- integration 정책 22 passed, canonical SVG 두 건 mismatch 0
- unsupported backend false selection·fallback disappearance 0

따라서 D5는 같은 full 계측을 이유 없이 반복하지 않는다. D5-A/B에서 추가되는 test-only 측정 하네스의 focused
검증과 D5-C 최종 source의 full gate를 한 번 실행한다. D4-D 수치는 pre-D5 baseline으로 그대로 인용한다.

## 2. 계측 원칙

### 2.1 A/B/A wall-time

동일한 공개 synthetic HWP5 code-2 표 셀과 동일 Noto source를 사용한다.

- **A1**: exact source를 등록하지 않아 기존 legacy vertical layout·TextRun으로 원자 fallback한 fresh core
- **B**: exact source를 등록해 bounded layout·GlyphRun·CanvasKit replay가 모두 활성화된 fresh core
- **A2**: A1과 같은 별도 fresh core를 B 뒤에 다시 실행한 drift control

source 등록과 fixture 생성은 timed region 밖에 둔다. 한 process에서 A1→B→A2 순서로 실행하고, Q3-E5와 같은
release 계측 관행을 재사용해 process 9개, warm-up 20회, 본 측정 64회로 고정한다. 각 process의 raw JSON을
보존하고 p50·p95, `B - mean(A1,A2)`, `A2 - A1` drift를 함께 보고한다.

측정 축은 서로 섞지 않는다.

1. render-tree/layout cold 1회와 warm 64회
2. 이미 만든 render tree의 layer build 64회
3. layer JSON serialization과 Rust CanvasKit replay-plan 분석 64회
4. actual CanvasKit surface의 legacy TextRun A1/A2와 bounded `drawGlyphs` B replay 64회

font blob/typeface/font 생성 비용은 cold와 warm cache hit을 분리한다. actual draw warm 측정은 검증된 face/font를
미리 준비하고 draw만 재며, cold 준비 비용은 별도 필드로 기록한다. backend가 다시 shape하거나 위치를 재측정하지
않는다.

### 2.2 판정 방식

새 절대 wall-time SLA나 측정 뒤 만든 임계치를 적용하지 않는다. A1/A2 drift가 bracket한 noise와 effect size를
있는 그대로 공개한다. 재현 가능한 B slowdown은 숨기지 않고 결과 승인에서 메인테이너가 수용·정정 여부를
판단한다.

hard gate는 구조적이다.

- A1/A2: vertical GlyphRun·font blob/face 0, legacy tree/hash 서로 일치
- B: fallback TextRun 2, vertical GlyphRun 2, unique font blob/face 각 1
- 반복 64회에도 source/resource/payload 개수가 반복 횟수에 비례해 증가하지 않음
- B glyph id·position·origin·pixel bounds와 canonical mismatch 0
- unsupported backend·malformed tuple은 fallback 100%, blank·partial publication 0

## 3. causal WASM size 대조

D4-D의 9,883,530 bytes는 이전 측정 사이에 upstream 23커밋이 들어가 Q4-D 인과 delta가 아니다. D5는 같은 최신
B tree에서 Q4-D 제품 커밋만 역적용한 disposable A worktree를 만든다.

역적용 대상은 다음 여섯 제품 checkpoint다.

1. `b1a33b6fc` — Studio actual CanvasKit replay
2. `c9b551909` — Rust CanvasKit selector
3. `d49400aad` — vertical GlyphRun publication
4. `bb334756b` — source/leaf shadow mapping
5. `992fe4acaf92` — bounded vertical HWP5 table layout
6. `d0948166a` — exact-source-bound dormant owner

임시 A와 B는 같은 upstream·toolchain·Docker image를 사용하고 별도 target/pkg 디렉터리에서 표준 Docker WASM을
각각 한 번 빌드한다. 최적화 WASM bytes·SHA-256과 delta bytes·percent를 기록한다. build wall time은 cache·열 상태가
달라 성능 결론으로 쓰지 않는다.

역적용은 disposable worktree에서만 수행하고 current branch를 바꾸지 않는다. reverse patch conflict가 나거나
A↔B diff가 위 여섯 checkpoint의 source/test allowlist 밖을 건드리면 강제로 맞추지 않고 `causal-size-unavailable`로
중단한다. 이 경우 과거 절대값으로 delta를 대신하지 않으며 최종 판정은 `blocked` 후보로 올린다.

## 4. 실행 절편

### Q4-D5-A — 측정 계약·재현 하네스

1. `tests/cases/issue_4969_shaping_atomic_activation.rs`의 기존 원본에 local ignored A/B/A receipt를 추가한다.
2. 기존 `rhwp-studio/e2e/issue-4969-shaping-replay.test.mjs`에 명시적 환경변수로만 실행하는 actual CanvasKit
   A/B/A receipt를 추가한다.
3. release profile, process/warm-up/iteration 수, fixture/font digest, host·toolchain metadata와 JSON schema를
   고정한다.
4. counter·hash·pixel assertion을 wall-time 수집보다 먼저 실행한다.
5. 새 integration source, product `#[cfg(test)]` seam, public API·schema, production timer를 만들지 않는다.

**종료 게이트**: 하네스가 기본 CI에서는 ignored/비활성이고 명시 실행에서 A1/B/A2 raw JSON과 구조적 counter를
결정적으로 낸다. 제품 출력 변화 0. 결과 승인·checkpoint 뒤에만 D5-B를 실행한다.

**결과**: [Q4-D5-A 결과 보고서](../working/task_m100_4969_w10_q4_d5_a.md)는 기존 integration·Studio E2E
원본에만 test-only receipt를 추가하고 구조·hash·actual pixel gate를 통과했다. 1회 pilot의 Rust B 비용 신호는
D5-B 원인 분해 대상으로 넘기며 성능 결론으로 사용하지 않는다. 판정 `qualified-measurement-contract`와
checkpoint 생성을 자동 승인 처리한다.

### Q4-D5-B — local A/B/A·resource·causal size

1. Rust A/B/A를 독립 process 9개로 실행해 layout, layer build, JSON, replay-plan cold/warm 값을 수집한다.
2. Studio actual CanvasKit A/B/A를 독립 process 9개로 실행해 cold resource 준비와 warm draw를 분리한다.
3. 각 raw run의 구조적 counter·hash·pixel을 먼저 대사한 뒤 p50·p95와 bracket drift를 집계한다.
4. 같은 최신 tree의 disposable A와 current B를 표준 Docker WASM으로 빌드해 causal size delta를 얻는다.
5. retained source/resource가 반복 횟수에 비례하지 않는지와 JSON/font payload 절대값을 회계한다.

**종료 게이트**: raw 9/9 완전성, counter·hash·pixel mismatch 0, resource multiplicity bounded, causal WASM
comparison 유효. wall-time signal이 크더라도 사후 임계치로 자동 pass/fail하지 않고 원인과 수용 판단을 결과에
명시한다. 결과 승인 전 D5-C를 시작하지 않는다.

### Q4-D5-C — 최종 회귀·Q4-D 분류

1. 최신 upstream을 다시 fetch하고 incoming이 있으면 overlap·merge-tree를 먼저 판정한다.
2. #4969 계보, #6029, horizontal Q2/Q3, malformed·oversized resource 상한과 canonical 두 건을 실행한다.
3. prepared review harness에서 manifest/policy와 full nextest를 한 번 실행한다.
4. native/wasm32 Clippy, Native Skia fallback, Node WASM, Studio unit/build/focused E2E와 표준 Docker WASM을
   최종 source에서 통과시킨다. D5-B의 유효 B Docker build가 같은 source면 중복 빌드하지 않는다.
5. 지원·거부·backend·비용 matrix를 기계 판독 최종 증적과 결과 보고서에 고정한다.

**최종 분류**:

- `qualified-bounded-subset`: correctness/parity/canonical 회귀 0, 구조적 비용 bounded, A/B/A·causal size 자료
  완전, 지원 범위 과장 0
- `dormant`: activation을 안전하게 유지할 수 없어 제품 선택을 다시 닫아야 함
- `blocked`: 정확한 판정을 위한 causal baseline·runtime 증적을 재현하지 못했거나 미해결 회귀가 있음

제품 정정은 D5-B의 9-run 원인 계보를 근거로 별도 수정 수행계획을 작성했고, 메인테이너의 자동 승인 지시에 따라
승인했다. 정정 범위는 registry-owned source 준비 결과의 내부 재사용뿐이며 지원 tuple과 출력 계약은 바꾸지 않는다.

### Q4-D5-D — clean-room causal WASM 복구

D5-C의 전체 commit reverse는 WASM과 무관한 문서·Studio E2E 충돌까지 끌어들여 A축 구성에 실패했다. 별도
[수정 수행계획](task_m100_4969_w10_q4_d5_d_revised.md)에 따라 B exact head에서 일곱 제품 commit의 Rust
`src/**` diff만 역적용했다. A diff는 계획한 13개 파일 allowlist와 정확히 일치했고 수동 conflict resolution·
reject·Cargo/toolchain drift는 0이었다.

같은 Docker image와 분리된 빈 volume에서 A/B 표준 WASM을 완전 빌드한 결과 A 9,858,318 bytes, B
9,894,929 bytes, delta +36,611 bytes(+0.371371668%)를 얻었다. 이에 따라 causal size hard gate는
`available`이며 D5-C의 역사적 blocker가 해소됐다.

## 5. 검증 범위

최종 gate는 현행 review harness의 실제 suite 배치를 조회해 실행한다. 고정 suite 번호를 추정하지 않는다.

- #4969 request → layout → mapping → lowering → publication → selector 계보
- #6029 vertical cell과 `samples/table-004.hwp` canonical SVG
- Q2/Q3 horizontal/no-request/default 계약
- Rust layer renderer·CanvasKit policy units
- actual CanvasKit #4969 pixel·font coverage·renderer contract
- Node WASM publication parity, Docker WASM release build
- full nextest, Native Skia fallback, native/wasm32 Clippy
- Studio 전체 test·production build
- `cargo fmt --all`, `cargo fmt --all -- --check`, `git diff --check`

D5-A의 integration test 변경은 source 원본만 제출한다. generated suite·manifest·Cargo marker와 `pkg`, `dist`,
measurement raw temp는 stage하지 않는다.

## 6. 보호 불변식

1. Q4-D4가 연 CanvasKit bounded tuple을 넓히지 않는다.
2. HWPX·code 1·horizontal·mixed/Latin/punctuation·multi-line/run/column은 계속 fallback한다.
3. Native Skia·SVG·Canvas2D의 vertical authority를 열지 않는다.
4. no exact source와 malformed tuple의 legacy output/hash를 바꾸지 않는다.
5. font bytes 32 MiB, text/glyph/position 4,096 등 기존 상한을 완화하지 않는다.
6. layout·backend가 shaping owner의 glyph/position을 다시 측정하거나 reshape하지 않는다.
7. private corpus·Hyper-V·한컴 Oracle·GitHub mutation을 사용하지 않는다.
8. 결과·checkpoint·다음 절편·push·PR·GitHub comment는 각각 별도 승인이다.

## 7. 중단 조건

- A/B/A가 같은 fixture·font·toolchain을 사용하지 못하거나 raw run이 9/9 완전하지 않음
- A1과 A2의 구조/hash가 다르거나 B counter가 반복 수에 따라 증가함
- causal A worktree reverse diff가 allowlist 밖을 변경하거나 conflict를 강제로 풀어야 함
- target pixel·glyph/position·canonical 또는 unsupported fallback mismatch 발생
- 성능 원인이 제품 정정을 요구함

## 다음 승인 경계

메인테이너가 상세 수행계획을 승인하고 checkpoint `a4248b9c6`으로 고정했다. D5-A measurement contract,
D5-B 성능 정정·9-process 재계측, 최신 devel 병합 및 D5-C full gate까지 완료했다. D5-C의 역사적 `blocked`
결과는 checkpoint `a7de96b44`로 보존했다. 이어 hard gate를 완화하지 않는 D5-D 계획을 checkpoint
`e866bc0b8`로 고정하고 source-only A축을 구성해 causal WASM 증적을 완성했다. 최종 분류는
`qualified-bounded-subset`이며 결과 checkpoint `95d450dd5`로 고정했다. 최신 `upstream/devel@3b301f725`를
merge `3d239b5a7`로 병합한 뒤 #4969 112/112와 wasm32·tier·format을 재통과했다.
