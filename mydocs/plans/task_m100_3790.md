# 수행계획서 — task_m100_3790

- **이슈**: [#3790](https://github.com/edwardkim/rhwp/issues/3790) `ci: 변경 영향축 classifier로 frontend-only 과잉 CI를 줄인다`
- **연계 이슈**: [#3684](https://github.com/edwardkim/rhwp/issues/3684), [#3789](https://github.com/edwardkim/rhwp/issues/3789)
- **브랜치**: Stage 1 `codex/issue-3790-ci-impact-shadow`, Stage 2·2.5
  `codex/issue-3790-shadow-observation`, Stage 3 `codex/issue-3790-stage3-frontend`, Stage 4
  `issue-3790-stage4-rust-native`, Stage 5A `issue-3790-stage5a-codeql-safety`, Stage 5B
  `issue-3790-stage5b-codeql-languages`, Stage 2.6 enforcement
  `issue-3790-stage26-enforcement`
- **분기 기준**: Stage 1 `upstream/devel` `3d4863a0d531`, Stage 2
  `upstream/devel` `91f5131815dc`
- **Stage 2.6 분기 기준**: `upstream/devel` `88012c7e09a6` (#4603 merge)
- **최신 확인 기준**: `upstream/devel` `55eb2860b7fa`
- **기록 시각**: 2026-08-13 KST
- **절차 상태**: Stage 3·4 merge와 canary 측정, cache 회귀 분리까지 완료했다. Stage 5A PR #4341은
  CI·CodeQL과 리뷰 보정을 통과해 `8ea92cdad120`으로 merge됐다. 전용 worktree와 로컬·fork 원격 브랜치는
  merge 뒤 정리했고 Stage 2.6 controller 유일본은 보존했다. 2026-08-11 live branch metadata에서
  `devel`의 required context가 `Build & Test` 하나임을 확인했다. Stage 5B는 PR #4519로 merge했고,
  measurement-only PR #4573의 같은 head에서 전체 job elapsed 94.8%, 실제 병렬 완료시간 85.2%,
  CodeQL job elapsed 81.6% 감소를 확인했다. Stage 3~5는 현재 main에 포함됐고 controller는 아직
  main에 없으므로, 최신 devel에서 세 workflow의 job·step 진리표를 독립 감사하는 Stage 2.6을 재개했다.
  base-only 단일 controller job과 same-head aggregate policy를 구현해 Draft PR #4682를 만들고 첫 CI를
  통과했다. maintainer 요청 전 self-review에서 외부 fork run 조회, 최신 run 선택, enforcement 변경
  PR의 trailing fast-pass 정합을 보정했다. 보정 commit `4ba5e431d`와 최신 devel merge
  `30bbcf9fe`의 로컬 focused 검증을 통과했다. 게시 self-review의 후속 지적에 따라 Node policy 테스트
  CI 배선, 안전한 full 상위 집합 허용, 취소·stale status 차단과 감사 lane 완전성 계약을 추가하고 최신
  devel `55eb2860b`을 병합했다. 보정 head를 push한 뒤 전체 CI와 maintainer 명시적 승인을 다시 받아야
  한다. main 등록·live audit 전까지는 원형과 새 worktree를 모두 보존한다.

## 1. 문제

현재 `CI`는 review-only fast-pass와 단일 `frontend_required`만 구분한다. 따라서 Studio 단축키나
명령 정책처럼 Rust·WASM·렌더 출력과 무관한 변경도 Rust lint, 세 builder가 만드는 네 test archive,
네 worker, Native Skia와 Canvas, Rust/Python CodeQL까지 실행한다. Rust 검증의 결함 검출력은 유지하되
관련 없는 변경에서만 worker를 생략하려면 frontend 여부가 아닌 독립 영향축이 필요하다.

#3892는 default-feature test를 `slow/1/2/3` 네 archive와 세 builder·네 worker로 재배치해 wall time을
줄였지만 변경 영향과 무관한 worker 실행 자체는 유지한다. 따라서 이 계획의 가치는 남아 있으며,
후속 조건화와 artifact retry는 #3892 이후 topology를 기준으로 설계한다. Stage 2에서 기록한 8-shard
runner-minute는 당시 topology의 historical 수치로만 보존한다.

## 2. 목표 출력

classifier는 다음 값을 하나의 판정으로 만든다.

| 출력 | 값 | 의미 |
| --- | --- | --- |
| `rust_required` | `true|false` | Rust lint·default test 필요 여부 |
| `frontend_mode` | `none|unit|package` | Studio 전역 typecheck·단위 gate 또는 전체 package gate 범위 |
| `render_required` | `true|false` | Canvas visual diff와 CanvasKit readiness를 포함한 Render Diff 필요 여부 |
| `native_skia_required` | `true|false` | Native Skia 검증 필요 여부 |
| `codeql_languages` | 언어 CSV 또는 `none` | JavaScript/TypeScript·Python·Rust 분석 범위 |
| `classification_status` | `classified|full` | 정상 분류 또는 fail-closed 전체 검증 |
| `classifier_version` | 정수 문자열 | 판정 규칙 버전 |
| `reason` | 안정된 reason 목록 | 실행 기록과 회귀 분석 근거 |

## 3. 안전 경계

- Stage 1·2.5의 shadow 판정과 기존 worker 조건 불변 계약은 완료됐다. Stage 3부터 영향축을 실제 frontend
  gate와 Render Diff 조건에 연결한다.
- classifier·workflow·Cargo 변경, `src/main.rs`, rename, 빈 파일 목록, API 오류, PR 3,000개 및 push
  300개 경계, 미분류 경로는 `classification_status=full`로 닫는다.
- rename은 현재 경로와 이전 경로를 모두 평가하되 Stage 1에서는 전체 검증 판정으로 고정한다.
- Stage 1·2의 pull request merge ref classifier는 advisory로만 취급한다. Stage 2.5부터 PR base SHA의
  classifier를 실행하고 `pr-base-trusted-shadow` authority로 별도 표본을 누적한다.
- Stage 2.5가 base SHA에 고정하는 것은 classifier 파일 하나뿐이다. `pull_request` workflow YAML,
  인라인 collect script, classifier 호출부와 후속 worker `if`는 PR merge ref의 제어를 받으므로
  `pr-base-trusted-shadow`를 실제 skip 권한으로 해석하지 않는다.
- 기존 `pull_request` workflow의 조건화는 contributor/collaborator를 구분하지 않고 모든 genuine
  frontend-only PR에 적용한다. workflow/classifier를 함께 바꾸는 PR은 full이며, PR-controlled workflow를
  독립 감사하는 default-branch controller는 정상 릴리즈 뒤 후속 enforcement로 둔다.
- controller가 main에 등록되기 전에도 기존 `ci.yml`, `render-diff.yml`, `codeql.yml`의 devel 변경은 이후
  devel 대상 PR에서 실행되므로 Stage 3~5와 canary를 진행할 수 있다.
- 수동 `workflow_dispatch`를 강제 full 경로로 유지한다. 일반 label 변경은 실행 중인 검증을 취소하고
  같은 영향축을 중복 실행하므로 Stage 3에서는 `labeled|unlabeled` activity와 `ci:full` label 경로를
  도입하지 않는다. label 기반 강제 실행은 post-main trusted controller가 required 정책으로 채택될 때
  별도 설계한다.
- review-only fast-pass와 required check 이름 `Build & Test`의 의미는 바꾸지 않는다.
- `src/main.rs`는 #3789에서 render/export 진입점이 모듈화되기 전까지 render 포함 full로 처리한다.
- `frontend_mode=unit`도 `rhwp-studio/tsconfig.json`의 전체 `src`를 대상으로 하는 `tsc --noEmit`과
  전체 Studio unit test를 실행한다. 다만 최초 활성화에서 unit으로 진입하는 소스는 historical fixture로
  검증한 `src/command/**`, `src/engine/command.ts`로 제한한다. 나머지 Studio runtime은 `package`로
  승격해 실제 생성 binding과 Vite·extension·package build를 검증한다. fresh WASM 산출물이 없는 unit
  lane은 CI 전용 tsconfig에서 `@wasm/rhwp.js` 경계만 최소 stub으로 치환한다.
- 현재 Render Diff workflow는 좁은 Canvas visual diff와 Studio 앱 전체를 띄우는 CanvasKit readiness를
  함께 실행한다. 활성화 전에는 두 gate의 영향축을 분리하거나 실제 의존 경로의 보수적 합집합을
  `render_required`로 확정한다. 경로·파일명 heuristic만으로 skip하지 않는다.

## 4. 단계와 조정된 실행 순서

1. **Stage 1 — shadow classifier와 fixture**: #3785, #3656, #3670, #3672, #3690의 실제 파일
   목록을 fixture로 고정하고 판정만 기록한다.
2. **Stage 2 — shadow 관찰**: PR별 예상 worker와 실제 worker 결과를 대조하고 false negative가 없음을
   확인한다.
3. **Stage 2.5 — trusted-base shadow**: PR base SHA의 classifier를 실행하되 worker 조건은 그대로 두고,
   classifier-source authority의 운영 표본을 새로 누적한다.
4. **Stage 2.6 조정 — enforcement 분리**: 약 1,500줄 default-branch controller 프로토타입은 원격에
   게시하지 않고 후속 설계 근거로 보존한다. Stage 3~5의 최종 job 진리표가 확정된 뒤 축소하고, 정상
   `devel → main` 릴리즈와 required status 결정을 통해 독립 enforcement를 완성한다. controller는
   workflow run을 source branch로 조회한 뒤 repository·branch·SHA와 가능한 PR/base 연결 정보를
   base policy에서 다시 검증한다. enforcement surface가 바뀐 PR은 trailing review-only commit에서도
   과거 candidate를 재사용하지 않고 현재 head의 full validation을 실행한다.
5. **Stage 3 — frontend 축 활성화**: 모든 devel 대상 PR에서 `unit`, `package`, `render`를 분리하고
   aggregate에서
   `success|skipped` 진리표를 검증한다.
6. **frontend-only canary**: Stage 3 merge 직후 frontend-only PR의 selective run과 같은 SHA의 수동
   `workflow_dispatch` full run을 실행해 wall time, job별 runner-minute와 expected `success|skipped`를
   대조한다. PR #3951에서 unit 59초, package 2분 47초, Canvas 5분 59초를 확인해 직접 runner time
   7분 47초를 절감했다. 수동 full의 기존 cold release archive timeout은 별도 #4029에서 추적한다.
7. **Stage 4 — Rust·Native Skia 조건화**: canary에서 unit/package/render 진리표를 확인한 뒤 Rust 비영향
   PR에서 lint와 세 builder·네 worker를, render 비영향 Rust PR에서 Native Skia를 생략한다. aggregate는
   각 builder/worker와 Native Skia의 `success|skipped` 조합을 영향축별 진리표로 검증한다. Native Skia
   job이 직접 실행하는 두 Rust 통합 테스트와 default-feature 테스트가 직접 소비하는
   `ttfs/**`, `tests/fixtures/fonts/**`, 표적 HWPX sample은 Rust 경계로 고정한다. 누락된 기존
   `issue_2293_chart_png_text` Native target은 #4040으로 분리해 추적한다. PR #4032를 merge commit
   `8d48a4c07`로 반영했다.
8. **Stage 4 canary**: #3951과 같은 2파일 frontend-only 변경으로 PR #4078을 만들어 Stage 4만이 차이가
   되도록 대조했다. Lint 218초, archive a/b/slow 387·239·324초, shard 1/2/3 202·180·133초, slow shard
   228초, Native Skia 368초 — 새로 생략된 9개 job의 직접 runner time 2,279초를 확인했다. CI workflow
   runner time은 2,355초에서 137초로 2,218초(94.2%) 줄었고, CI·CodeQL·Render Diff 합계는 3,238초에서
   939초로 2,299초(71.0%) 줄었으며 wall clock은 857초에서 575초가 됐다. `Build & Test` aggregate가
   해당 `success|skipped` 조합을 수용해 진리표도 검증됐다. Frontend unit gates의 59초→127초 증가는
   Studio 테스트 증가와 runner 편차이므로 절감 계산에서 분리한다. 측정 뒤 #4078은 merge하지 않고
   close했다.
9. **cache 회귀 확인 — 완료, 회귀 있음(Stage 4 무관)**: #3810 정리 직후 기준선 4.73GB(24개)와 Stage 4
   이후 스윕 직후 총량을 같은 시점 조건으로 대조했다. 스윕 직후 총량은 4.73GB → 5.85GB(08-02) →
   7.16GB(08-03) → 8.46GB(08-04) → **8.84GB(08-05, 50개)** 로, 기준선 대비 +4.11GB(+87%)이며 무료 한도
   10GB의 88%다. 마지막 값은 비파괴 `workflow_dispatch dry_run=true` 실행
   [31030157435](https://github.com/edwardkim/rhwp/actions/runs/31030157435)의 예상치다.

   회귀는 Stage 4가 원인이 아니다. Stage 4는 08-05 16:42 UTC merge인데 위 추세는 그 전에 이미
   완성돼 있었고, 방향으로는 오히려 반대다 — frontend-only PR에서 Rust lint·세 builder·네 worker·
   Native Skia가 생략되므로 그 job들이 만들던 캐시 세대도 생성되지 않는다.

   세대 상한도 정상이다. (그룹, ref) 쌍 42개 중 2세대를 초과한 쌍은 3개뿐이고 전부 마지막 스윕 이후
   생성분이다. 실제 원인은 ① 쌍 수 증가로 KEEP=2의 하한 자체가 올라간 것(`refs/heads/devel` 한 ref만
   19개 5.84GB로 기준선 전체 초과, 열린 PR ref는 스윕에서 보호), ② 삭제된 브랜치의 고아 캐시가
   (그룹, ref)별 최신 2세대 규칙 때문에 영구히 정리 대상이 아닌 것(현재 약 0.53GB)이다.

   대응은 [#4080](https://github.com/edwardkim/rhwp/issues/4080)으로 분리했다. 4.73GB는 쌍 수가 적던
   시점의 값이라 더는 유효한 목표가 아니므로 Stage 5의 판단 기준으로 쓰지 않는다.
10. **Stage 5A — CodeQL 안전 선행 작업**: `codeqlResult`가 workflow의 `Analyze (...)` job뿐 아니라
    같은 PR candidate SHA에 연결된 GitHub Advanced Security `CodeQL` check까지 성공해야 green을
    반환하도록 보강한다. 기존 blocking Rust matrix lane은 그대로 둔 채 별도 PR 전용
    `build-mode: none` shadow lane을 `upload: never`로 실행해 SARIF artifact와 소요 시간을 비교한다. 이 단계는
    CodeQL 언어를 생략하지 않으며 required check 이름도 바꾸지 않는다.
11. **Stage 5B — CodeQL 언어 분리 활성화**: Stage 5A canary와 merge로 보안 결과·Rust no-prebuild
    동등성 gate를 완료했다. 2026-08-11 `devel` branch metadata에서 required context는 `Build & Test`
    하나이고 세 `Analyze (...)`와 GHAS `CodeQL`은 required가 아님을 확인했다. PR에서는 base SHA의
    trusted classifier가 낸 `codeql_languages`만 실제 analyze에 사용하고, 비-PR 실행과 판정 오류는
    세 언어 full로 닫는다. matrix는 세 언어를 계속 생성해 check 이름을 보존하며 선택되지 않은 lane은
    명시적 no-op success로 끝낸다. cache 총량은 #4080의 새 기준선을 따르고 4.73GB를 게이트로 쓰지
    않는다. Stage 4 canary의 wall clock 575초 중 `Analyze (rust)` 단독이 563초였으므로 기대 효과가
    가장 큰 구간이다.
12. **후속 enforcement**: 보존 prototype을 그대로 복사하지 않고 Stage 3~5 진리표에 맞게 축소한다.
    default-branch controller는 `CI`, `CodeQL`, `Render Diff` 완료 이벤트마다 같은 PR head의 세 workflow
    증거를 함께 조회한다. 일부가 미완료면 pending, 하나라도 실패·누락이면 failure, 모두 기대한
    `success|skipped`와 CodeQL analyze/skip step을 충족할 때만 success를 발행한다. PR head code나
    artifact는 실행·다운로드하지 않는다. trailing review-only fast-pass는 CI 실행 surface가 base와
    같을 때만 허용한다. 정상 릴리즈로 main에 등록한 뒤 live audit를 수행하고, repository admin이
    `devel`의 strict up-to-date와 GitHub Actions expected source를 실제 설정에서 확인한 경우에만 required
    `CI Impact Policy` 적용 여부를 판단한다. 같은 head에서 base만 이동하는 B1→B2 표본은 merge 차단 후
    새 head 재검증으로 고정한다.
13. **Stage 6 — artifact retry**: 논리 label `slow/1/2/3`별 test archive, archive expected count와
   worker run count의 primary/retry 중 하나 이상을 aggregate에서 인정한다.
14. **Stage 7 — draft 경량화**: draft에는 영향별 경량 검증을 수행하고 `ready_for_review`에서 최종 검증을
   보장한다.

각 활성화 단계는 별도 PR로 진행한다. 자연 발생 frontend 표본 5건을 추가로 기다리지 않고 historical
60건·고정 fixture·관측 false negative 0건을 근거로 Stage 3에 착수한다. controller 프로토타입은 대체
controller의 main 등록·live audit 완료 또는 maintainer의 required policy 미채택 결정까지 보존한 뒤,
재사용 근거를 문서화하고 별도 사용자 승인으로 branch/worktree를 정리한다.

Stage 3의 로컬 구현·집중 검증과 PR #3943 리뷰 보정 결과는
`mydocs/working/task_m100_3790_stage3.md`와 `mydocs/pr/archives/pr_3943_review.md`에 기록한다. Stage 4의
Rust·Native Skia 진리표와 로컬 검증 결과는 `mydocs/working/task_m100_3790_stage4.md`에 기록한다.

## 5. Stage 5 보정 계약

- 세 `Analyze (...)` job은 언어별 action·upload 성공을 각각 확인한다. 별도 GHAS `CodeQL` check는
  실측상 가장 먼저 도착한 언어 분석에서 생성·종결되고, 뒤에 도착한 언어 분석으로 갱신되지 않았다.
  따라서 이 단일 check에서 JavaScript/TypeScript·Rust의 policy 결과까지 추론하지 않는다.
- `codeqlResult`는 PR event·base/head repository·head branch·candidate SHA에 묶은 CodeQL workflow run을
  기준으로 세 Analyze job을 모두 확인한다. 이어 candidate SHA의 check-runs 중 app slug
  `github-advanced-security`, name `CodeQL`, 동일 `head_sha`, 현재 workflow run attempt 시작 이후라는
  조건의 단일 policy check도 확인한다. 항목이 없거나 이전 attempt이면 `missing`, 미완료면 `pending`,
  성공 외 결론이면 `failed`로 닫는다.
- Stage 5A는 세 언어의 blocking matrix와 기본 Rust build mode를 보존하면서 중복 수동 cache·prebuild를
  제거했다. 별도 shadow와 raw SARIF는 canary 동등성 확인 뒤 제거했고 PR #4341로 merge했다.
- 기존 prototype의 정적 language selector는 workflow job 성공과 GHAS policy check 성공을 같은 것으로
  취급해 사용하지 않는다. 빈 SARIF나 과거 base 결과를 현재 candidate 결과처럼 다시 업로드하지 않는다.
- Stage 5B는 고정 세 언어 matrix를 유지하고 `codeql_languages`에 없는 lane만 no-op success로 끝낸다.
  실제 checkout·init·toolchain·analyze는 선택된 lane에서만 실행한다. PR은 base SHA classifier만 신뢰하며
  push·schedule·workflow_dispatch와 checkout·API·classifier·출력 검증 실패는 모두 세 언어 full이다.
  preflight가 cancel·runner 장애로 output 자체를 발행하지 못해도 consumer job의 빈 값은 세 언어 full로
  다시 닫는다. fast-pass Summary는 언어 판정을 실행하지 않았음을 `n/a (fast-pass)`로 구분한다.
  현재 required context는 `Build & Test` 하나지만 배포 전 branch metadata를 다시 확인한다.
  `schedule`, `pull_request_target`, `workflow_run`은 기본 브랜치 정의로만 실행되는 운영 경계를 포함해
  main 반영 시점을 별도로 검증한다.
- Rust no-build 설계는 GitHub의 CodeQL workflow configuration 및 compiled-language build mode 문서를
  따른다: [workflow configuration options](https://docs.github.com/en/enterprise-cloud@latest/code-security/reference/code-scanning/workflow-configuration-options),
  [build modes](https://docs.github.com/en/code-security/reference/code-scanning/codeql/build-options-for-compiled-languages).

## 6. Stage 1 범위

- 순수 함수 형태의 classifier와 Node 단위 테스트
- 과거 PR 5개의 GitHub 실제 파일 목록 fixture
- `CI preflight`의 shadow output과 Job Summary 기록
- 현재 heavy worker 조건이 shadow output을 참조하지 않는 정적 계약 테스트
- 계획·단계 보고서·오늘할일 기록

## 7. Stage 1 비범위

- Rust·frontend·Native Skia worker 조건 변경
- Render Diff와 CodeQL workflow 조건 변경
- cache key, restore/save 정책 변경
- artifact upload 재시도
- draft PR 이벤트 정책 변경
- #3789의 `src/main.rs` 모듈 분리 구현

## 8. 검증

```bash
node --test scripts/tests/ci-impact-classifier.test.cjs
python3 -m unittest scripts/tests/test_ci_impact_workflow.py
python3 -m unittest scripts/tests/test_render_diff_workflow.py
npm --prefix rhwp-studio run e2e:renderer-contract
git diff --check
```

사용 가능한 경우 `actionlint .github/workflows/ci.yml`도 실행한다. 장시간 Rust 전체 CI는 Stage 1의 변경
범위가 아니므로 수행하지 않는다.

## 9. Stage 1 완료 기준

- 5개 historical fixture가 이슈의 예상 판정과 일치한다.
- rename·파일 수 경계·classifier/workflow/Cargo·`src/main.rs`·미분류 경로가 full로 닫힌다.
- CI summary에서 모든 영향축, version, reason과 advisory 신뢰 경계가 보인다.
- 기존 worker `if` 조건과 aggregate 계약은 변경되지 않는다.
