# 수행계획 — Task M100 #3931

- **이슈**: [#3931](https://github.com/edwardkim/rhwp/issues/3931)
- **브랜치**: `task/3931-declared-rowbreak`
- **작업공간**: `/tmp/rhwp-3931`
- **기준 commit**: `upstream/devel` `99f6c93124f4c6bc63992a0fc01732e85b27177a`
- **작성일**: 2026-08-13 KST
- **재기준화**: 2026-08-15 KST
- **원격 상태**: 이슈 OPEN, 담당자 `edwardkim`, 연결된 Open PR 없음
- **절차 상태**: Stage 1·2·3, #4763 및 PR 직전 최신 devel 재기준화, Stage 4 자동 검증 및 작업지시자 시각 판정 완료

## 1. 목표

빈 host 문단의 다행 RowBreak 표에서 저장된 내부 `vpos` reset이 다음 물리 조각을 가리키는데도,
flow anchor가 약간 아래로 밀린 상태에서 declared 선이월이 실제 fragment scan보다 먼저 표 전체를
다음 쪽으로 보내는 문제를 고친다.

한컴 2020 PDF를 문단 단위로 다시 대조한 결과, 과거 #3927의 "16줄을 한 쪽에 18.8px pitch로
배치한다"는 판정은 잘못이었다. 실제로는 앞 문단 12줄과 뒤 문단 4줄이 서로 인접한 두 물리 쪽에
나뉘며, 줄 pitch는 저장값과 같은 약 24.5px이다. 따라서 전역 셀 줄높이를 줄이지 않는다.

다음 선후 관계를 지킨다.

1. 현재 저장 줄높이와 잘못된 통짜 이월을 동작 불변 래칫으로 고정한다.
2. HWP의 내부 reset·저장 span·다음 문단 되감김을 함께 충족하는 경우 flow anchor를 저장 anchor로
   재동기화해 기존 fragment scanner가 12+4 조각을 구성하게 한다.
3. `pi=14`의 declared 선이월과 HWPX 무증거 구조는 별도 단계에서 일반화한다.

페이지 수만 383으로 맞추는 상수·관용치 조절은 하지 않는다. 표의 실제 높이와 각 물리 페이지의
콘텐츠 소유권이 한컴 PDF와 맞는지를 완료 기준으로 삼는다.

## 2. 현재 기준선

현재 최신 `upstream/devel` #4763과 #3931 후보의 `release-test` 바이너리로 다시 측정했다.

| 입력·계약 | 현재 | 오라클·기대 |
| --- | ---: | ---: |
| `samples/2025 행정업무운영 편람(최종).hwp` | 383쪽 | PDF 383쪽 |
| 같은 문서 HWPX | 383쪽 | PDF 383쪽 |
| HWP sec=10 pi=14 | 물리 p284 head, p285 tail | 현재 쪽에서 첫 조각 시작 후 다음 쪽으로 이어짐 |
| HWP sec=10 pi=23 | 물리 p287의 12줄 + p288의 4줄 | PDF와 같은 fragment 소유권 |
| pi=23 문제 행의 대표 셀 | `req=422.5`, `content=392.3`, 16줄, pitch 24.51px | PDF pitch 약 18.36pt = 24.48 CSS px |
| `issue_3738_rowbreak_table_footnote_fragment` | 33/33 통과 | 전건 유지 |

초기 기준에서 `pi=14`는 선언 합이 본문 가용 높이를 약 0.1px 넘는 순간 분할 준비 이전에 이월됐고,
`pi=23`은 저장 `vpos` reset이 가리키는 12+4 분할을 무시하고 통째 이월했다. Stage 2·3은 두 좌표를
하나의 tolerance 완화가 아니라 서로 다른 구조 증거로 처리했다.

### 2.1 #4763 이전 devel 재측정 (2026-08-15)

계획 기준 `8479c24f7` 이후 원격 `devel`이 60커밋 전진해 composer 줄바꿈·text measurement 등
관련 접합면이 바뀌었다. 전용 worktree를 `fbca0aa6c` 위로 rebase한 뒤 같은 release-test
바이너리로 다시 측정했다.

- HWP 393쪽, HWPX 386쪽, 한컴 2020 `11.0.0.9136` KoPub 설치 PDF 383쪽 — 기존 기준 유지.
- sec=10 `pi=14`: `cur_h=566.7`, `table_total=554.6`, `declared=175.2`에서 `DIAG_ADVC` — 유지.
- sec=10 `pi=23`: `cur_h=352.2`, `table_total=557.8`, 문제 행 `422.5`, 대표 셀
  `content=392.3`에서 `DIAG_ADVC` — 유지.
- `issue_3738_rowbreak_table_footnote_fragment`: 33/33 통과.

직접 정답지 교차검증에서 PDF 물리 p287은 대상 셀의 첫 문단 12줄, p288은 둘째 문단 4줄로
시작함을 확인했다. p287의 연속 줄 `yMin` 간격은 약 18.36pt, CSS 96dpi 환산 약 24.48px로
저장 LINE_SEG pitch 24.51px와 일치한다. 따라서 `content=392.3`, `required=422.5`는 16줄 전체의
높이로 정상이며, 줄높이 축소는 정답 소유권을 파괴한다.

브라우저 조사에서 발견한 KoPub Canvas2D local face 오인(#4741)과 160쪽 묶음 개체 자식 Image의
표 hit-test 선점(#4753)은 독립 이슈로 분리했다. #4741은 최종 Studio 시각 판정 전에 해결해야 하지만
Rust 페이지네이션 Stage 1~3의 수정 범위에는 포함하지 않는다. #4753은 정적 조판 판정의 선행 조건이
아니다.

### 2.2 #4763 통합 재기준화 (2026-08-15)

원격 `devel`의 #4763(`f8c784235`)은 HWP/HWPX 전체를 각각 383쪽으로 맞추고 source-frame tail
계약을 추가했다. Stage 1~3 여섯 커밋을 이 기준 위로 rebase한 뒤, terminal response tail이 질문 7의
plain-text 저장 reset을 건너는 새 접합 결함을 확인했다. ordinary cut이 가시 텍스트를 가진
control-free 두 문단 사이 reset 직전에 끝나는 경우만 tail 확장을 억제했다. control-only 문단의
로컬 `vpos=0`은 제외해 질문 14 중첩 도형 조판과 전체 383쪽을 보존했다.

최신 결과는 HWP/HWPX 383쪽, 질문 7 물리 p284/p285, 질문 11 물리 p287/p288이다. 상세한 통합
근거와 전체 검증은 `mydocs/working/task_m100_3931_stage4.md`에 기록한다.

### 2.3 PR 직전 최신 devel 재기준화 (2026-08-15)

원격 `devel`이 `99f6c9312`까지 전진해 전용 브랜치를 다시 rebase했다. `f8c784235..99f6c9312`
사이에는 Studio Vite/Subsecond 설정, gym, parser 재귀 제한과 절차 문서 변경이 있으며
`src/renderer/` 변경은 없다. rebase는 충돌 없이 완료됐다.

최신 기준에서 #3930 3건과 #3931 5건, `release-test` 6,027건, Native Skia 58+2+4건,
clippy·fmt·diff 검사와 공식 Docker WASM 빌드가 모두 통과했다. Studio 단위 테스트는 923건 중
922건 통과·1건 skip이다. 재시작한 Windows Chrome 150 CDP에서 Vite HTTP fixture를 사용한 실제
브라우저 검증도 HWP/HWPX 각각 383쪽과 지정 page owner를 통과했다. 비공개 10k A/B 수치는
renderer 기준이었던 `f8c784235` 비교 결과를 보존하며, 이번 renderer 무변경 재기준화만을 이유로
원본 10,000건을 다시 읽지 않았다.

## 3. 기존 시도에서 재사용할 것과 버릴 것

- 과거 커밋 `d783d0666`은 다음 문단 저장 vpos의 되감김을 증거로 `pi=14`를 분할 경로에 보내
  HWP 393→391쪽을 만들었다.
- 그러나 `issue_3738`의 `pi=1778`에서 표 높이 723.5px 대 선언 333px의 과대측정 상태로 분할을
  강제해 당시 회귀 3건이 발생했다. 가드 추가 커밋 `5aa35959c`도 해결하지 못해 최종적으로
  `806c45cd3`에서 동작 변경을 제거했다.
- 이 세 커밋은 재현 근거와 RED 참고자료로만 사용한다. 현재 `typeset.rs`에는 이후 #3820 등
  저장 사다리·각주·내부 reset 특례가 많이 추가됐으므로 과거 diff를 cherry-pick하지 않는다.
- 저장 되감김은 HWP의 양성 증거로 사용할 수 있지만, 이슈의 핵심인 **무증거 HWPX 일반화**를
  대신하지 못한다.

## 4. 가설과 판정 순서

### H1 — 셀 줄높이 과대측정 가설: 기각

현재 문제 셀은 저장 LINE_SEG의 줄높이와 줄간격을 합산해 약 24.51px pitch를 만들며 16줄을
392.3px로 측정한다. PDF 직접 측정도 약 24.48px pitch이고, 16줄은 한 쪽이 아니라 12+4줄로
나뉜다. Stage 1 진단은 이 값을 보존해야 할 계약으로 고정한다.

### H2 — 내부 reset 표의 flow anchor 재동기화

`pi=23`은 저장 span 하단이 현재 쪽에 들어가지만 flow anchor가 저장 상단보다 약 27.5px 아래로
밀려 `saved_object_bottom_fits_current` 판정을 통과하지 못한다. 다행 RowBreak, 빈 host,
내부 저장 `vpos` reset, 다음 문단 되감김, 저장 span이 현재 쪽에 들어감이라는 구조 증거를 모두
요구한 뒤 저장 상단으로 anchor를 재동기화하고 기존 fragment scan으로 보낸다.

### H3 — 분할 가능성을 먼저 확인하는 일반 규칙

`pi=14`처럼 현재 쪽에서 첫 조각을 만들 수 있는 다행 RowBreak 표가 남으면,
`declared_overflows_current` 하나로 즉시 이월하지 않는다. 실제 fragment scan이 의미 있는 첫 조각을
만들 수 있는지 확인하고, 가능할 때만 현재 쪽에서 분할을 시작한다.

- `row_count > 1`, RowBreak, 빈 host, 자리차지 표라는 구조 경계를 유지한다.
- 첫 블록/행 내부 컷과 각주 예약을 기존 scanner가 안전하게 계산하지 못하면 기존 이월을 유지한다.
- 1×1 개체 표(#874·#2439), 단일행 선언 신뢰(#3236), 표 각주·reset 경계(#3738)는 제외한다.
- 전역 tolerance 확대나 특정 `pi`·파일명·쪽수 조건은 사용하지 않는다.

## 5. 단계

### Stage 1 — 식별 가능한 RED 계약과 기준선 고정

1. `RHWP_DIAG_ROWH`에 이슈 좌표를 식별할 최소 문맥을 추가하거나 동등한 테스트 전용 계측을 만든다.
2. `pi=23`의 16줄과 24.5px pitch가 PDF와 일치함을 고정한다.
3. 현재 두 셀 문단이 같은 page index 291을 소유하는 실패와, 정답인 인접 쪽 소유를 RED로 고정한다.
4. HWP 393쪽 기준선과 한컴 2020 PDF 383쪽 계약을 각각 고정한다.

Stage 1은 동작 변경 없이 진단과 RED 계약만 추가한다.

### Stage 2 — HWP 내부 reset의 저장 anchor 복원

**완료(2026-08-15)** — 저장 24.5px pitch와 16줄 전체 높이를 유지하면서 `pi=23`을 page
index 290의 12줄과 index 291의 4줄로 분할했다. 첫 fragment의 본문 bbox 수용도 래칫했으며,
HWP 전체는 393쪽에서 392쪽으로 변경됐다. 상세 근거는
`mydocs/working/task_m100_3931_stage2.md`에 기록한다.

1. `typeset.rs`의 declared 이월 직전에서 다행 RowBreak·빈 host·내부 저장 reset·다음 문단
   되감김·현재 쪽에 들어가는 저장 span을 함께 검사한다.
2. 구조 증거가 완결된 경우에만 flow anchor를 저장 상단으로 재동기화하고 기존 fragment scanner가
   물리 조각을 만들게 한다.
3. `pi=23`의 첫 문단과 둘째 문단이 인접한 서로 다른 쪽을 소유하는지 확인한다.
4. 저장 24.5px pitch와 16줄 전체 높이는 변경하지 않는다.

### Stage 3 — declared 선이월 일반화

Stage 2 뒤 남은 `pi=14`와 HWPX 사례에만 적용한다.

**완료(2026-08-15)** — `pi=14`의 저장 하단·내부 reset·다음 문단 되감김이 완결된 rowspan
형상에만 기존 scanner 진입을 허용했다. scanner는 174.1px 예산에 155.8px 첫 조각을 만들고
답변을 page index 286/287로 나눈다. HWPX는 이미 scanner 경로이며 남은 한 문단 차이는 선행
flow 누적 축으로 판정해 동작을 바꾸지 않았다. 상세 근거는
`mydocs/working/task_m100_3931_stage3.md`에 기록한다.

1. declared 초과 시 즉시 이월하는 분기와 기존 fragment scan 사이의 판정을 구조화한다.
2. `pi=14`가 현재 쪽에서 첫 조각을 만들고 다음 쪽으로 이어지는지 고정한다.
3. 저장 LineSeg가 없는 HWPX에서도 같은 구조가 같은 결과를 내는지 확인한다.
4. 수정 전환을 제거하면 RED가 재발하는 red-check를 수행한다.

### Stage 4 — 광범위 회귀와 시각 판정

**자동 검증 완료(2026-08-15)** — PR 직전 최신 devel 위로 재기준화한 후보에서 `release-test` 전체 6,027건,
Native Skia 공식 3종, Docker WASM, Studio 923건과 비공개 10k 비교를 완료했다. 비공개 코퍼스는
성공/실패 전이 0건, 쪽수 변화 5건 모두 감소 방향이며 해당 5건의 overflow-cell 증가도 0건이다.
상세 집계와 명령은
`mydocs/working/task_m100_3931_stage4.md`에 기록한다.

한컴 2020 PDF와 rhwp의 동일 물리 쪽을 나란히 둔 검토 이미지 4개는
`output/3931/visual/current/issue3931-current/review/`에
생성했다. 자동 게이트를 완료했고, 작업지시자는 동일 물리 쪽 비교와 7702 Studio의 최신 WASM
렌더링을 확인하고 시각 판정 통과를 선언했다.

1. 편람 HWP/HWPX의 전체 페이지 수, 문제 구간 텍스트 소유 순서와 PDF 대응 페이지를 재측정한다.
2. 관련 focused 회귀와 `release-test` 전체를 실행한다.
3. 비공개 10k 코퍼스는 기준 head와 후보 head의 실패·쪽수·overflow 변화를 비식별 집계로 비교한다.
   원본이나 파일 식별 목록은 커밋·push·이슈·PR에 포함하지 않는다.
4. Native Skia 3종과 WASM 빌드를 수행한다.
5. PDF/SVG 시각 근거와 검토용 이미지는 Git 제외 경로 `output/3931/`에 생성해 작업지시자의
   시각 판정을 받는다.

## 6. 변경 범위

### 포함

- Stage 1 진단을 위한 `src/renderer/height_measurer.rs`
- `src/renderer/typeset.rs`의 declared 선이월과 fragment scan 진입 판정
- #3931 전용 focused test, 기존 실제 fixture를 이용한 페이지 소유권 래칫
- 계획·단계 보고·최종 보고 문서와 `output/3931/` 로컬 증적

### 제외

- parser·serializer와 저장 LINE_SEG 값 변조
- 편람 총쪽수만 맞추는 문서별 예외 또는 tolerance 확대
- 기존 #3738·#3820 각주/fragment 소유 정책의 재설계
- 레이아웃 변화와 무관한 Studio·편집 기능
- 신규 HWP/HWPX/PDF fixture 추가 또는 정답지 교체

## 7. 검증 게이트

### focused

- 신규 `issue_3931_*` 계약 테스트
- `issue_3738_rowbreak_table_footnote_fragment` — 현재 기준 33/33 유지
- `issue_3930_hwpx_hwp_save_layout`
- `issue_874_ktx_toc_page_number_right_align`
- `issue_2097_*`, `issue_2105_rowbreak_table_declared_fits`
- `issue_2439`, `issue_3236_split_single_cell_table`
- `issue_1156_rowbreak_fragment_fit`, `issue_1748_rowbreak_straddle_rowspan`

### 전체·플랫폼

- `cargo nextest run --cargo-profile release-test --target-dir target/pr-review --tests --test-threads 12 --no-fail-fast`
- `cargo fmt --check`
- `cargo clippy --all-targets -- -D warnings`
- Native Skia 공식 3종
- `docker compose --env-file .env.docker run --rm wasm`
- `git diff --check`

## 8. 완료 조건

1. `pi=23`의 16줄·24.5px pitch를 유지하면서 첫 문단 12줄과 둘째 문단 4줄이 한컴처럼 인접한
   두 물리 쪽을 소유한다.
2. `pi=14`는 declared 근소 초과만으로 새 쪽에 선이월되지 않고, 현재 쪽 첫 조각과 다음 쪽 꼬리를
   기존 fragment 계약으로 배치한다.
3. HWPX에서도 저장 되감김 증거 없이 같은 구조 규칙이 작동한다.
4. 편람 HWP/HWPX 총쪽수는 진단 ledger로 기록하고, `pi=14`·`pi=23`의 실제 fragment 소유권이
   PDF 방향으로 교정됐는지를 완료 판정의 우선 기준으로 삼는다. 독립 누적 오차를 한 문턱의
   총쪽수 상수로 상쇄하지 않는다.
5. #3738 33건을 포함한 focused·전체 회귀가 통과하고 신규 overflow-cell 악화가 없다.
6. 비공개 10k 비교에서 신규 parse/render 실패와 기존 오라클 정합 문서의 악화가 0건이다.
7. 작업지시자가 `output/3931/` 시각 증적을 판정한다.

## 9. 승인 경계

작업지시자가 2026-08-15에 순차 진행을 승인했다. Stage 1 진단·RED 계약부터 시작하고, Stage 2는
저장 anchor 재동기화와 기존 fragment scan 재사용으로 한정한다. Stage 3은 `pi=14`와 HWPX에 남은
declared 문턱에만 착수한다.
원격 push, PR 생성, GitHub 댓글과 이슈 close는 별도 승인을 받는다.
