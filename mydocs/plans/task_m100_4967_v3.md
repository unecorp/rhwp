# 수행계획 — Task M100 #4967 W8 rank 7 face 교정 qualification

- **이슈**: [#4967](https://github.com/edwardkim/rhwp/issues/4967)
- **상위 tracker**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행**: #4962 W4, #4963 W5, #4966 W7, #5955 W7.5, rank 8·rank 1 qualification
- **후행**: qualification을 통과한 경우에만 별도 face product-correction 자식 이슈
- **작성일**: 2026-08-26 KST
- **작업 브랜치**: `task_m100_4967_v3`
- **통합 기준**: `upstream/devel@70ebacc4c9589e8c778907e179a6dab18cce8eb0`
- **원격 상태**: #4967 OPEN, 담당자 `edwardkim`, rank 8·rank 1은 각각 `no-change` 완료
- **절차 상태**: Stage W8-R7-Q5 완료, rank 7 `no-change`, PR #6106 self-review 기록 준비·trailing push 승인 대기

## 1. 이번 절편의 결정과 목표

이번 절편은 W4 action rank 7 `KoPubWorld돋움체 Light`를 같은 KoPubWorld family의 rank 8 결과로
추정하지 않고 독립적으로 심사한다.

핵심 질문은 다음과 같다.

> 현재 rhwp가 `KoPubWorld돋움체 Light`의 실제 layout advance를 선택하지 못하고 있다면, 공식 exact
> TTF의 `hmtx`를 사용한 한 decision-plane 교정이 실제 HWP 편집 습관의 장평·자간·fixed frame 조판을
> 개선하면서 비대상 문서와 native·WASM·브라우저 backend를 안전하게 보존하는가?

그 답에 따라 이번 절편의 산출은 다음 셋 중 하나다.

1. **qualified**: 현재 동작과 기대 동작의 첫 divergence, 한 decision plane, 실제 영향 cohort와
   비회귀 조건을 고정하고 별도 product-correction 자식 이슈 초안을 준비한다.
2. **no-change**: exact metric 후보가 실제 fixed frame을 개선하지 않거나 회귀를 만들거나, 현행 동작이
   이미 동등한 metric을 사용하면 제품 규칙을 바꾸지 않는다.
3. **blocked**: exact·CDN artifact identity, 같은 snapshot의 실제 geometry 또는 portable 정책을 판정할
   근거가 부족하면 재개 조건만 기록하고 제품 규칙을 바꾸지 않는다.

qualification 통과 자체는 metric DB·registry·fallback·paint·font supply 변경이나 GitHub 자식 이슈 등록을
승인하지 않는다.

## 2. 기존 증거와 재사용 원칙

### 2.1 동결 입력

| 입력 | SHA-256 / semantic digest | 이미 확인된 사실 |
| --- | --- | --- |
| W4 공개 ranking | `6947e9e8a6c67a60a54b04dc6f1abf75e3cc66d9096a978d301ba2c10bb4ee3a` | band B, 위험 63,732자, base mass 439,387, 압축·fixed-context 51,924자 |
| W5 rank 7 ladder file | `d6f217f4ce544ee8808d30e4357b7a3b79e5dc6fc5b8921aa88d0973a8ffb83f` | exact/subst/missing 세 물리 상태 완료 |
| W5 exact profile | `e1952e4136b6ad756c9b51f424e56db032389f4848960f7e75342a322794beca` | exact export가 `KoPubWorldDotumLight` subset과 exact bytes를 사용 |
| W5 exact TTF | `069494cce21a4222c88e537f256b6f46fee209375aba769f82431b2d382bc84f` | localized·English names가 같은 SFNT, unitsPerEm 1000, OS/2 fsType 8 |
| W5 fixture | `1cc8062c6fd0da39cfddc4182115226717516d4250e693b43596293374236f9e` | font bytes 없는 공개 HWPX 입력, 30줄·304 span·1,556 glyph |
| W5 fixture generator | `b601fdc400d092c796ee26f72a117d61c9d5ed23be9d41ca2c8d6355d2f7b4fa` | 동일 입력을 결정적으로 재생성 가능 |
| W7 projection baseline | `1b05ace839408178681ce89af2c58311958e9b42b73d59e6b6fae2bab553a3b9` | backend별 기존 공급 projection |
| v2 registry raw / rules semantic | `fbab4413007a29600e5d667503e80b861ec4096827a8936943bdf74e58a5ae16` / `bd9469aa16156a16ea262f608015cb0b78e925700ae7df69c38602ba6670c029` | 현행 runtime rule authority와 semantic parent |

W4는 위험 문자 전부를 stored lane으로 분류했고 HWP 3,159자, HWPX 60,699자로 집계했다. W5는
exact-only에서 exact KoPubWorld 돋움 bytes를 사용했지만, 문서가 선언한 `KoPubWorld바탕체 Light`는
subst-only에서 실제 선택되지 않았음을 증명했다. subst-only와 none-related의 font·page·line·span·glyph·
advance projection은 byte-exact하다. 따라서 document substitution을 제품 fallback이나 identity relation으로
승격하지 않는다.

### 2.2 source·공급·metric 경계

- `/home/edward/mygithub/ttfs/kopubworld/KoPubWorld Dotum Light.ttf`는 기존 W5 hash와 일치하는 local-only
  계측 입력이다. 저장소로 복사하거나 font bytes·절대 경로를 공개 산출물에 넣지 않는다.
- W5 official record는 KOGL 1유형 배포와 exact TTF hash를 연결하지만 기존 결정대로 font 재배포는 이번
  범위에 포함하지 않는다.
- 현행 v2 registry에는 `KoPubWorld돋움체 Light`의 Canvas2D WOFF2와 CanvasKit OTF **supply-plane** rule
  두 개만 있다. supply availability는 layout metric compatibility를 증명하지 않는다.
- jsDelivr `font-kopubworld@1.0.3`의 OTF·WOFF2는 실행 시 hash를 고정하고 exact TTF와 cmap·unitsPerEm·
  `hmtx`를 대사한다. 이름이 같거나 같은 package라는 이유만으로 byte·metric identity를 가정하지 않는다.
- localized family, English family, PostScript name, document `substFont`, official successor, surrogate를 서로
  다른 relation으로 유지한다.

### 2.3 선행 qualification에서 재사용할 것과 재사용하지 않을 것

rank 8의 same-snapshot read-only query와 rank 1의 HWP/HWPX runtime boundary·bounded resource guard는
도구 계약으로 재사용한다. 다음 결과는 재사용하지 않는다.

- rank 8 exact metric의 개선·악화 수치와 `no-change` 결론
- rank 1의 `MBatang` virtual relation·전체 domain 동치와 `no-change` 결론
- 같은 KoPubWorld family라는 이유로 rank 8 바탕 metric·fallback을 rank 7 돋움에 적용하는 추정

## 3. 착수 가설과 원인 계보

현재 증거는 다음 흐름을 보인다.

```text
문서 face: KoPubWorld돋움체 Light
  -> W4 raw face coverage: face-miss 63,732자
  -> W5 Hancom exact-installed: exact Dotum bytes 선택
  -> W5 subst-only / missing: 둘 다 HCRBatang-Bold로 동일 귀결
  -> rhwp v2 registry: Canvas2D·CanvasKit supply rule만 존재
  -> Rust layout-name / layout-metric: explicit rank 7 rule 확인되지 않음
```

따라서 첫 가설은 다음과 같다.

```text
provisional hypothesis:
  current rhwp는 rank 7을 heuristic layout advance로 처리할 가능성이 있다. exact TTF hmtx를 사용하면
  current와 Oracle의 선택->advance 경계 차이를 줄일 수 있지만, 실제 fixed frame의 비회귀까지 확인해야
  제품 교정 후보가 된다.

provisional targetDecisionPlane:
  미확정 — Q1에서 layout-name 또는 layout-metric 중 하나로 좁히거나 no-change로 종료
```

HWP와 HWPX는 독립 판정한다. stored LineSeg가 있다는 사실만으로 metric 교정을 적용하지 않고, 현재
문단의 admission validity를 같은 snapshot에서 기능 탐지한다.

## 4. 범위

### 4.1 범위 안

- W3·W4·W5·W7.5 digest와 exact TTF identity의 재확인
- 기존 W3 checkpoint/journal에서 rank 7 cohort를 새 corpus parse 없이 제한 재선정
- W5 generator로 hash-sealed 공개 rank 7 HWPX fixture를 복원하고 결정적 HWP 짝을 생성
- HWP/HWPX의 runtime name·metric trace, native·Docker WASM parity와 첫 divergence 판정
- exact TTF와 현행 heuristic, CDN OTF·WOFF2의 실제 사용 문자 `hmtx` 비교
- 장평·자간·justification·actual fixed frame에서 누적 advance와 첫 crossing 비교
- 후보가 살아남을 때만 기존 cohort의 same-snapshot bounded qualification
- backend별 layout·paint·supply·online/offline 분리 판정과 필요한 직접 시각 증적
- `qualified`·`no-change`·`blocked` 최종 disposition과 qualified 전용 후속 이슈 초안

### 4.2 범위 밖

- 10k corpus 전수 재계측 또는 W4 순위 재산출
- Hyper-V three-state 전체 재실행
- rank 1·8이나 다른 KoPubWorld weight·family로 결과 일반화
- kerning, vertical metric, GPOS/GSUB, variable font 구현
- 여러 decision plane의 동시 수정
- metric DB·registry·fallback·paint·font supply 제품 source 변경
- schema 1.0 registry 또는 generated projection 직접 수정
- font bytes, private corpus 문서·경로·본문·파일명·식별 hash 게시
- GitHub comment·자식 이슈·commit·push·PR 생성

## 5. 단계별 수행계획

### Stage W8-R7-Q0 — 증거 호환성·bounded cohort 고정

1. W4 ranking, W5 ladder/profile, W7.5 registry·projection의 hash와 semantic parent를 검사한다.
2. exact TTF의 name table, unitsPerEm, cmap, `hmtx`, face index, OS/2 fsType를 재확인한다.
3. W3 checkpoint/journal에서 rank 7 문서 집합을 재파싱 없이 결정적으로 선택한다.
4. local-only 원장에는 문서 identity를 mode `0600`으로 남기고 tracked baseline에는 aggregate와 증거 digest만
   투영한다.
5. 입력 크기·line 크기·문서 수·중복 index·출력 symlink를 fail-closed 검증한다.

**종료 게이트**: 동결 digest가 일치하고 W4 수치가 기존 journal cohort와 일치하며, 새 전수 계측 없이
동일 cohort를 재선정할 수 있다.

**실행 결과 (2026-08-26)**: W3 journal을 원문 재parse 없이 1회 streaming 조회해 rank 7의 기존
5문서(HWP 3, HWPX 2)를 재선정했다. target 63,858자 중 W4 위험 63,732자와 category·format·
compressed fixed-context 수치가 모두 일치했고 위험 문자는 전부 stored lane이었다. table-cell은 52,149자,
압축 노출은 63,082자이며 가장 큰 style 축은 ratio 95·spacing -9의 51,226자다. bold 4,468자와 italic
0자도 다음 stage 입력으로 고정했다. exact TTF의 SHA-256, localized·English family, PostScript name,
unitsPerEm 1000, cmap 25,974, OS/2.fsType 8을 재확인했다. 현행 registry·projection에는 supply rule 두 개만
있고 Rust layout-name·layout-metric rule은 0이다. local-only 원장은 mode `0600`, tracked baseline은
mode `0644`로 분리했고 10k·Hyper-V·제품 mutation은 모두 0이다.

### Stage W8-R7-Q1 — 공개 fixture와 current runtime boundary

1. W5 generator로 rank 7 HWPX를 복원해 W5 fixture SHA-256과 일치하는지 확인한다. 불일치하면 fixture를
   새 정답지로 사용하지 않고 중단한다.
2. HWPX에서 결정적 HWP 짝을 만들고 IR·page count·반복 변환 hash를 고정한다.
3. raw document face, parser normalization, layout-name, layout-metric lookup, heuristic와 paint 요청을 각각
   기록한다.
4. HWP/HWPX와 native/Docker WASM trace를 독립 비교한다.
5. 실제 render line bbox·run membership·stored/fresh admission을 같은 snapshot에서 관찰한다.

**종료 게이트**: current miss 여부와 첫 divergence가 한 decision plane으로 좁혀지고 native/WASM parity가
성립하거나, 증거 불일치로 `blocked`가 된다.

**실행 결과 (2026-08-26)**: W5 generator로 rank 7 HWPX를 재생성해 봉인 SHA-256
`1cc8062c…36f9e`와 byte-exact함을 확인했다. `rhwp convert --verify --verify-pages`를 두 번 실행한 HWP5는
동일 SHA-256 `3a844e05…ee7f`, IR 차이 없음, 1쪽 동치를 유지했다. 새 release native와 Docker WASM에서
HWPX·HWP5 각 1,556건의 trace가 형식별 byte-exact했고 metric entry는 전건 `null`, match kind는 전건
`none`이었다. HWPX는 document `substFont`를 paint 후보 체인에 보존하고 HWP5는 보존하지 않지만,
두 형식의 source+layoutMetric projection, fontFamily를 제외한 actual layout run과 fixed geometry는 모두
byte-equivalent했다. 그러므로 substitution metadata 차이는 paint-plane 경계이고 current layout의 첫
divergence는 `layout-metric`으로 확정했다. Q2는 제품 mutation 없이 exact TTF와 CDN OTF·WOFF2의 metric
identity 및 current heuristic 대비 누적 advance만 비교한다.

### Stage W8-R7-Q2 — exact metric·web 공급 artifact 제한 비교

1. exact TTF와 현재 layout 선택 결과를 공개 fixture 전체 문자와 W4 aggregate의 실제 style domain에서 비교한다.
2. CDN OTF·WOFF2를 local temporary cache에만 내려받고 URL·hash·size·SFNT identity를 고정한다. 출력에는
   bytes나 임의 절대 경로를 넣지 않는다.
3. exact TTF·OTF·WOFF2의 cmap·unitsPerEm·glyph advance를 비교해 동일 이름과 metric identity를 분리한다.
4. current transform 순서를 그대로 재생해 장평·자간·justification·clamp 뒤 누적 advance와 첫 frame
   crossing을 계산한다.
5. regular/light face에 bold·italic 요청이 섞여 있으면 현행 style fallback이 advance를 바꾸는지 별도
   audit한다.

**종료 게이트**: 정확히 한 plane의 current->candidate 차이가 설명되고 실제 개선 가능성이 있으면 Q3으로
진입한다. delta가 0이거나 공개 fixture부터 회귀하면 `no-change`로 회수한다.

**실행 결과 (2026-08-26)**: exact TTF와 pinned CDN OTF·WOFF2는 bytes·name·outline identity가 아니지만
공통 cmap 25,973자의 advance mismatch가 0이고 fixture 53자도 metric-compatible했다. current transform
1,556건을 mismatch 0으로 재생한 뒤 exact base를 적용하면 전체 advance는 847,977에서 807,233 HWPUNIT로
40,744 감소했다. record 726건은 개별적으로 넓어지므로 평균만으로 판정하지 않았고, actual fixed-frame
6축에서는 crossing 앞당김·신규 발생 0, 지연 3, 제거 1, 불변 2였다. Q0 실제 style domain 63,858자 전부의
ratio·spacing 축을 지원하지만 aggregate에는 codepoint 분포가 없어 weighted delta는 Q3에 남겼다. bold
4,468자는 regular metric + synthetic bold advance 불변식을 source로 확인했으며 Q3 동적 검증을 필수로
남겼다. 판정은 `qualified-for-q3`, target은 `layout-metric` 한 plane이고 제품 mutation은 0이다.

### Stage W8-R7-Q3 — 기존 실사용 cohort same-snapshot qualification

Q2 후보가 살아남을 때만 진입한다.

1. Q0에서 동결한 문서만 read-only로 비교하고 새 corpus 전수 pass는 하지 않는다.
2. trace·actual `TextLine` frame·run membership·stored-row admission을 한 tree에서 대사한다.
3. HWP/HWPX, body/table-cell/text-box/header/footer, 장평·자간·style 축별 개선·불변·악화를 분리한다.
4. 평균 개선으로 신규 overflow나 line-boundary 회귀를 상쇄하지 않는다.
5. 공개 산출물에는 aggregate와 비식별 좌표만 투영한다.

**종료 게이트**: 최소 한 실제 문서의 설명 가능한 개선과 모든 modelled 비대상 줄의 비악화가 있거나,
그렇지 않으면 `no-change` 또는 `blocked`로 종료한다.

**실행 결과 (2026-08-26)**: Q0 동결 5문서만 same-snapshot evidence query로 재판정했다. source usage
63,858자와 render observation 74,969자는 반복 story 때문에 별도 회계이며, exact metric 74,132자 적용과
current transform mismatch 0을 확인했다. overflow 제거·감소도 301 line 있었지만 신규·증가 171 line은
전부 table-cell이었다. 그중 modelled 51건은 HWPX admitted stored-row의 동일 boundary signature로,
current 0px에서 candidate 0.707px overflow가 새로 생기고 line advance가 +162 HWPUNIT 증가했다. bold가
포함된 modelled regression은 0이지만 nested style 63,465자와 cache 55,461자는 unmodelled이라 전체 evidence
complete를 주장하지 않는다. 결정적 modelled regression은 open gap보다 우선하므로 rank 7 일괄 exact
layout-metric 후보를 `no-change`로 회수한다. private/public 결과는 반복 실행 byte-exact했고 10k·Hyper-V·
제품 mutation은 0이다.

### Stage W8-R7-Q4 — portable·backend·시각 정책 판정

Q3 후보가 살아남을 때만 진입한다.

1. native/WASM layout과 Canvas2D/CanvasKit paint·supply capability를 각각 판정한다.
2. exact installed, online CDN, offline no-supply의 예상 결과를 분리한다.
3. CDN artifact metric이 exact TTF와 다르면 supply rule을 layout truth로 사용하지 않는다.
4. 제품-visible 후보가 있으면 공개 fixture로 Canvas2D·CanvasKit 직접 시각 증적을 만들고, source hash·
   viewport·DPR·backend·font readiness를 manifest에 고정한다.
5. FI-01~FI-14 전항과 비대상 backend semantic hash 불변을 검사한다.

**종료 게이트**: backend별 적용·비대상·미지원 이유와 portable 정책이 모순 없이 설명되고 시각 회귀가 없다.

### Stage W8-R7-Q5 — 최종 disposition과 후속 제안

1. `qualified`, `no-change`, `blocked` 중 하나를 최종 판정한다.
2. qualified일 때만 correction hypothesis, 한 decision plane, affected cohort, registry operation 후보와
   acceptance matrix를 포함한 product-correction 자식 이슈 초안을 작성한다.
3. 실제 GitHub 자식 이슈 등록은 메인테이너의 별도 승인을 받는다.
4. evidence-reopen lane이 운영 tracker로 계속 필요한지 별도로 감사한 뒤 #4967 close 여부를 제안한다.

**실행 결과 (2026-08-26)**: rank 7 최종 disposition을 `no-change`로 확정했다. Q3의 modelled HWPX
table-cell 신규 overflow 때문에 qualified 전용 product-correction 자식 이슈·registry operation·acceptance
matrix와 Q4는 만들지 않는다. GitHub read-only 감사 결과 #4967은 OPEN이지만 실제 sub-issue 0개이고,
#4960의 W8 checkbox도 미완료다. rank 1·7·8은 모두 `no-change`, 나머지 14개는 외부 evidence 변화 전까지
terminal이므로 현재 W8 queue는 비어 있다. 병합 뒤 #4967을 #4960 sub-issue로 연결하고 W8 checkbox를
완료한 다음, 최종 comment와 명시적 reopen 조건을 남겨 #4967을 completed close하는 것을 권고한다. 이번
stage의 GitHub mutation은 0이다.

## 6. 자원·보안·개인정보 보호 불변식

- trace query는 `maxCharacters <= 4096`을 강제하고 0·음수·초과 값을 거부한다.
- 입력은 크기 상한이 있는 regular non-symlink file만 허용한다. journal은 1회 streaming하며 line 크기와
  중복 index를 제한한다.
- 외부 child 실행은 60초 timeout과 64 MiB 출력 상한을 적용한다.
- local-only cohort 출력은 mode `0600`, tracked public projection은 `0644`로 분리하고 기존 출력 symlink는
  거부한다.
- private corpus의 절대 경로·문서명·본문·문서 hash·font bytes는 공개 JSON·Markdown·Git diff에 넣지 않는다.
- CDN 다운로드 파일은 계측 cache일 뿐 tracked asset이 아니며, license 판정 없이 재배포하지 않는다.
- version/build 분기가 아니라 현재 face resolution·metric lookup·LineSeg admission 상태를 기능 탐지한다.
- layout metric, glyph/paint identity와 supply availability를 서로 독립 판정한다.
- actual fixed geometry의 한 줄 회귀도 평균 폭·page count 개선으로 상쇄하지 않는다.

다음 중 하나면 해당 stage에서 중단한다.

- W3/W4/W5/W7.5 입력 hash 또는 exact SFNT identity 불일치
- W5 rank 7 fixture hash를 결정적으로 복원할 수 없음
- HWP/HWPX의 첫 divergence를 한 decision plane으로 좁힐 수 없음
- CDN artifact의 byte·metric identity가 불명인데 supply를 layout truth로 사용해야만 후보가 성립함
- same-snapshot actual frame 또는 stored-row admission을 판정할 수 없음
- modelled cohort에서 신규 overflow·line-boundary 회귀 발생

## 7. 효율·검증 전략

- Q0은 기존 W3 journal의 decision만 읽으며 10k 원문을 다시 열지 않는다.
- Q1·Q2는 공개 fixture와 이미 확보한 exact source를 먼저 사용한다. 여기서 후보가 사라지면 private cohort,
  브라우저 시각 검증과 제품 구현에 진입하지 않는다.
- Q3도 Q0에서 동결한 문서만 제한 재실행한다. 기존 증거가 drift했을 때만 drift한 입력 범위를 다시 만든다.
- Hyper-V는 exact/subst/missing ladder나 source identity가 충돌한 경우에만 별도 계획·승인을 받아 제한 재실행한다.
- 각 stage는 focused contract test와 `git diff --check`를 통과한 뒤 결과를 보고한다. commit은 메인테이너의
  별도 승인 뒤 수행한다.

## 8. 승인 게이트

본 수행계획 승인 전에는 Stage W8-R7-Q0 실행, 새 계측 script·fixture 작성, CDN artifact 다운로드,
private cohort 조회, 제품 source 수정, commit, GitHub comment·push·PR·자식 이슈 생성을 하지 않는다.
계획 승인 뒤에도 각 stage 결과를 보고하고 다음 단계 승인을 받아 진행한다.
