# 수행계획 — Task M100 #4967 W8 rank 8 face 교정 qualification

- **이슈**: [#4967](https://github.com/edwardkim/rhwp/issues/4967)
- **상위 tracker**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행**: #4962 W4, #4963 W5, #4966 W7, #5955 W7.5
- **후행**: qualification을 통과한 경우에만 별도 face product-correction 자식 이슈
- **작성일**: 2026-08-25 KST
- **작업 브랜치**: `task_m100_4967`
- **통합 기준**: `upstream/devel@242c104bd686928844d2444ce84e4f48f861b2e2`
- **원격 상태**: #4967 OPEN, 담당자 `edwardkim`, milestone `v1.0.0`, 연결된 열린 PR 없음
- **절차 상태**: Stage W8-Q0~Q5R 완료, rank 8 qualification `no-change`

## 1. 이번 절편의 결정과 목표

#4967은 여러 font face를 한 PR에서 일괄 수정하는 구현 이슈가 아니라 face별 교정을 통제하는 tracker다.
이번 절편은 첫 process canary인 rank 8 `KoPubWorld바탕체 Light`가 실제 제품 교정 자식 이슈의 진입 조건을
충족하는지 판정한다.

핵심 질문은 다음과 같다.

> 현재 rhwp의 정확히 어느 decision plane이 `KoPubWorld바탕체 Light` 사용 문서의 조판을 잘못 결정하며,
> exact metric 교정이 실제 사용자 문서의 첫 줄 경계 divergence를 개선하면서 비대상 cohort와 backend를
> 그대로 유지하는가?

이번 절편의 산출은 다음 둘 중 하나다.

1. **qualified**: 공개·비공개 증거 경계를 지킨 correction hypothesis와 정확히 한 decision plane을 고정하고,
   별도 product-correction 자식 이슈 초안을 준비한다.
2. **no-change / blocked**: 실제 오류, portable 목표 또는 재배포·backend 조건을 증명하지 못한 이유를 기록하고
   제품 규칙을 바꾸지 않는다.

qualification 통과 자체는 registry change set 작성, 제품 source 수정, 원격 자식 이슈 등록을 승인하지 않는다.

## 2. 착수 기준선과 기존 증거 재사용

### 2.1 Git·이슈 기준선

- 로컬 `devel`, `origin/devel`, `upstream/devel`은 착수 당시 모두
  `242c104bd686928844d2444ce84e4f48f861b2e2`였다.
- 작업 트리는 깨끗했고 같은 이름의 local·remote 작업 브랜치와 #4967 연결 열린 PR은 없었다.
- #4967은 이미 메인테이너에게 할당되어 있어 GitHub comment를 잠금 대체 수단으로 추가하지 않는다.

### 2.2 동결 입력

| 입력 | SHA-256 / semantic digest | 이번 절편의 용도 |
| --- | --- | --- |
| W4 공개 ranking | `6947e9e8a6c67a60a54b04dc6f1abf75e3cc66d9096a978d301ba2c10bb4ee3a` | rank 8 위험량·편집축·lane |
| W5 rank 8 ladder | `d6e8a4371dd049a899a88fb975d6499ed435154b72e1fc804b701addf3cb75ec` | exact/subst/missing Oracle 재사용 |
| W5 exact TTF | `e3ee21a86b6a6728c567a95aaebd8883480f27ce4f230207b0d7266b5cb3fb18` | source identity·`hmtx` anchor, bytes 비공개 |
| v2 registry raw | `fbab4413007a29600e5d667503e80b861ec4096827a8936943bdf74e58a5ae16` | current rule authority |
| v2 rule semantic | `bd9469aa16156a16ea262f608015cb0b78e925700ae7df69c38602ba6670c029` | parent semantic 기준선 |

W5는 같은 rank 8 fixture에 대해 다음을 이미 증명했다.

- exact-only는 PDF subset `KoPubWorldBatangLight`, U+AC00 source `hmtx` 936/1000을 사용했다.
- subst-only와 none-related는 모두 `HCRBatang-Bold`로 수렴했고 typesetting projection이 같았다.
- fallback PDF U+AC00 advance는 exact보다 약 4.3% 컸다.
- exact/subst/missing three-state와 baseline 복구는 제3자 Hyper-V canary에서도 재현됐다.

따라서 이번 절편에서 10k 전수 계측과 Hyper-V three-state 전체 재실행은 하지 않는다.

### 2.3 기존 W3 자료에서 바로 얻은 실사용 cohort

보존된 `final-stage4-c-10k-r2.json`과 checkpoint journal을 새 corpus parse 없이 제한 조회했다.

| 항목 | 기존 자료의 rank 8 결과 |
| --- | ---: |
| 실제 사용 문서 | 6건: HWP 3, HWPX 3 |
| aggregate decision usage row | 164 |
| 전체 문자 | 43,432 |
| 위험 문자 | 43,374: face-miss 43,342 + heuristic 32 |
| 압축 노출 문자 | 30,882, 이 중 위험 category 30,838 |
| 압축·fixed-context proxy 교차 | 9,034 |
| 문맥 | body 23,442, table-cell 18,937, footnote 928, header 125 |
| LineSeg lane | stored 43,432, fresh 0 |

`documentCount`를 usage row 사이에서 합산하지 않고 checkpoint index의 distinct count로 6건을 판정했다.
파일명·경로·본문·index와 문서 hash는 local-only cohort 원장에만 둔다. 공개 산출물에는 위 aggregate와
비식별 cohort ID만 남긴다.

실사용 rank 8 위험량이 전부 stored lane이므로 stored LineSeg 문서는 영향 cohort다. fresh lane은 실제
영향량을 과장하지 않고 공개 합성 fixture의 비대상 회귀·feature-detection 가드로만 사용한다.

## 3. 현행 제품 상태와 작업 가설

v2 registry에서 `KoPubWorld바탕체 Light`가 소유한 active rule은 다음 `supply` 두 개뿐이다.

| projection | 현행 동작 |
| --- | --- |
| `canvas2d-webfont` | jsDelivr WOFF2 exact family 공급 |
| `canvaskit-sfnt` | online OTF exact source plan, offline unavailable |

`rust-layout-name`, `rust-layout-metric`, `canvas2d-paint`에는 이 face의 규칙이 없다. source에서도
`KoPubWorld바탕체 Light`는 기존 KoPub 전용 폭 표의 이름 탐지와 metric DB 모두에 걸리지 않아
`heuristicFullwidth`, `heuristicHalfwidth`, `heuristicNarrow`로 측정된다. 기존 generator로 임시 생성한
rank 8 fixture를 기존 local release binary로 조회한 1,024자도 metric entry 0, `matchKind=none`이었다.
이 trace는 current HEAD build의 정식 증적이 아니므로 Stage W8-Q1에서 다시 고정한다.

작업 가설은 다음과 같다.

```text
correctionHypothesis:
  exact webfont를 paint에 공급하면서 Rust layout은 heuristic advance를 쓰므로
  장평·자간과 fixed frame에서 layout/paint 누적 advance가 벌어진다.

provisional targetDecisionPlane:
  layout-metric
```

다음 사실이 확인되기 전에는 이 가설을 제품 결정으로 승격하지 않는다.

- exact TTF와 현행 Canvas2D WOFF2·CanvasKit OTF의 glyph/advance identity 또는 허용 가능한 차이
- 실제 6문서 cohort에서 heuristic → exact metric이 만드는 최초 줄 경계·overflow 변화
- stored LineSeg가 유효한 문단과 fresh 재조판이 필요한 문단의 기능 탐지 결과
- portable default에서 font bytes를 내장하지 않고도 exact-derived metric을 사용할 근거
- offline paint가 exact font를 조달하지 못할 때 layout exact metric을 유지해도 되는지에 대한 명시적 정책

## 4. 범위

### 4.1 범위 안

- 기존 W3 journal에서 rank 8의 6문서 local-only cohort를 결정적으로 재선정하는 bounded projector
- 공개 rank 8 HWPX qualification fixture와 manifest
- current HEAD의 native/WASM Font Decision Trace 기준선
- exact TTF에서 필요한 문자별 advance를 재현 가능하게 추출한 public aggregate·digest
- 실제 장평·자간·fixed geometry의 누적 advance와 최초 line-boundary divergence 비교
- stored LineSeg validity feature detection과 fresh layout 비대상 가드
- Canvas2D·CanvasKit online/offline capability를 분리한 backend disposition
- qualification 결과와 별도 product-correction 자식 이슈 초안

### 4.2 범위 밖

- 10k corpus 전수 재계측
- W5 Hyper-V three-state 전체 재실행
- 여러 font face 일괄 처리, rank 7·rank 1로 결과 일반화
- kerning, vertical metric, GPOS/GSUB, variable font 구현
- schema 1.0, v2 query model 또는 generated projection 직접 편집
- 제품 registry change set, metric DB, fallback, paint·supply source 변경
- private corpus 문서·경로·본문·식별 hash 또는 font bytes 게시
- GitHub 자식 이슈 등록, commit, push, PR 생성

## 5. 단계별 수행계획

### Stage W8-Q0 — 증거 호환성·재계측 금지선 고정

1. W4 ranking, W5 ladder, W7.5 registry의 digest와 semantic parent를 검사한다.
2. 보존된 W3 checkpoint에서 rank 8 distinct document 6건을 다시 선택하되 corpus를 재파싱하지 않는다.
3. local-only cohort 원장은 입력 index·경로를 외부 출력과 분리하고 owner-only 권한을 유지한다.
4. 공개 결과는 6건, format·context·장평·자간·LineSeg aggregate만 허용한다.

**종료 게이트**: 기존 증거 hash가 일치하고 새 전수 계측 없이 cohort가 결정적으로 재선정된다.

### Stage W8-Q1 — 공개 qualification fixture와 current trace 기준선

1. 기존 `generate_oracle_typesetting_fixture.py`로 exact face와 distinct substitution을 지정한 rank 8
   fixture를 생성한다.
2. ratio 100/90/80, spacing 0/-5/-10, body/table-cell/text-box/header/footer와 stored/fresh lane을
   manifest로 고정한다. kerning bit는 cohort 분류만 보존하며 W9 기능을 변경하지 않는다.
3. fixed frame은 context proxy가 아니라 실제 cell/text-box width와 남은 slack을 측정한다.
4. current HEAD native와 Docker WASM trace가 face miss·heuristic 기준선을 동일하게 설명하는지 확인한다.
5. trace 상한으로 일부 문자가 잘리면 fixture를 숨겨 줄이는 대신 bounded page/paragraph query 또는 테스트
   helper로 전건 coverage를 증명한다.

**종료 게이트**: 공개 fixture bytes·semantic hash, 전건 current trace와 actual geometry 기준선이 고정된다.

**실행 결과 (2026-08-25)**: W5 봉인 generator가 같은 rank 8 fixture SHA-256을 재현했다. current release
native와 Docker WASM trace는 1,556/1,556 record가 canonical byte exact였고 metric entry는 0이었다.
표 셀·글상자의 실제 content width와 장평·자간 대표 6축의 line count·최소 slack·frame crossing을 공개
baseline에 고정했다. 10k·Hyper-V 재실행과 제품 규칙 변경은 0이다.

### Stage W8-Q2 — exact source와 correction hypothesis 검증

1. W5 exact TTF의 name table, unitsPerEm, glyph coverage와 `hmtx`를 재검증한다.
2. 현행 WOFF2·OTF 공급물이 TTF와 동일 계열·weight이며 metric 정책에 사용할 수 있는지 source digest와
   publisher/license 근거로 판정한다.
3. fixture의 실제 문자 집합에 대해 heuristic과 exact `hmtx`의 문자별·run별 누적 advance를 계산한다.
4. 장평과 자간 transform을 같은 순서로 적용하고 첫 line-boundary divergence와 frame slack crossing을 찾는다.
5. `layout-name`, `paint`, `supply`를 바꾸지 않고 `layout-metric` 하나만으로 가설이 성립하는지 반증한다.

**종료 게이트**: target decision plane이 정확히 하나로 확정되거나 no-change/blocked로 종료된다.

**실행 결과 (2026-08-25)**: exact TTF와 CDN OTF·WOFF2의 fixture 53개 및 TTF/CDN 공통 cmap
25,970개 advance가 모두 일치했다. bytes·name·outline identity는 성립하지 않아 paint와 supply에는 이 결과를
적용하지 않는다. current transform 1,556건을 0 mismatch로 재생하고 base만 exact `hmtx`로 바꾼 결과,
fixed-frame 6축은 crossing 지연 3·제거 1·불변 2, 앞당김·신규 0이었다. 따라서 `layout-metric` 한 plane만
`qualified-for-q3`로 판정했다. 제품·registry 변경, 10k·Hyper-V 재실행은 0이다.

### Stage W8-Q3 — 실사용 6문서 bounded qualification

1. local-only 6문서에 current와 candidate metric을 read-only 비교한다. 새 corpus 전수 pass는 하지 않는다.
2. document별 최초 divergence를 paragraph/run의 비식별 좌표와 원인만 기록한다.
3. actual table/header/body geometry, 장평·자간, stored LineSeg validity와 overflow/line boundary를 판정한다.
4. exact metric으로 개선되는 문서, 변화 없는 문서, 악화되는 문서를 분리한다.
5. 문서 identity를 제거한 aggregate만 tracked evidence로 투영한다.

**종료 게이트**: 최소 한 실제 문서의 설명 가능한 개선과 모든 비대상 문서의 비악화가 있거나,
그렇지 않으면 제품 변경 없이 종료한다.

**실행 결과 (2026-08-25)**: Q0의 6문서만 읽어 source usage 43,432자와 실제 페이지 render observation
44,117자를 별도 회계로 유지했다. exact metric 적용 가능 43,735자, cmap miss 보존 213자, 특수 advance
보존 169자였고 current transform은 0 mismatch로 재생됐다. 같은 composition snapshot으로 frame을 조인할 수
있었던 문서는 개선 3·악화 1이었으며, 표 셀 한 줄에서 target 9자의 합계가 +144 HWPUNIT 넓어져 1.92px
overflow가 새로 생겼다. 별도 render-tree exporter와 text-layout query가 다른 run set을 내는 2문서의 target
4,397자는 frame을 조인할 수 없어 `unmodelled`로 남겼다. 또한 기존 query는 stored-row cache admission을
노출하지 않으므로 LineSeg validity claim을 만들지 않았다. 따라서 결과는 `blocked`, 제품 변경 권한은 false이며
W8-Q4에 진입하지 않는다. 재개하려면 같은 snapshot에서 TextLine frame·context와 stored-row admission을 함께
내는 읽기 전용 query 계약을 별도 계획·승인으로 마련한 뒤 동일 6문서만 다시 비교해야 한다.

### Stage W8-Q3R — same-snapshot evidence query 보강과 제한 재판정

Q3의 차단 원인은 metric 후보가 아니라 서로 다른 tree build를 소비한 독립 query의 run set 불일치다. 따라서
Q3R은 제품 font 정책을 바꾸지 않고 다음 읽기 전용 증거 계약만 보강한다.

1. 기존 Font Decision Trace 계산을 이미 만들어진 `PageRenderTree`를 받는 내부 경로로 분리한다. 기존 공개
   API의 출력·상한·해시는 바꾸지 않는다.
2. 새 결합 query는 쪽당 `build_page_tree`를 정확히 한 번 호출하고, 그 snapshot에서 trace record와
   `TextLine` bbox/context 및 동일 순회의 run index 대응표를 함께 낸다.
3. stored-row 상태는 원본 `LineSeg` 존재만으로 추정하지 않는다. 동일 snapshot에서 확인한 source paragraph,
   actual frame과 현행 cache-key 술어를 사용해 `admitted`·`rejected`·`unmodelled`·`notApplicable`을 구분한다.
   외부 wrap geometry나 source mapping을 온전히 재구성할 수 없으면 fail-closed `unmodelled`이다.
4. 문서를 바꾸는 command, metric DB·registry·fallback·paint·supply 및 pagination 결과에는 손대지 않는다.
   query는 `maxCharacters <= 4096` 상한과 page 범위를 그대로 강제한다.
5. 공개 합성 fixture로 단일 tree build, trace 기존 출력 불변, line↔run 전건 join, cache disposition의
   admitted/rejected/unmodelled 경계를 검증한다. 새 integration 원본은 `tests/cases/`에만 두고 generated
   suite·manifest는 제출하지 않는다.
6. 구현 검증 뒤 Q0의 동일 6문서만 다시 읽어 4,397자 미조인을 해소하고 table-cell +1.92px 사례를
   actual disposition과 함께 재판정한다. 10k 전수·Hyper-V는 실행하지 않는다.

**종료 게이트**: snapshot build count가 쪽당 1이고 trace↔line↔run join 누락이 0이며, 모든 target line에
cache disposition 또는 명시적 `unmodelled` 사유가 있다. 이 게이트를 통과해도 Q4 진입은 재판정 결과 승인
뒤에만 가능하다.

**실행 결과 (2026-08-25)**: 공개 fixture에서 쪽당 tree build 1, 기존 trace 불변, line/run join 누락 0과
`admitted`·`rejected`·`unmodelled` 경계를 검증했다. 같은 6문서의 44,117자를 재판정해 이전 미조인 4,397자를
0으로 만들었다. cache disposition은 admitted 15,788자, rejected 10,187자, unmodelled 18,142자였다.
metric-capacity 회귀 5줄 가운데 4줄은 admitted, 1줄은 rejected였고 표 셀 +1.92px 신규 overflow도 admitted로
확인됐다. 따라서 미해석 문자를 더 풀지 않아도 일괄 exact metric 후보는 `no-change`이며 Q4에 진입하지 않는다.

### Stage W8-Q4 — backend·portable 정책과 FI-01~FI-14 판정

1. native, WASM layout parity를 확인한다.
2. Canvas2D WOFF2, CanvasKit online OTF, CanvasKit offline unavailable을 서로 다른 capability로 판정한다.
3. stored-valid, stored-invalid/obsolete, fresh-candidate를 버전이 아닌 feature detection으로 나눈다.
4. FI-01~FI-14를 전항 표로 판정하고 특히 다음 불변식을 hard gate로 둔다.
   - layout metric과 paint identity를 자동으로 합치지 않는다.
   - identity, document substitution, successor, surrogate와 missing-font를 섞지 않는다.
   - web supply 가능성과 metric compatibility를 분리한다.
   - private corpus와 font bytes를 공개하지 않는다.
   - 실제 압축·fixed geometry를 평균 폭이나 page count 하나로 대체하지 않는다.
5. portable default, installed exact-font opt-in과 offline no-supply의 예상 결과를 문장으로 고정한다.

**종료 게이트**: backend별 적용·비대상·미지원 이유와 portable policy가 모순 없이 설명된다.

### Stage W8-Q5 — qualification 판정과 후속 이슈 제안

1. `qualified`, `no-change`, `blocked` 중 하나를 최종 판정한다.
2. qualified일 때만 correction hypothesis, exact one decision plane, affected cohort, registry operation 후보,
   acceptance matrix를 포함한 product-correction 자식 이슈 초안을 작성한다.
3. 실제 자식 이슈 등록은 메인테이너의 별도 승인을 받는다.
4. #4967 tracker는 rank 1·7 및 evidence-reopen lane이 남으므로 rank 8 판정만으로 close하지 않는다.

**종료 게이트**: qualification 보고서 승인. 제품 구현·registry change set은 후속 이슈의 새 계획과 승인 뒤 시작한다.

**실행 결과 (2026-08-25)**: rank 8 일괄 exact metric 후보를 `no-change`로 최종 판정했다. Q3R에서
modelled 회귀가 이미 증명됐으므로 적용 정책을 설계하는 Q4는 미진입으로 종결한다. qualified 전용
product-correction 자식 이슈 초안·등록과 제품 mutation은 0이다. #4967 tracker는 rank 1·7 및
evidence-reopen lane을 위해 OPEN으로 유지한다. 최종 보고서는
[`task_m100_4967_report.md`](../report/task_m100_4967_report.md)이며 메인테이너가 승인해 종료 게이트를 통과했다.

### Stage W8-Q5R — push-preflight 병렬 계수기 격리 정정

Q5 승인 뒤 기본 병렬 `regression_suite_009`를 다시 실행하자 same-snapshot 테스트가 page-tree build를
1이 아니라 4로 관찰했다. 같은 테스트의 단독 실행과 140건 직렬 실행은 통과했다. 원인은 query가 tree를
여러 번 만든 것이 아니라 프로세스 전역 성능 계수기가 generated suite의 다른 병렬 테스트 빌드까지
합산한 데 있었다.

승인된 최소 정정은 다음 경계를 지킨다.

1. 기존 전역 계수기와 이를 사용하는 전용 성능 테스트 계약은 유지한다.
2. generated integration suite용 current-thread page-tree 계수기를 추가한다.
3. #4967 테스트는 다른 스레드의 빌드가 현재 측정 구간에 섞이지 않음을 결정적으로 검증한다.
4. layout, query JSON, metric DB·registry·fallback·paint·supply와 qualification 판정은 바꾸지 않는다.

**종료 게이트**: 기본 병렬 suite 반복 140/140, 직렬 140/140, 기존 전역 계수기 focused test, Clippy,
Python 17/17, fmt와 privacy·문서 검사를 모두 통과하고 정정 결과를 local stage commit으로 고정한다.

**실행 결과 (2026-08-25)**: fresh `--prepare`가 #4967 원본을 `regression_suite_008`에 배치했고 기본 병렬
3회 420/420, 직렬 140/140을 통과했다. 기존 process-global 성능 가드 4건도 focused 통과했고 Python
17/17, Clippy `-D warnings`, manifest check, Markdown 링크, fmt, diff check와 Docker WASM 5분 56초가
성공했다. `pkg` 추적 delta와 제품 query JSON·layout·font 판정 변화는 0이다.

## 6. 재계측·재실행 정책

다음 조건이 아니면 10k 또는 Hyper-V를 다시 실행하지 않는다.

- 동결 W3/W4/W5 입력 hash가 불일치한다.
- exact font bytes 또는 대상 supply bytes가 기존 source identity와 다르다.
- 기존 W5 profile이 필요한 문자·advance·state를 포함하지 않아 correction hypothesis를 판정할 수 없다.
- comparator canonicalization 결함으로 기존 projection을 신뢰할 수 없다는 재현 증거가 생긴다.

이 경우에도 전체 재실행부터 하지 않고 해당 6문서 또는 필요한 physical state 하나만 제한 재실행하며,
사유와 비용을 먼저 보고해 계획 변경 승인을 받는다.

## 7. 보호 불변식

- W4 ranking과 W5 Oracle profile을 다시 쓰거나 현재 환경 결과와 혼합하지 않는다.
- exact font가 없을 때 한컴의 `HCRBatang-Bold` 선택을 rhwp의 portable identity로 오인하지 않는다.
- document `substFont=KoPubWorld돋움체 Light`를 실제 사용된 fallback으로 승격하지 않는다.
- stored LineSeg 존재를 validity로 부르지 않고 문단별 기능 탐지 결과를 남긴다.
- current v2 registry, schema 1.0, metric DB와 generated source는 qualification 동안 0-delta다.
- private 10k source, 문서명·경로·본문·식별 hash와 local font bytes는 commit·PR·log에 넣지 않는다.
- 출력 파일을 저장할 때 host absolute path를 public JSON에 복사하지 않는다.
- rank 8 결과를 같은 KoPub family의 rank 7이나 다른 weight에 추정 적용하지 않는다.

## 8. 중단 조건

- W4/W5/W7.5 parent hash 또는 의미가 현재 정본과 충돌한다.
- exact TTF와 현재 공급 OTF/WOFF2의 metric identity를 설명할 수 없다.
- source provenance·license·redistribution 경계가 불명확하다.
- page count만 같고 glyph position·line boundary가 발산한다.
- 실제 6문서에서 개선 없이 회귀만 생기거나, 개선을 stored LineSeg 강제 신뢰로만 만들 수 있다.
- 두 decision plane을 함께 바꿔야만 가설이 성립한다.
- private 문서 identity를 공개해야만 acceptance를 설명할 수 있다.

중단 시 제품 변경 없이 원인과 재개 조건을 보고하고 다음 계획 승인을 기다린다.

## 9. 검증 전략

qualification 단계에서는 변경 범위에 비례해 다음을 사용한다.

- existing artifact digest·schema·semantic validator
- rank 8 fixture generator 결정성 test
- current native/WASM Font Decision Trace parity
- SFNT name/`hmtx` 추출의 재실행 일치
- actual frame slack·line boundary·accumulated advance comparator test
- 6문서 local-only bounded current/candidate 비교
- Canvas2D·CanvasKit focused test와 시각 판정
- `node scripts/font_rule_registry_v2.mjs check`
- `node scripts/font_rule_projection_gen.mjs check`
- `cargo fmt --all`과 `cargo fmt --all -- --check`
- `git diff --check`

제품 source를 아직 바꾸지 않으므로 full Cargo/npm/Docker WASM·visual sweep은 qualification 결과 승인 뒤
별도 product-correction 계획에서 영향 범위에 맞춰 확정한다.

## 10. 완료 조건

- 기존 계측을 재사용해 rank 8 실제 영향 cohort 6건과 공개 qualification fixture를 분리했다.
- 현재 오류와 기대 결과의 첫 divergence를 설명했다.
- exact one `targetDecisionPlane`과 portable/backend 정책을 확정했거나 no-change/blocked 근거를 남겼다.
- 실제 장평·자간·fixed geometry·stored LineSeg cohort와 fresh 비대상 가드를 판정했다.
- native/WASM과 Canvas2D/CanvasKit의 적용 범위를 분리했다.
- FI-01~FI-14와 비대상 회귀를 전항 판정했다.
- private corpus·font bytes·식별 자료 공개가 0이다.
- qualification 결과를 메인테이너가 승인했다.
- 제품 규칙 변경과 자식 이슈 등록은 0이다.

## 11. 계획 self-review

- **목적**: tracker를 곧바로 구현 이슈로 오인하지 않고 rank 8의 correction eligibility만 판정한다.
- **계보**: W4 위험량, W5 Oracle, W7.5 mutation contract를 서로 다른 증거 역할로 보존한다.
- **효율**: 10k 재실행 대신 기존 journal에서 6문서만 재선정하고 필요한 bounded 비교만 수행한다.
- **CQRS**: qualification query와 future product change command를 같은 단계에 섞지 않는다.
- **SOLID**: layout metric, paint, supply와 LineSeg validity를 독립 책임으로 판정한다.
- **회귀**: fresh 실사용 위험량 0을 숨기지 않고 비대상 guard로만 둔다.
- **개인정보**: 공개 aggregate에서 private 문서 identity를 역추적할 수 있는 필드를 제거한다.
- **운영**: stage, commit, remote mutation, 자식 이슈와 product implementation을 별도 승인 게이트로 둔다.

## 12. 승인 요청 범위

이 계획의 승인은 Stage W8-Q0의 local-only cohort projector·증거 호환성 검사와 Stage W8-Q1의 공개
qualification fixture·current trace 기준선 작성까지다. 다음 행위는 포함하지 않는다.

- 제품 registry change set, metric DB, fallback·paint·supply 구현
- 10k 전수 또는 Hyper-V three-state 재실행
- GitHub 자식 이슈·comment 생성
- commit, remote push, PR 생성 또는 merge
