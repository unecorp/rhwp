# Task M100 #4969 W10-Q3-E — bounded atomic activation 수정 수행계획

- **기준 checkpoint**: `558e27f6d` (Q3-D explicit request owner·dormant transaction)
- **상태**: 수정 수행계획·explicit-only candidate 정책 정정 승인; Q3-E0 `qualified-red-baseline` 결과 승인,
  checkpoint 완료; 최신 devel merge `b5fbed37c` 완료, E0 `qualified-red-baseline-reconfirmed` 결과 승인·
  증적 checkpoint `8a35f820b` 완료; PDF 정답지 갱신 최신 devel merge `351817b21` 완료;
  Q3-E1 `qualified-dormant-reversible` 결과 승인·checkpoint 완료; Q3-E2 `qualified-public-dormant` 결과 승인·
  checkpoint 완료; Q3-E3 `qualified-bounded-explicit-geometry` 결과 승인·checkpoint 완료; 최신 devel merge
  `24055078b` 완료·`qualified-merge-reconfirmed` 재자격화 결과 승인·증적 checkpoint 완료;
  Q3-E4 `qualified-atomic-portable-publication` 결과 승인·checkpoint 생성 승인; 최신 devel merge `a457f289e`
  완료·`qualified-merge-reconfirmed` 재자격화 결과 승인·증적 checkpoint 생성 승인; Q3-E5 성능 병목
  원인 분해·layer-tree cache 정정 및 측정 noise-floor 보완안 승인; Q3-E5 전체 검증 완료·`qualified` 결과 및
  checkpoint 생성 승인
- **작성일**: 2026-08-29 KST
- **제품 출력 변경**: Q3-E3 이후 명시 요청·검증된 bounded subset에서만 있음
- **공개 API 변경**: Q3-E2 별도 승인 뒤에만 있음
- **기계 판독 계획**:
  [`w10_q3_e_execution_plan.json`](../tech/investigations/issue-4969/w10_q3_e_execution_plan.json)

## 1. 수정 이유

Q3-D는 exact `(charShapeId, languageIndex)` slot에 canonical instance request를 소유하고 source/request generation을
cache provenance에 넣었지만, 제품 composer는 여전히 기본 transaction만 사용한다. 기존 Q3-E 초안은 이 다음을
한 절편으로 묶어 다음 세 위험을 충분히 분리하지 못했다.

1. 공개 caller를 열면 malformed 요청뿐 아니라 정상 요청을 해제해 기존 Q2 출력으로 돌아가는 역방향 command도
   필요하다.
2. paragraph transaction은 `HorizontalShapingTransaction` 구체 타입만 받으므로 explicit transaction을 억지로
   끼우면 default lane까지 바꾸기 쉽다.
3. backend capability가 같지 않다. CanvasKit Browser는 producer-resolved monochrome `GlyphOutline`을 재생할 수
   있지만 Native Skia variable `GlyphRun` selector와 SVG·Canvas2D outline replay는 아직 열리지 않았다.

따라서 Q3-E는 **public request**, **composer activation**, **portable publication**, **backend selection**을 서로 다른
승인·checkpoint로 나눈다. 모든 backend를 동시에 여는 것이 아니라 기능 탐지 결과에 따라 지원되는 replay만
선택하고 나머지는 기존 `TextRun`으로 결정론적으로 닫는다.

### 1.1 Q3-E0 사전 감사에서 확인한 계획 정정

현행 Q2 product gate는 `is_bounded_horizontal_shaping_candidate_text()`에서 옛한글 자모가 있는 문자열만 통과시키고,
`eligible_segments()`도 옛한글 자모를 포함한 Hangul scalar 구간만 measurement 대상으로 만든다. 따라서 Q3의 공식
Happiness Sans fixture인 modern Hangul `가변`과 Latin `Typography`는 exact instance request가 있어도 기존 Q2
transaction에 진입하지 않는다.

이를 모르고 공통 gate를 modern Hangul·Latin까지 넓히면 request가 없는 기존 문서도 더 넓은 projection·transaction
경로를 타므로 Q2 무회귀 불변식을 훼손한다. Q3-E는 기존 Q2 old-Hangul gate와 `eligible_segments()`를 그대로 두고,
**exact request 존재 확인 뒤에만** 평가되는 `boundedHorizontalLtrV1` explicit-only single-run policy를 별도로 둔다.
이 정정은 메인테이너 승인을 받았으며 Q3-E0 source test·baseline에 반영한다.

## 2. 고정 결정

### 2.1 명시 요청 API와 권위

최초 public authority는 host가 exact font source를 이미 등록한 뒤 호출하는 다음 두 command다.

- native: `set_exact_font_instance_native`, `clear_exact_font_instance_native`
- WASM: `setExactFontInstance(optionsJson)`, `clearExactFontInstance(optionsJson)`

`setExactFontInstance`의 strict options schema는 다음과 같다.

```json
{
  "charShapeId": 7,
  "languageIndex": 0,
  "mode": "boundedHorizontalLtrV1",
  "axes": [
    { "tag": "wght", "value": 650.0 },
    { "tag": "opsz", "value": 400.0 }
  ]
}
```

`clearExactFontInstance`는 `charShapeId`, `languageIndex`, `mode`만 받는다. JSON은 16 KiB, axis는 16개,
language index는 0..6으로 제한하고 unknown field·unknown mode·중복 tag·비유한 값·실제 `fvar` 범위 밖 값은 snapshot
변경 전에 거절한다. font bytes는 JSON에 넣지 않고 기존 `registerExactFontSource`가 소유한다. 빈 axis와 explicit
default는 유효한 default instance 요청이며 **clear와 같지 않다**.

두 command는 상태·canonical axis·source/request generation만 JSON으로 반환한다. raw font bytes, raw text, host path,
private corpus identity는 반환하거나 trace에 기록하지 않는다. HWP/HWPX parser, 문서 버전, font name, bold,
장평·자간은 이 API를 자동 호출할 권위가 없다.

### 2.2 되돌릴 수 있는 request owner

Q3-D registry에 exact slot 단위 remove를 추가한다. 존재하지 않는 slot clear는 멱등이고 generation을 올리지 않는다.
실제 remove만 request generation을 올리고 measurement context·composition·pagination·page tree를 같은 CQRS command에서
무효화한다. exact source 전체 clear가 request 전체를 비우는 기존 계약도 유지한다.

### 2.3 default lane과 explicit-only candidate lane의 분리

Q2의 old-Hangul candidate predicate와 segment selector는 변경하지 않는다. Q3는 composer가 exact slot request의
존재를 먼저 확인한 경우에만 별도 explicit-only predicate를 평가한다. 이 predicate는 아래 activation surface의 전체
한 run을 하나의 target range로 만들며, 기존 Q2 segment discovery를 일반 script로 확대하지 않는다.

line convergence가 공통으로 소비하는 좁은 measurement session 계약을 만들고 Q2 default transaction과 Q3 explicit
transaction이 각각 구현한다. explicit-only line transaction은 전체 run measurement와 기존 candidate boundary·
available width 검증을 재사용하되, Q2의 `NoComplexRequiredText`/old-Hangul segment 판정은 호출하지 않는다. exact
request가 없거나 preflight가 실패하면 지금과 같은 Q2/default path만 사용한다.

default transaction이 registry를 몰래 조회해 variation을 주입하는 방식은 금지한다. 이 분리로 요청이 없는 모든
문서는 Q2 cache identity·geometry·layer JSON을 그대로 유지한다.

### 2.4 최초 activation surface

다음 조건을 **모두** 만족하는 paragraph만 `boundedHorizontalLtrV1` 후보가 된다.

1. 최종 composed paragraph가 한 줄·한 text run이며 model text와 display text가 같다.
2. 모든 scalar가 같은 exact `(charShapeId, languageIndex)` slot과 같은 shaping style을 사용한다.
3. pure modern Hangul 또는 pure Latin non-ligature이고 language category가 하나다.
4. horizontal LTR, bidi level 0이며 vertical·RTL·rotation·glyph transform이 없다.
5. inline control·tab·field/note split·char overlap·border/background·decoration이 없다.
6. synthetic bold/italic, super/subscript, letter/word/extra spacing, display projection이 없다.
7. 글꼴 크기·장평과 glyph/cluster/run/resource 상한이 기존 Q2/Q3 bounded limit 안이다.
8. exact source, request generation, canonical instance와 replay certificate가 모두 같은 snapshot이다.

한 조건이라도 어긋나거나 line decision이 수렴하지 않으면 paragraph 전체를 Q2/default `TextRun` 결과로 되돌린다.
부분 run만 variable instance로 게시하지 않는다.

### 2.5 publication과 backend 기능 탐지

eligible explicit instance는 같은 measurement `Arc`에서 다음 세 동등 variant를 만든다.

- 기존 `TextRun`: 항상 남는 default fallback
- canonical instance `GlyphRun`: glyph/cluster/geometry와 instance identity 증적
- producer-resolved monochrome `GlyphOutline`: backend reshaping 없는 portable visual replay

모든 validation과 outline 생성은 resource·variant mutation 전에 끝낸다. `GlyphRun`과 `GlyphOutline`이 모두 완전할 때만
같은 equivalence group에 원자적으로 붙이고 nominal lowerer claim을 얻는다. 하나라도 실패하면 둘 다 게시하지 않고
`TextRun`만 남긴다.

최초 backend matrix는 다음처럼 제한한다.

| backend | non-default instance 선택 | Q3-E 동작 |
| --- | --- | --- |
| CanvasKit Browser/Studio | `GlyphOutline` | 활성화 대상 |
| CanvasKit policy analysis | `GlyphOutline` | 선택·음성 계약 검증 |
| Native Skia | 미지원 | variable `GlyphRun` 거절 후 `TextRun` fallback |
| SVG layer | 미지원 | `TextRun` fallback |
| Canvas2D | 미지원 | `TextRun` fallback |

Q3-C의 Native Skia coordinate proof는 capability 증거이지 product selector 승인으로 확대하지 않는다. variable
typeface cache key·construction을 여는 변경과 SVG/Canvas2D outline replay는 후속 작업으로 남긴다.

## 3. 단계와 승인 경계

### Q3-E0 — 최신 기준 확인·red baseline

1. `git fetch upstream devel` 뒤 현재 branch와 최신 devel의 ahead/behind·merge-base를 확인한다.
2. remote가 앞섰으면 구현 전에 merge 수행계획과 승인을 별도로 받고, merge head에서 Q3-D focused 계약을 재통과한다.
3. Q2 old-Hangul gate가 modern Hangul·Latin으로 확대되지 않는 음성 baseline을 고정한다.
4. checkpoint `558e27f6d`에서 no-request/default layer JSON, CanvasKit selection, native/Studio/WASM 크기·성능 baseline을
   같은 명령·release profile로 저장한다.
5. 아래 API·clear·explicit-only activation·rollback 계약을 기존 integration source에 red test로 추가한다.

**종료 게이트**: red 원인이 public adapter 부재, per-slot clear 부재, explicit-only candidate/session 부재,
atomic outline publication 부재의 네 항목과 정확히 일치해야 한다. 결과 승인·checkpoint 뒤 E1로 간다.

**결과**: [`Q3-E0 최신 기준·red baseline 결과`](../working/task_m100_4969_w10_q3_e0.md)는 최신 devel
`0 behind`, default layer·CanvasKit hash 9회 일치, green control 21건 통과와 계획에 대응하는 red 4건을 확인했다.
판정은 `qualified-red-baseline`이며 결과를 승인받았다. checkpoint와 Q3-E1 착수는 각각 별도 승인 경계다.

### Q3-E1 — reversible owner·strict native contract

1. per-slot clear와 strict request DTO/validation을 구현한다.
2. native set/clear command가 effective mutation에만 generation·recompose·pagination을 갱신하는지 검증한다.
3. publication은 계속 0으로 유지한다.

**종료 게이트**: native positive/default/clear/idempotence와 malformed no-mutation 계약 통과. 공개 WASM adapter와 제품
출력은 아직 0. 결과 승인·checkpoint 뒤 E2로 간다.

**결과**: [`Q3-E1 reversible owner·strict native contract 결과`](../working/task_m100_4969_w10_q3_e1.md)는
per-slot clear, strict native set/clear DTO와 effective mutation-only invalidation을 연결했다. native green 24건이
통과했고 E1 red만 green으로 전환됐으며 E2~E4 red 세 건과 제품 게시 0을 유지했다. 동일 시간대 parent/current A/B/A,
Docker WASM·Studio·Clippy까지 통과해 `qualified-dormant-reversible`로 판정했고 결과를 승인받아 checkpoint로
고정했다.

### Q3-E2 — WASM public adapter

1. `setExactFontInstance(optionsJson)`와 `clearExactFontInstance(optionsJson)` 얇은 adapter를 추가한다.
2. Rust와 Node WASM에서 같은 options/result/error schema를 검증한다.
3. TypeScript declaration·consumer 문서·CHANGELOG API 항목을 현행화한다.

**종료 게이트**: public API expansion을 메인테이너가 별도로 승인해야 한다. adapter가 열린 뒤에도 composer publication은
0이어야 한다. 결과 승인·checkpoint 뒤 E3로 간다.

**결과**: [`Q3-E2 public WASM adapter 결과`](../working/task_m100_4969_w10_q3_e2.md)는 strict native
authority를 그대로 호출하는 두 wasm-bindgen adapter와 TypeScript·consumer·CHANGELOG 계약을 열었다. 생성 binding과
실제 Node/WASM 왕복, native green 24건, default output hash, Docker WASM·Studio·Clippy를 통과했다. E2 red만 green으로
전환되고 E3·E4 red 및 제품 게시 0이 유지돼 `qualified-public-dormant`로 판정했고 결과를 승인받아 checkpoint로
고정했다.

### Q3-E3 — bounded explicit composer activation

1. default/explicit measurement session 계약을 도입하되 기존 Q2 old-Hangul predicate·segment selector는 보존한다.
2. exact request 확인 뒤에만 modern Hangul·Latin single-run 전체 범위를 만드는 explicit-only policy를 평가한다.
3. 한 줄·한 run·한 slot preflight를 통과한 경우에만 explicit session으로 line transaction을 실행한다.
4. line width·selection·bbox·next origin·sidecar가 같은 request provenance measurement를 소비하도록 고정한다.
5. negative matrix는 paragraph 전체를 기존 Q2 결과로 rollback한다.

**종료 게이트**: default/no-request canonical output delta 0, eligible instance geometry 변화 증명, 부분 activation 0.
결과 승인·checkpoint 뒤 E4로 간다.

### Q3-E4 — atomic portable publication·backend matrix

1. variable `GlyphRun`과 `GlyphOutline`을 같은 source/equivalence group에 원자적으로 게시한다.
2. CanvasKit Browser/Studio는 outline을 선택하고 malformed/incomplete outline은 `TextRun`으로 되돌린다.
3. Native Skia·SVG·Canvas2D는 non-default instance를 선택하지 않는 음성 계약을 고정한다.
4. explicit default는 Q2와 pixel/geometry가 같고 provenance만 분리되는지 검증한다.

**종료 게이트**: CanvasKit만 non-default visual delta를 보이며 다른 backend는 default fallback과 동일하다. resource·cache
부분 mutation 0. 결과 승인·checkpoint 뒤 E5로 간다.

### Q3-E5 — 성능·크기·전체 회귀·최종 판정

1. default/no-request와 explicit default/interior/max를 같은 환경에서 각각 warm-up 뒤 9회 측정한다.
2. `1/2/8 instance × 1/2/8 run`의 parse/cache/resource/outline count와 payload bytes를 기록한다.
3. native full, all-target Clippy, Native Skia fallback, Node WASM, Docker WASM, Studio full/build와 실제 CanvasKit E2E를
   수행한다.
4. 결과를 `qualified`, `bounded-subset`, `blocked` 중 하나로 판정하고 Q4에 인계한다.

성능·크기 hard gate는 다음과 같다.

- no-request p50 <= baseline +3%, p95 <= baseline +5%
- explicit activation p50 <= default exact-source baseline +25%, p95 <= +40%
- 위 64-build receipt의 default baseline p50이 `1,000,000ns` 미만인 측정 noise 구간에서는 상대비율만으로
  판정하지 않는다. 이때 activation의 default 대비 절대 증가량은 p50 `1,000,000ns` 이하,
  p95 `2,000,000ns` 이하(호출당 각각 15.625us·31.25us 이하)여야 한다.
- page render tree 준비 시간과 warm cache 수치 사이에서 cold first-layer-build p50·p95를 분리해 공개한다.
  기존 영수증은 warm-up 뒤 64-build 합계만 기록해 비교 가능한 cold baseline이 없으므로 cold 수치에는
  사후 hard gate를 만들지 않는다. Q3-E5 결과를 이후 cold 회귀의 최초 기준으로 고정한다.
- release WASM raw delta <= 65,536 bytes이면서 baseline의 0.75% 이하
- font blob은 unique source당 1개, instance face/result/outline cache는 canonical instance 수에 선형이며 상한을 넘지 않음

동일 환경 9회 측정에서도 gate를 넘으면 원인을 최적화하고 재측정하기 전에는 `qualified`로 판정하지 않는다.

## 4. 검증 matrix

### Positive

- pure Hangul·pure Latin 각각 default, `wght=650`, `wght=900`, `opsz=650`, Title 900/900
- axis 순서 permutation·`-0.0`·partial default canonical idempotence
- set -> update -> clear -> set의 generation·cache lineage
- stored one-line LineSeg와 no-LineSeg fresh one-line surface

### Negative·rollback

- source 없음, request 없음, slot mismatch, invalid language index
- unknown/duplicate axis, NaN/Infinity, range 밖, 17 axes, oversized JSON, unknown field/mode
- two lines, two runs, mixed slot/script/language, ligature fixture, RTL/vertical
- bold/italic, spacing, display projection, control/tab/field, overlap, decoration, border/background
- stale source/request generation, outline bbox mismatch, outline command/resource/cache limit
- malformed/incomplete variant를 CanvasKit이 선택하지 않고 cache count가 변하지 않음

### 불변 회귀

- request가 없는 전체 Q1/Q2/Q3 fixture의 candidate 판정·layer JSON·geometry·selection 변화 0
- Q2 old-Hangul predicate·segment selector의 modern Hangul·Latin 확대 0
- existing exact source registration·W9 kerning·Q2 common shaping 무회귀
- font fallback registry v2·generated projection semantic hash 변화 0
- 새 integration source·generated suite·manifest·Cargo marker stage 0

## 5. 제출·검증 경계

- 기존 `tests/cases/issue_4969_*` source만 수정하고 새 integration source를 만들지 않는다.
- `src/**`의 `#[cfg(test)]`를 바꾸면 `node scripts/rust-unit-test-tiers.mjs --check`를 추가한다.
- review worktree와 CI에서만 generated integration suite·manifest를 준비하며 파생 파일은 stage하지 않는다.
- 각 checkpoint·push 전 `cargo fmt --all`과 `cargo fmt --all -- --check`를 실행한다.
- private 10k corpus, Hyper-V, 한컴 Oracle, static/modified font 생성은 사용하지 않는다.
- Q3-E 각 절편의 구현·결과·checkpoint, public API expansion, remote push, PR, GitHub comment는 각각 별도 승인이다.

## 6. 현재 승인 요청 범위

최초 계획과 Q3-E0 explicit-only candidate 정책 정정·결과·checkpoint, 최신 devel 병합·재자격화 계획을 승인받았다.
merge-base 양측 변경 집합의 실제 교집합은 오늘할일 한 파일뿐이었고 merge `b5fbed37c` 뒤 E0 green 21건·계획된
red 4건·default hash·Docker WASM·Studio를 재검증했다.
[`qualified-red-baseline-reconfirmed` 결과](../working/task_m100_4969_w10_q3_e0_requalification.md)를 승인받아
checkpoint `8a35f820b`로 고정했다. 이후 `upstream/devel@d29b5fd4ba40`의 PDF 정답지 갱신 2 commit을 merge
`351817b21`로 충돌 없이 통합했다. Q3-E1 착수 승인을 받아 reversible native owner를 구현·검증했고
`qualified-dormant-reversible` 결과를 승인받아 checkpoint로 고정했다. Q3-E2 착수 승인을 받아 public WASM adapter와
consumer 계약을 구현·검증했고 `qualified-public-dormant` 결과를 승인받아 checkpoint로 고정했다. Q3-E3 착수 승인을
받아 request-gated modern Hangul·ASCII Latin 한 줄·한 run·한 slot geometry를 구현·검증했다. default hash 무변화,
explicit geometry 변화, negative paragraph rollback과 E4 전 paint 부분 게시 0을 확인해
`qualified-bounded-explicit-geometry`로 판정했다. 작업 중 `upstream/devel`이 `2bcf9b261`까지 7 commit 전진했지만
신규 변경 39개 파일과 E3 변경 파일의 교집합은 0이다. Q3-E3 결과와 구현·결과 checkpoint 생성을 승인받아 고정했다.
최신 devel 병합·재자격화 계획을 승인받아 `upstream/devel@2bcf9b261`을 merge commit `24055078b`로 통합했고,
focused/Q2/product/CI 정책·Clippy·Docker WASM을 재검증해 `qualified-merge-reconfirmed` 결과와 정정·재자격화 증적
checkpoint 생성을 승인받아 고정했다. Q3-E4 착수 승인을 받아 atomic pair publication과 backend matrix를 구현했다.
explicit default 제품 출력 변화 결함을 검증 중 발견해 default request는 provenance만 소유하고 product activation에는
진입하지 않도록 원인을 정정했다. focused·Studio·Clippy·Docker WASM을 통과해
[`qualified-atomic-portable-publication` 결과](../working/task_m100_4969_w10_q3_e4.md)를 작성했다. 검증 뒤
`upstream/devel@97c4d7155`가 5 commit 전진했으나 E4 변경 파일과 교집합 및 merge conflict는 0이다. Q3-E4 결과와
구현·결과 checkpoint 생성을 승인받았다. 최신 devel 병합·재자격화 계획을 승인받아
`upstream/devel@965c050f0`을 merge commit `a457f289e`로 충돌 없이 통합했다. focused Q3-E4 33건, 원격 #6373
2건, default hash, Studio, Clippy, Docker WASM을 재검증해
[`qualified-merge-reconfirmed` 결과](../working/task_m100_4969_w10_q3_e4_requalification.md)를 작성하고 결과·증적
checkpoint 생성을 승인받았다. Q3-E5 착수 뒤 반복 page-layer build가 page-local 1.5MB font digest·복사·outline
생성을 매번 되풀이하는 원인을 분해했고, page tree·JSON과 같은 세대에서 무효화되는 bounded PageLayerTree cache와
font-byte `Arc` 공유로 정정했다. 수정 전 대비 약 99.9% 개선 뒤 default 기준이 sub-millisecond noise 구간으로
내려가 상대비율 gate만으로는 호출당 약 2us의 activation clone 비용도 실패시키는 문제를 확인했다. 메인테이너가
절대시간 noise floor와 cold first-build 공개·회귀 ceiling을 추가하는 보완안을 승인했다. 보완 gate에 따른
release 재계측과 Q3-E5 잔여 검증을 완료했다. 성능·WASM 크기·resource 선형성·CanvasKit E2E와 최종
nextest 8,759/8,759, Native Skia·Studio·lint gate가 모두 통과해
[`qualified` 결과 후보](../working/task_m100_4969_w10_q3_e5.md)를 작성했다. 첫 full nextest가 탐지한 exact-font
instance set/clear API는 문서 IR이 아닌 session request/cache만 변경하므로 #2724 EXEMPT에 근거를 명시했고,
focused 5/5 및 최종 full 재실행으로 고정했다. 최종 diff 감사에서는 빈 문서 교체와 머리글·바닥글 숨김의
직접 page-tree 무효화가 새 파생 cache를 놓치는 문제를 발견해 공통 cache-lineage 헬퍼와 보호 테스트 2건으로
정정했고, 최종 소스에서 full·Native Skia·Docker WASM을 다시 통과했다. 메인테이너가 Q3-E5 `qualified` 결과와
구현·결과 checkpoint 생성을 승인했다. 다음 경계는 Q4 인계 여부 결정이며 push, PR, GitHub comment는 별도
승인이다.
