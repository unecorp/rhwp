# 수행계획 — Task M100 #5955 W7.5 font rule registry lifecycle·evidence delta

- **이슈**: [#5955](https://github.com/edwardkim/rhwp/issues/5955)
- **상위 tracker**: [#4960](https://github.com/edwardkim/rhwp/issues/4960)
- **선행**: #4939 W0·W1, #4961 W2, #4963 W5, #4964 W6, #4966 W7
- **후행**: #4967 W8, cohort gate를 통과한 #4968 W9
- **작성일**: 2026-08-24 KST
- **통합 기준**: 최초 `upstream/devel@8a880baea3be295477b62196958c2ada90e3f505`,
  W7.5-7 재확인 `upstream/devel@385e93b2c317d1f50d874fd655e88cf4b2a1ba07`
- **작업 브랜치**: `task_m100_5955`
- **절차 상태**: Stage W7.5-7 self-review·최종 보고서·PR 준비 완료, 결과 승인 대기

## 1. 목적

W7은 W1·W6의 역사 증거와 현재 runtime 규칙을 830개 active rule의 schema 1.0 registry로 일회 이행했다.
이 판은 현재 제품 선택을 재현하는 봉인 snapshot이므로 rule 수, status, W1/W6 anchor와 backend별 population을
고정한다. 이 제약을 단순히 풀면 다음 mapping 수정이 과거 evidence를 현재 정책으로 위장하거나 여러 backend의
변화를 한 번에 숨길 수 있다.

#5955의 목적은 schema 1.0을 수정 가능한 파일로 바꾸는 것이 아니다. 다음 세 층을 분리해 W8의 실제 face
교정이 감사 가능한 command로만 들어오게 하는 것이다.

```text
sealed v1 registry + approved append-only change sets
                         │
                         ▼
              schema 2.0 lifecycle registry
                         │
                         ▼
       active rules only → five backend projections

all rules + evidence + change sets → migration/delta audit
```

초기 v1→v2 이행 자체는 제품 규칙을 바꾸지 않는다. 830개 rule의 selection tuple과 active 순서, 다섯
projection의 semantic hash, native/WASM renderer 결과가 기존과 같아야 한다.

## 2. 현재 구조와 이 문제가 생긴 이유

### 2.1 schema 1.0은 의도적으로 변경을 표현하지 못한다

현재 `scripts/font_rule_registry.mjs`는 다음 세 역사 입력에서 registry 전체를 다시 만든다.

- W7 pre-migration projection baseline
- W1 ledger·candidate
- W6 metric lineage

validator는 830개 전부 `active`, projection별 171/67/281/153/158, legacy unknown 43개와 W1/W6 anchor를
고정한다. 이는 W7 당시에는 올바른 보호 장치였지만 다음 rule의 추가·교정·retirement를 표현할 command
source가 없다.

### 2.2 현재 evidence는 역사 anchor만 소유한다

각 rule의 `evidence`는 W1 rule/candidate, source boundary, W6 metric entry와 pre-migration projection
hash를 설명한다. W5 Oracle Profile이나 이후 공개 fixture·source discovery가 어느 rule 변경을 정당화했는지,
그 evidence의 parent와 digest가 무엇인지 표현할 일반 계보는 없다.

### 2.3 배열 행 삭제·제자리 수정은 audit를 파괴한다

현재 배열 순서는 generated projection의 순서다. 행을 삭제하면 과거 trace가 가리킨 `ruleId`를 잃고,
target이나 decision plane을 같은 ID에서 바꾸면 같은 `ruleId`가 시점별로 다른 의미를 갖는다. schema 2.0은
행 삭제나 in-place semantic mutation을 허용하지 않고 lifecycle event로만 표현해야 한다.

### 2.4 backend projection과 registry provenance가 결합돼 있다

projection generator, `font_metric_lineage.mjs`, public projection integration test와 generated source의
sentinel이 현재 v1 경로를 직접 참조한다. 새 판을 추가할 때 consumer inventory를 함께 이행하지 않으면 v1과
v2가 동시에 authority가 되는 이중 정본이 생긴다.

## 3. 2026-08-24 baseline

계획 작성 전에 다음 검사를 실행해 모두 통과했다.

```text
font rule registry: ok
font rule projections: ok
font rule projection baseline: ok
Node contract: 29/29 pass
```

봉인 artifact SHA-256은 다음과 같다.

| artifact | SHA-256 |
| --- | --- |
| `assets/font-rules/font_rule_registry.json` | `f549ca3a8807be712cc197daf14d96abb1e5f075ac55f1d9142db67c1a56681a` |
| `assets/font-rules/font_rule_registry.schema.json` | `068327e9f49843c54d0f4da16d6f0081bca86b38fe85e362c8416462f15d3ab4` |
| `mydocs/tech/investigations/issue-4966/font_rule_projection_manifest_v1.json` | `77089c7dfbb3c6759161d839f5cb8b753c3271e07bb556d6eba87ef45cfaa20d` |
| W7 one-time migration manifest | `11b93350a0702c75af07ffde7bae4aff2dab332c43ad9bb57d1e3cf1a1747e83` |

현재 projection content 기준선도 보존한다.

| projection | rule 수 | content SHA-256 |
| --- | ---: | --- |
| `rust-layout-name` | 171 | `fb16d4219f641e84702ddee52df84a15f13b28dcddee64161f598dcb982ba7ea` |
| `rust-layout-metric` | 67 | `adea2db08707ce3737904005bc566e790d919294db5b52927770b02ef9c81171` |
| `canvas2d-paint` | 281 | `c523f9681184598f6cd4d7ed113439fb8510e0cdb2e157afd2b5f642d71ba0a9` |
| `canvas2d-webfont` | 153 | `4a9c0ac3db7e042b785721ba9622e4f9c13ae3d0e4922517bacd4ff1781a004d` |
| `canvaskit-sfnt` | 158 | `711a34d57bcec39fa5d420bb257f2a17783a1692cc647cf205d4bec32b0b2807` |

generated sentinel이나 manifest provenance가 v2 경로를 표시하면서 source content hash는 달라질 수 있다.
그 경우에도 projection row의 canonical semantic hash와 컴파일된 선택 결과는 위 기준과 같아야 한다.

## 4. 핵심 설계 결정

### 4.1 schema 2.0을 별도 판으로 추가한다

`status`, lifecycle, evidence graph와 change-set command가 추가돼 v1 validator와 호환되지 않으므로 다음 판은
`1.1`이 아니라 `2.0`으로 한다. v1 registry·schema·W7 migration은 현재 경로와 byte를 그대로 보존한다.

신규 정본 후보는 versioned path를 사용한다.

```text
assets/font-rules/font_rule_registry_v2.schema.json
assets/font-rules/font_rule_change_set.schema.json
assets/font-rules/font_rule_registry_v2.json
assets/font-rules/font_rule_change_sets/*.json
mydocs/tech/investigations/issue-5955/
  font_rule_registry_v1_to_v2_migration.schema.json
  font_rule_registry_v1_to_v2_migration.json
```

v2 통합 뒤 projection generator와 현재 consumer가 v2만 읽도록 전환한다. v1은 역사 입력·audit anchor로만
남기며 두 판을 제품 선택에 동시에 사용하지 않는다.

### 4.2 change set은 command, registry와 projection은 query model이다

W8 이후 제품 규칙 변경은 generated registry JSON을 직접 편집하지 않고 승인된 append-only change set을
추가한다. reducer는 봉인 v1과 순서가 고정된 change set을 읽어 v2 registry를 결정론적으로 만든다.

각 change set은 최소한 다음을 가진다.

- `changeSetId`, issue, parent registry SHA-256과 sequence
- 정확히 한 `decisionPlane`
- `add-rule`, `augment-evidence`, `retire-rule`, `retire-and-replace` operation
- evidence record와 parent evidence ID·artifact digest
- 예상 pre/post selection tuple, active projection delta와 비대상 projection 무변화 선언

parent digest가 현재 reducer head와 다르면 stale change set으로 fail-closed한다. 병렬 W8 PR은 같은 parent에서
갈라진 두 event를 자동 병합하지 않고 최신 registry 위에서 뒤 change set을 다시 계산한다.

### 4.3 semantic tuple이 바뀌면 같은 ruleId를 재사용하지 않는다

selection tuple은 다음 필드의 canonical JSON으로 정의한다.

```text
relationType, decisionPlane, sourceBoundaryId, sourceFace, targetFaceOrPolicy,
conditions, decision-group order, projection id/mode,
metricEntryIds, supply payload
```

- tuple이 동일하고 evidence·설명만 보강되면 기존 `ruleId`를 유지한다.
- tuple 중 하나라도 바뀌면 기존 rule을 `retired`로 남기고 새 `ruleId`를 만든다.
- generator 직렬화나 주석 정정은 registry change set이 아니며 같은 semantic hash를 유지한다.

이 규칙으로 한 `ruleId`가 시점에 따라 다른 선택 의미를 갖는 것을 막는다.

### 4.4 retired rule은 보존하지만 runtime에서 제외한다

v2 rule은 `active|retired` 상태와 lifecycle을 가진다.

- active: `introducedBy`, optional `lastEvidenceChangeBy`
- retired: `retiredBy`, reason, successor rule ID 목록과 retirement evidence
- 모든 상태: immutable selection tuple hash와 projection sequence

retired rule은 registry·migration·offline trace audit에는 남지만 다섯 runtime projection에서는 제외한다.
active rule의 projection sequence만 유일·연속이어야 하며, replacement가 retirement된 rule의 active slot을
명시적으로 승계할 수 있다. 기존 decision-group `order`는 이 sequence와 혼합하지 않는다.

### 4.5 W2 trace는 runtime schema를 불필요하게 늘리지 않는다

현재 trace가 내보내는 active `ruleId`는 그대로 유지한다. 별도 lifecycle resolver가 trace `ruleId`를 v2
registry에 join해 다음을 설명한다.

- carried-forward active
- 새 active rule과 도입 change set
- retired rule, retirement reason과 successor

초기 이행에서 active trace JSON이나 renderer output 필드를 늘리지 않는다. future W8에서 public trace
schema 변경이 실제로 필요하면 그 face 이슈에서 별도 승인한다.

### 4.6 source boundary는 legacy evidence가 아니라 selection semantic이다

Stage W7.5-3 착수 검사에서 projection generator가 모든 backend row에 단일 source boundary를 요구하지만,
신규·replacement rule payload는 이를 표현하지 못하는 계약 누락을 발견했다. carry-forward rule만 legacy
`evidence.sourceBoundaryIds[0]`에 우연히 의존하면 초기 이행은 통과해도 W8의 `add-rule`이 projection 단계에서
실패한다.

v2 rule과 change-set payload는 단일 `sourceBoundaryId`를 필수 필드로 가진다. 이 값은 selection tuple에
포함하며 evidence-only operation으로 바꿀 수 없다. 초기 830개는 v1의 정확히 한
`evidence.sourceBoundaryIds[0]`에서 무변화 이행하고, legacy evidence 배열도 감사 근거로 그대로 보존한다.
신규 rule은 legacy evidence가 `null`이어도 독립적인 source boundary를 가지므로 projection 가능해야 한다.

## 5. schema·보안 상한

schema와 수기 validator는 같은 제한을 가져야 하며 mutation test로 둘의 불일치를 막는다.

| 항목 | 상한·규칙 |
| --- | --- |
| 전체 rule | 최대 4,096, `ruleId` 전 상태 전역 유일 |
| 한 change set operation | 최대 64, #4967의 face·decision-plane 단위 PR 원칙 우선 |
| evidence record | change set당 최대 128 |
| rule당 evidence/successor 참조 | 각각 최대 16 / 8 |
| 문자열 | 기존 계약과 같은 최대 2,048자 |
| metric entry 참조 | rule당 기존 최대 600 유지 |
| evidence graph | cycle·self-parent·dangling parent 거부 |
| path | checkout 상대경로만, `..`, symlink escape, host absolute와 `file://` 거부 |
| 외부 자료 | HTTPS와 digest·license/provenance 분류가 있는 경우만 허용 |
| unknown field | 전 계층 거부 |

private corpus 원본·본문·식별 파일 목록·host path·font bytes는 change set과 evidence에 넣지 않는다. 필요한
경우 기존처럼 aggregate·비식별 결과나 공개 controlled fixture의 repository path와 digest만 기록한다.

## 6. 초기 v1→v2 migration 계약

초기 migration은 미래 mutation fixture와 구분한다. 실제 canonical v2 결과는 다음이어야 한다.

| 항목 | 기대값 |
| --- | --- |
| v1 rule | 830 active |
| v2 rule | 830 active, 0 retired |
| ruleId | 830개 전부 동일 |
| disposition | 830개 전부 `carry-forward` |
| selection tuple | 전부 동일 hash |
| active projection 순서·수량 | 171/67/281/153/158 동일 |
| projection semantic delta | 다섯 plane 모두 `unchanged` |
| product metric·fallback·paint·supply | 변화 0 |

migration manifest는 v1/v2 schema·registry path와 SHA-256, per-rule before/after hash·tuple hash·disposition,
projection별 pre/post semantic hash·row delta를 가진다. aggregate만 같고 개별 rule이 바뀌는 상쇄 변경은
통과시키지 않는다.

## 7. 예상 파일 영향면

### 7.1 신규 source·artifact

- v2 registry, change-set과 migration JSON schema
- 초기 carry-forward change set과 generated v2 registry
- v1→v2 migration manifest
- `scripts/font_rule_registry_v2.mjs`와 focused test
- lifecycle audit/resolver fixture와 필요 시 public integration 원본
- `mydocs/tech/investigations/issue-5955/README.md`, stage·최종 보고서

### 7.2 현행 consumer 변경 후보

- `scripts/font_rule_projection_gen.mjs`와 tests: v2 active rule만 projection
- `scripts/font_metric_lineage.mjs`: canonical metric rule join을 v2로 전환
- `assets/font-rules/font_rule_projection_manifest*.json`: v2 registry provenance와 active/retired count
- generated Rust·TypeScript sentinel·source: generator 결과만 반영, 직접 편집 금지
- `tests/cases/issue_4966_font_rule_projection.rs`: W7 historical assertion과 current-v2 assertion 역할 분리
- 필요 시 `tests/cases/issue_5955_font_rule_lifecycle.rs`: public trace의 active join만 검증

새 integration source는 `tests/cases/` 원본만 PR에 포함한다. generated suite·manifest와 일반 PR의 Cargo
generated target은 review worktree·CI 증적일 뿐 stage하지 않는다.

## 8. 단계별 수행과 승인 게이트

각 stage는 결과 문서를 작성하고 커밋한 뒤 다음 stage 승인을 받는다. 구현 중 schema·artifact 계약이 이
계획과 달라져야 하면 코드를 우회하지 않고 계획 변경 승인을 먼저 받는다.

### Stage W7.5-1 — executable contract red

1. v2 registry, change-set, migration의 최소 schema와 fixture vocabulary를 확정한다.
2. carry-forward, evidence-only, add, retirement, replacement의 positive fixture를 만든다.
3. in-place semantic mutation, stale parent, cross-plane, graph cycle, retired projection, path·상한 위반의
   negative test를 먼저 실패시킨다.
4. v1 artifact 네 파일의 SHA-256 불변 guard를 추가한다.

**종료 게이트**: 실패가 구현 부재 때문이며 v1 현재 검증은 계속 29/29 통과하는지 확인한다.

### Stage W7.5-2 — v2 reducer와 initial migration

1. sealed v1 + ordered change set에서 v2를 만드는 side-effect-free reducer를 구현한다.
2. initial import는 830개 전부 carry-forward하고 lifecycle·selection tuple hash·projection sequence를
   부여한다.
3. schema validator와 수기 semantic validator를 모두 통과시킨다.
4. v1→v2 migration manifest를 생성하고 per-rule 830개를 대사한다.

**종료 게이트**: v1 네 artifact byte 불변, v2 830 active/0 retired, tuple delta 0.

### Stage W7.5-2C — source boundary 계약 최소 정정

1. v2 rule·change-set payload에 단일 `sourceBoundaryId`를 추가한다.
2. selection tuple에 boundary를 포함하고 in-place boundary mutation을 거부한다.
3. 초기 830개 boundary를 v1 legacy evidence와 전건 대사한다.
4. v2 registry·migration·fixture·Stage W7.5-2 보고서의 파생 hash를 재생성한다.

**종료 게이트**: boundary mismatch 0, before/after tuple delta 0, focused·v1 baseline 전부 통과. 이 정정은
실제 mapping이나 projection output을 바꾸지 않으며 완료 뒤 W7.5-3을 다시 승인받는다.

### Stage W7.5-3 — projection cutover와 minimal-impact 증명

1. projection generator가 v2 active rule만 읽도록 전환한다.
2. retired rule이 어떤 projection에도 나오지 않는 negative contract를 통과시킨다.
3. 다섯 projection의 active row 순서·semantic hash를 v1과 대사한다.
4. sentinel·manifest provenance 때문에 source content hash가 바뀌면 semantic 0-delta와 분리해 기록한다.
5. `font_metric_lineage.mjs`와 current consumer를 v2 단일 authority로 전환한다.

**종료 게이트**: active population 171/67/281/153/158, 비대상 projection semantic delta 0, current runtime
selection snapshot 0-delta.

### Stage W7.5-4 — lifecycle audit와 W2 trace join

1. trace `ruleId`를 v2 lifecycle에 resolve하는 offline audit API/CLI를 구현한다.
2. carried, new, retired, replaced와 dangling ruleId를 구조화된 reason으로 판정한다.
3. successor cycle·cross-plane replacement와 evidence parent 손상을 fail-closed한다.
4. current active trace envelope와 renderer output은 바꾸지 않는다.

**종료 게이트**: active W2 trace join 전건 성공, synthetic retired/replacement fixture 설명 성공, current trace
byte delta 0.

### Stage W7.5-5 — future W8 mutation rehearsal

실제 제품 mapping을 바꾸지 않는 임시 fixture에서 다음 네 흐름을 실행한다.

1. evidence-only: 같은 ruleId·tuple 유지
2. add: 새 ruleId와 한 decision plane의 active projection 추가
3. retire: 역사 row 보존·runtime 제외
4. retire-and-replace: old successor와 새 predecessor 연결, active slot 승계

각 fixture는 pre/post tuple, projection delta, 비대상 backend 무변화와 rollback을 검증한다. fixture 결과를
canonical product registry에는 적용하지 않는다.

**종료 게이트**: 네 lifecycle path와 모든 negative mutation이 결정론적으로 통과.

### Stage W7.5-6 — 제품 0-delta와 전체 검증

순차로 다음을 실행한다.

```bash
node scripts/font_rule_registry.mjs check
node scripts/font_rule_registry_v2.mjs check
node scripts/font_rule_projection_gen.mjs check
node scripts/font_rule_projection_baseline.mjs check
node --test scripts/tests/font_rule_*.test.mjs
node scripts/rust-unit-test-tiers.mjs --check
```

review worktree에서 integration suite를 prepare·check한 뒤 lifecycle/public trace focused test, release-test
전체, Clippy와 format을 실행한다. renderer·WASM 경계의 0-delta 주장이 있으므로 Native Skia 공식 3종,
Studio TypeScript·focused test, Docker optimized WASM과 공개 representative native/WASM parity도 확인한다.
private 10k corpus·Hyper-V Oracle은 재실행하지 않는다.

**종료 게이트**: selection·trace·renderer output 0-delta, required local gate 전부 성공.

### Stage W7.5-7 — self-review·보고서·PR 준비

1. stage 보고서, 최종 보고서와 canonical font fallback 전략을 현행화한다.
2. artifact path·hash·population·test 수와 생략 항목을 과거형 사실로 기록한다.
3. 최신 `upstream/devel` merge simulation과 변경 범위별 gate를 실행한다.
4. commit, remote push, PR 생성은 각각 별도 승인을 받는다.

**종료 게이트**: #5955 완료 조건만 충족한다. #4967 rank 8 qualification이나 제품 mapping 변경을 자동
승인하지 않는다.

## 9. 검증 사다리

### 9.1 schema·reducer

- JSON schema와 semantic validator의 positive/negative fixture 일치
- canonical serialization·hash 결정성
- stale parent·fork·sequence gap·중복 changeSetId 거부
- unknown field, size cap, unsafe path·symlink escape 거부
- 실패 시 canonical/generated set 전체 rollback

### 9.2 lifecycle·evidence

- same ID는 tuple hash 불변일 때만 허용
- semantic change는 retire-and-replace 강제
- retired row 삭제 금지, successor·predecessor 양방향 대사
- evidence artifact digest·parent graph·issue scope 검증
- W1/W6 historical anchor를 current change evidence로 다시 쓰지 않음

### 9.3 projection·runtime

- active rule만 다섯 allowlisted output으로 1회 projection
- one rule/one decision plane/one projection
- initial v2의 active ruleId·order·semantic hash가 v1과 동일
- Canvas2D availability, webfont plan, CanvasKit SFNT capability·load success 분리
- layout metric과 paint face 분리
- native/WASM public output byte parity

### 9.4 제출 경계

- generated Rust·TypeScript는 generator 결과만 stage
- integration은 `tests/cases/` 원본만 stage
- generated suite·manifest·unit-tier inventory는 stage 금지
- font binary, private sample, host path·identifying corpus evidence 0
- 모든 PR·push 직전 `cargo fmt --all`, `cargo fmt --all -- --check`

## 10. 보호 불변식

- v1 registry·schema·W7 migration·pre-migration baseline의 byte와 historical hash를 다시 쓰지 않는다.
- 초기 v2 이행에서 830개 rule의 selection tuple, active order와 의미를 바꾸지 않는다.
- ruleId를 의미가 다른 선택에 재사용하지 않는다.
- retired rule을 삭제하거나 runtime projection에 포함하지 않는다.
- evidence-only 변경을 제품 correction으로, source discovery를 identity alias로 승격하지 않는다.
- 한 change set에서 decision plane을 둘 이상 바꾸지 않는다.
- 한 backend 변경이 비대상 generated semantic hash를 바꾸면 중단한다.
- W5 blocked face를 추정·반복 계측으로 actionable하게 만들지 않는다.
- private corpus·font bytes·host 식별 경로를 artifact나 GitHub에 게시하지 않는다.
- #5955 안에서 rank 8 또는 다른 face의 실제 metric·fallback·paint·supply를 바꾸지 않는다.

## 11. 중단 조건

- v1 artifact hash 또는 W7 baseline 29건 중 하나가 계획 기준과 달라짐
- 같은 목적의 registry lifecycle PR·issue가 동시 진행됨
- 초기 v2 carry-forward에서 ruleId·tuple·projection semantic hash가 하나라도 달라짐
- v2를 단일 authority로 만들 수 없어 v1·v2 runtime dual-read가 필요함
- retired rule을 보존하면서 generator에서 제외할 수 없음
- 같은 ruleId의 in-place semantic mutation 없이는 요구사항을 만족할 수 없음
- current base 변경이 schema·generator·consumer 영향면과 충돌함
- 제품 mapping 변경이나 private Oracle 재실행이 W7.5 완료의 필수 조건으로 드러남

중단 조건이 발생하면 해당 stage commit 전 원인·영향·대안과 새 base SHA를 보고하고 계획 변경 승인을
받는다.

## 12. 완료 조건

- v1 sealed artifact가 원 byte·hash로 보존된다.
- schema 2.0과 append-only change-set command가 add·evidence-only·retire·replace를 표현한다.
- 초기 v2는 830 active/0 retired이며 v1과 selection tuple·projection semantic delta가 0이다.
- evidence parent·digest와 migration pre/post tuple이 결정론적으로 재생성된다.
- inactive rule은 runtime projection에서 제외되지만 lifecycle audit와 trace join에 남는다.
- 한 decision plane 변경이 필요한 backend만 바꾸는 mutation contract가 있다.
- current active W2 trace와 native/WASM renderer output이 기존과 동일하다.
- W8은 schema 1.0 직접 편집 없이 issue-scoped change set으로 시작할 수 있다.
- source·test·generated 제출 경계와 보안 상한이 자동 검사된다.
- 후속 #4967 제품 교정은 별도 계획·승인 없이는 시작하지 않는다.

## 13. 계획 self-review

- **원인 계보**: W7의 고정 수량을 오류로 취급하지 않고 당시 보호 목적과 다음 변경 command 부재를 분리했다.
- **CQRS**: change set command, lifecycle registry query model과 backend projection을 별도 책임으로 둔다.
- **SOLID**: evidence, lifecycle, projection, trace audit가 서로 다른 변경 이유를 가진다.
- **기능 탐지**: backend version이 아니라 active status, evidence, font bytes·glyph·capability 상태를 판정한다.
- **결정론**: parent digest·sequence·canonical hash로 change-set 순서와 결과를 고정한다.
- **보안**: rule·operation·evidence·문자열 상한과 graph/path 검증을 계획에 포함했다.
- **회귀**: 초기 이행은 per-rule tuple과 projection semantic hash로 0-delta를 증명한다.
- **운영**: stage commit, push, PR, merge와 W8 착수를 각각 별도 승인 게이트로 유지한다.

## 14. Stage W7.5-8 PR self-review 정정

PR #6049의 첫 Full CI 성공 뒤 수행한 self-review에서 v2 validator가 v1의 semantic guard를 완전히 이관하지
않았음을 재현했다. 이 정정은 새 기능이나 제품 mapping 변경이 아니라, 본 계획 10절의 “relation 혼합 금지”와
5절의 path·validator 보안 경계를 실제 change-set 입력에 적용하는 누락 복구다.

- 다섯 projection의 relation allowlist와 metric/supply payload 소유권을 v1 validator와 대사한다.
- Canvas2D family·URL·external, CanvasKit capability agreement와 신규 unknown legacy rule 금지를 고정한다.
- malformed nested collection이 `TypeError`가 아니라 구조 오류 목록으로 fail-closed하게 한다.
- 최초 허용된 세 변조를 negative contract로 고정하고 전체 font-rule 계약을 재실행한다.
- correction은 canonical registry·projection·renderer를 바꾸지 않지만 code head가 바뀌므로 기존 녹색 CI를
  재사용하지 않고 새 Full CI를 요구한다.

세부 재현·정정·검증 결과는 [Stage W7.5-8 기록](../working/task_m100_5955_w7_5_stage8.md)에 보존한다.

## 15. 최초 승인 경계(역사 기록)

최초 수행계획 승인 범위는 Stage W7.5-1 executable contract red까지였다. 당시 승인은 다음 행위를 허용하지
않았고, 이후 각 단계는 별도 승인을 받아 진행했다.

- schema 2.0 reducer·generator green 구현
- canonical v2 registry 또는 migration artifact 생성
- 기존 projection generator의 authority 전환
- rank 8을 포함한 실제 font mapping 변경
- remote push, PR 생성, issue close 또는 merge
