# 수행 계획 — Task M100 #6108

- **이슈**: [#6108](https://github.com/edwardkim/rhwp/issues/6108)
- **브랜치**: `codex/issue-6108-zoom-fit`
- **최초 기준 commit**: `upstream/devel` `2166f4065`
- **최종 통합 기준 commit**: `upstream/devel` `1fe0d1480`
- **작성일**: 2026-08-27 KST
- **우선순위**: P1, 82/100
- **절차 상태**: Stage 3 결과 승인·commit·최신 devel 통합 재검증 완료, native stack 게시 준비

## 절차 상태와 stacked PR 경계

- 2026-08-27 작업지시자가 #6108 착수와 후속 #6109 stacked PR 구성을 요청했다.
- 열린 PR을 검색한 결과 #6108·#6109를 선점한 PR은 없었고, GitHub 계정 `postmelee`를 #6108
  담당자로 지정했다.
- 별도 #4608 작업 브랜치는 변경하지 않고 최신 `upstream/devel@2166f4065`에서 독립 worktree를
  만들었다.
- 이 문서는 코드 변경 전 사전 계획이다. 구현은 작업지시자가 수행·구현 계획을 승인한 뒤 시작한다.
- 2026-08-27 작업지시자가 수행·구현 계획을 승인하고 Stage 1 진행을 지시했다.
- 2026-08-27 작업지시자가 Stage 1 공용 계산 계약과 focused 검증 결과를 승인하고 Stage 2 진행을
  지시했다.
- 2026-08-28 작업지시자가 Stage 2 진입점 단일화·무사용 경로 정리와 focused 검증 결과를 승인하고
  Stage 3 진행을 지시했다.
- 2026-08-28 Studio 전체 test·build와 실제 Chrome의 다섯 쪽 배치 E2E를 완료하고 Stage 3 결과 승인을
  기다린다. 작업 중 `upstream/devel`이 67 commits 전진했으므로 승인·commit 뒤 최신 devel 통합 게이트를
  별도로 수행한다.
- 2026-08-28 작업지시자가 Stage 3 결과를 승인했다. Stage 3를 commit하고 최신
  `upstream/devel@1a43a507c` 위로 rebase한 뒤 전체 Studio test·build·Chrome E2E 28/28을 다시 통과했다.
- 2026-08-28 작업지시자가 native stacked PR 게시를 승인했다. 공식 `gh-stack`에 #6108·#6109 기존
  브랜치를 등록하고 `upstream/devel@1fe0d1480` 위로 cascading rebase한 뒤, 정확한 #6108 하단 head에서
  Studio 1,225건(1,224 pass·1 skip), production build 238 modules, Chrome E2E 28/28을 재통과했다.
- #6108은 `devel`을 trunk로 하는 stack의 bottom PR로 완결한다. #6109는 #6108 결과 승인 뒤 #6108
  head에서 별도 브랜치를 만들고 native `gh stack`의 top PR로 분리한다.
- #6108의 remote push와 PR 생성, #6109의 remote push와 stacked PR 생성은 각각 로컬 검증 완료 뒤
  별도 승인을 받는다.

## 목표

1. 자동·한 쪽·두 쪽·맞쪽·여러 쪽의 화면 점유 행·열을 하나의 맞춤 배율 계산 계약으로 표현한다.
2. `폭 맞춤`과 `쪽 맞춤`이 선택한 쪽 배치 전체와 내부 페이지 간격을 뷰포트에 포함하게 한다.
3. 보기 메뉴·상태 표시줄·확대/축소 대화상자·저장된 맞춤 복원이 같은 계산과 명령 경로를 사용하게 한다.
4. 화면 배율의 10~500% 범위를 단일 상수와 clamp 경로로 관리한다.
5. 실제 소비처가 없는 쪽 보기 이벤트·CanvasView 공개 wrapper와 스타일 소비처가 없는 class 토글을
   근거 확인 뒤 제거한다.

## 범위

### 포함

- 자동·한 쪽은 1×1, 두 쪽·맞쪽은 2×1, 여러 쪽은 지정한 가로×세로 블록으로 계산한다.
- `폭 맞춤`은 블록의 가로 폭을, `쪽 맞춤`은 블록의 가로·세로 전체를 뷰포트에 맞춘다.
- 여러 쪽에서는 지정 배열 전체 맞춤이 강제되는 현재 계약을 공통 calculator로 옮기고, 적용되지 않는
  비율 선택 UI는 비활성화한다.
- 명령·대화상자·문서 로드 복원에 동일한 `PageArrangement` snapshot과 page gap을 전달한다.
- 10%, 100%, 400%, 500% 경계와 모든 쪽 배치의 회귀 테스트를 추가한다.
- `page-arrangement-changed`, `CanvasView`의 단일 필드 wrapper getter/setter, `is-neutral` class 토글은
  실제 소비처를 다시 확인한 뒤 무사용 항목만 정리한다.

### 제외

- 사용자 배율의 빈 값·비숫자·범위 오류 표시와 `aria-invalid`: #6109
- 쪽 배치와 배율을 하나의 view-settings transaction으로 적용하는 작업: #6109
- 핀치·슬라이더 preview와 topology commit 분리: #6040
- 적응형 Canvas render scale과 surface 예산: #6041
- 페이지 LRU·행 가상화·prefetch scheduler: #6042
- 반응형 눈금자 표시 정책과 resize 깜빡임: #6187

## 단계와 산출물

1. **Stage 1 — 맞춤 배율 순수 계산 계약**
   - 쪽 배치별 행·열 점유 크기, 폭/쪽 맞춤, page gap, 10~500% clamp를 단일 순수 계산으로 고정한다.
   - 기존 여러 쪽 전용 계산을 공통 calculator로 통합한다.
   - 완료 기록: `mydocs/working/task_m100_6108_stage1.md`
2. **Stage 2 — 명령·UI 진입점 단일화와 무사용 경로 정리**
   - 메뉴·상태 표시줄·대화상자·저장 복원이 같은 metrics와 calculator를 사용하게 한다.
   - 여러 쪽의 무효 비율 UI를 비활성화하고 소비되지 않는 이벤트·wrapper·class 토글만 제거한다.
   - 완료 기록: `mydocs/working/task_m100_6108_stage2.md`
3. **Stage 3 — 통합 회귀와 실제 브라우저 검증**
   - 자동/한 쪽/두 쪽/맞쪽/여러 쪽에서 폭·쪽 맞춤과 모든 진입점의 결과를 비교한다.
   - TypeScript·Studio 전체 테스트·build·실제 browser 검증을 수행한다.
   - 최종 보고서: `mydocs/report/task_m100_6108_report.md`

각 Stage는 focused 검증과 결과 보고 뒤 작업지시자의 결과 승인을 받고 다음 Stage로 이동한다.

## 검증 계획

- `node --test`로 `zoom-fit`, `zoom-dialog`, `zoom-fit-mode-persistence`, `canvas-view-page-arrangement`
  focused test 실행
- `(cd rhwp-studio && npx tsc --noEmit)`
- `npm --prefix rhwp-studio test`
- `npm --prefix rhwp-studio run build`
- 실제 Chrome에서 다섯 쪽 배치와 폭/쪽 맞춤의 메뉴·상태 표시줄·대화상자 결과 비교
- `cargo fmt --all`
- `cargo fmt --all -- --check`
- `git diff --check`

Studio 화면 전용 TypeScript 변경이므로 Rust release-test·Native Skia·WASM 전체 빌드는 기본 범위에서
제외한다. 렌더 엔진 출력은 바꾸지 않으므로 PDF/SVG visual sweep 대신 실제 browser 기능 스모크를
사용한다.

## 위험과 완화

| 위험 | 완화 |
| --- | --- |
| 자동 모드까지 다중 열 기준으로 맞춤 | 자동은 한컴 계약대로 항상 1×1 점유로 고정하고 별도 회귀 테스트를 둔다. |
| 맞쪽 첫 빈 슬롯을 세 쪽 폭으로 오해 | 표시 블록은 두 열로 계산하고 실제 페이지 topology와 맞춤 계산 책임을 분리한다. |
| 여러 쪽 계산 통합 중 기존 1×1~8×8 결과 변화 | 기존 전용 계산 테스트를 공통 calculator 매트릭스로 이관해 before/after 값을 검토한다. |
| 상한 400→500% 변경이 좌표 경로에 영향 | 수치 clamp만 공통 10~500% 상수를 사용하고 Canvas 좌표·문서 모델은 변경하지 않는다. |
| 무사용 API 정리가 숨은 소비처를 깨뜨림 | `rg`와 TypeScript 전체 검사로 저장소 내 소비처가 0인 항목만 삭제한다. |
| #6109와 범위가 섞임 | invalid 입력 표시와 원자 transaction은 테스트도 추가하지 않고 후속 stacked PR로 남긴다. |

## 승인 게이트

1. 작업지시자가 수행 계획과 구현 계획을 승인한다.
2. 승인 뒤 Stage 1을 구현·검증하고 결과 승인을 기다린다.
3. Stage 1 승인 뒤 Stage 2를 구현·검증하고 결과 승인을 기다린다.
4. Stage 2 승인 뒤 Stage 3 통합 검증 범위를 다시 확인하고 실행한다.
5. 최종 결과 승인 뒤 로컬 commit·PR 준비를 완료한다.
6. remote push와 `gh stack submit`을 통한 #6108·#6109 PR 생성은 별도 승인을 받는다.
7. #6109 착수·브랜치 분기·담당자 지정도 #6108 결과 승인 뒤 별도 단계로 진행한다.
