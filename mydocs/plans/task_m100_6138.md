# 수행계획 — Task M100 #6138 기본 도구 상자 한 줄 그룹 스크롤

- **이슈**: [#6138](https://github.com/edwardkim/rhwp/issues/6138)
- **작성일**: 2026-08-26 KST
- **작업 브랜치**: `codex/issue-6118-responsive-style-bar`
- **통합 기준**: `upstream/devel@1011a8947`
- **절차 상태**: Stage 3 완료, #6118 통합 PR 승인 대기
- **관련 작업**: 기본 도구 상자 접기/펴기 #6112·PR #6115, 서식 도구 모음 반응형 #6118

## 1. 배경과 기준선

현재 `#icon-toolbar`는 #3243·PR #3255 이후 `.tb-group` 단위 줄바꿈을 허용하고, 1023px 이하에서는
label을 숨기고 버튼 폭을 44px에서 36px로 줄인다. 이 방식은 모든 명령을 즉시 노출하지만 창 너비에
따라 높이·밀도·명령 위치가 달라진다.

2026-08-26 로컬 Chrome에서 기본 모드의 가시 group을 실측한 결과는 다음과 같다.

| viewport | 현행 높이 | group 행 | 버튼 밀도 | 콘텐츠 폭 |
| ---: | ---: | ---: | --- | ---: |
| 1920, 1280px | 56px | 1행 | label 포함 44px | track 1219px |
| 1024px | 99px | 2행 | label 포함 44px | 1172px |
| 976, 883, 768px | 53px | 2행 | icon-only 36px | 988px |
| 412, 375px | 77px | 3행 | icon-only 36px | 988px |

1024px에서 높이가 거의 두 배가 되고, 1023px 경계를 넘으면 다시 작은 버튼으로 바뀐다. 따라서 device
breakpoint보다 한 줄 scroll viewport가 편집 영역의 위치와 명령의 공간 기억을 안정적으로 보존한다.

## 2. 제품 결정

- `#icon-toolbar`는 지원하는 모든 너비에서 56px 한 줄을 유지한다.
- 1023px 이하에서도 기존 데스크톱의 label 포함 44px 버튼 밀도를 유지한다.
- 콘텐츠가 들어가면 이동 버튼을 숨기고 현재 전체 도구를 그대로 표시한다.
- 콘텐츠가 넘치면 좌우 이동 버튼과 한 줄 scroll viewport를 표시한다.
- 버튼 클릭은 다음·이전의 **가시 divider 경계**와 4px nav 간격으로 이동한다.
- trackpad 수평 wheel과 touch pan은 native horizontal scroll을 사용한다.
- 첫·끝에서는 해당 이동 버튼을 disabled·`aria-hidden` 처리한다. nav는 absolute 24px 표면이라 track을
  밀지 않으며 toolbar 고유 8px 양끝 padding을 유지한다.
- 끝점 이동은 240ms scroll과 목적지 nav 퇴장을 같은 frame에 시작하고 같은 시점에 끝낸다.
- mode group이 바뀌면 시작 위치로 복귀하고 overflow·edge hidden 상태를 다시 계산한다.
- 기존 명령 DOM, 순서, ID, listener, active/disabled 상태를 복제하지 않는다.

```text
#icon-toolbar
├─ [이전 그룹]
├─ .tb-scroll-viewport
│  └─ .tb-scroll-track
│     ├─ 기존 .tb-group / .tb-sep 순서
│     └─ 기존 mode 전용 group
└─ [다음 그룹]
```

## 3. 기존 자산 재사용

- 현재 `.tb-group`, `.tb-btn`, `.tb-label`, sprite·text icon과 command wiring
- `#icon-toolbar`의 theme background, border와 `--ui-*` 상태 token
- #6115의 `#icon-toolbar` ID 기반 visibility와 `Ctrl+F1` focus 계약
- 머리말/꼬리말, 주석, 개체 선택 시 기존 inline `display` 전환
- [기존 구조 계약 테스트](../../rhwp-studio/tests/responsive-toolbar-layout.test.ts)와
  [반응형 E2E](../../rhwp-studio/e2e/responsive.test.mjs)

좌우 이동 기호는 별도 그래픽 자산을 복제하지 않고 현재 글꼴·theme token으로 그리는 이중 꺾쇠를 사용한다.

## 4. 범위

### 4.1 범위 안

- 기존 group을 담는 scroll viewport·track과 좌우 이동 버튼
- 한 줄·56px 고정, desktop label·button 밀도 보존
- divider 경계 이동, native horizontal wheel·touch pan, focus 가시성
- resize와 mode group 변경 시 overflow 재계산
- 시작·끝 방향 버튼 퇴장, toolbar 고유 양끝 padding, accessible label과 hidden 상태
- 1920~375px, 세 skin × light/dark, toolbox 접기/펴기 통합 검증

### 4.2 범위 밖

- `#style-bar`의 #6118 반응형 정책 재변경
- 최상단 `#menu-bar`, 상태 표시줄, renderer·WASM 변경
- 명령 순서·동작·단축키 또는 user setting schema 변경
- 명령 우선순위 기반 자동 숨김이나 더보기 메뉴
- 한글 2024 그래픽 자산 복제

## 5. 단계별 수행계획

### Stage 1 — 기준선과 구조 계약 고정

1. 1920~375px의 높이·행 수·버튼 밀도와 기본 mode 콘텐츠 폭을 실측한다.
2. 외부 ID, 명령 DOM·순서, mode·visibility 계약을 목록화한다.
3. 한 줄 viewport, 이동 버튼, divider 경계 이동과 접근성 계약을 문서화한다.

**완료 결과(2026-08-26)**: [Stage 1 보고서](../working/task_m100_6138_stage1.md)에 현행 1~3행과
1219px desktop track 기준선을 고정했다. 제품 source 변경은 0건이다.

### Stage 2 — DOM·CSS·controller 구현

1. 기존 group을 track 안으로 감싸되 명령 요소를 복제하지 않는다.
2. device breakpoint의 wrap·label 숨김·버튼 축소 규칙을 제거한다.
3. ResizeObserver·MutationObserver 기반 controller와 divider 경계 이동을 구현한다.
4. source 계약과 controller 테스트, responsive E2E를 갱신한다.
5. `mydocs/working/task_m100_6138_stage2.md`에 결과를 기록한다.

**종료 게이트**: 1920~375px에서 한 줄·56px이며 첫 group부터 마지막 group까지 버튼·wheel·touch·focus로
도달하고 #6115 visibility와 mode 전환이 유지된다.

**완료 결과(2026-08-26)**: [Stage 2 보고서](../working/task_m100_6138_stage2.md)에 single track DOM,
동적 controller, theme nav와 split menu clipping 보정을 기록했다. 통합 responsive E2E 609건이 통과했다.
Stage 3 사용자 검토에서는 바깥 toolbar 기준이 섞인 32px anchor 오차를 track 기준으로 정규화하고 양 끝
방향 버튼을 absolute overlay로 바꿔 기존 8px padding을 복원하는 후속 보정을 추가했다.

### Stage 3 — 테마·통합 검증과 PR 준비

1. 1920, 1280, 1024, 962, 961, 883, 808, 807, 768, 460, 459, 412, 390, 375px을 검증한다.
2. default/flat/oldschool × light/dark의 경계·hover·focus·disabled를 확인한다.
3. #6118의 1·2행·더보기와 #6138의 한 줄 도구막대를 같은 viewport에서 통합 검증한다.
4. Studio test/build, TypeScript, E2E, 문서·format 게이트와 대표 screenshot을 남긴다.
5. `mydocs/working/task_m100_6138_stage3.md`와 `mydocs/report/task_m100_6138_report.md`를 작성한다.

**종료 게이트**: 두 이슈의 동작을 구분할 수 있는 재현 근거가 있고 통합 PR 한 건을 제출할 준비가 된다.

**완료 결과(2026-08-27)**: [Stage 3 보고서](../working/task_m100_6138_stage3.md)와
[최종 보고서](../report/task_m100_6138_report.md)에 14개 viewport, 24개 theme case, divider 이동·keyboard·
wheel·focus·mode·visibility 조작과 #6118 통합 검증을 고정했다. 사용자 검토 후 divider anchor,
양끝 overlay nav와 동기 퇴장, 데스크톱 서식 시작축을 추가 검증해 통합 E2E 821건이 통과했다.

## 6. 완료 조건

- [x] 모든 지원 너비에서 `#icon-toolbar`가 한 줄·56px이다.
- [x] viewport만으로 label이나 버튼 폭이 바뀌지 않는다.
- [x] overflow가 없으면 이동 버튼이 숨겨진다.
- [x] overflow가 있으면 이동 버튼과 native scroll로 양 끝까지 도달한다.
- [x] 이동 버튼은 track 기준 가시 divider 경계로 이동하며 시작·끝 해당 방향 버튼만 숨긴다.
- [x] mode group 변경과 #6115 접기/펴기 뒤 상태를 재계산한다.
- [x] 기존 명령 DOM·순서·ID·listener와 상태 authority를 유지한다.
- [x] 세 skin의 light/dark와 #6118 통합 반응형 검증을 통과한다.
- [ ] 이슈·계획·커밋·검증을 분리한 채 #6118과 하나의 PR로 제출한다.

## 7. 승인 게이트

사용자가 #6118 Stage 3의 시각 확인과 #6138 Stage 1~3 진행을 승인했다. 두 이슈의 로컬 구현·통합 검증은
완료했다. remote push와 PR 생성은 별도 승인을 받은 뒤 한 건으로 수행한다.
