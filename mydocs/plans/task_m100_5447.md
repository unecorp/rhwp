---
kind: plan
status: done
canonical: mydocs/plans/task_m100_5447.md
last_verified: 2026-08-18
---

# #5447 계획서 — B2 스파이크: 행·열·라벨 구조 편집 실현성 실측

- **Issue**: [#5447](https://github.com/edwardkim/rhwp/issues/5447)
- **부모**: [#3683](https://github.com/edwardkim/rhwp/issues/3683) Track B — 착수 순서 B1 → **B2**
- **브랜치**: `task5447` (`upstream/devel` `e79f11308` 기준)

## 1. 배경

B1 축이 닫혔다 — #4055 스파이크 → #4100 엔진(PR #4603) → #4694 UI(PR #4717,
2026-08-13 merge). #3683 의 B2 착수 판정 코멘트(2026-08-18, 2차 재검증 반영 patch)가
코퍼스 28종 실측으로 구현 정책 3종을 정했고, 이 스파이크는 "정해달라"가 아니라
**"정한 것이 한컴에서 통하는가"** 를 확인한다.

| 정책 | 내용 | 근거 |
|---|---|---|
| `c:f` | **무갱신** (원본 바이트 그대로) | `c:externalData` 0/28 — 캐시가 유일한 진실 원천, 한컴 스스로 깨진 범위 사용(분산형 ser0 `Sheet1!$B$2:$A$4`) |
| `c:ptCount` | **항상 재계산** | 28/28 실제 점 개수와 일치하는 한컴 불변식 |
| `c:pt idx` | 삭제·삽입 후 **0..n-1 전수 재번호** | rhwp 스캐너 fail-closed(`crates/rhwp-ooxml-chart/src/data.rs:363-398`) — 희소 idx 산출물은 rhwp 자신이 못 읽음 |

B1 은 개수를 안 바꿔 `c:f` 가 계속 정합이었다. B2 는 **범위 길이와 실제 개수가 어긋난
파일을 처음으로 내보낸다** — 그 어긋남을 한컴 렌더러와 **차트 데이터 편집기**가 어떻게
다루는지가 판정 대상이다.

## 2. 질문

| # | 질문 | 판정 |
|---|---|---|
| S1 | `c:f` 무갱신 상태에서 개수가 바뀐 차트를 한컴이 정상 개봉·렌더하는가 | 144DPI 래스터 |
| S2 | **편집기 적재**도 정상인가 — 편집기가 `c:f` 를 재해석해 행을 잘라내지 않는가 | 작업지시자 육안(편집기) |
| S3 | 계열 신설(`c:ser`+채번+이름·값)·삭제(잔여 재번호)가 통하는가 — 누적·3D 포함 | 144DPI 래스터 |
| S4 | 거부해야 할 경계(원형 계열 추가, 주식형 계열 개수 변경)가 실제로 깨지는가 | 작업지시자 육안 |

**S2 가 `c:f` 무갱신 정책이 뒤집힐 수 있는 유일한 지점이다** (#4100 판정에서 편집기
적재까지 확인한 선례를 따른다).

## 3. 방법 — 바이트 수술 + 기존 주입 경로

구조 변종은 `set_chart_data_native` 로 만들 수 없다(fail-closed:
seriesCountMismatch/valueCountMismatch/categoryMismatch). 스파이크는 코퍼스 XML 을
**바이트 문자열 수술**(한컴 단일 라인 기계 생성이라 균일 — 28종 실측)로 가공해
기존 주입 경로로 넣는다:

- HWPX — `replace_chart_representations`(`tests/issue_4100_chart_data_edit.rs:761`):
  ①②에 **같은** 변종 XML 동시 주입 (다르면 representationMismatch 로 재독 거부)
- HWP5 — `replace_chart_nested_only`(신규, ②만): `replace_ole_stream` 경유
  (루트 CLSID·③④ 보존 — #4097 계약)
- 산출마다 자기검증: `scan_chart_values` 재통과 → export → `from_bytes` 재개방 →
  봉투 구조 확인(계열 수·라벨·값) → ①==②==변종 XML, ③④ 바이트 불변

`.hwp` ② 추출은 `chart_streams` 경유 — DocInfo BIN_DATA **참조 해석**을 거친다
(코퍼스 유일의 다중 BinData 파일 `분산형/곡선이있는분산형.hwp` 에 미참조 잔류 BIN 이
있어 첫 스트림을 집으면 오판한다. 착수 판정 코멘트 §5 실측).

## 4. 변종·판정 대상 (판정 번들 38 파일)

- **본선 6종** — 기준 문서 `세로막대형/묶은세로막대형`: 행추가·행삭제·계열추가·
  계열삭제·계열명변경·라벨변경 (× hwpx/hwp = 12)
- **경계 2종** — `원형대원형-계열추가`(1계열 고정 위반), `시가고가저가종가-계열삭제`
  (4→3) — 거부 경계의 실측 (× 2 = 4)
- **종류 커버리지 6종** — 본선 처치(행추가/점추가/계열삭제)를 나머지 판정 대상에:
  묶은가로막대형·표식이있는꺽은선형·직선및표식이있는분산형(xVal/yVal 동기 점추가)·
  특이케이스(numLit 1→2점)·**누적세로막대형-계열삭제**(누적 합 변화)·
  **3차원묶은세로막대형-행추가**(`c:view3D`) (× 2 = 12)
- 대조군 9 (원본 hwpx) + 변환 축 1 (`행추가` → `export_hwp_with_adapter_snapshot`)

판정 대상은 B1 의 7종 + **누적 1종 + 3D 1종** = 9종 (#5447 §5). 누적·3D 는
`samples/chart/` 실측으로 `누적세로막대형`(grouping=stacked)·`3차원묶은세로막대형`
(bar3DChart+view3D, 3계열이라 변종 전부 성립)을 골랐다.

## 5. 단계

| 단계 | 내용 | 산출 |
|---|---|---|
| 1 | 수술 헬퍼 + 변종 카탈로그 + 프로브 2종 + 번들 생성기 | `tests/issue_4100_chart_data_edit.rs` Stage 7 (기존 예외 타깃 확장 — 신규 파일 없음) |
| 2 | 번들 생성 실행 | `output/issue_5447_b2_judgment/` 38 파일 + `PANJEONG.md` (gitignored) |
| 3 | **작업지시자 한컴 판정** — (a)오류창 (b)기대 모양 (c)편집기 열림 (d)**편집기 행·열 수** | PDF 회신 + (d) 파일별 기록 |
| 4 | 144DPI 래스터 해시 판정 + 보고서 + #3683 회신 | `mydocs/report/task_m100_5447_report.md` |

## 6. 지키는 계약

- `tests/issue_3546_chart_preserved_on_save.rs` — 편집하지 않은 차트 blob 보존.
  스파이크는 프로덕션 동작 무변경이라 green 유지.
- `tests/issue_4098_legacy_chart_grid.rs` `checked == 56` 2곳 — 변종은 `output/`
  (gitignored)에만 쓴다. `samples/chart/` 무변경.
- ③(레거시 Contents)·④(프리뷰) 바이트 불변 — 산출마다 자기검증으로 고정.
- `chart_ole_v1_boundary.md`(#5424/#5438, in-flight) 렌더 v1 경계 선언과 별개 —
  편집 트랙은 #3683 go 결정(2026-08-01) 소관 (#5447 본문에 명시).
- #5168(LINK 그림 BIN_DATA)은 직교 — 판정 문서에 배제 명시.

## 7. 검증

변경 범위 = Rust 테스트 축(렌더러·studio·src 무변경, 신규 fixture 0) → Skia·wasm 불필요.
기존 예외 타깃 파일만 수정하므로 suite manifest 재생성도 불필요.

```bash
cargo test --profile release-test --test issue_4100_chart_data_edit
cargo test --profile release-test --test issue_4100_chart_data_edit \
    generate_b2_structure_judgment_bundle -- --ignored --nocapture
cargo fmt --all && cargo fmt --all -- --check
node scripts/rust-unit-test-tiers.mjs --check
cargo clippy --all-targets -- -D warnings
```

## 8. 승인 게이트

- PR 생성·push·#3683/#5447 코멘트는 각각 사용자 확인 후
- 단계 3 은 작업지시자 한컴 판정 회신 후 단계 4 진행
