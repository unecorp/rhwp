# 수정 수행계획 — Task M100 #4969 W10-Q4-D bounded vertical activation

- **상위 계획**: [`task_m100_4969_w10_q4.md`](task_m100_4969_w10_q4.md)
- **기준 checkpoint**: `f449960c7` (Q4-C `qualified-dormant`)
- **D2 착수 기준 upstream/devel**: `628944a33cb5` (merge commit `7a7b316506ff`)
- **최신 병합 upstream/devel**: `3b301f725` (merge `3d239b5a7`, 일지 add/add 1건 보존 합성)
- **기계 판독 계획**:
  [`w10_q4_d_execution_plan.json`](../tech/investigations/issue-4969/w10_q4_d_execution_plan.json)
- **상태**: 메인테이너 수정 수행계획 승인·checkpoint `7b3a7bab4`; Q4-D0
  `qualified-red-baseline` 결과 승인·checkpoint `87d7a13a2`; Q4-D1
  `qualified-dormant-certified` 결과 승인·checkpoint `d0948166a`, 최신 devel 병합 재자격화 통과;
  Q4-D2 `qualified-atomic-layout` checkpoint `992fe4acaf92`, 최신 devel merge `049b73d0cd80` 및
  재자격화 기록 `7bfefff07a99`; Q4-D3 수정 수행계획 checkpoint `e64d6fd67`;
  D3-A `qualified-shadow-mapping` checkpoint `bb334756b`; D3-B
  `qualified-atomic-leaf-publication` checkpoint `d49400aad`; D3-C 표준 Docker WASM까지 통과,
  `qualified-publication-parity` checkpoint `43d50ad99`; 최신 devel merge `bcaf86e18`; Q4-D4 상세
  수행계획 checkpoint `c11e84935`; D4-B `qualified-rust-bounded-selector` checkpoint `c9b551909`;
  D4-C `qualified-studio-runtime-pixel-proof` checkpoint `b1a33b6fc`; 최신 devel merge `eb7d020e4`;
  D4-D `qualified-canvaskit-bounded-vertical-runtime` checkpoint `f40987220`; Q4-D5 상세 수행계획 checkpoint
  `a4248b9c6`; D5-A measurement contract, D5-B 성능 정정·9-process 계측, D5-C full gate 완료;
  correctness·성능 qualified, causal WASM size unavailable로 D5-C 역사적 `blocked` 결과 checkpoint
  `a7de96b44`; D5-D clean-room 계획 checkpoint `e866bc0b8`, same-tree causal WASM +36,611 bytes
  (+0.371371668%) 확보로 Q4-D 최종 `qualified-bounded-subset`, 결과 checkpoint `95d450dd5`
- **작성일**: 2026-08-30 KST
- **제품 출력 변경**: D2 이후 증명된 HWP5 표 셀 subset에서만 가능
- **공개 API 변경**: 없음; 기존 `registerExactFontSource` 권위를 재사용

## 1. 재검토 결론

Q4-C는 exact vertical shaping과 bbox·inline/column next origin을 한 `Arc`로 묶었지만 제품 module에
등록하지 않았고 caller·sidecar·paint publication도 0이다. 이 상태에서 `layout_vertical_cell_text`에
바로 호출을 추가하면 다음 네 권위를 한 번에 열게 된다.

1. `(charShapeId, languageIndex)` exact source를 vertical transaction에 인증하는 권위
2. legacy per-character column 대신 shaped run geometry를 commit하는 레이아웃 권위
3. `TextRun` fallback과 vertical `GlyphRun`을 같은 source로 게시하는 publication 권위
4. CanvasKit이 vertical-upright glyph positions를 선택하는 backend 권위

각 경계는 독립적으로 fail-closed해야 한다. 따라서 Q4-D를 D0~D5로 나누고, 각 결과 승인과
checkpoint 뒤에만 다음 절편으로 이동한다.

## 2. 현재 경로 감사

### 2.1 문서 의도

- HWP5 표 셀은 `Cell.text_direction`의 0/1/2를 보존한다. 최초 target은 code 2, 즉
  `vertical-rl + Latin upright` 의미를 가진 표 셀만 허용한다.
- HWPX 표 셀 parser·serializer는 현재 `HORIZONTAL`/`VERTICAL`만 code 0/1로 다루고
  `VERTICALALL`을 code 2로 보존하지 않는다. 따라서 HWPX 표 셀은 Q4-D target이 아니다.
- format version 번호로 분기하지 않는다. 현재 `Document` provenance와 실제 cell field, exact slot,
  run geometry를 feature detection한다.

### 2.2 레이아웃

`layout_vertical_cell_text`는 composed run을 문자 단위로 분해하고 font size 또는 반칸 휴리스틱으로
advance를 정한 뒤, 문자마다 `TextLine`과 `TextRun`을 만든다. Q4-C transaction과 page-local
sidecar는 아직 연결되지 않았다. 실패 뒤 legacy로 돌아갈 때 node id·bbox·children 순서까지 같아야 하므로,
`next_id()` 또는 tree mutation 전에 target 전체를 검증하고 한 commit 함수만 최종 mutation을 수행해야 한다.

### 2.3 publication·backend

- horizontal sidecar lowerer는 vertical y-position을 `VerticalPositioningAuthorityPending`으로 계속 거부해야 한다.
- nominal font lowerer, Rust layer selector, `text_v2`, Native Skia와 Studio CanvasKit replay는 현재 vertical
  orientation/writing mode를 각각 거부한다.
- 최초 backend는 explicit glyph positions를 재생할 수 있는 CanvasKit Browser/Studio와 Rust CanvasKit policy만
  연다. Native Skia·SVG·Canvas2D는 Q4-D에서 `TextRun` fallback을 유지한다.
- pure CJK upright만 대상이므로 `glyphTransforms`는 만들지 않는다. Latin sideways·mixed-per-glyph는 열지 않는다.

## 3. 최초 activation surface

다음 조건을 모두 만족할 때만 `boundedVerticalHwp5TableCellV1` 후보가 된다.

1. 현재 provenance가 HWP5 의미 계보이고 표 셀 `text_direction == 2`다.
2. one cell·one paragraph·one composed line·one text run·one column이다.
3. visible text가 pure CJK upright이며 공백·Latin·문장부호·mixed class가 없다.
4. run 전체가 같은 `char_shape_id`, language category, font size, 장평 1.0, 자간 0인 exact slot이다.
5. slot에 host가 등록한 공개 Noto Sans KR exact bytes와 face index가 있고 generation·SHA-256·길이가 일치한다.
6. bold/italic·합성 스타일·테두리/배경·겹침·field/note·tab·inline control·display projection·variation이 없다.
7. 한 열의 shaped bbox와 next inline/column origin이 cell inner area 안에서 유효하고 모든 glyph/cluster/resource
   상한을 만족한다.
8. request→shape→layout commit→sidecar→font resource→GlyphRun→CanvasKit 선택이 모두 성공한다.

한 조건이라도 실패하면 run 일부를 게시하지 않고 기존 per-character `TextRun` tree를 byte/canonical 동일하게
생성한다. parser나 font name이 exact source를 자동 등록하지 않으며 public API mode를 새로 추가하지 않는다.

## 4. target과 control

### target

기존 integration source 안에서 공개 Noto Sans KR를 exact slot에 등록한 HWP5-provenance synthetic document를
만든다. 한 셀·한 문단·한 run의 pure CJK 문자열과 `text_direction=2`를 사용하고, Q4-C oracle에서 계산한
glyph id·cluster·origin·bbox·next origin을 layer JSON까지 고정한다. binary fixture를 새로 위장 생성하거나
font name fallback을 사용하지 않는다.

### non-target control

- `samples/table-004.hwp`: code 2 의미·legacy output control. exact slot 미등록 상태에서는 활성화 0.
- `samples/issue6029/3200477_icao_procedure.hwpx`: code 1, 문자 보존·한 열 구조·roundtrip control.
- horizontal cell, HWPX cell, code 1, unknown raw, no exact source, stale/mismatched source
- multi paragraph/run/column, mixed CJK+Latin, punctuation, Latin sideways/upright, variation
- resource limit, cell overflow, sidecar duplicate/limit, unsupported backend

두 기존 sample의 canonical SVG/layer tree와 HWP/HWPX roundtrip은 Q4-D 전후 mismatch 0이어야 한다.

## 5. 절편과 승인 경계

### Q4-D0 — red baseline·target contract

1. 기존 `tests/cases/issue_4969_shaping_request_contract.rs`에 synthetic target builder와 activation predicate
   계약을 추가한다. 새 integration `.rs` source는 만들지 않는다.
2. Q4-C dormant 27건, `table-004`와 #6029 canonical output, CanvasKit vertical rejection reason을 고정한다.
3. red는 product module registration, source-bound owner, atomic layout commit, vertical publication,
   CanvasKit selection 부재의 다섯 항목과 정확히 일치해야 한다.

**종료 게이트**: green control 변화 0, red 원인 5건 일치, 제품 source·output 변경 0.

**결과**: [Q4-D0 결과 보고서](../working/task_m100_4969_w10_q4_d0.md)는 green 28/28,
integration 정책 19/19, Studio control 1/1과 `table-004`·#6029 canonical baseline을 고정했다.
product source 변경 없이 계획한 다섯 경계만 0/5 red로 분리돼 `qualified-red-baseline`으로 판정했고,
메인테이너가 결과를 승인했다. 이 commit을 D0 checkpoint으로 고정한다.

### Q4-D1 — exact-source-bound dormant owner

1. `shaping_vertical`을 제품 module에 등록하되 product caller는 열지 않는다.
2. exact registry snapshot에서 slot/source identity를 재검증하고 Q4-C request를 만드는 vertical context를 추가한다.
3. source certificate는 Arc bytes·handle·generation만 소유하고 Debug/trace에 bytes·path·raw text를 남기지 않는다.
4. source·intent·run class·variation·limit 실패는 typed reason과 pristine legacy geometry만 반환한다.

**종료 게이트**: synthetic target만 certified dormant transaction 생성, publication 0, no-request control mismatch 0.

**결과**: [Q4-D1 결과 보고서](../working/task_m100_4969_w10_q4_d1.md)는 immutable exact registry
snapshot과 source handle·generation·동일 bytes `Arc`를 인증한 dormant owner를 제품 module에 등록했다.
native green 30/30, 격리 wasm32+Node 1/1, 표준 Docker WASM build와 정책·Clippy를 통과했고 layout·sidecar·
paint caller와 product publication은 0이다. D0 red는 D1 두 경계만 green, D2~D4 세 경계는 red로 유지됐다.
메인테이너가 `qualified-dormant-certified` 결과와 checkpoint 진행을 승인했다. checkpoint 뒤 최신
`upstream/devel@d3b40a3d7` 병합 head에서도 native 30/30, Docker WASM, Studio 1,314건·build와 교차
guard를 재통과해 `qualified-dormant-certified-merge-reconfirmed`로 재판정했다.

### Q4-D2 — 한 표 셀 atomic layout commit

1. target predicate와 exact transaction을 tree mutation 전에 모두 준비한다.
2. glyph geometry·run bbox·line bbox·next inline/column origin을 Q4-C의 동일 `Arc`에서 읽는다.
3. page-local vertical sidecar와 render nodes를 하나의 commit 경계에서 생성한다.
4. commit 전 실패는 `next_id`, children, bbox, sidecar count, legacy output을 바꾸지 않는다.

**종료 게이트**: target exact position mismatch 0, pointer owner 1, rejection canonical mismatch 0,
partial layout publication 0.

**결과**: [Q4-D2 결과 보고서](../working/task_m100_4969_w10_q4_d2.md)는 target predicate와
exact transaction, node batch, page-local vertical sidecar를 tree mutation 전에 모두 준비하고 한 commit
경계로 게시한다. synthetic HWP5 target은 Q4-C 좌표 mismatch 0·owner `Arc` 1을 확인했고, source 부재와
비-Noto exact source는 legacy tree를 유지했다. D0의 D2 red만 green이 되어 D3·D4 두 red는 유지된다.
표준 Docker WASM, focused Clippy, format, 두 canonical sample을 통과했으며 메인테이너가 결과를 승인했다.
최신 `devel`은 결과 후보 작성 중 22커밋 전진했으므로, 결과 승인·D2 checkpoint 뒤 `bd78a53122e4`를
정상 merge하고 재검증해야 한다. 올바른 incoming 범위에서 D2 WIP 파일과 직접 중첩은 0이다.

### Q4-D3 — atomic vertical GlyphRun publication

상세 정정 계획은
[`task_m100_4969_w10_q4_d3_revised.md`](task_m100_4969_w10_q4_d3_revised.md)를 따른다.
D2 target은 문자별 fallback leaf와 `TextSourceId`를 가지므로 줄 전체를 한 equivalence group으로 묶지 않는다.
대신 line 전체 publication을 한 transaction으로 준비하고 각 fallback leaf에 고유 `text-N` group과 한 glyph
candidate를 게시한다. 모든 leaf와 한 portable source가 준비된 경우에만 함께 commit한다.

1. D2 line sidecar와 exact source certificate를 한 번 대사해 portable font resource와 모든 leaf의
   vertical-upright `GlyphRun`을 mutation 없이 준비한다.
2. staged line subtree가 leaf/source/cluster/variant 검증을 통과한 뒤에만 subtree와 resource delta를 게시한다.
3. 각 `TextRun` fallback은 그대로 남고 equivalence group은 leaf를 가로지르지 않는다.
4. 성공한 line의 source id만 nominal lowerer slot에서 claim한다. 실패하면 resource·variant·claim 0이다.
5. horizontal lowerer와 Rust·Studio backend selector의 vertical authority pending은 변경하지 않는다.

**종료 게이트**: target `N TextRun + N GlyphRun`, leaf별 equivalence group `N`, portable resource 1,
resource mismatch 0, failed publication residue 0, horizontal publication mismatch 0, backend false selection 0.

### Q4-D4 — CanvasKit feature detection·pixel proof

상세 절차는 [`task_m100_4969_w10_q4_d4.md`](task_m100_4969_w10_q4_d4.md)를 따른다.

1. Rust CanvasKit policy와 Studio replay는 진단 provenance가 `boundedVerticalHwp5TableCellV1`이고
   `vertical-rl + vertical-upright + no transforms`인 candidate만 선택한다.
2. glyph ids·positions·font resource를 그대로 재생하고 backend에서 reshape하지 않는다.
3. Native Skia·SVG·Canvas2D와 vertical-sideways/mixed candidate는 기존 reason으로 `TextRun` fallback한다.
4. Node WASM layer JSON과 Studio CanvasKit pixel E2E에서 target exact position·bbox를 대사한다.

**종료 게이트**: target CanvasKit selection/pixel mismatch 0, unsupported backend false selection 0,
fallback disappearance 0.

### Q4-D5 — 회귀·성능·최종 판정

상세 절차는 [`task_m100_4969_w10_q4_d5.md`](task_m100_4969_w10_q4_d5.md)를 따른다.

1. native·Docker Node WASM·Studio focused gate와 표 셀 roundtrip/canonical controls를 재실행한다.
2. target/non-target A/B/A 계측으로 layout·layer build·CanvasKit replay wall time과 WASM 크기 delta를 기록한다.
3. Q4-D 실제 지원 subset과 rejection reason을 기계 판독 matrix에 고정한다.
4. 결과가 게이트를 충족하면 `qualified-bounded-subset`, 그렇지 않으면 `dormant` 또는 `blocked`로 판정한다.
5. wall time은 동일 process A1/B/A2의 bracket drift와 함께 공개하고 사후 절대 SLA를 만들지 않는다.
6. WASM delta는 같은 최신 tree에서 Q4-D 제품 checkpoint만 제외한 causal A/B로 측정한다.

**종료 게이트**: 회귀 0, 성능 자료 확보, 지원 범위 과장 0. 결과 승인·checkpoint 뒤 Q4-E로 이동한다.

## 6. 보호 불변식

1. no exact source·horizontal·HWPX·code 1·unknown intent output은 바뀌지 않는다.
2. format build number가 아니라 현재 provenance·field·source capability·geometry를 판정한다.
3. parser/font name/fallback matrix는 activation authority가 아니다.
4. layout과 paint가 서로 다시 측정하거나 backend가 reshape하지 않는다.
5. run 일부, glyph 일부, resource 일부를 게시하지 않는다.
6. horizontal `VerticalPositioningAuthorityPending`을 전역 해제하지 않는다.
7. private corpus·Hyper-V는 사용하지 않는다. 공개 synthetic와 추적 sample만 사용한다.
8. commit·push·GitHub comment·PR은 각각 별도 승인 전에는 수행하지 않는다.

## 7. 검증·제출 규칙

- Rust integration 변경은 기존 `tests/cases/issue_4969_shaping_*.rs` 중 해당 계보 원본만 사용한다. D5 측정
  receipt는 상세 계획이 지정한 `issue_4969_shaping_atomic_activation.rs`에만 추가한다.
- Studio 계약은 기존 `rhwp-studio/e2e/issue-4969-shaping-replay.test.mjs`와 관련 unit source를 사용한다.
- generated integration suite·manifest·Cargo marker는 source 변경에 포함하지 않는다.
- 각 checkpoint 전 `cargo fmt --all`과 `cargo fmt --all -- --check`를 실행한다.
- `src/**`의 `#[cfg(test)]`를 변경하면 `node scripts/rust-unit-test-tiers.mjs --check`를 추가한다.
- D4 Docker WASM·Studio CanvasKit 실행 명령은 환경 가이드의 현행 절차를 따른다.

## 다음 승인 경계

Q4-D5-C의 역사적 `blocked` 판정 뒤 D5-D가 hard gate를 완화하지 않고 source-only clean-room A를 구성했다.
동일 Docker image·분리 volume의 A/B 빌드로 causal WASM +36,611 bytes(+0.371371668%)를 확보했으므로 Q4-D
최종 분류는 `qualified-bounded-subset`이다. D5-D 결과 checkpoint 뒤 최신 upstream을 확인하고 후속 경계를
결정한다. 결과는 checkpoint `95d450dd5`로 고정했고 최신 devel merge `3d239b5a7` 뒤 #4969 112/112와
wasm32·tier·format을 재통과했다.
