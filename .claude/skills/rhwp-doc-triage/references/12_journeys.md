# 12 — 실사용 여정

gym 시나리오가 아니다. 실제 에이전트가 긴 한글 문서를 받을 때의 경로다.

## J01 — 이 hwp 뭐야?

- 쪽수 가정: `any`
- 표본: `samples/para-001.hwp`
- 명령: info → explain
- 정지: `S04`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J02 — 몇 쪽짜리야? 암호 걸려 있어?

- 쪽수 가정: `any`
- 표본: `samples/hwp3-sample.hwp`
- 명령: info
- 정지: `S03`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J03 — 목차만 뽑아줘

- 쪽수 가정: `medium+`
- 표본: `samples/hwp3-sample16.hwp`
- 명령: info → export-structure
- 정지: `S05`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J04 — 긴 문서인데 다 읽지 말고 훑어줘

- 쪽수 가정: `large`
- 표본: `samples/hwp3-sample.hwp`
- 명령: info → digest
- 정지: `S06`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J05 — 어디에 위임전결이 나와?

- 쪽수 가정: `large`
- 표본: `samples/hwp3-sample.hwp`
- 명령: info → search
- 정지: `S07`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J06 — 금액만 쪽과 함께 뽑아줘

- 쪽수 가정: `large`
- 표본: `samples/hwp3-sample.hwp`
- 명령: info → extract-data
- 정지: `S09`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J07 — 이 서식 채울 수 있어?

- 쪽수 가정: `small`
- 표본: `samples/field-01.hwp`
- 명령: info → explain
- 정지: `S11`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J08 — 표가 많은 문서야?

- 쪽수 가정: `any`
- 표본: `samples/table-001.hwp`
- 명령: info → explain
- 정지: `S11`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J09 — 짧은 메모 전문 보여줘

- 쪽수 가정: `tiny`
- 표본: `samples/para-001.hwp`
- 명령: info → export-text
- 정지: `tiny band`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J10 — 뒷부분도 더 읽어줘

- 쪽수 가정: `large`
- 표본: `samples/hwp3-sample.hwp`
- 명령: digest --pages
- 정지: `S10`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J11 — 절 단위로 나눠서 요약해

- 쪽수 가정: `medium+`
- 표본: `samples/hwp3-sample16.hwp`
- 명령: digest --sections
- 정지: `S06`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J12 — 폴더에서 위임전결 있는 파일만

- 쪽수 가정: `folder`
- 표본: `docs/*.hwp`
- 명령: batch search
- 정지: `S14`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J13 — 암호 걸린 문서 열어봐

- 쪽수 가정: `any`
- 표본: `samples/hwp3-sample16-hwp5-2024-password-123456.hwp`
- 명령: info
- 정지: `S02`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J14 — 이 문서 보내도 돼?

- 쪽수 가정: `any`
- 표본: `samples/para-001.hwp`
- 명령: info → handoff security-sweep
- 정지: `S12`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.
## J15 — 날짜가 언제로 되어 있어?

- 쪽수 가정: `any`
- 표본: `samples/hwp3-sample.hwp`
- 명령: extract-data --kind date
- 정지: `S09`
- 금지: 이 여정에서 사다리 나머지 단을 의례 실행하지 않는다.
- 답 형식: 쪽 주소(사람용 page+1) 또는 결정론 한 줄. 전문 인용 금지.


## 변형 D01~D80

### D01 — J01 변형 «요약해 줘»

기본 여정 J01 와 같은 진입 `info` 을 쓰고, 정지는 S04 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/para-001.hwp`.
### D02 — J02 변형 «어디 있어?»

기본 여정 J02 와 같은 진입 `info` 을 쓰고, 정지는 S03 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample.hwp`.
### D03 — J03 변형 «숫자만»

기본 여정 J03 와 같은 진입 `info` 을 쓰고, 정지는 S05 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample16.hwp`.
### D04 — J04 변형 «목차»

기본 여정 J04 와 같은 진입 `info` 을 쓰고, 정지는 S06 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample.hwp`.
### D05 — J05 변형 «암호야?»

기본 여정 J05 와 같은 진입 `info` 을 쓰고, 정지는 S07 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample.hwp`.
### D06 — J06 변형 «표 있어?»

기본 여정 J06 와 같은 진입 `info` 을 쓰고, 정지는 S09 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample.hwp`.
### D07 — J07 변형 «채워 줘»

기본 여정 J07 와 같은 진입 `info` 을 쓰고, 정지는 S11 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/field-01.hwp`.
### D08 — J08 변형 «보내도 돼?»

기본 여정 J08 와 같은 진입 `info` 을 쓰고, 정지는 S11 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/table-001.hwp`.
### D09 — J09 변형 «뒷장»

기본 여정 J09 와 같은 진입 `info` 을 쓰고, 정지는 tiny band 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/para-001.hwp`.
### D10 — J10 변형 «절별로»

기본 여정 J10 와 같은 진입 `digest --pages` 을 쓰고, 정지는 S10 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample.hwp`.
### D11 — J11 변형 «요약해 줘»

기본 여정 J11 와 같은 진입 `digest --sections` 을 쓰고, 정지는 S06 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample16.hwp`.
### D12 — J12 변형 «어디 있어?»

기본 여정 J12 와 같은 진입 `batch search` 을 쓰고, 정지는 S14 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `docs/*.hwp`.
### D13 — J13 변형 «숫자만»

기본 여정 J13 와 같은 진입 `info` 을 쓰고, 정지는 S02 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample16-hwp5-2024-password-123456.hwp`.
### D14 — J14 변형 «목차»

기본 여정 J14 와 같은 진입 `info` 을 쓰고, 정지는 S12 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/para-001.hwp`.
### D15 — J15 변형 «암호야?»

기본 여정 J15 와 같은 진입 `extract-data --kind date` 을 쓰고, 정지는 S09 이다. 변형 발화가 더 넓어 보여도 전문 덤프로 넓히지 않는다. 표본 `samples/hwp3-sample.hwp`.
