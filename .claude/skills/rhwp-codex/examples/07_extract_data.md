# 07 — 날짜·금액 — extract-data

갈래: **수확**. 장: `20_표와_데이터.md`. gym 아님. 새 CLI 아님.

권위는 생성 장이 아니라, 생성 장을 **읽는 순서**다. 표본 JSON 을 손대지 않는다.

## 요청

`수출입 현황에서 날짜랑 금액.`

## 명령

```bash
rhwp extract-data "samples/156636617_240617 2024년 5월 월간 수출입 현황(확정치).hwp" --json
```

원문 표기와 주소를 함께 인용한다. 숫자를 재계산해 지어내지 않는다.
