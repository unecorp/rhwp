# 20 — capabilities --search 폴백

갈래: **자기서술**. 장: `70_자기서술.md`. gym 아님. 새 CLI 아님.

권위는 생성 장이 아니라, 생성 장을 **읽는 순서**다. 표본 JSON 을 손대지 않는다.

## 요청

`누름틀 관련 명령이 뭐야?`

## 명령

```bash
rhwp capabilities --search 누름틀
```

히트가 장 번호로 이어진다. 0건이면 표면 밖(X03). 새 명령을 만들지 않는다.
