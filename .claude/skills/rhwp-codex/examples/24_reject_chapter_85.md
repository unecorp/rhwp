# 24 — 85장 거절

갈래: **진단**. 장: `85_진단_프로브.md`. gym 아님. 새 CLI 아님.

권위는 생성 장이 아니라, 생성 장을 **읽는 순서**다. 표본 JSON 을 손대지 않는다.

## 요청

`dump 로 조판부호 까 봐.`

## 판정

85장은 개발자 전용이다. 통상 문서 작업 에이전트는 들어가지 않는다(X07).
레이아웃 버그 조사면 `rhwp-cli` 디버깅 순서만.

이 스킬은 `dump` 플래그를 발명하거나 생성 장을 고쳐 예시를 넣지 않는다.
