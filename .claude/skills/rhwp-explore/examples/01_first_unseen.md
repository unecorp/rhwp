# 예 01_first_unseen.md — 특수 신호 없는 짧은 메모

종류: `no-special` · 정지 `X05` · gym 아님.

## 첫 수

```bash
rhwp explore samples/blank2010.hwp --json
```

## 엔진 개수 (본문 아님)

`page_count=2` ·
`field_count=0` ·
`table_count=0` ·
`chart_count=0` ·
`injection=0` ·
`hidden=0` ·
`encrypted=False`

## 메뉴

`triage-overview`

첫 명령: `rhwp digest <file> --json`

전체 봉투는 `fixtures/envelopes/S01.json`.
`<file>` 만 실제 경로로 치환한다. 새 명령을 만들지 않는다.

메뉴는 triage-overview 하나. digest 로 파악하고 멈출 수 있다.

처음 보는 짧은 파일. 특수 어포던스가 없으면 digest 한 방으로 충분하다.
