# 예 07_long_law.md — 40쪽 법령 편람

종류: `long` · 정지 `X10` · gym 아님.

## 첫 수

```bash
rhwp explore samples/law-40.hwp --json
```

## 엔진 개수 (본문 아님)

`page_count=40` ·
`field_count=0` ·
`table_count=0` ·
`chart_count=0` ·
`injection=0` ·
`hidden=0` ·
`encrypted=False`

## 메뉴

`structure-outline → long-doc-digest → triage-overview`

첫 명령: `rhwp export-structure <file> --json`

전체 봉투는 `fixtures/envelopes/S12.json`.
`<file>` 만 실제 경로로 치환한다. 새 명령을 만들지 않는다.

long-doc high + structure high. 통독 금지.

40쪽은 long-doc high. 전문 dump 금지. 조문이 있으면 구조가 장문보다 위.
