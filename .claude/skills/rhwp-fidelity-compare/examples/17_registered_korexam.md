# 예제 — korexam A3

이슈 #5329. 실 에이전트 경로. gym 아님.

## 명령

```bash
venv/bin/python tools/fidelity_compare/fidelity_compare.py korexam 0 2 \
  --out-dir /tmp/rhwp-fidelity-korexam
```

고정 1920×1080 창을 붙이지 않는다. 하네스가 SVG 판형을 읽는다.
잘리면 도구 후보지, 문서 회귀로 승격하지 않는다.

잔여 줄바꿈은 폰트 폴백 메트릭 의심. `RHWP_FONT_PATH_DIR` 재측정이
다음 실험이다 (10장).

관련: `references/18_registered_keys.md`, `04_page_sheets.md`.
정지 F03.
