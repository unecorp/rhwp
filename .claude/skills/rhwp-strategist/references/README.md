# references/

SKILL.md 가 가리키는 자식 장이다. 번호 순이 읽는 순이다.

`_gen_pack.py` 가 이 장과 `../fixtures`, `../examples` 의 기계 파일을
다시 쓴다. 장을 고칠 때 계약 시험이 기대하는 토큰(ST-*, EV-, CLAIM-,
engagement.py, page, failed)을 지우지 마라.

정본은 항상 `mydocs/manual/strategist_playbook.md` 다.
