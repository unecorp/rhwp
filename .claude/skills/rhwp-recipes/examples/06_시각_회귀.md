# 예: 시각 회귀 요청

이슈 #5331. 파일 `06_시각_회귀.md`.

사용자: "편집 전후 레이아웃 숫자로"

라우터: 레시피 06 — 편집 전후를 눈이 아니라 숫자로 비교하기.

정본: `mydocs/manual/recipes/06_visual_regression_before_after.md`

첫 수:

```bash
rhwp render-diff <file> --via hwpx
```

다음 스킬: `rhwp-visual-regression` (재작성하지 않음).

정지: STRUCT_MISMATCH 를 반사 실패로 처리하지 말 것 — 경로를 먼저 읽음

untrustedContent: render-diff 는 --json 이 없다(정본 2026-08-03). 문서 원문을 봉투에 싣지 않는다. 판정은 종료 코드와 텍스트 status.

전체 JSON 표본은 `fixtures/transcripts/` 의 정본 발췌를 연다. 여기서 새 봉투를 만들지 않는다.
