# 예: 표 요청

이슈 #5331. 파일 `02_표.md`.

사용자: "표를 CSV 로 뽑아줘"

라우터: 레시피 02 — 표 데이터를 CSV로 뽑아 스프레드시트에서 고치고 되돌리기.

정본: `mydocs/manual/recipes/02_table_csv_roundtrip.md`

첫 수:

```bash
rhwp export-tables <file> --json
```

다음 스킬: `rhwp-table-exchange` (재작성하지 않음).

정지: 대상 표에 colSpan/rowSpan > 1 이면 CSV 왕복 금지

untrustedContent: table-to-csv 봉투는 untrustedContent:true, untrustedFields: [tables[].csv]. 출처 모르는 문서면 04 먼저.

전체 JSON 표본은 `fixtures/transcripts/` 의 정본 발췌를 연다. 여기서 새 봉투를 만들지 않는다.
