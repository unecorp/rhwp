{
  "id": "T09-B08",
  "recipe": "09",
  "kind": "bash",
  "sourceFile": "mydocs/manual/recipes/09_bulk_extract_convert.md",
  "excerpted": true,
  "fabricatedLive": false,
  "raw": "입력=$(wc -l < 목록.txt); 성공=$(jq -s '[.[]|select(.error|not)]|length' 결과.ndjson); 실패=$(jq -s '[.[]|select(.error)]|length' 결과.ndjson)\necho \"입력 $입력 = 성공 $성공 + 실패 $실패\"",
  "firstLine": "입력=$(wc -l < 목록.txt); 성공=$(jq -s '[.[]|select(.error|not)]|length' 결과.ndjson); 실패=$(jq -s '[.[]|select(.error)]|length' 결과.ndjson)",
  "notGym": true
}
