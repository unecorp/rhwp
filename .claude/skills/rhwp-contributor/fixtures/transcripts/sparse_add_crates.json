{
  "id": "sparse_add_crates",
  "issue": 5322,
  "argv": [
    "git sparse-checkout add crates",
    "cargo fmt --all -- --check"
  ],
  "forbids": [
    "git add -A",
    "cargo fmt --check",
    "gh pr create --base main"
  ],
  "hardGate": "cargo fmt --all -- --check"
}
